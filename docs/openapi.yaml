openapi: 3.0.3
info:
  title: API سامانه مدیریت مدرسه
  version: 1.0.0
  description: |2

    وب‌سرویس یکپارچه مدیریت مدرسه: پذیرش، ثبت‌نام، آموزش، حضور و غیاب، آزمون،
    دفتر نمره و کارنامه، منابع انسانی، مالی و حسابداری دوبل، خرید و انبار و اموال،
    و خدمات دانش‌آموزی.

    ### احراز هویت
    تمام مسیرها به‌جز `/api/v1/auth/token/` و `/api/v1/auth/token/refresh/`
    نیازمند هدر زیر هستند:

        Authorization: Bearer <access_token>

    ### انتخاب Context کاری
    اکثر منابع به مدرسه، شعبه و سال تحصیلی محدود می‌شوند. Context مؤثر با هدرهای
    زیر ارسال می‌شود و در لایه Repository اعمال می‌گردد:

        X-School-Id: <uuid>
        X-Campus-Id: <uuid>
        X-Academic-Year-Id: <uuid>

    ### Idempotency
    عملیات نوشتنی حساس (پرداخت، ثبت‌نام، تحویل آزمون) هدر `Idempotency-Key`
    می‌پذیرند؛ ارسال مجدد با همان کلید، همان نتیجه را برمی‌گرداند.

    ### همزمانی خوش‌بینانه
    پاسخ هر منبع دارای فیلد `version` است. برای جلوگیری از Lost Update،
    هدر `If-Match: <version>` را در PUT/PATCH ارسال کنید.

    ### صفحه‌بندی
    پیش‌فرض صفحه‌ای است (`?page=1&page_size=25`). برای فهرست‌های بزرگ و پرتغییر،
    صفحه‌بندی مبتنی بر Cursor نیز در دسترس است (`?cursor=...`).

    ### قالب خطا (بخش ۱۲.۳ سند تحلیل)

        {
          "code": "CLASS_CAPACITY_EXCEEDED",
          "message": "ظرفیت کلاس تکمیل است.",
          "correlationId": "01J7ZS...",
          "fieldErrors": [{"field": "classGroupId", "reason": "capacity"}],
          "retryable": false
        }
paths:
  /api/v1/auth/token/:
    post:
      operationId: auth_token_create
      description: |-
        با نام کاربری و رمز عبور، جفت توکن `access`/`refresh` صادر می‌شود.

        **نکته امنیتی (بخش ۱۵.۱ سند تحلیل):** پیام خطای ورود ناموفق عمومی است و وجود یا نبود حساب را افشا نمی‌کند.

        پس از ورود، برای تعیین محیط کاری، `/api/v1/auth/me/` را صدا بزنید و از فهرست `contexts` یکی را انتخاب کرده و در درخواست‌های بعدی هدرهای `X-School-Id` / `X-Campus-Id` / `X-Academic-Year-Id` را بفرستید.
      summary: ورود و دریافت توکن
      tags:
      - Auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
            examples:
              درخواست:
                value:
                  username: vp.academic
                  password: SecurePass!2026
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LoginRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LoginRequest'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPair'
              examples:
                پاسخموفق:
                  value:
                    access: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                    refresh: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                    expiresIn: 1800
                    mustChangePassword: false
                    mfaRequired: false
                  summary: پاسخ موفق
          description: ورود موفق
        '401':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                پاسخناموفق:
                  value:
                    code: AUTHENTICATION_FAILED
                    message: نام کاربری یا رمز عبور درست نیست.
                    correlationId: 9f2c1a4b8e7d
                    fieldErrors: []
                    retryable: false
                  summary: پاسخ ناموفق
          description: نام کاربری یا رمز عبور نادرست
        '429':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعداد تلاش بیش از حد مجاز
        '400':
          $ref: '#/components/responses/ValidationError'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/auth/token/refresh/:
    post:
      operationId: auth_token_refresh_create
      description: با ارسال `refresh` معتبر، توکن `access` جدید دریافت می‌شود.
      summary: تازه‌سازی توکن دسترسی
      tags:
      - Auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenRefreshRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TokenRefreshRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TokenRefreshRequest'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenRefresh'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/auth/logout/:
    post:
      operationId: auth_logout_create
      description: توکن refresh باطل می‌شود و رخداد خروج ممیزی می‌گردد.
      summary: خروج از سامانه
      tags:
      - Auth
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/auth/me/:
    get:
      operationId: auth_me_retrieve
      description: |-
        همه‌چیزی که فرانت برای ساخت منو، کنترل نمایش دکمه‌ها و صفحه «انتخاب محیط کاری» لازم دارد:

        - `permissions`: فهرست کدهای مجوز مؤثر (مثلاً `student.read`). فرانت باید نمایش هر عمل را با این فهرست کنترل کند.
        - `contexts`: محیط‌های کاری مجاز (نقش + دامنه). اگر فقط یک مورد باشد، صفحه انتخاب محیط Skip می‌شود (بخش ۵.۲ سند فرانت).
      summary: پروفایل کاربر جاری، مجوزها و محیط‌های کاری
      tags:
      - Auth
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUser'
              examples:
                نمونهپاسخ:
                  value:
                    id: 0f8f9b2c-7d3e-4a1b-9c5d-2e6f7a8b9c0d
                    username: vp.academic
                    displayName: زهرا محمدی
                    email: vp@example.school
                    mobile: 09120000000
                    personId: 6b1d2f3a-4c5e-4d7f-8a9b-0c1d2e3f4a5b
                    tenantId: 11111111-2222-3333-4444-555555555555
                    status: ACTIVE
                    mfaEnabled: false
                    mustChangePassword: false
                    isSuperuser: false
                    roles:
                    - ACADEMIC_VP
                    permissions:
                    - attendance.read
                    - attendance.finalize
                    - class_group.read
                    - grade.publish
                    contexts:
                    - roleCode: ACADEMIC_VP
                      roleTitle: معاون آموزشی
                      scopeType: CAMPUS
                      scopeId: aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee
                  summary: نمونه پاسخ
          description: عملیات با موفقیت انجام شد.
        '401':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: احراز هویت نشده است.
        '400':
          $ref: '#/components/responses/ValidationError'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/auth/password/change/:
    post:
      operationId: auth_password_change_create
      description: |-
        تغییر رمز عبور کاربرِ واردشده. رمز فعلی برای تأیید هویت الزامی است.

        - رمز جدید با اعتبارسنج‌های رمز جنگو بررسی می‌شود (طول، سادگی، شباهت به نام کاربری).
        - پس از موفقیت، `mustChangePassword` صفر می‌شود و رخداد در ممیزی ثبت می‌گردد.
        - توکن‌های صادرشده پیش از تغییر رمز، تا پایان عمر خودشان معتبر می‌مانند؛ ابطال سمت سرور در این نسخه پیاده‌سازی نشده است.
      summary: تغییر رمز عبور
      tags:
      - Auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
            examples:
              تغییررمز:
                value:
                  current_password: P@ssw0rd!2025
                  new_password: P@ssw0rd!2026
                summary: تغییر رمز
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
              examples:
                پاسخموفق:
                  value:
                    success: true
                    message: رمز عبور با موفقیت تغییر کرد.
                  summary: پاسخ موفق
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست.
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/auth/password/reset/:
    post:
      operationId: auth_password_reset_create
      description: پاسخ همیشه یکسان است تا وجود یا نبود حساب افشا نشود (بخش ۱۵.۱).
        در صورت وجود حساب، لینک/کد بازیابی از کانال ترجیحی ارسال می‌شود.
      summary: درخواست بازیابی رمز عبور
      tags:
      - Auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PasswordResetRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PasswordResetRequestRequest'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/iam/persons/:
    post:
      operationId: iam_persons_create
      description: |-
        اشخاص — هویت پایه مستقل از نقش.

        بخش ۵ سند تحلیل: «Person هویت پایه فرد؛ مستقل از نقش دانش‌آموز، ولی یا کارمند.»

        ایجاد شخص تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `person.create`
      summary: ایجاد شخص
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PersonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PersonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_persons_list
      description: جست‌وجو در اشخاص با نام، نام خانوادگی یا شماره ملی. این فهرست پایه
        انتخاب دانش‌آموز، ولی و کارمند است.
      summary: فهرست اشخاص
      parameters:
      - in: query
        name: birth_date_from
        schema:
          type: string
          format: date
      - in: query
        name: birth_date_to
        schema:
          type: string
          format: date
      - in: query
        name: full_name
        schema:
          type: string
        description: جست‌وجو در نام کامل
      - in: query
        name: gender
        schema:
          type: string
          title: جنسیت
          enum:
          - FEMALE
          - MALE
          - OTHER
          - UNDISCLOSED
        description: |-
          * `MALE` - مرد
          * `FEMALE` - زن
          * `OTHER` - سایر
          * `UNDISCLOSED` - اعلام‌نشده
      - in: query
        name: has_user_account
        schema:
          type: boolean
        description: دارای حساب کاربری
      - in: query
        name: national_id
        schema:
          type: string
      - in: query
        name: nationality
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: search
        schema:
          type: string
        description: جست‌وجوی متنی در نام، نام خانوادگی و شماره ملی
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - DECEASED
          - INACTIVE
          - MERGED
        description: |-
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `MERGED` - ادغام‌شده در پرونده دیگر
          * `DECEASED` - متوفی
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPersonListList'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '401':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نیازمند احراز هویت
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا خارج از Scope
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت یا نسخه
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
  /api/v1/iam/persons/{id}/:
    put:
      operationId: iam_persons_update
      description: |-
        اشخاص — هویت پایه مستقل از نقش.

        بخش ۵ سند تحلیل: «Person هویت پایه فرد؛ مستقل از نقش دانش‌آموز، ولی یا کارمند.»

        جایگزینی کامل شخص.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `person.update`
      summary: ویرایش کامل شخص
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شخص را شناسایی میکند.
        required: true
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PersonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PersonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_persons_retrieve
      description: |-
        اشخاص — هویت پایه مستقل از نقش.

        بخش ۵ سند تحلیل: «Person هویت پایه فرد؛ مستقل از نقش دانش‌آموز، ولی یا کارمند.»

        دریافت جزئیات یک شخص با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر شخص خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `person.read`
      summary: جزئیات شخص
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شخص را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_persons_partial_update
      description: |-
        اشخاص — هویت پایه مستقل از نقش.

        بخش ۵ سند تحلیل: «Person هویت پایه فرد؛ مستقل از نقش دانش‌آموز، ولی یا کارمند.»

        ویرایش جزئی شخص.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `person.update`
      summary: ویرایش جزئی شخص
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شخص را شناسایی میکند.
        required: true
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPersonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPersonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPersonRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_persons_destroy
      description: |-
        اشخاص — هویت پایه مستقل از نقش.

        بخش ۵ سند تحلیل: «Person هویت پایه فرد؛ مستقل از نقش دانش‌آموز، ولی یا کارمند.»

        حذف نرم شخص.

        شخص از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `person.delete`
      summary: حذف نرم شخص
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شخص را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/persons/{id}/documents/:
    get:
      operationId: iam_persons_documents_list
      description: |-
        اشخاص — هویت پایه مستقل از نقش.

        بخش ۵ سند تحلیل: «Person هویت پایه فرد؛ مستقل از نقش دانش‌آموز، ولی یا کارمند.»
      summary: مدارک شخص
      parameters:
      - in: query
        name: birth_date_from
        schema:
          type: string
          format: date
      - in: query
        name: birth_date_to
        schema:
          type: string
          format: date
      - in: query
        name: full_name
        schema:
          type: string
        description: جست‌وجو در نام کامل
      - in: query
        name: gender
        schema:
          type: string
          title: جنسیت
          enum:
          - FEMALE
          - MALE
          - OTHER
          - UNDISCLOSED
        description: |-
          * `MALE` - مرد
          * `FEMALE` - زن
          * `OTHER` - سایر
          * `UNDISCLOSED` - اعلام‌نشده
      - in: query
        name: has_user_account
        schema:
          type: boolean
        description: دارای حساب کاربری
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شخص را شناسایی میکند.
        required: true
      - in: query
        name: national_id
        schema:
          type: string
      - in: query
        name: nationality
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - DECEASED
          - INACTIVE
          - MERGED
        description: |-
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `MERGED` - ادغام‌شده در پرونده دیگر
          * `DECEASED` - متوفی
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPersonDocumentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/contact-points/:
    post:
      operationId: iam_contact_points_create
      description: |-
        ایجاد راه تماس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `person.create`
      summary: افزودن راه تماس
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactPointRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ContactPointRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ContactPointRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactPoint'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_contact_points_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده راه‌های تماس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `person`، `contact_type`، `is_primary`، `is_verified`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `person.read`
      summary: فهرست راه‌های تماس
      parameters:
      - in: query
        name: contact_type
        schema:
          type: string
          title: نوع تماس
          enum:
          - EMAIL
          - EMERGENCY
          - MOBILE
          - PHONE
        description: |-
          * `MOBILE` - تلفن همراه
          * `PHONE` - تلفن ثابت
          * `EMAIL` - پست الکترونیک
          * `EMERGENCY` - تماس اضطراری
      - in: query
        name: is_primary
        schema:
          type: boolean
      - in: query
        name: is_verified
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: person
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedContactPointList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/contact-points/{id}/:
    put:
      operationId: iam_contact_points_update
      description: |-
        جایگزینی کامل راه تماس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش کامل راه تماس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این راه تماس را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContactPointRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ContactPointRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ContactPointRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactPoint'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_contact_points_retrieve
      description: |-
        دریافت جزئیات یک راه تماس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر راه تماس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `person.read`
      summary: جزئیات راه تماس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این راه تماس را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactPoint'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_contact_points_partial_update
      description: |-
        ویرایش جزئی راه تماس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش جزئی راه تماس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این راه تماس را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedContactPointRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedContactPointRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedContactPointRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactPoint'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_contact_points_destroy
      description: |-
        حذف نرم راه تماس.

        راه تماس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `person.delete`
      summary: حذف راه تماس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این راه تماس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/addresses/:
    post:
      operationId: iam_addresses_create
      description: |-
        ایجاد نشانی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `person.create`
      summary: ایجاد نشانی
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddressRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AddressRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AddressRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_addresses_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نشانی‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `city`، `province`، `postal_code`

        **جست‌وجو** — پارامتر `search` روی `line`، `city`، `postal_code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `person.read`
      summary: فهرست نشانی‌ها
      parameters:
      - in: query
        name: city
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: postal_code
        schema:
          type: string
      - in: query
        name: province
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAddressList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/addresses/{id}/:
    put:
      operationId: iam_addresses_update
      description: |-
        جایگزینی کامل نشانی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش کامل نشانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddressRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AddressRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AddressRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_addresses_retrieve
      description: |-
        دریافت جزئیات یک نشانی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نشانی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `person.read`
      summary: جزئیات نشانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_addresses_partial_update
      description: |-
        ویرایش جزئی نشانی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش جزئی نشانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAddressRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAddressRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAddressRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_addresses_destroy
      description: |-
        حذف نرم نشانی.

        نشانی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `person.delete`
      summary: حذف نشانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/person-addresses/:
    post:
      operationId: iam_person_addresses_create
      description: |-
        ایجاد نشانی شخص تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `person.create`
      summary: ایجاد نشانی شخص
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonAddressRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PersonAddressRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PersonAddressRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonAddress'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_person_addresses_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نشانی‌های اشخاص.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `person`، `address_type`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `person.read`
      summary: نشانی‌های اشخاص
      parameters:
      - in: query
        name: address_type
        schema:
          type: string
          title: نوع نشانی
          enum:
          - BILLING
          - HOME
          - OTHER
          - WORK
        description: |-
          * `HOME` - محل سکونت
          * `WORK` - محل کار
          * `BILLING` - نشانی صورتحساب
          * `OTHER` - سایر
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: person
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPersonAddressList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/person-addresses/{id}/:
    put:
      operationId: iam_person_addresses_update
      description: |-
        جایگزینی کامل نشانی شخص.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش کامل نشانی شخص
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی شخص را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonAddressRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PersonAddressRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PersonAddressRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonAddress'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_person_addresses_retrieve
      description: |-
        دریافت جزئیات یک نشانی شخص با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نشانی شخص خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `person.read`
      summary: جزئیات نشانی شخص
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی شخص را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonAddress'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_person_addresses_partial_update
      description: |-
        ویرایش جزئی نشانی شخص.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش جزئی نشانی شخص
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی شخص را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPersonAddressRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPersonAddressRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPersonAddressRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonAddress'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_person_addresses_destroy
      description: |-
        حذف نرم نشانی شخص.

        نشانی شخص از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `person.delete`
      summary: حذف نشانی شخص
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نشانی شخص را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/person-documents/:
    post:
      operationId: iam_person_documents_create
      description: |-
        ایجاد مدرک شخص تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `person.create`
      summary: بارگذاری مدرک
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonDocumentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PersonDocumentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PersonDocumentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonDocument'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_person_documents_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده مدارک اشخاص.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `person`، `document_type`، `verification_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `person.read`
      summary: مدارک اشخاص
      parameters:
      - in: query
        name: document_type
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: person
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: verification_status
        schema:
          type: string
          title: وضعیت تأیید
          enum:
          - EXPIRED
          - PENDING
          - REJECTED
          - VERIFIED
        description: |-
          * `PENDING` - در انتظار بررسی
          * `VERIFIED` - تأییدشده
          * `REJECTED` - ردشده
          * `EXPIRED` - منقضی
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPersonDocumentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/person-documents/{id}/:
    put:
      operationId: iam_person_documents_update
      description: |-
        جایگزینی کامل مدرک شخص.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش کامل مدرک شخص
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرک شخص را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonDocumentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PersonDocumentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PersonDocumentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_person_documents_retrieve
      description: |-
        دریافت جزئیات یک مدرک شخص با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر مدرک شخص خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `person.read`
      summary: جزئیات مدرک شخص
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرک شخص را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_person_documents_partial_update
      description: |-
        ویرایش جزئی مدرک شخص.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `person.update`
      summary: ویرایش جزئی مدرک شخص
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرک شخص را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPersonDocumentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPersonDocumentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPersonDocumentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_person_documents_destroy
      description: |-
        حذف نرم مدرک شخص.

        مدرک شخص از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `person.delete`
      summary: حذف مدرک شخص
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرک شخص را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/person-documents/{id}/verify/:
    post:
      operationId: iam_person_documents_verify_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تأیید مدرک
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرک شخص را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/iam/permissions/:
    get:
      operationId: iam_permissions_list
      description: کاتالوگ کامل مجوزهای اتمی به شکل `resource.action`. فرانت می‌تواند
        از این فهرست برای ساخت صفحه «تعریف نقش» استفاده کند.
      summary: فهرست مجوزهای سامانه
      parameters:
      - in: query
        name: action
        schema:
          type: string
      - in: query
        name: is_sensitive
        schema:
          type: boolean
      - in: query
        name: module
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - in: query
        name: resource
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Permission'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/permissions/{id}/:
    get:
      operationId: iam_permissions_retrieve
      description: |-
        دریافت جزئیات یک مجوز با شناسه UUID.

        اگر مجوز خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `role.read`
      summary: جزئیات مجوز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مجوز را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Permission'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/iam/roles/:
    post:
      operationId: iam_roles_create
      description: |-
        ایجاد نقش تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `role.create`
      summary: ایجاد نقش سفارشی
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RoleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RoleRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_roles_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نقش‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `is_system`، `requires_mfa`

        **جست‌وجو** — پارامتر `search` روی `code`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `role.read`
      summary: فهرست نقش‌ها
      parameters:
      - in: query
        name: is_system
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: requires_mfa
        schema:
          type: boolean
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRoleList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/roles/{id}/:
    put:
      operationId: iam_roles_update
      description: |-
        جایگزینی کامل نقش.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `role.update`
      summary: ویرایش نقش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نقش را شناسایی میکند.
        required: true
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RoleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RoleRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_roles_retrieve
      description: |-
        دریافت جزئیات یک نقش با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نقش خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `role.read`
      summary: جزئیات نقش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نقش را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_roles_partial_update
      description: |-
        ویرایش جزئی نقش.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `role.update`
      summary: ویرایش جزئی نقش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نقش را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedRoleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedRoleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedRoleRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_roles_destroy
      description: نقش سیستمی قابل حذف نیست (بخش ۷.۳).
      summary: حذف نقش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نقش را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/users/:
    post:
      operationId: iam_users_create
      description: اگر `password` ارسال نشود، حساب بدون رمز قابل استفاده ساخته می‌شود
        و کاربر باید از مسیر بازیابی رمز، رمز خود را تعیین کند.
      summary: ایجاد حساب کاربری
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserAccountCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserAccountCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserAccountCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccountCreate'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_users_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده حساب‌های کاربری.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `status`، `is_active`، `mfa_enabled`، `must_change_password`، `role_code`، `scope_id`

        **جست‌وجو** — پارامتر `search` روی `username`، `email`، `mobile`، `person__first_name`، `person__last_name`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `user.read`
      summary: فهرست حساب‌های کاربری
      parameters:
      - in: query
        name: is_active
        schema:
          type: boolean
      - in: query
        name: mfa_enabled
        schema:
          type: boolean
      - in: query
        name: must_change_password
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: role_code
        schema:
          type: string
        description: کد نقش
      - in: query
        name: scope_id
        schema:
          type: string
          format: uuid
        description: شناسه دامنه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت حساب
          enum:
          - ACTIVE
          - DISABLED
          - INVITED
          - LOCKED
          - SUSPENDED
        description: |-
          * `INVITED` - دعوت‌شده
          * `ACTIVE` - فعال
          * `LOCKED` - قفل‌شده
          * `SUSPENDED` - تعلیق‌شده
          * `DISABLED` - غیرفعال
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserAccountList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/users/{id}/:
    put:
      operationId: iam_users_update
      description: |-
        جایگزینی کامل حساب کاربری.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `user.update`
      summary: ویرایش کامل حساب کاربری
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب کاربری را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserAccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserAccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserAccountRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_users_retrieve
      description: |-
        دریافت جزئیات یک حساب کاربری با شناسه UUID.

        اگر حساب کاربری خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `user.read`
      summary: جزئیات حساب کاربری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب کاربری را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_users_partial_update
      description: |-
        ویرایش جزئی حساب کاربری.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `user.update`
      summary: ویرایش جزئی حساب کاربری
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب کاربری را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedUserAccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedUserAccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedUserAccountRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_users_destroy
      description: |-
        حذف حساب کاربری.

        این منبع از نوع سند قطعی است و حذف فیزیکی ندارد؛ اگر عملیات مجاز نباشد پاسخ `422` با کد قاعده کسب‌وکار برمی‌گردد و اصلاح فقط با سند معکوس ممکن است (بخش ۷.۸ سند تحلیل).

        **مجوز لازم:** `user.delete`
      summary: حذف حساب کاربری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب کاربری را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/users/{id}/disable/:
    post:
      operationId: iam_users_disable_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: غیرفعال‌سازی حساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب کاربری را شناسایی میکند.
        required: true
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '401':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نیازمند احراز هویت
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا خارج از Scope
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت یا نسخه
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
  /api/v1/iam/users/{id}/enable/:
    post:
      operationId: iam_users_enable_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: فعال‌سازی مجدد حساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب کاربری را شناسایی میکند.
        required: true
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/iam/role-assignments/:
    post:
      operationId: iam_role_assignments_create
      description: '`scopeType` و `scopeId` دامنه اعمال نقش را تعیین می‌کنند (بخش
        ۳.۲). ترکیب باید با `allowedScopeTypes` نقش سازگار باشد.'
      summary: انتساب نقش به کاربر
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRoleAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserRoleAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserRoleAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleAssignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_role_assignments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده انتساب‌های نقش.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `user`، `role`، `scope_type`، `scope_id`، `status`، `role_code`، `active_on`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `role.read`
      summary: فهرست انتساب‌های نقش
      parameters:
      - in: query
        name: active_on
        schema:
          type: string
          format: date
        description: فعال در تاریخ مشخص
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: role
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: role_code
        schema:
          type: string
      - in: query
        name: scope_id
        schema:
          type: string
          format: uuid
      - in: query
        name: scope_type
        schema:
          type: string
          title: نوع دامنه
          enum:
          - ACADEMIC_YEAR
          - CAMPUS
          - CLASS_GROUP
          - COURSE_OFFERING
          - SCHOOL
          - SELF
          - TENANT
        description: |-
          * `TENANT` - کل سازمان
          * `SCHOOL` - مدرسه
          * `CAMPUS` - شعبه
          * `ACADEMIC_YEAR` - سال تحصیلی
          * `CLASS_GROUP` - کلاس
          * `COURSE_OFFERING` - ارائه درس
          * `SELF` - فقط رکوردهای خود
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - EXPIRED
          - REVOKED
        description: |-
          * `ACTIVE` - فعال
          * `EXPIRED` - منقضی
          * `REVOKED` - لغوشده
      - in: query
        name: user
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserRoleAssignmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/role-assignments/{id}/:
    put:
      operationId: iam_role_assignments_update
      description: |-
        جایگزینی کامل انتساب نقش.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `role.update`
      summary: ویرایش کامل انتساب نقش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب نقش را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRoleAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserRoleAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserRoleAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_role_assignments_retrieve
      description: |-
        دریافت جزئیات یک انتساب نقش با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر انتساب نقش خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `role.read`
      summary: جزئیات انتساب نقش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب نقش را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_role_assignments_partial_update
      description: |-
        ویرایش جزئی انتساب نقش.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `role.update`
      summary: ویرایش جزئی انتساب نقش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب نقش را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedUserRoleAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedUserRoleAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedUserRoleAssignmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_role_assignments_destroy
      description: |-
        حذف نرم انتساب نقش.

        انتساب نقش از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `role.delete`
      summary: حذف انتساب نقش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب نقش را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/role-assignments/{id}/revoke/:
    post:
      operationId: iam_role_assignments_revoke_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: لغو انتساب نقش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب نقش را شناسایی میکند.
        required: true
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/iam/access-reviews/:
    post:
      operationId: iam_access_reviews_create
      description: |-
        ایجاد بازبینی دسترسی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `role.create`
      summary: ایجاد دوره بازبینی
      tags:
      - IAM
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessReviewRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AccessReviewRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AccessReviewRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReview'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_access_reviews_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده بازبینی‌های دسترسی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `role.read`
      summary: دوره‌های بازبینی دسترسی
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CLOSED
          - IN_PROGRESS
          - OPEN
        description: |-
          * `OPEN` - باز
          * `IN_PROGRESS` - در حال بررسی
          * `CLOSED` - بسته
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAccessReviewList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/access-reviews/{id}/:
    put:
      operationId: iam_access_reviews_update
      description: |-
        جایگزینی کامل بازبینی دسترسی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `role.update`
      summary: ویرایش کامل بازبینی دسترسی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessReviewRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AccessReviewRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AccessReviewRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReview'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_access_reviews_retrieve
      description: |-
        دریافت جزئیات یک بازبینی دسترسی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر بازبینی دسترسی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `role.read`
      summary: جزئیات بازبینی دسترسی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReview'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_access_reviews_partial_update
      description: |-
        ویرایش جزئی بازبینی دسترسی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `role.update`
      summary: ویرایش جزئی بازبینی دسترسی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAccessReviewRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAccessReviewRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAccessReviewRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReview'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_access_reviews_destroy
      description: |-
        حذف نرم بازبینی دسترسی.

        بازبینی دسترسی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `role.delete`
      summary: حذف بازبینی دسترسی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی دسترسی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/access-reviews/{id}/close/:
    post:
      operationId: iam_access_reviews_close_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بستن دوره بازبینی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReview'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/iam/access-reviews/{id}/populate/:
    post:
      operationId: iam_access_reviews_populate_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تولید اقلام بازبینی برای همه انتساب‌های نقش حساس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/iam/access-review-items/:
    post:
      operationId: iam_access_review_items_create
      description: |-
        ایجاد قلم بازبینی دسترسی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `role.create`
      summary: ایجاد قلم بازبینی دسترسی
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessReviewItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AccessReviewItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AccessReviewItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReviewItem'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_access_review_items_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام بازبینی دسترسی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `review`، `decision`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `role.read`
      summary: اقلام بازبینی دسترسی
      parameters:
      - in: query
        name: decision
        schema:
          type: string
          title: تصمیم
          enum:
          - KEEP
          - MODIFY
          - PENDING
          - REVOKE
        description: |-
          * `PENDING` - در انتظار
          * `KEEP` - ابقا
          * `REVOKE` - لغو دسترسی
          * `MODIFY` - اصلاح دامنه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: review
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAccessReviewItemList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/access-review-items/{id}/:
    put:
      operationId: iam_access_review_items_update
      description: |-
        جایگزینی کامل قلم بازبینی دسترسی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `role.update`
      summary: ویرایش کامل قلم بازبینی دسترسی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessReviewItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AccessReviewItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AccessReviewItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReviewItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: iam_access_review_items_retrieve
      description: |-
        دریافت جزئیات یک قلم بازبینی دسترسی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم بازبینی دسترسی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `role.read`
      summary: جزئیات قلم بازبینی دسترسی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReviewItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: iam_access_review_items_partial_update
      description: |-
        ویرایش جزئی قلم بازبینی دسترسی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `role.update`
      summary: ویرایش جزئی قلم بازبینی دسترسی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم بازبینی دسترسی را شناسایی میکند.
        required: true
      tags:
      - iam
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAccessReviewItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAccessReviewItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAccessReviewItemRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessReviewItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: iam_access_review_items_destroy
      description: |-
        حذف نرم قلم بازبینی دسترسی.

        قلم بازبینی دسترسی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `role.delete`
      summary: حذف قلم بازبینی دسترسی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم بازبینی دسترسی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - iam
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/iam/audit-logs/:
    get:
      operationId: iam_audit_logs_list
      description: رکوردهای ممیزی فقط‌خواندنی و Append-only هستند (بخش ۷.۳). صفحه‌بندی
        این فهرست از نوع Cursor است.
      summary: گزارش ممیزی
      parameters:
      - in: query
        name: action
        schema:
          type: string
          title: عمل
          enum:
          - BREAK_GLASS
          - CREATE
          - DELETE
          - EXPORT
          - LOGIN
          - LOGIN_FAILED
          - LOGOUT
          - PERMISSION_CHANGE
          - READ_SENSITIVE
          - STATE_TRANSITION
          - UPDATE
        description: |-
          * `CREATE` - ایجاد
          * `UPDATE` - ویرایش
          * `DELETE` - حذف
          * `READ_SENSITIVE` - مشاهده داده حساس
          * `LOGIN` - ورود
          * `LOGIN_FAILED` - ورود ناموفق
          * `LOGOUT` - خروج
          * `PERMISSION_CHANGE` - تغییر مجوز
          * `STATE_TRANSITION` - تغییر وضعیت
          * `EXPORT` - خروجی‌گیری
          * `BREAK_GLASS` - دسترسی اضطراری
      - in: query
        name: actor_user_id
        schema:
          type: string
          format: uuid
      - in: query
        name: classification
        schema:
          type: string
          title: طبقه‌بندی
          enum:
          - CONFIDENTIAL
          - INTERNAL
          - PUBLIC
          - RESTRICTED
        description: |-
          * `PUBLIC` - عمومی
          * `INTERNAL` - داخلی
          * `CONFIDENTIAL` - محرمانه
          * `RESTRICTED` - بسیار حساس
      - in: query
        name: correlation_id
        schema:
          type: string
      - name: cursor
        required: false
        in: query
        description: مقدار نشانگر صفحه‌بندی.
        schema:
          type: string
      - in: query
        name: entity_id
        schema:
          type: string
          format: uuid
      - in: query
        name: entity_type
        schema:
          type: string
      - in: query
        name: occurred_from
        schema:
          type: string
          format: date-time
      - in: query
        name: occurred_to
        schema:
          type: string
          format: date-time
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAuditLogList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/iam/audit-logs/{id}/:
    get:
      operationId: iam_audit_logs_retrieve
      description: |-
        دریافت جزئیات یک رکورد ممیزی با شناسه UUID.

        اگر رکورد ممیزی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `audit.read`
      summary: جزئیات رکورد ممیزی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رکورد ممیزی را شناسایی میکند.
        required: true
      tags:
      - IAM
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/org/schools/:
    post:
      operationId: org_schools_create
      description: |-
        ایجاد مدرسه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `school.create`
      summary: ایجاد مدرسه
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SchoolRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SchoolRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SchoolRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/School'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_schools_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده مدارس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school_type`، `status`

        **جست‌وجو** — پارامتر `search` روی `name`، `code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `school.read`
      summary: فهرست مدارس
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school_type
        schema:
          type: string
          title: نوع مدرسه
          enum:
          - COMBINED
          - LOWER_SECONDARY
          - PRESCHOOL
          - PRIMARY
          - UPPER_SECONDARY
        description: |-
          * `PRESCHOOL` - پیش‌دبستان
          * `PRIMARY` - دبستان
          * `LOWER_SECONDARY` - متوسطه اول
          * `UPPER_SECONDARY` - متوسطه دوم
          * `COMBINED` - مجتمع چندمقطعی
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - DRAFT
          - INACTIVE
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `ARCHIVED` - بایگانی‌شده
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedSchoolList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/schools/{id}/:
    put:
      operationId: org_schools_update
      description: |-
        جایگزینی کامل مدرسه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `school.update`
      summary: ویرایش مدرسه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرسه را شناسایی میکند.
        required: true
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SchoolRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SchoolRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SchoolRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/School'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_schools_retrieve
      description: |-
        دریافت جزئیات یک مدرسه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر مدرسه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `school.read`
      summary: جزئیات مدرسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرسه را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/School'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_schools_partial_update
      description: |-
        ویرایش جزئی مدرسه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `school.update`
      summary: ویرایش جزئی مدرسه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرسه را شناسایی میکند.
        required: true
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedSchoolRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedSchoolRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedSchoolRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/School'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_schools_destroy
      description: |-
        حذف نرم مدرسه.

        مدرسه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `school.delete`
      summary: حذف نرم مدرسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مدرسه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/campuses/:
    post:
      operationId: org_campuses_create
      description: |-
        ایجاد شعبه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `campus.create`
      summary: ایجاد شعبه
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampusRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CampusRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CampusRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campus'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_campuses_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده شعب.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`

        **جست‌وجو** — پارامتر `search` روی `name`، `code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `campus.read`
      summary: فهرست شعب
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - DRAFT
          - INACTIVE
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `ARCHIVED` - بایگانی‌شده
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCampusList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/campuses/{id}/:
    put:
      operationId: org_campuses_update
      description: |-
        جایگزینی کامل شعبه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `campus.update`
      summary: ویرایش کامل شعبه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شعبه را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampusRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CampusRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CampusRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campus'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_campuses_retrieve
      description: |-
        دریافت جزئیات یک شعبه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر شعبه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `campus.read`
      summary: جزئیات شعبه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شعبه را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campus'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_campuses_partial_update
      description: |-
        ویرایش جزئی شعبه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `campus.update`
      summary: ویرایش جزئی شعبه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شعبه را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCampusRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCampusRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCampusRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campus'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_campuses_destroy
      description: |-
        حذف نرم شعبه.

        شعبه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `campus.delete`
      summary: حذف شعبه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شعبه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/academic-years/:
    post:
      operationId: org_academic_years_create
      description: |-
        ایجاد سال تحصیلی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `academic_year.create`
      summary: ایجاد سال تحصیلی
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AcademicYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AcademicYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AcademicYearRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_academic_years_list
      description: برای صفحه «سال و ترم» و همچنین Dropdown انتخاب محیط کاری.
      summary: فهرست سال‌های تحصیلی
      parameters:
      - in: query
        name: is_default
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - CLOSED
          - CLOSING
          - PLANNING
        description: |-
          * `PLANNING` - در حال برنامه‌ریزی
          * `ACTIVE` - فعال
          * `CLOSING` - در حال بستن
          * `CLOSED` - بسته
          * `ARCHIVED` - بایگانی
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAcademicYearList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/academic-years/{id}/:
    put:
      operationId: org_academic_years_update
      description: |-
        جایگزینی کامل سال تحصیلی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `academic_year.update`
      summary: ویرایش کامل سال تحصیلی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AcademicYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AcademicYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AcademicYearRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_academic_years_retrieve
      description: |-
        دریافت جزئیات یک سال تحصیلی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سال تحصیلی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `academic_year.read`
      summary: جزئیات سال تحصیلی به همراه ترم‌ها
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_academic_years_partial_update
      description: |-
        ویرایش جزئی سال تحصیلی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `academic_year.update`
      summary: ویرایش جزئی سال تحصیلی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAcademicYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAcademicYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAcademicYearRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_academic_years_destroy
      description: |-
        حذف نرم سال تحصیلی.

        سال تحصیلی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `academic_year.delete`
      summary: حذف سال تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/academic-years/{id}/activate/:
    post:
      operationId: org_academic_years_activate_create
      description: 'Checklist فعال‌سازی را کنترل می‌کند (بخش ۱۱.۱): وجود ترم‌ها، پوشش
        بازه، و تعریف پایه‌ها. با موفقیت، این سال به سال پیش‌فرض مدرسه تبدیل می‌شود
        و سال پیش‌فرض قبلی از حالت پیش‌فرض خارج می‌گردد.'
      summary: فعال‌سازی سال تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                خطایپیش‌نیازناقص:
                  value:
                    code: ACADEMIC_YEAR_NOT_READY
                    message: پیش‌نیازهای فعال‌سازی سال تحصیلی کامل نیست.
                    correlationId: 7b3d9e1a
                    fieldErrors:
                    - field: terms
                      reason: حداقل یک ترم باید تعریف شود.
                    retryable: false
                  summary: خطای پیش‌نیاز ناقص
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/org/academic-years/{id}/clone-structure/:
    post:
      operationId: org_academic_years_clone_structure_create
      description: فقط ساختار (کلاس‌ها) کپی می‌شود. دانش‌آموز، بدهی، نمره و حضور کپی
        نمی‌شوند (بخش ۱۱.۱).
      summary: ایجاد ساختار سال جدید از روی سال جاری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloneYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CloneYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CloneYearRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/org/academic-years/{id}/close/:
    post:
      operationId: org_academic_years_close_create
      description: پیش از بستن، وجود ثبت‌نام‌های در وضعیت میانی کنترل می‌شود. سال
        بسته فقط‌خواندنی است.
      summary: بستن سال تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/YearCloseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/YearCloseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/YearCloseRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/org/academic-years/{id}/reopen/:
    post:
      operationId: org_academic_years_reopen_create
      description: عملیات حساس؛ نیازمند مجوز سطح بالا و ثبت علت (بخش ۱۱.۱).
      summary: بازگشایی سال بسته
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال تحصیلی را شناسایی میکند.
        required: true
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/YearCloseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/YearCloseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/YearCloseRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcademicYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/org/terms/:
    post:
      operationId: org_terms_create
      description: |-
        ایجاد ترم تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `academic_year.create`
      summary: ایجاد ترم
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TermRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TermRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TermRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Term'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_terms_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده ترم‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `academic_year`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `academic_year.read`
      summary: فهرست ترم‌ها
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CLOSED
          - PLANNED
        description: |-
          * `PLANNED` - برنامه‌ریزی‌شده
          * `ACTIVE` - جاری
          * `CLOSED` - بسته
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTermList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/terms/{id}/:
    put:
      operationId: org_terms_update
      description: |-
        جایگزینی کامل ترم.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `academic_year.update`
      summary: ویرایش کامل ترم
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترم را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TermRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TermRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TermRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Term'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_terms_retrieve
      description: |-
        دریافت جزئیات یک ترم با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ترم خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `academic_year.read`
      summary: جزئیات ترم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترم را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Term'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_terms_partial_update
      description: |-
        ویرایش جزئی ترم.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `academic_year.update`
      summary: ویرایش جزئی ترم
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترم را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTermRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTermRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTermRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Term'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_terms_destroy
      description: |-
        حذف نرم ترم.

        ترم از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `academic_year.delete`
      summary: حذف ترم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترم را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/grade-levels/:
    post:
      operationId: org_grade_levels_create
      description: |-
        ایجاد پایه تحصیلی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `grade_level.create`
      summary: ایجاد پایه
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeLevelRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeLevelRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeLevelRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeLevel'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_grade_levels_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پایه‌های تحصیلی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`، `stage`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `grade_level.read`
      summary: فهرست پایه‌های تحصیلی
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: stage
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - DRAFT
          - INACTIVE
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `ARCHIVED` - بایگانی‌شده
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGradeLevelList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/grade-levels/{id}/:
    put:
      operationId: org_grade_levels_update
      description: |-
        جایگزینی کامل پایه تحصیلی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `grade_level.update`
      summary: ویرایش کامل پایه تحصیلی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پایه تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeLevelRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeLevelRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeLevelRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeLevel'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_grade_levels_retrieve
      description: |-
        دریافت جزئیات یک پایه تحصیلی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پایه تحصیلی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `grade_level.read`
      summary: جزئیات پایه تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پایه تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeLevel'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_grade_levels_partial_update
      description: |-
        ویرایش جزئی پایه تحصیلی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `grade_level.update`
      summary: ویرایش جزئی پایه تحصیلی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پایه تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedGradeLevelRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedGradeLevelRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedGradeLevelRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeLevel'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_grade_levels_destroy
      description: |-
        حذف نرم پایه تحصیلی.

        پایه تحصیلی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `grade_level.delete`
      summary: حذف پایه تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پایه تحصیلی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/programs/:
    post:
      operationId: org_programs_create
      description: |-
        ایجاد رشته تحصیلی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `course.create`
      summary: ایجاد رشته تحصیلی
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudyProgramRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudyProgramRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudyProgramRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudyProgram'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_programs_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده رشته‌های تحصیلی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`

        **جست‌وجو** — پارامتر `search` روی `title`، `code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `course.read`
      summary: فهرست رشته‌ها
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - DRAFT
          - INACTIVE
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `ARCHIVED` - بایگانی‌شده
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudyProgramList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/programs/{id}/:
    put:
      operationId: org_programs_update
      description: |-
        جایگزینی کامل رشته تحصیلی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `course.update`
      summary: ویرایش کامل رشته تحصیلی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رشته تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudyProgramRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudyProgramRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudyProgramRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudyProgram'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_programs_retrieve
      description: |-
        دریافت جزئیات یک رشته تحصیلی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رشته تحصیلی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `course.read`
      summary: جزئیات رشته تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رشته تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudyProgram'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_programs_partial_update
      description: |-
        ویرایش جزئی رشته تحصیلی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `course.update`
      summary: ویرایش جزئی رشته تحصیلی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رشته تحصیلی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudyProgramRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudyProgramRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudyProgramRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudyProgram'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_programs_destroy
      description: |-
        حذف نرم رشته تحصیلی.

        رشته تحصیلی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `course.delete`
      summary: حذف رشته تحصیلی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رشته تحصیلی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/courses/:
    post:
      operationId: org_courses_create
      description: |-
        ایجاد درس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `course.create`
      summary: تعریف درس
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CourseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CourseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Course'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_courses_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده دروس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`، `assessment_scheme`

        **جست‌وجو** — پارامتر `search` روی `title`، `code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `course.read`
      summary: فهرست دروس
      parameters:
      - in: query
        name: assessment_scheme
        schema:
          type: string
          title: طرح ارزشیابی
          enum:
          - COMPETENCY
          - DESCRIPTIVE
          - LETTER
          - MIXED
          - NUMERIC
        description: |-
          * `NUMERIC` - عددی
          * `DESCRIPTIVE` - توصیفی
          * `LETTER` - حرفی
          * `COMPETENCY` - شایستگی‌محور
          * `MIXED` - ترکیبی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - DRAFT
          - INACTIVE
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `ARCHIVED` - بایگانی‌شده
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCourseList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/courses/{id}/:
    put:
      operationId: org_courses_update
      description: |-
        جایگزینی کامل درس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `course.update`
      summary: ویرایش کامل درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CourseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CourseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Course'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_courses_retrieve
      description: |-
        دریافت جزئیات یک درس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر درس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `course.read`
      summary: جزئیات درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Course'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_courses_partial_update
      description: |-
        ویرایش جزئی درس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `course.update`
      summary: ویرایش جزئی درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCourseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCourseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCourseRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Course'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_courses_destroy
      description: |-
        حذف نرم درس.

        درس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `course.delete`
      summary: حذف درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/program-courses/:
    post:
      operationId: org_program_courses_create
      description: |-
        ایجاد درس رشته/پایه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `course.create`
      summary: ایجاد درس رشته/پایه
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProgramCourseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProgramCourseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProgramCourseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgramCourse'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_program_courses_list
      description: برنامه درسی مصوب هر رشته/پایه با ساعات هفتگی.
      summary: نگاشت درس به رشته و پایه
      parameters:
      - in: query
        name: course
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: is_required
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: program
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedProgramCourseList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/program-courses/{id}/:
    put:
      operationId: org_program_courses_update
      description: |-
        جایگزینی کامل درس رشته/پایه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `course.update`
      summary: ویرایش کامل درس رشته/پایه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس رشته/پایه را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProgramCourseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProgramCourseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProgramCourseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgramCourse'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_program_courses_retrieve
      description: |-
        دریافت جزئیات یک درس رشته/پایه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر درس رشته/پایه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `course.read`
      summary: جزئیات درس رشته/پایه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس رشته/پایه را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgramCourse'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_program_courses_partial_update
      description: |-
        ویرایش جزئی درس رشته/پایه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `course.update`
      summary: ویرایش جزئی درس رشته/پایه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس رشته/پایه را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedProgramCourseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedProgramCourseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedProgramCourseRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgramCourse'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_program_courses_destroy
      description: |-
        حذف نرم درس رشته/پایه.

        درس رشته/پایه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `course.delete`
      summary: حذف درس رشته/پایه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درس رشته/پایه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/rooms/:
    post:
      operationId: org_rooms_create
      description: |-
        ایجاد اتاق تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `room.create`
      summary: ایجاد اتاق
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoomRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RoomRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RoomRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Room'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_rooms_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اتاق‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `campus`، `room_type`، `status`

        **جست‌وجو** — پارامتر `search` روی `code`، `title`، `building`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `room.read`
      summary: فهرست اتاق‌ها
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: room_type
        schema:
          type: string
          title: نوع اتاق
          enum:
          - CLASSROOM
          - EXAM_HALL
          - GYM
          - LAB
          - LIBRARY
          - OFFICE
          - OTHER
          - WORKSHOP
        description: |-
          * `CLASSROOM` - کلاس درس
          * `LAB` - آزمایشگاه
          * `WORKSHOP` - کارگاه
          * `GYM` - سالن ورزش
          * `LIBRARY` - کتابخانه
          * `EXAM_HALL` - حوزه آزمون
          * `OFFICE` - اداری
          * `OTHER` - سایر
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ARCHIVED
          - DRAFT
          - INACTIVE
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `INACTIVE` - غیرفعال
          * `ARCHIVED` - بایگانی‌شده
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRoomList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/rooms/{id}/:
    put:
      operationId: org_rooms_update
      description: |-
        جایگزینی کامل اتاق.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `room.update`
      summary: ویرایش کامل اتاق
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اتاق را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoomRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RoomRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RoomRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Room'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_rooms_retrieve
      description: |-
        دریافت جزئیات یک اتاق با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر اتاق خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `room.read`
      summary: جزئیات اتاق
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اتاق را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Room'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_rooms_partial_update
      description: |-
        ویرایش جزئی اتاق.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `room.update`
      summary: ویرایش جزئی اتاق
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اتاق را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedRoomRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedRoomRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedRoomRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Room'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_rooms_destroy
      description: |-
        حذف نرم اتاق.

        اتاق از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `room.delete`
      summary: حذف اتاق
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اتاق را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/class-groups/:
    post:
      operationId: org_class_groups_create
      description: |-
        ایجاد کلاس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `class_group.create`
      summary: ایجاد کلاس
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClassGroupRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ClassGroupRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ClassGroupRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassGroup'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_class_groups_list
      description: هر رکورد شامل `occupiedSeats` و `availableSeats` است تا فرانت بتواند
        نوار ظرفیت را بدون درخواست اضافی رسم کند.
      summary: فهرست کلاس‌ها
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: has_free_seats
        schema:
          type: boolean
        description: فقط کلاس‌های دارای ظرفیت خالی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: program
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CLOSED
          - DRAFT
          - FULL
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `FULL` - تکمیل ظرفیت
          * `CLOSED` - بسته
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedClassGroupListList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/class-groups/{id}/:
    put:
      operationId: org_class_groups_update
      description: |-
        جایگزینی کامل کلاس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `class_group.update`
      summary: ویرایش کامل کلاس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کلاس را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClassGroupRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ClassGroupRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ClassGroupRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassGroup'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_class_groups_retrieve
      description: |-
        دریافت جزئیات یک کلاس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر کلاس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `class_group.read`
      summary: جزئیات کلاس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کلاس را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassGroup'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_class_groups_partial_update
      description: |-
        ویرایش جزئی کلاس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `class_group.update`
      summary: ویرایش جزئی کلاس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کلاس را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedClassGroupRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedClassGroupRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedClassGroupRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassGroup'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_class_groups_destroy
      description: |-
        حذف نرم کلاس.

        کلاس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `class_group.delete`
      summary: حذف کلاس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کلاس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/class-groups/{id}/offerings/:
    get:
      operationId: org_class_groups_offerings_list
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: ارائه‌های درس کلاس
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: has_free_seats
        schema:
          type: boolean
        description: فقط کلاس‌های دارای ظرفیت خالی
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کلاس را شناسایی میکند.
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: program
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CLOSED
          - DRAFT
          - FULL
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - فعال
          * `FULL` - تکمیل ظرفیت
          * `CLOSED` - بسته
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCourseOfferingList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/class-groups/{id}/timetable/:
    get:
      operationId: org_class_groups_timetable_retrieve
      description: همه اقلام برنامه هفتگی کلاس در همه ارائه‌های درس آن، مرتب بر اساس
        روز و ساعت. مناسب رندر مستقیم جدول برنامه (بخش ۸.۳ سند فرانت).
      summary: برنامه هفتگی کلاس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کلاس را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WeeklyTimetable'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/course-offerings/:
    post:
      operationId: org_course_offerings_create
      description: |-
        ایجاد ارائه درس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `class_group.create`
      summary: ایجاد ارائه درس برای کلاس
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseOfferingRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CourseOfferingRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CourseOfferingRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseOffering'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_course_offerings_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده ارائه‌های درس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `class_group`، `term`، `course`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `class_group.read`
      summary: فهرست ارائه‌های درس
      parameters:
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: course
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CANCELLED
          - COMPLETED
          - PLANNED
        description: |-
          * `PLANNED` - برنامه‌ریزی‌شده
          * `ACTIVE` - جاری
          * `COMPLETED` - پایان‌یافته
          * `CANCELLED` - لغوشده
      - in: query
        name: term
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCourseOfferingList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/course-offerings/{id}/:
    put:
      operationId: org_course_offerings_update
      description: |-
        جایگزینی کامل ارائه درس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `class_group.update`
      summary: ویرایش کامل ارائه درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ارائه درس را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseOfferingRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CourseOfferingRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CourseOfferingRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseOffering'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_course_offerings_retrieve
      description: |-
        دریافت جزئیات یک ارائه درس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ارائه درس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `class_group.read`
      summary: جزئیات ارائه درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ارائه درس را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseOffering'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_course_offerings_partial_update
      description: |-
        ویرایش جزئی ارائه درس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `class_group.update`
      summary: ویرایش جزئی ارائه درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ارائه درس را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCourseOfferingRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCourseOfferingRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCourseOfferingRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseOffering'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_course_offerings_destroy
      description: |-
        حذف نرم ارائه درس.

        ارائه درس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `class_group.delete`
      summary: حذف ارائه درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ارائه درس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/course-offerings/{id}/publish-schedule/:
    post:
      operationId: org_course_offerings_publish_schedule_create
      description: پیش از انتشار، تداخل معلم/اتاق/کلاس بررسی می‌شود.
      summary: انتشار برنامه هفتگی این ارائه درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ارائه درس را شناسایی میکند.
        required: true
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/org/schedule-entries/:
    post:
      operationId: org_schedule_entries_create
      description: پیش از ذخیره، تداخل با برنامه معلم، اتاق و کلاس بررسی می‌شود. در
        صورت تداخل، خطای `SCHEDULE_CONFLICT` با جزئیات هر تداخل در `fieldErrors` برمی‌گردد.
      summary: افزودن قلم برنامه
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleEntry'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
    get:
      operationId: org_schedule_entries_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده برنامه هفتگی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `room`، `teacher_profile_id`، `weekday`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `schedule.read`
      summary: فهرست اقلام برنامه هفتگی
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: room
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - DRAFT
          - PUBLISHED
          - SUPERSEDED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `PUBLISHED` - منتشرشده
          * `SUPERSEDED` - جایگزین‌شده
      - in: query
        name: teacher_profile_id
        schema:
          type: string
        description: فیلتر بر اساس معلم
      - in: query
        name: weekday
        schema:
          type: integer
        description: ۰=شنبه … ۶=جمعه
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedScheduleEntryList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/schedule-entries/check-conflicts/:
    post:
      operationId: org_schedule_entries_check_conflicts_create
      description: برای اعتبارسنجی زنده در فرم برنامه‌ریزی. قلم را ذخیره نمی‌کند و
        فقط فهرست تداخل‌ها را برمی‌گرداند.
      summary: بررسی تداخل بدون ذخیره
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: room
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - DRAFT
          - PUBLISHED
          - SUPERSEDED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `PUBLISHED` - منتشرشده
          * `SUPERSEDED` - جایگزین‌شده
      - in: query
        name: teacher_profile_id
        schema:
          type: string
          format: uuid
      - in: query
        name: weekday
        schema:
          type: integer
          title: روز هفته
          enum:
          - 0
          - 1
          - 2
          - 3
          - 4
          - 5
          - 6
        description: |-
          * `0` - شنبه
          * `1` - یکشنبه
          * `2` - دوشنبه
          * `3` - سه‌شنبه
          * `4` - چهارشنبه
          * `5` - پنجشنبه
          * `6` - جمعه
      tags:
      - Organization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedScheduleConflictList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/org/schedule-entries/{id}/:
    put:
      operationId: org_schedule_entries_update
      description: |-
        جایگزینی کامل قلم برنامه هفتگی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `schedule.update`
      summary: ویرایش کامل قلم برنامه هفتگی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم برنامه هفتگی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ScheduleEntryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleEntry'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_schedule_entries_retrieve
      description: |-
        دریافت جزئیات یک قلم برنامه هفتگی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم برنامه هفتگی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `schedule.read`
      summary: جزئیات قلم برنامه هفتگی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم برنامه هفتگی را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleEntry'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_schedule_entries_partial_update
      description: |-
        ویرایش جزئی قلم برنامه هفتگی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `schedule.update`
      summary: ویرایش جزئی قلم برنامه هفتگی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم برنامه هفتگی را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedScheduleEntryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedScheduleEntryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedScheduleEntryRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleEntry'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_schedule_entries_destroy
      description: |-
        حذف نرم قلم برنامه هفتگی.

        قلم برنامه هفتگی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `schedule.delete`
      summary: حذف قلم برنامه هفتگی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم برنامه هفتگی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/org/calendar-events/:
    post:
      operationId: org_calendar_events_create
      description: |-
        ایجاد رویداد تقویم تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `academic_year.create`
      summary: ایجاد رویداد تقویم
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalendarEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CalendarEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CalendarEventRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEvent'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_calendar_events_list
      description: تعطیلات، بازه امتحانات، جلسات اولیا و مناسبت‌ها.
      summary: تقویم آموزشی
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: event_type
        schema:
          type: string
          title: نوع رویداد
          enum:
          - CEREMONY
          - EXAM_PERIOD
          - FIELD_TRIP
          - HOLIDAY
          - OTHER
          - PARENT_MEETING
        description: |-
          * `HOLIDAY` - تعطیلی
          * `EXAM_PERIOD` - بازه امتحانات
          * `PARENT_MEETING` - جلسه اولیا
          * `CEREMONY` - مراسم
          * `FIELD_TRIP` - اردو
          * `OTHER` - سایر
      - in: query
        name: is_working_day
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Organization
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCalendarEventList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/org/calendar-events/{id}/:
    put:
      operationId: org_calendar_events_update
      description: |-
        جایگزینی کامل رویداد تقویم.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `academic_year.update`
      summary: ویرایش کامل رویداد تقویم
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رویداد تقویم را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalendarEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CalendarEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CalendarEventRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEvent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: org_calendar_events_retrieve
      description: |-
        دریافت جزئیات یک رویداد تقویم با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رویداد تقویم خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `academic_year.read`
      summary: جزئیات رویداد تقویم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رویداد تقویم را شناسایی میکند.
        required: true
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEvent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: org_calendar_events_partial_update
      description: |-
        ویرایش جزئی رویداد تقویم.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `academic_year.update`
      summary: ویرایش جزئی رویداد تقویم
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رویداد تقویم را شناسایی میکند.
        required: true
      tags:
      - org
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCalendarEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCalendarEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCalendarEventRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarEvent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: org_calendar_events_destroy
      description: |-
        حذف نرم رویداد تقویم.

        رویداد تقویم از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `academic_year.delete`
      summary: حذف رویداد تقویم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رویداد تقویم را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - org
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/students/:
    post:
      operationId: students_students_create
      description: |-
        ایجاد دانش‌آموز تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `student.create`
      summary: ایجاد پرونده دانش‌آموز
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Student'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_students_list
      description: فهرست اصلی دانش‌آموزان با فیلترهای سال، شعبه، پایه، کلاس و وضعیت
        (بخش ۱۲.۲ سند تحلیل). هر ردیف شامل کلاس و پایه جاری است تا فرانت نیازی به
        درخواست اضافی نداشته باشد.
      summary: فهرست دانش‌آموزان
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
        description: شناسه سال تحصیلی
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
        description: شعبه
      - in: query
        name: class_group
        schema:
          type: string
        description: شناسه کلاس
      - in: query
        name: enrollment_status
        schema:
          type: string
      - in: query
        name: gender
        schema:
          type: string
      - in: query
        name: grade_level
        schema:
          type: string
        description: شناسه پایه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: search
        schema:
          type: string
        description: جست‌وجو در نام و شماره دانش‌آموزی
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - GRADUATED
          - PROSPECTIVE
          - SUSPENDED
          - TRANSFERRED_OUT
          - WITHDRAWN
        description: |-
          * `PROSPECTIVE` - متقاضی
          * `ACTIVE` - در حال تحصیل
          * `SUSPENDED` - تعلیق
          * `TRANSFERRED_OUT` - انتقال به مدرسه دیگر
          * `WITHDRAWN` - ترک تحصیل
          * `GRADUATED` - فارغ‌التحصیل
      - in: query
        name: student_no
        schema:
          type: string
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentListList'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/students/{id}/:
    put:
      operationId: students_students_update
      description: |-
        جایگزینی کامل دانش‌آموز.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `student.update`
      summary: ویرایش پرونده دانش‌آموز
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Student'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_students_retrieve
      description: |-
        دریافت جزئیات یک دانش‌آموز با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر دانش‌آموز خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `student.read`
      summary: جزئیات دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Student'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_students_partial_update
      description: |-
        ویرایش جزئی دانش‌آموز.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `student.update`
      summary: ویرایش جزئی دانش‌آموز
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Student'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_students_destroy
      description: |-
        حذف نرم دانش‌آموز.

        دانش‌آموز از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `student.delete`
      summary: حذف نرم پرونده دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دانش‌آموز را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/students/{id}/profile-360/:
    get:
      operationId: students_students_profile_360_retrieve
      description: |-
        همه بخش‌های مجاز پرونده در یک درخواست: هویت، اولیا، ثبت‌نام‌ها، رضایت‌نامه‌ها و خلاصه حضور/مالی/تحصیلی/سلامت.

        بخش‌هایی که کاربر مجوز دیدنشان را ندارد `null` برمی‌گردند، نه خطا؛ این کار رندر Tabها را در فرانت ساده می‌کند (بخش ۷.۲ سند فرانت).
      summary: پرونده ۳۶۰ درجه دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Student360'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/students/{id}/status-history/:
    get:
      operationId: students_students_status_history_list
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تاریخچه وضعیت دانش‌آموز
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
        description: سال تحصیلی
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
        description: شعبه
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: enrollment_status
        schema:
          type: string
      - in: query
        name: gender
        schema:
          type: string
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
        description: پایه
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دانش‌آموز را شناسایی میکند.
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - GRADUATED
          - PROSPECTIVE
          - SUSPENDED
          - TRANSFERRED_OUT
          - WITHDRAWN
        description: |-
          * `PROSPECTIVE` - متقاضی
          * `ACTIVE` - در حال تحصیل
          * `SUSPENDED` - تعلیق
          * `TRANSFERRED_OUT` - انتقال به مدرسه دیگر
          * `WITHDRAWN` - ترک تحصیل
          * `GRADUATED` - فارغ‌التحصیل
      - in: query
        name: student_no
        schema:
          type: string
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentStatusHistoryList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/guardians/:
    post:
      operationId: students_guardians_create
      description: |-
        ایجاد ولی/سرپرست تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `guardian.create`
      summary: ایجاد پرونده ولی
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GuardianRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GuardianRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GuardianRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Guardian'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_guardians_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اولیا و سرپرستان.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **جست‌وجو** — پارامتر `search` روی `person__first_name`، `person__last_name`، `person__national_id`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `guardian.read`
      summary: فهرست اولیا
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGuardianList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/guardians/{id}/:
    put:
      operationId: students_guardians_update
      description: |-
        جایگزینی کامل ولی/سرپرست.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `guardian.update`
      summary: ویرایش کامل ولی/سرپرست
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ولی/سرپرست را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GuardianRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GuardianRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GuardianRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Guardian'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_guardians_retrieve
      description: |-
        دریافت جزئیات یک ولی/سرپرست با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ولی/سرپرست خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `guardian.read`
      summary: جزئیات ولی/سرپرست
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ولی/سرپرست را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Guardian'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_guardians_partial_update
      description: |-
        ویرایش جزئی ولی/سرپرست.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `guardian.update`
      summary: ویرایش جزئی ولی/سرپرست
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ولی/سرپرست را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedGuardianRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedGuardianRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedGuardianRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Guardian'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_guardians_destroy
      description: |-
        حذف نرم ولی/سرپرست.

        ولی/سرپرست از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `guardian.delete`
      summary: حذف ولی/سرپرست
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ولی/سرپرست را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/guardians/{id}/students/:
    get:
      operationId: students_guardians_students_list
      description: 'برای پرتال ولی: فهرست دانش‌آموزانی که این ولی مجاز به مشاهده آنهاست.'
      summary: فرزندان تحت سرپرستی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ولی/سرپرست را شناسایی میکند.
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentGuardianList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/student-guardians/:
    post:
      operationId: students_student_guardians_create
      description: |-
        ایجاد رابطه ولی و دانش‌آموز تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `guardian.create`
      summary: اتصال ولی به دانش‌آموز
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentGuardianRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentGuardianRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentGuardianRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentGuardian'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_student_guardians_list
      description: این رابطه منبع حقیقت دسترسی ولی است. چهار پرچم `hasCustody`، `canPickup`،
        `receivesReports` و `financiallyResponsible` مستقل از یکدیگرند (بخش ۷.۲).
      summary: فهرست روابط ولی و دانش‌آموز
      parameters:
      - in: query
        name: can_pickup
        schema:
          type: boolean
      - in: query
        name: financially_responsible
        schema:
          type: boolean
      - in: query
        name: guardian
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: has_custody
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: receives_reports
        schema:
          type: boolean
      - in: query
        name: relationship_type
        schema:
          type: string
          title: نوع رابطه
          enum:
          - FATHER
          - GRANDPARENT
          - LEGAL_GUARDIAN
          - MOTHER
          - OTHER
          - SIBLING
          - STEP_PARENT
        description: |-
          * `FATHER` - پدر
          * `MOTHER` - مادر
          * `STEP_PARENT` - ناپدری/نامادری
          * `GRANDPARENT` - پدربزرگ/مادربزرگ
          * `SIBLING` - خواهر/برادر
          * `LEGAL_GUARDIAN` - قیم قانونی
          * `OTHER` - سایر
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentGuardianList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/student-guardians/{id}/:
    put:
      operationId: students_student_guardians_update
      description: |-
        جایگزینی کامل رابطه ولی و دانش‌آموز.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `guardian.update`
      summary: ویرایش کامل رابطه ولی و دانش‌آموز
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رابطه ولی و دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentGuardianRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentGuardianRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentGuardianRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentGuardian'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_student_guardians_retrieve
      description: |-
        دریافت جزئیات یک رابطه ولی و دانش‌آموز با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رابطه ولی و دانش‌آموز خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `guardian.read`
      summary: جزئیات رابطه ولی و دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رابطه ولی و دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentGuardian'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_student_guardians_partial_update
      description: |-
        ویرایش جزئی رابطه ولی و دانش‌آموز.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `guardian.update`
      summary: ویرایش جزئی رابطه ولی و دانش‌آموز
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رابطه ولی و دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudentGuardianRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudentGuardianRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudentGuardianRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentGuardian'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_student_guardians_destroy
      description: |-
        حذف نرم رابطه ولی و دانش‌آموز.

        رابطه ولی و دانش‌آموز از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `guardian.delete`
      summary: حذف رابطه ولی و دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رابطه ولی و دانش‌آموز را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/admissions/:
    post:
      operationId: students_admissions_create
      description: |-
        ایجاد درخواست پذیرش تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `admission.create`
      summary: ثبت درخواست پذیرش (پیش‌ثبت‌نام)
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdmissionApplicationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdmissionApplicationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdmissionApplicationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_admissions_list
      description: فهرست درخواست‌های پذیرش با فیلتر وضعیت، برای نمای Kanban پذیرش.
      summary: Pipeline پذیرش
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: preferred_campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: preferred_grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: reviewer_id
        schema:
          type: string
          format: uuid
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACCEPTED
          - CONDITIONALLY_ACCEPTED
          - CONVERTED
          - DRAFT
          - INCOMPLETE
          - REJECTED
          - SUBMITTED
          - UNDER_REVIEW
          - WAITLISTED
          - WITHDRAWN
        description: |-
          * `DRAFT` - پیش‌نویس
          * `SUBMITTED` - ارسال‌شده
          * `INCOMPLETE` - نقص مدارک
          * `UNDER_REVIEW` - در حال بررسی
          * `WAITLISTED` - فهرست انتظار
          * `CONDITIONALLY_ACCEPTED` - پذیرش مشروط
          * `ACCEPTED` - پذیرفته‌شده
          * `REJECTED` - ردشده
          * `CONVERTED` - تبدیل به ثبت‌نام
          * `WITHDRAWN` - انصراف
      - in: query
        name: submitted_from
        schema:
          type: string
          format: date-time
      - in: query
        name: submitted_to
        schema:
          type: string
          format: date-time
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdmissionApplicationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/admissions/{id}/:
    put:
      operationId: students_admissions_update
      description: |-
        جایگزینی کامل درخواست پذیرش.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `admission.update`
      summary: ویرایش کامل درخواست پذیرش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdmissionApplicationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdmissionApplicationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdmissionApplicationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_admissions_retrieve
      description: |-
        دریافت جزئیات یک درخواست پذیرش با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر درخواست پذیرش خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `admission.read`
      summary: جزئیات درخواست پذیرش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_admissions_partial_update
      description: |-
        ویرایش جزئی درخواست پذیرش.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `admission.update`
      summary: ویرایش جزئی درخواست پذیرش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAdmissionApplicationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAdmissionApplicationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAdmissionApplicationRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_admissions_destroy
      description: |-
        حذف نرم درخواست پذیرش.

        درخواست پذیرش از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `admission.delete`
      summary: حذف درخواست پذیرش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/admissions/{id}/accept/:
    post:
      operationId: students_admissions_accept_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: پذیرش درخواست
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/admissions/{id}/accept-conditionally/:
    post:
      operationId: students_admissions_accept_conditionally_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: پذیرش مشروط
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/admissions/{id}/assign-reviewer/:
    post:
      operationId: students_admissions_assign_reviewer_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: ارجاع به بررسی‌کننده
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/admissions/{id}/convert/:
    post:
      operationId: students_admissions_convert_create
      description: پرونده دانش‌آموز (در صورت نبود) ساخته می‌شود و ثبت‌نام در وضعیت
        «در انتظار مدارک» ایجاد می‌گردد (بخش ۹.۱ و ۹.۲).
      summary: تبدیل پذیرش به ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConvertAdmissionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ConvertAdmissionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ConvertAdmissionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/admissions/{id}/reject/:
    post:
      operationId: students_admissions_reject_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: رد درخواست
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdmissionDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/admissions/{id}/submit/:
    post:
      operationId: students_admissions_submit_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: ارسال درخواست پذیرش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/admissions/{id}/waitlist/:
    post:
      operationId: students_admissions_waitlist_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: انتقال به فهرست انتظار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست پذیرش را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdmissionApplication'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/:
    post:
      operationId: students_enrollments_create
      description: |-
        ایجاد ثبت‌نام تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `enrollment.create`
      summary: ایجاد ثبت‌نام مستقیم
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnrollmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EnrollmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EnrollmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_enrollments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده ثبت‌نام‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `student`، `academic_year`، `campus`، `grade_level`، `program`، `status`، `class_group`

        **جست‌وجو** — پارامتر `search` روی `enrollment_no`، `student__student_no`، `student__person__first_name`، `student__person__last_name`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `enrollment.read`
      summary: فهرست ثبت‌نام‌ها
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: program
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CANCELLED
          - COMPLETED
          - GRADUATED
          - PENDING_DOCUMENTS
          - PENDING_FINANCE
          - PENDING_PLACEMENT
          - SUSPENDED
          - TRANSFERRED
          - WITHDRAWN
        description: |-
          * `PENDING_DOCUMENTS` - در انتظار مدارک
          * `PENDING_FINANCE` - در انتظار تسویه مالی
          * `PENDING_PLACEMENT` - در انتظار تخصیص کلاس
          * `ACTIVE` - فعال
          * `SUSPENDED` - تعلیق
          * `TRANSFERRED` - انتقالی
          * `WITHDRAWN` - ترک تحصیل
          * `COMPLETED` - پایان سال
          * `GRADUATED` - فارغ‌التحصیل
          * `CANCELLED` - لغوشده
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedEnrollmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/enrollments/{id}/:
    put:
      operationId: students_enrollments_update
      description: |-
        جایگزینی کامل ثبت‌نام.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش کامل ثبت‌نام
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnrollmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EnrollmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EnrollmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_enrollments_retrieve
      description: |-
        دریافت جزئیات یک ثبت‌نام با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ثبت‌نام خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `enrollment.read`
      summary: جزئیات ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_enrollments_partial_update
      description: |-
        ویرایش جزئی ثبت‌نام.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش جزئی ثبت‌نام
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEnrollmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedEnrollmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedEnrollmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_enrollments_destroy
      description: |-
        حذف نرم ثبت‌نام.

        ثبت‌نام از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `enrollment.delete`
      summary: حذف ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/enrollments/{id}/approve-documents/:
    post:
      operationId: students_enrollments_approve_documents_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تأیید مدارک ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/{id}/confirm-finance/:
    post:
      operationId: students_enrollments_confirm_finance_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تأیید شرط مالی ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/{id}/place-in-class/:
    post:
      operationId: students_enrollments_place_in_class_create
      description: ظرفیت کلاس، تطابق سال تحصیلی و پایه کنترل می‌شود. با موفقیت، ثبت‌نام
        از «در انتظار تخصیص کلاس» به «فعال» می‌رود.
      summary: تخصیص کلاس و فعال‌سازی ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaceInClassRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PlaceInClassRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PlaceInClassRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                خطایتکمیلظرفیت:
                  value:
                    code: CLASS_CAPACITY_EXCEEDED
                    message: ظرفیت کلاس تکمیل است.
                    correlationId: 3a7f2b9c
                    fieldErrors:
                    - field: classGroupId
                      reason: capacity
                    retryable: false
                  summary: خطای تکمیل ظرفیت
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/{id}/reinstate/:
    post:
      operationId: students_enrollments_reinstate_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بازگشت از تعلیق
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/{id}/suspend/:
    post:
      operationId: students_enrollments_suspend_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تعلیق ثبت‌نام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/{id}/transfer/:
    post:
      operationId: students_enrollments_transfer_create
      description: سابقه انتقال ثبت می‌شود و عضویت قبلی بسته می‌گردد (بخش ۹.۱۰).
      summary: انتقال بین کلاس‌ها
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferClassRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TransferClassRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TransferClassRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentTransfer'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/enrollments/{id}/withdraw/:
    post:
      operationId: students_enrollments_withdraw_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: ترک تحصیل
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Enrollment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/class-memberships/:
    post:
      operationId: students_class_memberships_create
      description: |-
        ایجاد عضویت کلاس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `enrollment.create`
      summary: ایجاد عضویت کلاس
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClassMembershipRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ClassMembershipRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ClassMembershipRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassMembership'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_class_memberships_list
      description: برای ساخت لیست حضور و غیاب و فهرست دانش‌آموزان یک کلاس.
      summary: فهرست عضویت‌های کلاس
      parameters:
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: is_primary
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ENDED
          - TRANSFERRED
        description: |-
          * `ACTIVE` - فعال
          * `TRANSFERRED` - منتقل‌شده
          * `ENDED` - پایان‌یافته
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedClassMembershipList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/class-memberships/{id}/:
    put:
      operationId: students_class_memberships_update
      description: |-
        جایگزینی کامل عضویت کلاس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش کامل عضویت کلاس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عضویت کلاس را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClassMembershipRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ClassMembershipRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ClassMembershipRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassMembership'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_class_memberships_retrieve
      description: |-
        دریافت جزئیات یک عضویت کلاس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر عضویت کلاس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `enrollment.read`
      summary: جزئیات عضویت کلاس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عضویت کلاس را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassMembership'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_class_memberships_partial_update
      description: |-
        ویرایش جزئی عضویت کلاس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش جزئی عضویت کلاس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عضویت کلاس را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedClassMembershipRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedClassMembershipRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedClassMembershipRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClassMembership'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_class_memberships_destroy
      description: |-
        حذف نرم عضویت کلاس.

        عضویت کلاس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `enrollment.delete`
      summary: حذف عضویت کلاس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عضویت کلاس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/consents/:
    post:
      operationId: students_consents_create
      description: نسخه متن سیاست در زمان اخذ رضایت Snapshot می‌شود (بخش ۷.۲).
      summary: ثبت رضایت‌نامه
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConsentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ConsentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ConsentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consent'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_consents_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده رضایت‌نامه‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `student`، `guardian`، `consent_type`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `consent.read`
      summary: فهرست رضایت‌نامه‌ها
      parameters:
      - in: query
        name: consent_type
        schema:
          type: string
          title: نوع رضایت
          enum:
          - CONTENT_PUBLISH
          - DATA_PROCESSING
          - EMERGENCY_MEDICAL
          - FIELD_TRIP
          - PHOTO
          - TRANSPORT
        description: |-
          * `PHOTO` - انتشار تصویر
          * `FIELD_TRIP` - سفر و اردو
          * `EMERGENCY_MEDICAL` - درمان اضطراری
          * `CONTENT_PUBLISH` - انتشار محتوا
          * `TRANSPORT` - استفاده از سرویس
          * `DATA_PROCESSING` - پردازش داده
      - in: query
        name: guardian
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - EXPIRED
          - GRANTED
          - REVOKED
        description: |-
          * `GRANTED` - اعطاشده
          * `REVOKED` - لغوشده
          * `EXPIRED` - منقضی
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedConsentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/consents/{id}/:
    put:
      operationId: students_consents_update
      description: |-
        جایگزینی کامل رضایت‌نامه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `consent.update`
      summary: ویرایش کامل رضایت‌نامه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رضایت‌نامه را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConsentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ConsentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ConsentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_consents_retrieve
      description: |-
        دریافت جزئیات یک رضایت‌نامه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رضایت‌نامه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `consent.read`
      summary: جزئیات رضایت‌نامه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رضایت‌نامه را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_consents_partial_update
      description: |-
        ویرایش جزئی رضایت‌نامه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `consent.update`
      summary: ویرایش جزئی رضایت‌نامه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رضایت‌نامه را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedConsentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedConsentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedConsentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_consents_destroy
      description: |-
        حذف نرم رضایت‌نامه.

        رضایت‌نامه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `consent.delete`
      summary: حذف رضایت‌نامه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رضایت‌نامه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/consents/{id}/revoke/:
    post:
      operationId: students_consents_revoke_create
      description: رضایت لغوشده حذف نمی‌شود؛ فقط وضعیت آن تغییر می‌کند (بخش ۷.۲).
      summary: لغو رضایت‌نامه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رضایت‌نامه را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/transfers/:
    get:
      operationId: students_transfers_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده انتقال‌های دانش‌آموز.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `student`، `enrollment`، `transfer_type`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `enrollment.read`
      summary: سوابق انتقال دانش‌آموزان
      parameters:
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: transfer_type
        schema:
          type: string
          title: نوع انتقال
          enum:
          - EXTERNAL_IN
          - EXTERNAL_OUT
          - INTERNAL_CAMPUS
          - INTERNAL_CLASS
        description: |-
          * `INTERNAL_CLASS` - انتقال بین کلاس
          * `INTERNAL_CAMPUS` - انتقال بین شعبه
          * `EXTERNAL_OUT` - انتقال به مدرسه دیگر
          * `EXTERNAL_IN` - انتقال از مدرسه دیگر
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentTransferList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/transfers/{id}/:
    get:
      operationId: students_transfers_retrieve
      description: |-
        دریافت جزئیات یک انتقال دانش‌آموز با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر انتقال دانش‌آموز خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `enrollment.read`
      summary: جزئیات انتقال دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتقال دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentTransfer'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/students/promotion-batches/:
    post:
      operationId: students_promotion_batches_create
      description: |-
        ایجاد دسته ارتقای پایه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `enrollment.create`
      summary: ایجاد دسته ارتقای پایه
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromotionBatchRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PromotionBatchRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PromotionBatchRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionBatch'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_promotion_batches_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده دسته‌های ارتقای پایه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `source_year`، `target_year`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `enrollment.read`
      summary: دسته‌های ارتقای پایه
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: source_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: status
        schema:
          type: string
      - in: query
        name: target_year
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPromotionBatchList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/promotion-batches/{id}/:
    put:
      operationId: students_promotion_batches_update
      description: |-
        جایگزینی کامل دسته ارتقای پایه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش کامل دسته ارتقای پایه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارتقای پایه را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromotionBatchRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PromotionBatchRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PromotionBatchRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionBatch'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_promotion_batches_retrieve
      description: |-
        دریافت جزئیات یک دسته ارتقای پایه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر دسته ارتقای پایه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `enrollment.read`
      summary: جزئیات دسته ارتقای پایه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارتقای پایه را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionBatch'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_promotion_batches_partial_update
      description: |-
        ویرایش جزئی دسته ارتقای پایه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش جزئی دسته ارتقای پایه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارتقای پایه را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPromotionBatchRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPromotionBatchRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPromotionBatchRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionBatch'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_promotion_batches_destroy
      description: |-
        حذف نرم دسته ارتقای پایه.

        دسته ارتقای پایه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `enrollment.delete`
      summary: حذف دسته ارتقای پایه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارتقای پایه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/students/promotion-batches/{id}/commit/:
    post:
      operationId: students_promotion_batches_commit_create
      description: تصمیم‌های پیش‌نمایش را به‌صورت رکورد مستقل ذخیره می‌کند. اجرای
        واقعی ثبت‌نام سال بعد در گام جداگانه و قابل تکرار انجام می‌شود.
      summary: ثبت تصمیم‌های ارتقا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارتقای پایه را شناسایی میکند.
        required: true
      tags:
      - Students
      requestBody:
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/PromotionDecisionRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/PromotionDecisionRecordRequest'
          multipart/form-data:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/PromotionDecisionRecordRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/promotion-batches/{id}/preview/:
    get:
      operationId: students_promotion_batches_preview_retrieve
      description: 'مرحله اول عملیات گروهی (بخش ۱۱.۶): تصمیم پیشنهادی هر دانش‌آموز
        بدون هیچ تغییری در داده. مرحله دوم `commit` است.'
      summary: پیش‌نمایش ارتقای پایه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارتقای پایه را شناسایی میکند.
        required: true
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionPreview'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/students/promotion-decisions/:
    post:
      operationId: students_promotion_decisions_create
      description: |-
        ایجاد تصمیم ارتقا تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `enrollment.create`
      summary: ایجاد تصمیم ارتقا
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromotionDecisionRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PromotionDecisionRecordRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PromotionDecisionRecordRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionDecisionRecord'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_promotion_decisions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تصمیم‌های ارتقا.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `batch`، `decision`، `applied`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `enrollment.read`
      summary: تصمیم‌های ارتقا
      parameters:
      - in: query
        name: applied
        schema:
          type: boolean
      - in: query
        name: batch
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: decision
        schema:
          type: string
          title: تصمیم
          enum:
          - CONDITIONAL
          - COUNCIL_REVIEW
          - GRADUATED
          - PROMOTED
          - REPEATED
          - TRANSFERRED
        description: |-
          * `PROMOTED` - ارتقا
          * `REPEATED` - تکرار پایه
          * `CONDITIONAL` - مشروط
          * `COUNCIL_REVIEW` - نیازمند تصمیم شورا
          * `TRANSFERRED` - انتقال
          * `GRADUATED` - فارغ‌التحصیل
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPromotionDecisionRecordList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/students/promotion-decisions/{id}/:
    put:
      operationId: students_promotion_decisions_update
      description: |-
        جایگزینی کامل تصمیم ارتقا.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش کامل تصمیم ارتقا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تصمیم ارتقا را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromotionDecisionRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PromotionDecisionRecordRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PromotionDecisionRecordRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionDecisionRecord'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: students_promotion_decisions_retrieve
      description: |-
        دریافت جزئیات یک تصمیم ارتقا با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تصمیم ارتقا خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `enrollment.read`
      summary: جزئیات تصمیم ارتقا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تصمیم ارتقا را شناسایی میکند.
        required: true
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionDecisionRecord'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: students_promotion_decisions_partial_update
      description: |-
        ویرایش جزئی تصمیم ارتقا.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `enrollment.update`
      summary: ویرایش جزئی تصمیم ارتقا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تصمیم ارتقا را شناسایی میکند.
        required: true
      tags:
      - students
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPromotionDecisionRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPromotionDecisionRecordRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPromotionDecisionRecordRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionDecisionRecord'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: students_promotion_decisions_destroy
      description: |-
        حذف نرم تصمیم ارتقا.

        تصمیم ارتقا از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `enrollment.delete`
      summary: حذف تصمیم ارتقا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تصمیم ارتقا را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - students
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/org-units/:
    post:
      operationId: hr_org_units_create
      description: |-
        ایجاد واحد سازمانی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ایجاد واحد سازمانی
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrgUnitRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OrgUnitRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/OrgUnitRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgUnit'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_org_units_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده واحدهای سازمانی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `campus`، `parent`

        **جست‌وجو** — پارامتر `search` روی `title`، `code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `employee.read`
      summary: واحدهای سازمانی
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: parent
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedOrgUnitList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/org-units/{id}/:
    put:
      operationId: hr_org_units_update
      description: |-
        جایگزینی کامل واحد سازمانی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل واحد سازمانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrgUnitRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OrgUnitRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/OrgUnitRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgUnit'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_org_units_retrieve
      description: |-
        دریافت جزئیات یک واحد سازمانی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر واحد سازمانی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: جزئیات واحد سازمانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgUnit'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_org_units_partial_update
      description: |-
        ویرایش جزئی واحد سازمانی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی واحد سازمانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedOrgUnitRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedOrgUnitRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedOrgUnitRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgUnit'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_org_units_destroy
      description: |-
        حذف نرم واحد سازمانی.

        واحد سازمانی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف واحد سازمانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سازمانی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/positions/:
    post:
      operationId: hr_positions_create
      description: |-
        ایجاد پست سازمانی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ایجاد پست سازمانی
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PositionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PositionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PositionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Position'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_positions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پست‌های سازمانی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `org_unit`، `position_type`

        **جست‌وجو** — پارامتر `search` روی `title`، `code`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `employee.read`
      summary: پست‌های سازمانی
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - in: query
        name: org_unit
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: position_type
        schema:
          type: string
          title: نوع سِمت
          enum:
          - ADMINISTRATIVE
          - COUNSELING
          - HEALTH
          - MANAGEMENT
          - SUPPORT
          - TEACHING
        description: |-
          * `TEACHING` - آموزشی
          * `ADMINISTRATIVE` - اداری
          * `MANAGEMENT` - مدیریتی
          * `SUPPORT` - پشتیبانی
          * `HEALTH` - بهداشت و درمان
          * `COUNSELING` - مشاوره
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPositionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/positions/{id}/:
    put:
      operationId: hr_positions_update
      description: |-
        جایگزینی کامل پست سازمانی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل پست سازمانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پست سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PositionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PositionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PositionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Position'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_positions_retrieve
      description: |-
        دریافت جزئیات یک پست سازمانی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پست سازمانی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: جزئیات پست سازمانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پست سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Position'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_positions_partial_update
      description: |-
        ویرایش جزئی پست سازمانی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی پست سازمانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پست سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPositionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPositionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPositionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Position'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_positions_destroy
      description: |-
        حذف نرم پست سازمانی.

        پست سازمانی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف پست سازمانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پست سازمانی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/employees/:
    post:
      operationId: hr_employees_create
      description: |-
        ایجاد کارمند تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ایجاد پرونده پرسنلی
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmployeeRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmployeeRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Employee'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_employees_list
      description: فهرست کارکنان با پست اصلی و نشانگر معلم بودن (بخش ۱۱.۱ سند فرانت).
      summary: فهرست پرسنل
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
        description: شعبه
      - in: query
        name: employee_no
        schema:
          type: string
      - in: query
        name: is_teacher
        schema:
          type: boolean
        description: فقط معلمان
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: position_type
        schema:
          type: string
        description: نوع پست
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - ONBOARDING
          - ON_LEAVE
          - SUSPENDED
          - TERMINATED
        description: |-
          * `ONBOARDING` - در حال جذب
          * `ACTIVE` - شاغل
          * `ON_LEAVE` - در مرخصی
          * `SUSPENDED` - تعلیق
          * `TERMINATED` - پایان همکاری
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedEmployeeListList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/employees/{id}/:
    put:
      operationId: hr_employees_update
      description: |-
        جایگزینی کامل کارمند.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل کارمند
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارمند را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmployeeRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmployeeRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Employee'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_employees_retrieve
      description: |-
        دریافت جزئیات یک کارمند با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر کارمند خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: پرونده کارمند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارمند را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Employee'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_employees_partial_update
      description: |-
        ویرایش جزئی کارمند.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی کارمند
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارمند را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Employee'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_employees_destroy
      description: |-
        حذف نرم کارمند.

        کارمند از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف کارمند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارمند را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/employees/{id}/terminate/:
    post:
      operationId: hr_employees_terminate_create
      description: قراردادهای فعال بسته می‌شوند و چک‌لیست تسویه دارایی/دسترسی (Offboarding)
        باید جداگانه پیگیری شود (بخش ۴.۵).
      summary: پایان همکاری کارمند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارمند را شناسایی میکند.
        required: true
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Employee'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/contracts/:
    post:
      operationId: hr_contracts_create
      description: |-
        ایجاد قرارداد تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `contract.create`
      summary: ایجاد قرارداد
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmploymentContractRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmploymentContractRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmploymentContractRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmploymentContract'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_contracts_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده قراردادها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `employee`، `contract_type`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `contract.read`
      summary: فهرست قراردادها
      parameters:
      - in: query
        name: contract_type
        schema:
          type: string
          title: نوع قرارداد
          enum:
          - CONTRACTOR
          - FIXED_TERM
          - HOURLY
          - INTERN
          - PERMANENT
        description: |-
          * `PERMANENT` - رسمی/دائم
          * `FIXED_TERM` - مدت معین
          * `HOURLY` - ساعتی/حق‌التدریس
          * `CONTRACTOR` - پیمانکاری
          * `INTERN` - کارآموز
      - in: query
        name: employee
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - DRAFT
          - EXPIRED
          - TERMINATED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - جاری
          * `EXPIRED` - منقضی
          * `TERMINATED` - فسخ‌شده
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedEmploymentContractList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/contracts/{id}/:
    put:
      operationId: hr_contracts_update
      description: |-
        جایگزینی کامل قرارداد.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `contract.update`
      summary: ویرایش کامل قرارداد
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmploymentContractRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmploymentContractRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmploymentContractRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmploymentContract'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_contracts_retrieve
      description: |-
        دریافت جزئیات یک قرارداد با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قرارداد خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `contract.read`
      summary: جزئیات قرارداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmploymentContract'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_contracts_partial_update
      description: |-
        ویرایش جزئی قرارداد.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `contract.update`
      summary: ویرایش جزئی قرارداد
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEmploymentContractRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedEmploymentContractRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedEmploymentContractRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmploymentContract'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_contracts_destroy
      description: |-
        حذف نرم قرارداد.

        قرارداد از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `contract.delete`
      summary: حذف قرارداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/contracts/{id}/activate/:
    post:
      operationId: hr_contracts_activate_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: فعال‌سازی قرارداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmploymentContract'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/contracts/{id}/close/:
    post:
      operationId: hr_contracts_close_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: خاتمه قرارداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد را شناسایی میکند.
        required: true
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmploymentContract'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/assignments/:
    post:
      operationId: hr_assignments_create
      description: |-
        ایجاد انتساب سازمانی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ایجاد انتساب سازمانی
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmployeeAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmployeeAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAssignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_assignments_list
      description: مجموع درصد تخصیص هم‌زمان هر کارمند در زمان ایجاد کنترل می‌شود.
      summary: انتساب‌های سازمانی
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: employee
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: is_primary
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: position
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedEmployeeAssignmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/assignments/{id}/:
    put:
      operationId: hr_assignments_update
      description: |-
        جایگزینی کامل انتساب سازمانی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل انتساب سازمانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmployeeAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmployeeAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_assignments_retrieve
      description: |-
        دریافت جزئیات یک انتساب سازمانی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر انتساب سازمانی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: جزئیات انتساب سازمانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_assignments_partial_update
      description: |-
        ویرایش جزئی انتساب سازمانی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی انتساب سازمانی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سازمانی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeAssignmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_assignments_destroy
      description: |-
        حذف نرم انتساب سازمانی.

        انتساب سازمانی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف انتساب سازمانی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سازمانی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/teachers/:
    post:
      operationId: hr_teachers_create
      description: |-
        ایجاد پرونده معلم تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ایجاد پرونده معلم
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeacherProfileRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeacherProfileRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeacherProfileRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherProfile'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_teachers_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پرونده‌های معلم.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `qualification_status`

        **جست‌وجو** — پارامتر `search` روی `employee__person__first_name`، `employee__person__last_name`، `specialization`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `employee.read`
      summary: فهرست معلمان
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: qualification_status
        schema:
          type: string
          title: وضعیت صلاحیت
          enum:
          - APPROVED
          - EXPIRED
          - PENDING
          - REVOKED
        description: |-
          * `PENDING` - در انتظار تأیید
          * `APPROVED` - تأییدشده
          * `EXPIRED` - منقضی
          * `REVOKED` - لغوشده
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTeacherProfileList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/teachers/{id}/:
    put:
      operationId: hr_teachers_update
      description: |-
        جایگزینی کامل پرونده معلم.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل پرونده معلم
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده معلم را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeacherProfileRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeacherProfileRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeacherProfileRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherProfile'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_teachers_retrieve
      description: |-
        دریافت جزئیات یک پرونده معلم با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پرونده معلم خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: پرونده معلم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده معلم را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherProfile'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_teachers_partial_update
      description: |-
        ویرایش جزئی پرونده معلم.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی پرونده معلم
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده معلم را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTeacherProfileRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTeacherProfileRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTeacherProfileRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherProfile'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_teachers_destroy
      description: |-
        حذف نرم پرونده معلم.

        پرونده معلم از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف پرونده معلم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده معلم را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/teachers/{id}/workload/:
    get:
      operationId: hr_teachers_workload_retrieve
      description: مقایسه بار موظف با بار انتساب‌یافته، به همراه فهرست ارائه‌های درس.
        برای هشدار «اضافه‌بار» در صفحه انتساب تدریس.
      summary: بار تدریس معلم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده معلم را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherWorkload'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/qualifications/:
    post:
      operationId: hr_qualifications_create
      description: |-
        ایجاد صلاحیت تدریس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `teaching_assignment.create`
      summary: ایجاد صلاحیت تدریس
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeacherQualificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeacherQualificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeacherQualificationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherQualification'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_qualifications_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده صلاحیت‌های تدریس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `teacher_profile`، `course`، `grade_level`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `teaching_assignment.read`
      summary: صلاحیت‌های تدریس
      parameters:
      - in: query
        name: course
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - EXPIRED
          - PENDING
          - REVOKED
        description: |-
          * `PENDING` - در انتظار تأیید
          * `APPROVED` - تأییدشده
          * `EXPIRED` - منقضی
          * `REVOKED` - لغوشده
      - in: query
        name: teacher_profile
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTeacherQualificationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/qualifications/{id}/:
    put:
      operationId: hr_qualifications_update
      description: |-
        جایگزینی کامل صلاحیت تدریس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `teaching_assignment.update`
      summary: ویرایش کامل صلاحیت تدریس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صلاحیت تدریس را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeacherQualificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeacherQualificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeacherQualificationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherQualification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_qualifications_retrieve
      description: |-
        دریافت جزئیات یک صلاحیت تدریس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر صلاحیت تدریس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `teaching_assignment.read`
      summary: جزئیات صلاحیت تدریس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صلاحیت تدریس را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherQualification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_qualifications_partial_update
      description: |-
        ویرایش جزئی صلاحیت تدریس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `teaching_assignment.update`
      summary: ویرایش جزئی صلاحیت تدریس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صلاحیت تدریس را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTeacherQualificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTeacherQualificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTeacherQualificationRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherQualification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_qualifications_destroy
      description: |-
        حذف نرم صلاحیت تدریس.

        صلاحیت تدریس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `teaching_assignment.delete`
      summary: حذف صلاحیت تدریس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صلاحیت تدریس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/teaching-assignments/:
    post:
      operationId: hr_teaching_assignments_create
      description: صلاحیت تدریس معلم برای درس و پایه، و وجود قرارداد فعال بررسی می‌شود
        (بخش ۷.۴).
      summary: انتساب معلم به ارائه درس
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeachingAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeachingAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeachingAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingAssignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
    get:
      operationId: hr_teaching_assignments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده انتساب‌های تدریس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `teacher_profile`، `responsibility`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `teaching_assignment.read`
      summary: انتساب‌های تدریس
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: responsibility
        schema:
          type: string
          title: مسئولیت
          enum:
          - ASSISTANT
          - CO_TEACHER
          - PRIMARY
          - SUBSTITUTE
        description: |-
          * `PRIMARY` - مدرس اصلی
          * `ASSISTANT` - دستیار
          * `SUBSTITUTE` - جانشین
          * `CO_TEACHER` - هم‌تدریس
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: teacher_profile
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTeachingAssignmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/teaching-assignments/{id}/:
    put:
      operationId: hr_teaching_assignments_update
      description: |-
        جایگزینی کامل انتساب تدریس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `teaching_assignment.update`
      summary: ویرایش کامل انتساب تدریس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب تدریس را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeachingAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeachingAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeachingAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_teaching_assignments_retrieve
      description: |-
        دریافت جزئیات یک انتساب تدریس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر انتساب تدریس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `teaching_assignment.read`
      summary: جزئیات انتساب تدریس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب تدریس را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_teaching_assignments_partial_update
      description: |-
        ویرایش جزئی انتساب تدریس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `teaching_assignment.update`
      summary: ویرایش جزئی انتساب تدریس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب تدریس را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTeachingAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTeachingAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTeachingAssignmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_teaching_assignments_destroy
      description: |-
        حذف نرم انتساب تدریس.

        انتساب تدریس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `teaching_assignment.delete`
      summary: حذف انتساب تدریس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب تدریس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/shifts/:
    post:
      operationId: hr_shifts_create
      description: |-
        ایجاد شیفت کاری تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ایجاد شیفت کاری
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkShiftRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WorkShiftRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WorkShiftRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkShift'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_shifts_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده شیفت‌های کاری.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `campus`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `employee.read`
      summary: شیفت‌های کاری
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedWorkShiftList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/shifts/{id}/:
    put:
      operationId: hr_shifts_update
      description: |-
        جایگزینی کامل شیفت کاری.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل شیفت کاری
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شیفت کاری را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkShiftRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WorkShiftRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WorkShiftRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkShift'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_shifts_retrieve
      description: |-
        دریافت جزئیات یک شیفت کاری با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر شیفت کاری خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: جزئیات شیفت کاری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شیفت کاری را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkShift'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_shifts_partial_update
      description: |-
        ویرایش جزئی شیفت کاری.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی شیفت کاری
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شیفت کاری را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedWorkShiftRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedWorkShiftRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedWorkShiftRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkShift'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_shifts_destroy
      description: |-
        حذف نرم شیفت کاری.

        شیفت کاری از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف شیفت کاری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این شیفت کاری را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/attendances/:
    post:
      operationId: hr_attendances_create
      description: |-
        ایجاد کارکرد کارمند تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `employee.create`
      summary: ثبت کارکرد روزانه
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeAttendanceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmployeeAttendanceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmployeeAttendanceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendance'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_attendances_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده کارکرد پرسنل.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `employee`، `work_date`، `source`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `employee.read`
      summary: کارکرد پرسنل
      parameters:
      - in: query
        name: employee
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: source
        schema:
          type: string
          title: منبع
          enum:
          - DEVICE
          - IMPORT
          - MANUAL
          - MOBILE
        description: |-
          * `DEVICE` - دستگاه تردد
          * `MANUAL` - ثبت دستی
          * `MOBILE` - اپلیکیشن موبایل
          * `IMPORT` - ورود گروهی
      - in: query
        name: status
        schema:
          type: string
      - in: query
        name: work_date
        schema:
          type: string
          format: date
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedEmployeeAttendanceList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/attendances/{id}/:
    put:
      operationId: hr_attendances_update
      description: |-
        جایگزینی کامل کارکرد کارمند.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `employee.update`
      summary: ویرایش کامل کارکرد کارمند
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارکرد کارمند را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeAttendanceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EmployeeAttendanceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EmployeeAttendanceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendance'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_attendances_retrieve
      description: |-
        دریافت جزئیات یک کارکرد کارمند با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر کارکرد کارمند خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `employee.read`
      summary: جزئیات کارکرد کارمند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارکرد کارمند را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendance'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_attendances_partial_update
      description: |-
        ویرایش جزئی کارکرد کارمند.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `employee.update`
      summary: ویرایش جزئی کارکرد کارمند
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارکرد کارمند را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeAttendanceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeAttendanceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedEmployeeAttendanceRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeAttendance'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_attendances_destroy
      description: |-
        حذف نرم کارکرد کارمند.

        کارکرد کارمند از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `employee.delete`
      summary: حذف کارکرد کارمند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارکرد کارمند را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/leaves/:
    post:
      operationId: hr_leaves_create
      description: |-
        ایجاد درخواست مرخصی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `leave.create`
      summary: ثبت درخواست مرخصی
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeaveRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeaveRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_leaves_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده درخواست‌های مرخصی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `employee`، `leave_type`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `leave.read`
      summary: درخواست‌های مرخصی
      parameters:
      - in: query
        name: employee
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: leave_type
        schema:
          type: string
          title: نوع مرخصی
          enum:
          - ANNUAL
          - BEREAVEMENT
          - HOURLY
          - MATERNITY
          - MISSION
          - SICK
          - UNPAID
        description: |-
          * `ANNUAL` - استحقاقی
          * `SICK` - استعلاجی
          * `UNPAID` - بدون حقوق
          * `MISSION` - مأموریت
          * `HOURLY` - ساعتی
          * `MATERNITY` - زایمان
          * `BEREAVEMENT` - فوت بستگان
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - CANCELLED
          - DRAFT
          - REJECTED
          - SUBMITTED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `SUBMITTED` - ارسال‌شده
          * `APPROVED` - تأییدشده
          * `REJECTED` - ردشده
          * `CANCELLED` - لغوشده
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedLeaveRequestList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/leaves/{id}/:
    put:
      operationId: hr_leaves_update
      description: |-
        جایگزینی کامل درخواست مرخصی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `leave.update`
      summary: ویرایش کامل درخواست مرخصی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeaveRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeaveRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_leaves_retrieve
      description: |-
        دریافت جزئیات یک درخواست مرخصی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر درخواست مرخصی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `leave.read`
      summary: جزئیات درخواست مرخصی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_leaves_partial_update
      description: |-
        ویرایش جزئی درخواست مرخصی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `leave.update`
      summary: ویرایش جزئی درخواست مرخصی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLeaveRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLeaveRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLeaveRequestRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_leaves_destroy
      description: |-
        حذف نرم درخواست مرخصی.

        درخواست مرخصی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `leave.delete`
      summary: حذف درخواست مرخصی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/leaves/{id}/approve/:
    post:
      operationId: hr_leaves_approve_create
      description: تأییدکننده نباید خودِ درخواست‌دهنده باشد (تفکیک وظایف — بخش ۱۶.۲).
      summary: تأیید مرخصی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/leaves/{id}/reject/:
    post:
      operationId: hr_leaves_reject_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: رد مرخصی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeaveDecisionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/leaves/{id}/submit/:
    post:
      operationId: hr_leaves_submit_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: ارسال درخواست مرخصی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست مرخصی را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/payroll-runs/:
    post:
      operationId: hr_payroll_runs_create
      description: |-
        ایجاد اجرای حقوق تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `payroll.create`
      summary: ایجاد دوره حقوق
      tags:
      - HR
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PayrollRunRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PayrollRunRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PayrollRunRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRun'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_payroll_runs_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اجراهای حقوق.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `payroll.read`
      summary: اجراهای حقوق و دستمزد
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - CALCULATED
          - CLOSED
          - DRAFT
          - PAID
        description: |-
          * `DRAFT` - پیش‌نویس
          * `CALCULATED` - محاسبه‌شده
          * `APPROVED` - تأییدشده
          * `PAID` - پرداخت‌شده
          * `CLOSED` - بسته
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPayrollRunList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/payroll-runs/{id}/:
    put:
      operationId: hr_payroll_runs_update
      description: |-
        جایگزینی کامل اجرای حقوق.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `payroll.update`
      summary: ویرایش کامل اجرای حقوق
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای حقوق را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PayrollRunRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PayrollRunRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PayrollRunRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_payroll_runs_retrieve
      description: |-
        دریافت جزئیات یک اجرای حقوق با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر اجرای حقوق خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `payroll.read`
      summary: جزئیات اجرای حقوق
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای حقوق را شناسایی میکند.
        required: true
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: hr_payroll_runs_partial_update
      description: |-
        ویرایش جزئی اجرای حقوق.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `payroll.update`
      summary: ویرایش جزئی اجرای حقوق
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای حقوق را شناسایی میکند.
        required: true
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPayrollRunRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPayrollRunRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPayrollRunRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: hr_payroll_runs_destroy
      description: |-
        حذف نرم اجرای حقوق.

        اجرای حقوق از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `payroll.delete`
      summary: حذف اجرای حقوق
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای حقوق را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - hr
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/hr/payroll-runs/{id}/approve/:
    post:
      operationId: hr_payroll_runs_approve_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تأیید اجرای حقوق
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای حقوق را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/payroll-runs/{id}/calculate/:
    post:
      operationId: hr_payroll_runs_calculate_create
      description: فیش پایه برای کارکنان دارای قرارداد فعال تولید می‌شود. قواعد قانونی
        محل استقرار (بیمه، مالیات) به افزونه محلی سپرده می‌شود (بخش ۲.۲).
      summary: محاسبه حقوق دوره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای حقوق را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/hr/payslips/:
    post:
      operationId: hr_payslips_create
      description: |-
        ایجاد فیش حقوقی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `payroll.create`
      summary: ایجاد فیش حقوقی
      tags:
      - hr
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PayslipRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PayslipRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PayslipRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payslip'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: hr_payslips_list
      description: فیش قطعی تغییرناپذیر است؛ اصلاح با فیش مابه‌التفاوت انجام می‌شود.
      summary: فیش‌های حقوقی
      parameters:
      - in: query
        name: employee
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: payroll_run
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPayslipList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/hr/payslips/{id}/:
    get:
      operationId: hr_payslips_retrieve
      description: |-
        دریافت جزئیات یک فیش حقوقی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر فیش حقوقی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `payroll.read`
      summary: جزئیات فیش حقوقی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این فیش حقوقی را شناسایی میکند.
        required: true
      tags:
      - HR
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payslip'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/teaching/sessions/:
    post:
      operationId: teaching_sessions_create
      description: |-
        ایجاد جلسه درسی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `session.create`
      summary: ایجاد جلسه درسی
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeachingSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeachingSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeachingSessionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingSession'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_sessions_list
      description: برای صفحه «امروز من» معلم و پایش حضور. با `date` و `teacher_profile`
        می‌توان جلسات امروز یک معلم را گرفت.
      summary: فهرست جلسات درسی
      parameters:
      - in: query
        name: attendance_pending
        schema:
          type: boolean
        description: فقط جلسات بدون حضور نهایی
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: date
        schema:
          type: string
        description: تاریخ جلسه (YYYY-MM-DD)
      - in: query
        name: date_from
        schema:
          type: string
          format: date
      - in: query
        name: date_to
        schema:
          type: string
          format: date
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: room
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: session_type
        schema:
          type: string
          title: نوع جلسه
          enum:
          - EXAM
          - EXTRA
          - MAKEUP
          - REGULAR
          - REMOTE
          - SUBSTITUTE
        description: |-
          * `REGULAR` - عادی
          * `MAKEUP` - جبرانی
          * `EXTRA` - فوق‌العاده
          * `SUBSTITUTE` - با معلم جانشین
          * `REMOTE` - مجازی
          * `EXAM` - جلسه آزمون
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - HELD
          - IN_PROGRESS
          - PLANNED
          - POSTPONED
        description: |-
          * `PLANNED` - برنامه‌ریزی‌شده
          * `IN_PROGRESS` - در حال برگزاری
          * `HELD` - برگزارشده
          * `CANCELLED` - لغوشده
          * `POSTPONED` - به تعویق افتاده
      - in: query
        name: teacher_profile
        schema:
          type: string
        description: شناسه پرونده معلم
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTeachingSessionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/sessions/{id}/:
    put:
      operationId: teaching_sessions_update
      description: |-
        جایگزینی کامل جلسه درسی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `session.update`
      summary: ویرایش کامل جلسه درسی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeachingSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeachingSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeachingSessionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_sessions_retrieve
      description: |-
        دریافت جزئیات یک جلسه درسی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر جلسه درسی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `session.read`
      summary: جزئیات جلسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_sessions_partial_update
      description: |-
        ویرایش جزئی جلسه درسی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `session.update`
      summary: ویرایش جزئی جلسه درسی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTeachingSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTeachingSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTeachingSessionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_sessions_destroy
      description: |-
        حذف نرم جلسه درسی.

        جلسه درسی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `session.delete`
      summary: حذف جلسه درسی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/sessions/{id}/cancel/:
    post:
      operationId: teaching_sessions_cancel_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: لغو جلسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AmendAttendanceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AmendAttendanceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AmendAttendanceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/sessions/{id}/finalize-attendance/:
    post:
      operationId: teaching_sessions_finalize_attendance_create
      description: پس از نهایی‌سازی، رویداد `AttendanceFinalized` منتشر می‌شود (بخش
        ۱۳.۱).
      summary: نهایی‌سازی حضور جلسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeachingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/sessions/{id}/attendance/:
    post:
      operationId: teaching_sessions_attendance_create
      description: همه ردیف‌ها در یک تراکنش ثبت می‌شوند. اگر `finalize=true` باشد،
        پس از ثبت، حضور جلسه نهایی می‌گردد و تغییر بعدی نیازمند علت و مجوز `attendance.update`
        خواهد بود.
      summary: ثبت گروهی حضور و غیاب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkAttendanceRequest'
            examples:
              درخواست:
                value:
                  rows:
                  - enrollmentId: 3f1c8a2b-9d4e-4f7a-8b1c-2d3e4f5a6b7c
                    attendanceStatus: PRESENT
                  - enrollmentId: 4a2d9b3c-0e5f-4a8b-9c2d-3e4f5a6b7c8d
                    attendanceStatus: LATE
                    lateMinutes: 12
                    note: تأخیر سرویس
                  - enrollmentId: 5b3e0c4d-1f6a-4b9c-0d3e-4f5a6b7c8d9e
                    attendanceStatus: ABSENT
                    reasonCode: UNKNOWN
                  finalize: true
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BulkAttendanceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BulkAttendanceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/sessions/{id}/roster/:
    get:
      operationId: teaching_sessions_roster_retrieve
      description: فهرست دانش‌آموزان عضو فعال کلاس در زمان جلسه، به‌همراه وضعیت حضور
        ثبت‌شده (در صورت وجود). اگر حضور هنوز ثبت نشده باشد، وضعیت پیش‌فرض `PRESENT`
        برمی‌گردد تا معلم فقط استثناها را تغییر دهد (بخش ۸.۴ سند فرانت).
      summary: فهرست حضور جلسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه درسی را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRoster'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/session-teachers/:
    post:
      operationId: teaching_session_teachers_create
      description: |-
        ایجاد معلم جلسه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `session.create`
      summary: ایجاد معلم جلسه
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionTeacherRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SessionTeacherRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SessionTeacherRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionTeacher'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_session_teachers_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده معلمان جلسه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `session`، `teacher_profile`، `duty`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `session.read`
      summary: معلمان جلسات
      parameters:
      - in: query
        name: duty
        schema:
          type: string
          title: وظیفه
          enum:
          - ASSISTANT
          - OBSERVER
          - SUBSTITUTE
          - TEACHER
        description: |-
          * `TEACHER` - مدرس
          * `ASSISTANT` - دستیار
          * `SUBSTITUTE` - جانشین
          * `OBSERVER` - ناظر
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: session
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: teacher_profile
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedSessionTeacherList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/session-teachers/{id}/:
    put:
      operationId: teaching_session_teachers_update
      description: |-
        جایگزینی کامل معلم جلسه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `session.update`
      summary: ویرایش کامل معلم جلسه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این معلم جلسه را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionTeacherRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SessionTeacherRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SessionTeacherRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionTeacher'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_session_teachers_retrieve
      description: |-
        دریافت جزئیات یک معلم جلسه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر معلم جلسه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `session.read`
      summary: جزئیات معلم جلسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این معلم جلسه را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionTeacher'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_session_teachers_partial_update
      description: |-
        ویرایش جزئی معلم جلسه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `session.update`
      summary: ویرایش جزئی معلم جلسه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این معلم جلسه را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedSessionTeacherRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedSessionTeacherRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedSessionTeacherRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionTeacher'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_session_teachers_destroy
      description: |-
        حذف نرم معلم جلسه.

        معلم جلسه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `session.delete`
      summary: حذف معلم جلسه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این معلم جلسه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/attendance/:
    post:
      operationId: teaching_attendance_create
      description: |-
        ایجاد رکورد حضور تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `attendance.create`
      summary: ایجاد رکورد حضور
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttendanceRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AttendanceRecordRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttendanceRecordRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRecord'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_attendance_list
      description: برای گزارش حضور دانش‌آموز، کلاس یا بازه زمانی.
      summary: رکوردهای حضور
      parameters:
      - in: query
        name: attendance_status
        schema:
          type: string
          title: وضعیت حضور
          enum:
          - ABSENT
          - EARLY_LEAVE
          - EXCUSED
          - LATE
          - PRESENT
          - REMOTE
          - SCHOOL_ACTIVITY
        description: |-
          * `PRESENT` - حاضر
          * `ABSENT` - غایب
          * `LATE` - تأخیر
          * `EXCUSED` - غیبت موجه
          * `SCHOOL_ACTIVITY` - مأموریت مدرسه
          * `REMOTE` - حضور مجازی
          * `EARLY_LEAVE` - خروج زودهنگام
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: date_from
        schema:
          type: string
          format: date
      - in: query
        name: date_to
        schema:
          type: string
          format: date
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: finalization_status
        schema:
          type: string
          title: وضعیت قطعی‌سازی
          enum:
          - AMENDED
          - DRAFT
          - FINALIZED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `FINALIZED` - نهایی‌شده
          * `AMENDED` - اصلاح‌شده پس از نهایی‌سازی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: session
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: student
        schema:
          type: string
          format: uuid
        description: دانش‌آموز
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAttendanceRecordList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/attendance/monitor/:
    get:
      operationId: teaching_attendance_monitor_list
      description: 'برای داشبورد «پایش حضور مدرسه» (بخش ۸.۵ سند فرانت): تعداد جلسات
        نهایی‌شده و در انتظار هر کلاس در تاریخ مشخص.'
      summary: پایش حضور کلاس‌ها
      parameters:
      - in: query
        name: attendance_status
        schema:
          type: string
          title: وضعیت حضور
          enum:
          - ABSENT
          - EARLY_LEAVE
          - EXCUSED
          - LATE
          - PRESENT
          - REMOTE
          - SCHOOL_ACTIVITY
        description: |-
          * `PRESENT` - حاضر
          * `ABSENT` - غایب
          * `LATE` - تأخیر
          * `EXCUSED` - غیبت موجه
          * `SCHOOL_ACTIVITY` - مأموریت مدرسه
          * `REMOTE` - حضور مجازی
          * `EARLY_LEAVE` - خروج زودهنگام
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: date
        schema:
          type: string
        description: تاریخ (YYYY-MM-DD)
      - in: query
        name: date_from
        schema:
          type: string
          format: date
      - in: query
        name: date_to
        schema:
          type: string
          format: date
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: finalization_status
        schema:
          type: string
          title: وضعیت قطعی‌سازی
          enum:
          - AMENDED
          - DRAFT
          - FINALIZED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `FINALIZED` - نهایی‌شده
          * `AMENDED` - اصلاح‌شده پس از نهایی‌سازی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: session
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: student
        schema:
          type: string
          format: uuid
        description: دانش‌آموز
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAttendanceMonitorRowList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/attendance/student-summary/:
    get:
      operationId: teaching_attendance_student_summary_retrieve
      description: درصد حضور، شمارش به تفکیک وضعیت و تعداد غیبت متوالی، برای هشدار
        نصاب حضور و ارجاع به مشاور (بخش ۴.۳).
      summary: خلاصه حضور دانش‌آموز
      parameters:
      - in: query
        name: enrollment
        schema:
          type: string
        description: شناسه ثبت‌نام
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentAttendanceSummary'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/attendance/{id}/:
    put:
      operationId: teaching_attendance_update
      description: |-
        جایگزینی کامل رکورد حضور.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `attendance.update`
      summary: ویرایش کامل رکورد حضور
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رکورد حضور را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttendanceRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AttendanceRecordRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttendanceRecordRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRecord'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_attendance_retrieve
      description: |-
        دریافت جزئیات یک رکورد حضور با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رکورد حضور خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `attendance.read`
      summary: جزئیات رکورد حضور
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رکورد حضور را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRecord'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_attendance_partial_update
      description: |-
        ویرایش جزئی رکورد حضور.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `attendance.update`
      summary: ویرایش جزئی رکورد حضور
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رکورد حضور را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAttendanceRecordRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAttendanceRecordRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAttendanceRecordRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRecord'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_attendance_destroy
      description: |-
        حذف نرم رکورد حضور.

        رکورد حضور از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `attendance.delete`
      summary: حذف رکورد حضور
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رکورد حضور را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/attendance/{id}/amend/:
    post:
      operationId: teaching_attendance_amend_create
      description: 'بخش ۷.۵: «اصلاح بعد از نهایی‌شدن نیازمند علت و مجوز است.» رکورد
        به وضعیت `AMENDED` می‌رود و تغییر در ممیزی ثبت می‌شود.'
      summary: اصلاح حضور پس از نهایی‌سازی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رکورد حضور را شناسایی میکند.
        required: true
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AmendAttendanceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AmendAttendanceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AmendAttendanceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRecord'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/justifications/:
    post:
      operationId: teaching_justifications_create
      description: معمولاً توسط ولی از پرتال ثبت می‌شود (بخش ۴.۳).
      summary: ثبت درخواست توجیه غیبت
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AbsenceJustificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AbsenceJustificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AbsenceJustificationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbsenceJustification'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_justifications_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده توجیهات غیبت.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `decision`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `attendance.read`
      summary: درخواست‌های توجیه غیبت
      parameters:
      - in: query
        name: decision
        schema:
          type: string
          title: تصمیم
          enum:
          - APPROVED
          - PENDING
          - REJECTED
        description: |-
          * `PENDING` - در انتظار بررسی
          * `APPROVED` - پذیرفته‌شده
          * `REJECTED` - ردشده
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAbsenceJustificationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/justifications/{id}/:
    put:
      operationId: teaching_justifications_update
      description: |-
        جایگزینی کامل توجیه غیبت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `attendance.update`
      summary: ویرایش کامل توجیه غیبت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این توجیه غیبت را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AbsenceJustificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AbsenceJustificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AbsenceJustificationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbsenceJustification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_justifications_retrieve
      description: |-
        دریافت جزئیات یک توجیه غیبت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر توجیه غیبت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `attendance.read`
      summary: جزئیات توجیه غیبت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این توجیه غیبت را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbsenceJustification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_justifications_partial_update
      description: |-
        ویرایش جزئی توجیه غیبت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `attendance.update`
      summary: ویرایش جزئی توجیه غیبت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این توجیه غیبت را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAbsenceJustificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAbsenceJustificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAbsenceJustificationRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbsenceJustification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_justifications_destroy
      description: |-
        حذف نرم توجیه غیبت.

        توجیه غیبت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `attendance.delete`
      summary: حذف توجیه غیبت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این توجیه غیبت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/justifications/{id}/approve/:
    post:
      operationId: teaching_justifications_approve_create
      description: با پذیرش، وضعیت حضور به «غیبت موجه» تغییر می‌کند.
      summary: پذیرش توجیه غیبت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این توجیه غیبت را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbsenceJustification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/justifications/{id}/reject/:
    post:
      operationId: teaching_justifications_reject_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: رد توجیه غیبت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این توجیه غیبت را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AbsenceJustification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/lesson-plans/:
    post:
      operationId: teaching_lesson_plans_create
      description: |-
        ایجاد طرح درس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `resource.create`
      summary: ایجاد طرح درس
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LessonPlanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LessonPlanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LessonPlanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LessonPlan'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_lesson_plans_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده طرح‌های درس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `session`، `status`

        **جست‌وجو** — پارامتر `search` روی `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `resource.read`
      summary: طرح‌های درس
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: session
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - DELIVERED
          - DRAFT
        description: |-
          * `DRAFT` - پیش‌نویس
          * `APPROVED` - تأییدشده
          * `DELIVERED` - اجراشده
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedLessonPlanList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/lesson-plans/{id}/:
    put:
      operationId: teaching_lesson_plans_update
      description: |-
        جایگزینی کامل طرح درس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `resource.update`
      summary: ویرایش کامل طرح درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این طرح درس را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LessonPlanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LessonPlanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LessonPlanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LessonPlan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_lesson_plans_retrieve
      description: |-
        دریافت جزئیات یک طرح درس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر طرح درس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `resource.read`
      summary: جزئیات طرح درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این طرح درس را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LessonPlan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_lesson_plans_partial_update
      description: |-
        ویرایش جزئی طرح درس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `resource.update`
      summary: ویرایش جزئی طرح درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این طرح درس را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLessonPlanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLessonPlanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLessonPlanRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LessonPlan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_lesson_plans_destroy
      description: |-
        حذف نرم طرح درس.

        طرح درس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `resource.delete`
      summary: حذف طرح درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این طرح درس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/assignments/:
    post:
      operationId: teaching_assignments_create
      description: |-
        ایجاد تکلیف تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `assignment.create`
      summary: ایجاد تکلیف
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Assignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_assignments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تکالیف.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `status`

        **جست‌وجو** — پارامتر `search` روی `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `assignment.read`
      summary: فهرست تکالیف
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ARCHIVED
          - CLOSED
          - DRAFT
          - PUBLISHED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `PUBLISHED` - منتشرشده
          * `CLOSED` - بسته
          * `ARCHIVED` - بایگانی
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAssignmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/assignments/{id}/:
    put:
      operationId: teaching_assignments_update
      description: |-
        جایگزینی کامل تکلیف.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `assignment.update`
      summary: ویرایش کامل تکلیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Assignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_assignments_retrieve
      description: |-
        دریافت جزئیات یک تکلیف با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تکلیف خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `assignment.read`
      summary: جزئیات تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Assignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_assignments_partial_update
      description: |-
        ویرایش جزئی تکلیف.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `assignment.update`
      summary: ویرایش جزئی تکلیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAssignmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Assignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_assignments_destroy
      description: |-
        حذف نرم تکلیف.

        تکلیف از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `assignment.delete`
      summary: حذف تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تکلیف را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/assignments/{id}/close/:
    post:
      operationId: teaching_assignments_close_create
      description: تحویل‌های ثبت‌نشده به وضعیت «ارسال‌نشده» می‌روند.
      summary: بستن تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تکلیف را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Assignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/assignments/{id}/publish/:
    post:
      operationId: teaching_assignments_publish_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: انتشار تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تکلیف را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Assignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/submissions/:
    post:
      operationId: teaching_submissions_create
      description: |-
        ایجاد تحویل تکلیف تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `assignment.create`
      summary: ایجاد پیش‌نویس تحویل
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignmentSubmissionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssignmentSubmissionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssignmentSubmissionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssignmentSubmission'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_submissions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تحویل‌های تکلیف.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `assignment`، `enrollment`، `status`، `is_late`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `assignment.read`
      summary: تحویل‌های تکلیف
      parameters:
      - in: query
        name: assignment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: is_late
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - DRAFT
          - GRADED
          - LATE
          - MISSING
          - RETURNED
          - SUBMITTED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `SUBMITTED` - ارسال‌شده
          * `LATE` - ارسال با تأخیر
          * `GRADED` - تصحیح‌شده
          * `RETURNED` - بازگشت برای اصلاح
          * `MISSING` - ارسال‌نشده
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAssignmentSubmissionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/submissions/{id}/:
    put:
      operationId: teaching_submissions_update
      description: |-
        جایگزینی کامل تحویل تکلیف.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `assignment.update`
      summary: ویرایش کامل تحویل تکلیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignmentSubmissionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssignmentSubmissionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssignmentSubmissionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssignmentSubmission'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_submissions_retrieve
      description: |-
        دریافت جزئیات یک تحویل تکلیف با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تحویل تکلیف خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `assignment.read`
      summary: جزئیات تحویل تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssignmentSubmission'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_submissions_partial_update
      description: |-
        ویرایش جزئی تحویل تکلیف.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `assignment.update`
      summary: ویرایش جزئی تحویل تکلیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAssignmentSubmissionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAssignmentSubmissionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAssignmentSubmissionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssignmentSubmission'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_submissions_destroy
      description: |-
        حذف نرم تحویل تکلیف.

        تحویل تکلیف از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `assignment.delete`
      summary: حذف تحویل تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل تکلیف را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/submissions/{id}/grade/:
    post:
      operationId: teaching_submissions_grade_create
      description: جریمه تأخیر بر اساس Snapshot سیاست همان تحویل اعمال می‌شود. نمره
        خام و نمره نهایی هر دو در بازخورد قابل ردیابی‌اند.
      summary: تصحیح و ثبت نمره تحویل
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل تکلیف را شناسایی میکند.
        required: true
      tags:
      - Teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeSubmissionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeSubmissionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeSubmissionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmissionFeedback'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/submissions/{id}/submit/:
    post:
      operationId: teaching_submissions_submit_create
      description: تأخیر با Snapshot سیاست همان تکلیف محاسبه و ذخیره می‌شود؛ تغییر
        بعدی سیاست بر این ارسال اثر ندارد (بخش ۷.۵).
      summary: ارسال نهایی تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل تکلیف را شناسایی میکند.
        required: true
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssignmentSubmission'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/teaching/feedbacks/:
    post:
      operationId: teaching_feedbacks_create
      description: |-
        ایجاد بازخورد تکلیف تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `assignment.create`
      summary: ایجاد بازخورد تکلیف
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmissionFeedbackRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SubmissionFeedbackRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SubmissionFeedbackRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmissionFeedback'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_feedbacks_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده بازخوردهای تکلیف.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `submission`، `is_final`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `assignment.read`
      summary: بازخوردهای تکلیف
      parameters:
      - in: query
        name: is_final
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: submission
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedSubmissionFeedbackList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/feedbacks/{id}/:
    put:
      operationId: teaching_feedbacks_update
      description: |-
        جایگزینی کامل بازخورد تکلیف.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `assignment.update`
      summary: ویرایش کامل بازخورد تکلیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازخورد تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmissionFeedbackRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SubmissionFeedbackRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SubmissionFeedbackRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmissionFeedback'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_feedbacks_retrieve
      description: |-
        دریافت جزئیات یک بازخورد تکلیف با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر بازخورد تکلیف خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `assignment.read`
      summary: جزئیات بازخورد تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازخورد تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmissionFeedback'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_feedbacks_partial_update
      description: |-
        ویرایش جزئی بازخورد تکلیف.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `assignment.update`
      summary: ویرایش جزئی بازخورد تکلیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازخورد تکلیف را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedSubmissionFeedbackRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedSubmissionFeedbackRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedSubmissionFeedbackRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmissionFeedback'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_feedbacks_destroy
      description: |-
        حذف نرم بازخورد تکلیف.

        بازخورد تکلیف از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `assignment.delete`
      summary: حذف بازخورد تکلیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازخورد تکلیف را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/teaching/resources/:
    post:
      operationId: teaching_resources_create
      description: |-
        ایجاد منبع آموزشی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `resource.create`
      summary: ایجاد منبع آموزشی
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LearningResourceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LearningResourceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LearningResourceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LearningResource'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_resources_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده منابع آموزشی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `resource_type`، `visibility`

        **جست‌وجو** — پارامتر `search` روی `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `resource.read`
      summary: منابع آموزشی
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: resource_type
        schema:
          type: string
          title: نوع منبع
          enum:
          - FILE
          - LINK
          - LIVE_SESSION
          - TEXT
          - VIDEO
        description: |-
          * `FILE` - فایل
          * `LINK` - پیوند
          * `VIDEO` - ویدئو
          * `TEXT` - متن
          * `LIVE_SESSION` - کلاس زنده
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: visibility
        schema:
          type: string
          title: سطح نمایش
          enum:
          - CLASS_ONLY
          - GRADE
          - PRIVATE
          - SCHOOL
        description: |-
          * `CLASS_ONLY` - فقط کلاس
          * `GRADE` - کل پایه
          * `SCHOOL` - کل مدرسه
          * `PRIVATE` - فقط معلم
      tags:
      - Teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedLearningResourceList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/teaching/resources/{id}/:
    put:
      operationId: teaching_resources_update
      description: |-
        جایگزینی کامل منبع آموزشی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `resource.update`
      summary: ویرایش کامل منبع آموزشی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این منبع آموزشی را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LearningResourceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LearningResourceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LearningResourceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LearningResource'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: teaching_resources_retrieve
      description: |-
        دریافت جزئیات یک منبع آموزشی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر منبع آموزشی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `resource.read`
      summary: جزئیات منبع آموزشی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این منبع آموزشی را شناسایی میکند.
        required: true
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LearningResource'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: teaching_resources_partial_update
      description: |-
        ویرایش جزئی منبع آموزشی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `resource.update`
      summary: ویرایش جزئی منبع آموزشی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این منبع آموزشی را شناسایی میکند.
        required: true
      tags:
      - teaching
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLearningResourceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLearningResourceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLearningResourceRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LearningResource'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: teaching_resources_destroy
      description: |-
        حذف نرم منبع آموزشی.

        منبع آموزشی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `resource.delete`
      summary: حذف منبع آموزشی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این منبع آموزشی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - teaching
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/question-banks/:
    post:
      operationId: assessment_question_banks_create
      description: |-
        ایجاد بانک سؤال تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `question.create`
      summary: ایجاد بانک سؤال
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionBankRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionBankRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionBankRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionBank'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_banks_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده بانک‌های سؤال.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `course`، `visibility`، `status`

        **جست‌وجو** — پارامتر `search` روی `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `question.read`
      summary: فهرست بانک‌های سؤال
      parameters:
      - in: query
        name: course
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      - in: query
        name: visibility
        schema:
          type: string
          title: سطح نمایش
          enum:
          - DEPARTMENT
          - PRIVATE
          - SCHOOL
          - SHARED
        description: |-
          * `PRIVATE` - شخصی
          * `DEPARTMENT` - گروه درسی
          * `SCHOOL` - مدرسه
          * `SHARED` - مشترک
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedQuestionBankList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/question-banks/{id}/:
    put:
      operationId: assessment_question_banks_update
      description: |-
        جایگزینی کامل بانک سؤال.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `question.update`
      summary: ویرایش کامل بانک سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بانک سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionBankRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionBankRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionBankRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionBank'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_banks_retrieve
      description: |-
        دریافت جزئیات یک بانک سؤال با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر بانک سؤال خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `question.read`
      summary: جزئیات بانک سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بانک سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionBank'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_question_banks_partial_update
      description: |-
        ویرایش جزئی بانک سؤال.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `question.update`
      summary: ویرایش جزئی بانک سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بانک سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedQuestionBankRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedQuestionBankRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedQuestionBankRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionBank'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_question_banks_destroy
      description: |-
        حذف نرم بانک سؤال.

        بانک سؤال از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `question.delete`
      summary: حذف بانک سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بانک سؤال را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/question-tags/:
    post:
      operationId: assessment_question_tags_create
      description: |-
        ایجاد برچسب سؤال تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `question.create`
      summary: ایجاد برچسب سؤال
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionTagRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionTagRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionTagRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionTag'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_tags_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده برچسب‌های سؤال.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `tag_type`

        **جست‌وجو** — پارامتر `search` روی `value`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `question.read`
      summary: برچسب‌های سؤال
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: tag_type
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedQuestionTagList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/question-tags/{id}/:
    put:
      operationId: assessment_question_tags_update
      description: |-
        جایگزینی کامل برچسب سؤال.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش کامل برچسب سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این برچسب سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionTagRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionTagRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionTagRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionTag'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_tags_retrieve
      description: |-
        دریافت جزئیات یک برچسب سؤال با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر برچسب سؤال خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `question.read`
      summary: جزئیات برچسب سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این برچسب سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionTag'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_question_tags_partial_update
      description: |-
        ویرایش جزئی برچسب سؤال.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش جزئی برچسب سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این برچسب سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedQuestionTagRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedQuestionTagRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedQuestionTagRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionTag'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_question_tags_destroy
      description: |-
        حذف نرم برچسب سؤال.

        برچسب سؤال از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `question.delete`
      summary: حذف برچسب سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این برچسب سؤال را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/questions/:
    post:
      operationId: assessment_questions_create
      description: |-
        ایجاد سؤال تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `question.create`
      summary: ایجاد سؤال
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_questions_list
      description: فیلتر بر اساس بانک، نوع، دشواری، مبحث و پایه — برای صفحه «بانک
        سؤال» (بخش ۹.۱ سند فرانت).
      summary: فهرست سؤالات
      parameters:
      - in: query
        name: bank
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: course
        schema:
          type: string
          format: uuid
        description: درس
      - in: query
        name: difficulty
        schema:
          type: string
        description: سطح دشواری نسخه جاری
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
        description: پایه
      - in: query
        name: lifecycle_status
        schema:
          type: string
          title: وضعیت چرخه عمر
          enum:
          - APPROVED
          - DRAFT
          - PUBLISHED
          - RETIRED
          - UNDER_REVIEW
        description: |-
          * `DRAFT` - پیش‌نویس
          * `UNDER_REVIEW` - در حال بازبینی
          * `APPROVED` - تأییدشده
          * `PUBLISHED` - منتشرشده
          * `RETIRED` - بازنشسته
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: question_type
        schema:
          type: string
          title: نوع سؤال
          enum:
          - AUDIO
          - ESSAY
          - FILE_UPLOAD
          - MATCHING
          - MULTIPLE_CHOICE
          - NUMERIC
          - ORDERING
          - SHORT_ANSWER
          - SINGLE_CHOICE
          - TRUE_FALSE
        description: |-
          * `SINGLE_CHOICE` - چندگزینه‌ای تک‌پاسخ
          * `MULTIPLE_CHOICE` - چندگزینه‌ای چندپاسخی
          * `TRUE_FALSE` - درست/نادرست
          * `SHORT_ANSWER` - کوتاه‌پاسخ
          * `ESSAY` - تشریحی
          * `NUMERIC` - عددی
          * `MATCHING` - تطبیقی
          * `ORDERING` - ترتیبی
          * `FILE_UPLOAD` - بارگذاری فایل
          * `AUDIO` - پاسخ صوتی
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: tag
        schema:
          type: string
        description: شناسه برچسب مبحث
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedQuestionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/questions/create-with-version/:
    post:
      operationId: assessment_questions_create_with_version_create
      description: یک درخواست برای کل ویرایشگر سؤال. سؤال، نسخه ۱، گزینه‌ها و برچسب‌ها
        در یک تراکنش ساخته می‌شوند.
      summary: ایجاد سؤال به‌همراه نسخه اول و گزینه‌ها
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionCreateRequest'
            examples:
              سؤالچندگزینه‌ای:
                value:
                  bank: 1a2b3c4d-5e6f-7a8b-9c0d-1e2f3a4b5c6d
                  question_type: SINGLE_CHOICE
                  body: پایتخت ایران کدام شهر است؟
                  explanation: تهران از سال ۱۱۶۵ خورشیدی پایتخت است.
                  default_score: 1
                  difficulty: EASY
                  options:
                  - option_key: A
                    body: اصفهان
                    is_correct: false
                    display_order: 1
                  - option_key: B
                    body: تهران
                    is_correct: true
                    display_order: 2
                  - option_key: C
                    body: شیراز
                    is_correct: false
                    display_order: 3
                  - option_key: D
                    body: تبریز
                    is_correct: false
                    display_order: 4
                summary: سؤال چندگزینه‌ای
              سؤالعددیباتلورانس:
                value:
                  bank: 1a2b3c4d-5e6f-7a8b-9c0d-1e2f3a4b5c6d
                  question_type: NUMERIC
                  body: مقدار تقریبی عدد پی را تا دو رقم اعشار بنویسید.
                  default_score: 2
                  correct_answer:
                    value: 3.14
                    tolerance: 0.01
                summary: سؤال عددی با تلورانس
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/questions/{id}/:
    put:
      operationId: assessment_questions_update
      description: |-
        جایگزینی کامل سؤال.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش کامل سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_questions_retrieve
      description: |-
        دریافت جزئیات یک سؤال با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سؤال خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `question.read`
      summary: جزئیات سؤال با نسخه جاری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_questions_partial_update
      description: |-
        ویرایش جزئی سؤال.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش جزئی سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedQuestionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedQuestionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedQuestionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_questions_destroy
      description: |-
        حذف نرم سؤال.

        سؤال از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `question.delete`
      summary: حذف سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/questions/{id}/approve/:
    post:
      operationId: assessment_questions_approve_create
      description: 'چرخه سؤال: پیش‌نویس → بازبینی → تأیید → انتشار → بازنشستگی (بخش
        ۴.۴).'
      summary: تأیید نسخه جاری سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/questions/{id}/retire/:
    post:
      operationId: assessment_questions_retire_create
      description: سؤال بازنشسته در آزمون‌های جدید قابل استفاده نیست، اما آزمون‌های
        گذشته دست‌نخورده می‌مانند.
      summary: بازنشستگی سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال را شناسایی میکند.
        required: true
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Question'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/question-versions/:
    post:
      operationId: assessment_question_versions_create
      description: نسخه‌های قبلی حفظ می‌شوند تا آزمون‌های گذشته تغییر نکنند (بخش ۷.۶).
      summary: ایجاد نسخه جدید سؤال
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionVersionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionVersionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionVersionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionVersion'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_versions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نسخه‌های سؤال.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `question`، `review_status`، `difficulty`، `grade_level`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `question.read`
      summary: نسخه‌های سؤال
      parameters:
      - in: query
        name: difficulty
        schema:
          type: string
          title: دشواری
          enum:
          - EASY
          - HARD
          - MEDIUM
          - VERY_EASY
          - VERY_HARD
        description: |-
          * `VERY_EASY` - خیلی آسان
          * `EASY` - آسان
          * `MEDIUM` - متوسط
          * `HARD` - دشوار
          * `VERY_HARD` - خیلی دشوار
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: question
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: review_status
        schema:
          type: string
          title: وضعیت بازبینی
          enum:
          - APPROVED
          - CHANGES_REQUESTED
          - PENDING
          - REJECTED
        description: |-
          * `PENDING` - در انتظار بازبینی
          * `APPROVED` - تأییدشده
          * `CHANGES_REQUESTED` - نیازمند اصلاح
          * `REJECTED` - ردشده
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedQuestionVersionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/question-versions/{id}/:
    put:
      operationId: assessment_question_versions_update
      description: |-
        جایگزینی کامل نسخه سؤال.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش کامل نسخه سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionVersionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionVersionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionVersionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionVersion'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_versions_retrieve
      description: |-
        دریافت جزئیات یک نسخه سؤال با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نسخه سؤال خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `question.read`
      summary: جزئیات نسخه سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionVersion'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_question_versions_partial_update
      description: |-
        ویرایش جزئی نسخه سؤال.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش جزئی نسخه سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedQuestionVersionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedQuestionVersionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedQuestionVersionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionVersion'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_question_versions_destroy
      description: |-
        حذف نرم نسخه سؤال.

        نسخه سؤال از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `question.delete`
      summary: حذف نسخه سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه سؤال را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/question-options/:
    post:
      operationId: assessment_question_options_create
      description: |-
        ایجاد گزینه سؤال تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `question.create`
      summary: ایجاد گزینه سؤال
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionOptionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionOptionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionOptionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionOption'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_options_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده گزینه‌های سؤال.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `question_version`، `is_correct`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `question.read`
      summary: گزینه‌های سؤال
      parameters:
      - in: query
        name: is_correct
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: question_version
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedQuestionOptionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/question-options/{id}/:
    put:
      operationId: assessment_question_options_update
      description: |-
        جایگزینی کامل گزینه سؤال.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش کامل گزینه سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این گزینه سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuestionOptionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/QuestionOptionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/QuestionOptionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionOption'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_question_options_retrieve
      description: |-
        دریافت جزئیات یک گزینه سؤال با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر گزینه سؤال خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `question.read`
      summary: جزئیات گزینه سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این گزینه سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionOption'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_question_options_partial_update
      description: |-
        ویرایش جزئی گزینه سؤال.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `question.update`
      summary: ویرایش جزئی گزینه سؤال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این گزینه سؤال را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedQuestionOptionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedQuestionOptionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedQuestionOptionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionOption'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_question_options_destroy
      description: |-
        حذف نرم گزینه سؤال.

        گزینه سؤال از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `question.delete`
      summary: حذف گزینه سؤال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این گزینه سؤال را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/exams/:
    post:
      operationId: assessment_exams_create
      description: |-
        ایجاد آزمون تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `exam.create`
      summary: ایجاد آزمون
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exams_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده آزمون‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `mode`، `purpose`، `status`، `class_group`، `course`، `term`

        **جست‌وجو** — پارامتر `search` روی `code`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: فهرست آزمون‌ها
      parameters:
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: course
        schema:
          type: string
          format: uuid
        description: درس
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: mode
        schema:
          type: string
          title: حالت
          enum:
          - HYBRID
          - IN_PERSON
          - ONLINE
          - PAPER_ENTRY
          - TAKE_HOME
        description: |-
          * `ONLINE` - آنلاین
          * `IN_PERSON` - حضوری
          * `HYBRID` - ترکیبی
          * `PAPER_ENTRY` - کاغذی با ورود نمره
          * `TAKE_HOME` - تکلیف زمان‌دار
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: purpose
        schema:
          type: string
          title: هدف
          enum:
          - FINAL
          - FORMATIVE
          - MAKEUP
          - MIDTERM
          - PLACEMENT
          - PRACTICE
        description: |-
          * `FORMATIVE` - مستمر
          * `MIDTERM` - میان‌ترم
          * `FINAL` - پایانی
          * `PRACTICE` - تمرینی
          * `MAKEUP` - جبرانی
          * `PLACEMENT` - تعیین سطح
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - ARCHIVED
          - CANCELLED
          - DRAFT
          - GRADING
          - IN_PROGRESS
          - MODERATION
          - PUBLISHED
          - PUBLISHED_RESULTS
          - RESULTS_READY
          - SCHEDULED
          - SUBMISSION_CLOSED
          - UNDER_REVIEW
        description: |-
          * `DRAFT` - پیش‌نویس
          * `UNDER_REVIEW` - در حال بازبینی
          * `APPROVED` - تأییدشده
          * `SCHEDULED` - زمان‌بندی‌شده
          * `PUBLISHED` - منتشرشده
          * `IN_PROGRESS` - در حال اجرا
          * `SUBMISSION_CLOSED` - پایان مهلت تحویل
          * `GRADING` - در حال تصحیح
          * `MODERATION` - بازبینی نمرات
          * `RESULTS_READY` - نتایج آماده
          * `PUBLISHED_RESULTS` - نتایج منتشرشده
          * `ARCHIVED` - بایگانی
          * `CANCELLED` - لغوشده
      - in: query
        name: term
        schema:
          type: string
          format: uuid
        description: ترم
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedExamList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/exams/{id}/:
    put:
      operationId: assessment_exams_update
      description: |-
        جایگزینی کامل آزمون.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `exam.update`
      summary: ویرایش کامل آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exams_retrieve
      description: برای «سازنده آزمون» (بخش ۹.۳ سند فرانت).
      summary: جزئیات آزمون با بخش‌ها و جلسات
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamDetail'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_exams_partial_update
      description: |-
        ویرایش جزئی آزمون.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `exam.update`
      summary: ویرایش جزئی آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedExamRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedExamRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedExamRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_exams_destroy
      description: |-
        حذف نرم آزمون.

        آزمون از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `exam.delete`
      summary: حذف آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/exams/{id}/analysis/:
    get:
      operationId: assessment_exams_analysis_list
      description: درصد پاسخ صحیح، میانگین نمره و میانگین زمان پاسخ هر سؤال (بخش ۴.۴
        — تحلیل کیفیت سؤال).
      summary: تحلیل سؤالات آزمون
      parameters:
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: course
        schema:
          type: string
          format: uuid
        description: درس
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      - in: query
        name: mode
        schema:
          type: string
          title: حالت
          enum:
          - HYBRID
          - IN_PERSON
          - ONLINE
          - PAPER_ENTRY
          - TAKE_HOME
        description: |-
          * `ONLINE` - آنلاین
          * `IN_PERSON` - حضوری
          * `HYBRID` - ترکیبی
          * `PAPER_ENTRY` - کاغذی با ورود نمره
          * `TAKE_HOME` - تکلیف زمان‌دار
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: purpose
        schema:
          type: string
          title: هدف
          enum:
          - FINAL
          - FORMATIVE
          - MAKEUP
          - MIDTERM
          - PLACEMENT
          - PRACTICE
        description: |-
          * `FORMATIVE` - مستمر
          * `MIDTERM` - میان‌ترم
          * `FINAL` - پایانی
          * `PRACTICE` - تمرینی
          * `MAKEUP` - جبرانی
          * `PLACEMENT` - تعیین سطح
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - ARCHIVED
          - CANCELLED
          - DRAFT
          - GRADING
          - IN_PROGRESS
          - MODERATION
          - PUBLISHED
          - PUBLISHED_RESULTS
          - RESULTS_READY
          - SCHEDULED
          - SUBMISSION_CLOSED
          - UNDER_REVIEW
        description: |-
          * `DRAFT` - پیش‌نویس
          * `UNDER_REVIEW` - در حال بازبینی
          * `APPROVED` - تأییدشده
          * `SCHEDULED` - زمان‌بندی‌شده
          * `PUBLISHED` - منتشرشده
          * `IN_PROGRESS` - در حال اجرا
          * `SUBMISSION_CLOSED` - پایان مهلت تحویل
          * `GRADING` - در حال تصحیح
          * `MODERATION` - بازبینی نمرات
          * `RESULTS_READY` - نتایج آماده
          * `PUBLISHED_RESULTS` - نتایج منتشرشده
          * `ARCHIVED` - بایگانی
          * `CANCELLED` - لغوشده
      - in: query
        name: term
        schema:
          type: string
          format: uuid
        description: ترم
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedQuestionAnalysisRowList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/exams/{id}/approve/:
    post:
      operationId: assessment_exams_approve_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تأیید آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exams/{id}/cancel/:
    post:
      operationId: assessment_exams_cancel_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: لغو آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exams/{id}/close-submission/:
    post:
      operationId: assessment_exams_close_submission_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بستن مهلت تحویل
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exams/{id}/publish/:
    post:
      operationId: assessment_exams_publish_create
      description: پیش از انتشار بررسی می‌شود که مجموع بارم سؤالات با نمره کل آزمون
        برابر باشد و حداقل یک جلسه آزمون تعریف شده باشد (بخش ۷.۶).
      summary: انتشار آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                خطایناسازگاریبارم:
                  value:
                    code: EXAM_NOT_READY_FOR_PUBLISH
                    message: آزمون برای انتشار آماده نیست.
                    correlationId: 8c4a1f2e
                    fieldErrors:
                    - field: maxScore
                      reason: مجموع بارم سؤالات (18.00) با نمره کل آزمون (20.00) برابر
                        نیست.
                    retryable: false
                  summary: خطای ناسازگاری بارم
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exams/{id}/release-results/:
    post:
      operationId: assessment_exams_release_results_create
      description: پس از انتشار، پنجره اعتراض (پیش‌فرض ۷ روز) باز می‌شود و رویداد
        `ScoreFinalized` منتشر می‌گردد.
      summary: انتشار نتایج
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exams/{id}/start-grading/:
    post:
      operationId: assessment_exams_start_grading_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: شروع مرحله تصحیح
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exams/{id}/submit-for-review/:
    post:
      operationId: assessment_exams_submit_for_review_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: ارسال آزمون برای بازبینی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Exam'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exam-sections/:
    post:
      operationId: assessment_exam_sections_create
      description: |-
        ایجاد بخش آزمون تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `exam.create`
      summary: ایجاد بخش آزمون
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamSectionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamSectionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamSectionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSection'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_sections_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده بخش‌های آزمون.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `exam`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: بخش‌های آزمون
      parameters:
      - in: query
        name: exam
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedExamSectionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/exam-sections/{id}/:
    put:
      operationId: assessment_exam_sections_update
      description: |-
        جایگزینی کامل بخش آزمون.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش کامل بخش آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بخش آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamSectionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamSectionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamSectionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSection'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_sections_retrieve
      description: |-
        دریافت جزئیات یک بخش آزمون با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر بخش آزمون خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات بخش آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بخش آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSection'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_exam_sections_partial_update
      description: |-
        ویرایش جزئی بخش آزمون.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش جزئی بخش آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بخش آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedExamSectionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedExamSectionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedExamSectionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSection'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_exam_sections_destroy
      description: |-
        حذف نرم بخش آزمون.

        بخش آزمون از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `exam.delete`
      summary: حذف بخش آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بخش آزمون را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/exam-questions/:
    post:
      operationId: assessment_exam_questions_create
      description: ارجاع به نسخه ثابت سؤال است، نه به خود سؤال (بخش ۷.۶).
      summary: افزودن سؤال به آزمون
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamQuestionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamQuestionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamQuestionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamQuestion'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_questions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده سؤالات آزمون.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `section`، `question_version`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: سؤالات آزمون
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: question_version
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: section
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedExamQuestionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/exam-questions/{id}/:
    put:
      operationId: assessment_exam_questions_update
      description: |-
        جایگزینی کامل سؤال آزمون.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش کامل سؤال آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamQuestionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamQuestionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamQuestionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamQuestion'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_questions_retrieve
      description: |-
        دریافت جزئیات یک سؤال آزمون با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سؤال آزمون خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات سؤال آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamQuestion'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_exam_questions_partial_update
      description: |-
        ویرایش جزئی سؤال آزمون.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش جزئی سؤال آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedExamQuestionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedExamQuestionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedExamQuestionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamQuestion'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_exam_questions_destroy
      description: |-
        حذف نرم سؤال آزمون.

        سؤال آزمون از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `exam.delete`
      summary: حذف سؤال آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سؤال آزمون را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/exam-sessions/:
    post:
      operationId: assessment_exam_sessions_create
      description: |-
        ایجاد جلسه آزمون تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `exam.create`
      summary: تعریف جلسه آزمون
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamSessionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSession'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_sessions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده جلسات آزمون.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `exam`، `room`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: جلسات آزمون
      parameters:
      - in: query
        name: exam
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: room
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - CLOSED
          - OPEN
          - PLANNED
        description: |-
          * `PLANNED` - برنامه‌ریزی‌شده
          * `OPEN` - باز
          * `CLOSED` - بسته
          * `CANCELLED` - لغوشده
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedExamSessionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/exam-sessions/{id}/:
    put:
      operationId: assessment_exam_sessions_update
      description: |-
        جایگزینی کامل جلسه آزمون.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `exam.update`
      summary: ویرایش کامل جلسه آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamSessionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_sessions_retrieve
      description: |-
        دریافت جزئیات یک جلسه آزمون با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر جلسه آزمون خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات جلسه آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_exam_sessions_partial_update
      description: |-
        ویرایش جزئی جلسه آزمون.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `exam.update`
      summary: ویرایش جزئی جلسه آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedExamSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedExamSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedExamSessionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_exam_sessions_destroy
      description: |-
        حذف نرم جلسه آزمون.

        جلسه آزمون از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `exam.delete`
      summary: حذف جلسه آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/exam-sessions/{id}/close/:
    post:
      operationId: assessment_exam_sessions_close_create
      description: تلاش‌های باز به‌صورت خودکار تحویل می‌شوند.
      summary: بستن جلسه آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exam-sessions/{id}/enroll-class/:
    post:
      operationId: assessment_exam_sessions_enroll_class_create
      description: همه اعضای فعال کلاسِ ارائه درس آزمون را ثبت‌نام می‌کند.
      summary: ثبت‌نام گروهی دانش‌آموزان یک کلاس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exam-sessions/{id}/open/:
    post:
      operationId: assessment_exam_sessions_open_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بازکردن جلسه آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/exam-registrations/:
    post:
      operationId: assessment_exam_registrations_create
      description: '`extraTimeMinutes` برای زمان اضافه فردی (دسترس‌پذیری) است.'
      summary: ثبت‌نام دانش‌آموز در جلسه آزمون
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamRegistrationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamRegistrationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamRegistrationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamRegistration'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_registrations_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده ثبت‌نام‌های آزمون.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `exam_session`، `enrollment`، `registration_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: ثبت‌نام‌های آزمون
      parameters:
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: exam_session
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: registration_status
        schema:
          type: string
          title: وضعیت ثبت‌نام
          enum:
          - ABSENT
          - DISQUALIFIED
          - EXCUSED
          - REGISTERED
        description: |-
          * `REGISTERED` - ثبت‌نام‌شده
          * `ABSENT` - غایب
          * `EXCUSED` - غیبت موجه
          * `DISQUALIFIED` - محروم
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedExamRegistrationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/exam-registrations/{id}/:
    put:
      operationId: assessment_exam_registrations_update
      description: |-
        جایگزینی کامل ثبت‌نام آزمون.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش کامل ثبت‌نام آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExamRegistrationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ExamRegistrationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ExamRegistrationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamRegistration'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_exam_registrations_retrieve
      description: |-
        دریافت جزئیات یک ثبت‌نام آزمون با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ثبت‌نام آزمون خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات ثبت‌نام آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamRegistration'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_exam_registrations_partial_update
      description: |-
        ویرایش جزئی ثبت‌نام آزمون.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش جزئی ثبت‌نام آزمون
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام آزمون را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedExamRegistrationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedExamRegistrationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedExamRegistrationRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamRegistration'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_exam_registrations_destroy
      description: |-
        حذف نرم ثبت‌نام آزمون.

        ثبت‌نام آزمون از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `exam.delete`
      summary: حذف ثبت‌نام آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ثبت‌نام آزمون را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/attempts/:
    get:
      operationId: assessment_attempts_list
      description: |-
        تلاش‌های آزمون.

        تلاش با Endpoint صریح `start` ساخته می‌شود، نه با POST معمولی روی منبع
        (بخش ۱۲.۴: عملیات تغییر وضعیت از Endpoint صریح استفاده می‌کند).

        دریافت فهرست صفحه‌بندی‌شده تلاش‌های آزمون.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `registration`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `attempt.read`
      summary: تلاش‌های آزمون
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: registration
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - AUTO_SUBMITTED
          - CREATED
          - EXPIRED
          - FINALIZED
          - GRADED
          - GRADING
          - INTERRUPTED
          - IN_PROGRESS
          - REGRADED
          - SUBMITTED
          - UNDER_APPEAL
        description: |-
          * `CREATED` - ایجادشده
          * `IN_PROGRESS` - در حال پاسخ‌دهی
          * `INTERRUPTED` - قطع اتصال
          * `SUBMITTED` - تحویل‌شده
          * `AUTO_SUBMITTED` - تحویل خودکار (پایان زمان)
          * `EXPIRED` - منقضی (شروع نشد)
          * `GRADING` - در حال تصحیح
          * `GRADED` - تصحیح‌شده
          * `UNDER_APPEAL` - در حال رسیدگی به اعتراض
          * `REGRADED` - بازتصحیح‌شده
          * `FINALIZED` - نهایی‌شده
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedExamAttemptList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/attempts/grading-queue/:
    get:
      operationId: assessment_attempts_grading_queue_list
      description: پاسخ‌های نیازمند تصحیح انسانی. با `anonymous=true` نام دانش‌آموز
        با شناسه مستعار جایگزین می‌شود (ناشناس‌سازی مصحح — بخش ۴.۴).
      summary: صف تصحیح تشریحی
      parameters:
      - in: query
        name: anonymous
        schema:
          type: boolean
        description: ناشناس‌سازی هویت دانش‌آموز
      - in: query
        name: exam
        schema:
          type: string
        description: فیلتر بر اساس آزمون
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: registration
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - AUTO_SUBMITTED
          - CREATED
          - EXPIRED
          - FINALIZED
          - GRADED
          - GRADING
          - INTERRUPTED
          - IN_PROGRESS
          - REGRADED
          - SUBMITTED
          - UNDER_APPEAL
        description: |-
          * `CREATED` - ایجادشده
          * `IN_PROGRESS` - در حال پاسخ‌دهی
          * `INTERRUPTED` - قطع اتصال
          * `SUBMITTED` - تحویل‌شده
          * `AUTO_SUBMITTED` - تحویل خودکار (پایان زمان)
          * `EXPIRED` - منقضی (شروع نشد)
          * `GRADING` - در حال تصحیح
          * `GRADED` - تصحیح‌شده
          * `UNDER_APPEAL` - در حال رسیدگی به اعتراض
          * `REGRADED` - بازتصحیح‌شده
          * `FINALIZED` - نهایی‌شده
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGradingQueueRowList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/attempts/start/:
    post:
      operationId: assessment_attempts_start_create
      description: |-
        **Idempotent است.** هدر `Idempotency-Key` را بفرستید؛ ارسال مجدد با همان کلید، همان تلاش را برمی‌گرداند و تلاش دوم نمی‌سازد.

        اگر تلاشی در وضعیت «در حال پاسخ‌دهی» یا «قطع اتصال» وجود داشته باشد، همان ادامه پیدا می‌کند (بخش ۹.۷ سند فرانت — بازیابی پس از قطع اتصال).
      summary: شروع تلاش آزمون
      parameters:
      - in: header
        name: Idempotency-Key
        schema:
          type: string
        description: کلید یکتای عملیات برای جلوگیری از تلاش تکراری
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartAttemptRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StartAttemptRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StartAttemptRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamAttempt'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/attempts/{id}/:
    get:
      operationId: assessment_attempts_retrieve
      description: |-
        تلاش‌های آزمون.

        تلاش با Endpoint صریح `start` ساخته می‌شود، نه با POST معمولی روی منبع
        (بخش ۱۲.۴: عملیات تغییر وضعیت از Endpoint صریح استفاده می‌کند).

        دریافت جزئیات یک تلاش آزمون با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تلاش آزمون خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `attempt.read`
      summary: جزئیات تلاش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تلاش آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamAttempt'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/assessment/attempts/{id}/paper/:
    get:
      operationId: assessment_attempts_paper_retrieve
      description: |-
        سؤالات آزمون به همراه پاسخ‌های ذخیره‌شده و زمان باقی‌مانده.

        **کلید پاسخ و پاسخ صحیح در این پاسخ برنمی‌گردد.**
      summary: برگه آزمون در حال اجرا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تلاش آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttemptPaper'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/attempts/{id}/answers/:
    post:
      operationId: assessment_attempts_answers_create
      description: |-
        هر بار فراخوانی، شماره بازنگری پاسخ یک واحد جلو می‌رود و `lastSavedAt` تلاش به‌روز می‌شود. پس از پایان مهلت، خطای `ATTEMPT_TIME_EXPIRED` برمی‌گردد.

        قالب `responsePayload` بر اساس نوع سؤال در توضیح مدل آمده است.
      summary: ذخیره خودکار پاسخ
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تلاش آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveAnswerRequest'
            examples:
              چندگزینه‌ای:
                value:
                  exam_question: 9d8c7b6a-5f4e-3d2c-1b0a-9e8d7c6b5a4f
                  response_payload:
                    selectedKeys:
                    - B
                  time_spent_seconds: 34
              تشریحی:
                value:
                  exam_question: 9d8c7b6a-5f4e-3d2c-1b0a-9e8d7c6b5a4f
                  response_payload:
                    text: پاسخ تشریحی دانش‌آموز…
                  time_spent_seconds: 240
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SaveAnswerRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SaveAnswerRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttemptAnswer'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/attempts/{id}/submit/:
    post:
      operationId: assessment_attempts_submit_create
      description: 'عملیات اتمیک و تکرارپذیر است: ارسال مجدد پس از تحویل، همان نتیجه
        را برمی‌گرداند و خطا نمی‌دهد (بخش ۷.۶). تصحیح خودکار بلافاصله اجرا می‌شود.'
      summary: تحویل نهایی آزمون
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تلاش آزمون را شناسایی میکند.
        required: true
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExamAttempt'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/attempt-answers/:
    post:
      operationId: assessment_attempt_answers_create
      description: |-
        ایجاد پاسخ تلاش تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `exam.create`
      summary: ایجاد پاسخ تلاش
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttemptAnswerRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AttemptAnswerRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttemptAnswerRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttemptAnswer'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_attempt_answers_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پاسخ‌های تلاش.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `attempt`، `exam_question`، `grading_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: پاسخ‌های تلاش
      parameters:
      - in: query
        name: attempt
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: exam_question
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grading_status
        schema:
          type: string
          title: وضعیت تصحیح
          enum:
          - AUTO_GRADED
          - MANUAL_GRADED
          - NEEDS_REVIEW
          - PENDING
        description: |-
          * `PENDING` - در انتظار تصحیح
          * `AUTO_GRADED` - تصحیح خودکار
          * `MANUAL_GRADED` - تصحیح دستی
          * `NEEDS_REVIEW` - نیازمند بازبینی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAttemptAnswerList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/attempt-answers/{id}/:
    get:
      operationId: assessment_attempt_answers_retrieve
      description: |-
        دریافت جزئیات یک پاسخ تلاش با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پاسخ تلاش خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات پاسخ تلاش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پاسخ تلاش را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttemptAnswer'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/assessment/attempt-answers/{id}/grade/:
    post:
      operationId: assessment_attempt_answers_grade_create
      description: نمره باید بین ۰ و بارم سؤال باشد. پس از ثبت، نمره تلاش مجدداً محاسبه
        و `calculationVersion` یک واحد جلو می‌رود (بخش ۱۱.۵).
      summary: ثبت نمره تصحیح دستی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پاسخ تلاش را شناسایی میکند.
        required: true
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManualGradeRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ManualGradeRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ManualGradeRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeReview'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/assessment/proctor-events/:
    post:
      operationId: assessment_proctor_events_create
      description: |-
        ایجاد رخداد مراقبت تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `exam.create`
      summary: ثبت رخداد مراقبت
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProctorEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProctorEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProctorEventRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProctorEvent'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_proctor_events_list
      description: رخدادها فقط «شاهد» هستند و تصمیم تخلف را به‌صورت خودکار نمی‌گیرند
        (بخش ۷.۶).
      summary: رخدادهای مراقبت
      parameters:
      - in: query
        name: attempt
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: event_type
        schema:
          type: string
          title: نوع رویداد
          enum:
          - COPY_PASTE
          - DISCONNECT
          - FOCUS_LOST
          - IP_CHANGE
          - MANUAL_FLAG
          - MULTIPLE_SESSION
          - RECONNECT
          - TAB_SWITCH
        description: |-
          * `TAB_SWITCH` - خروج از پنجره آزمون
          * `FOCUS_LOST` - از دست رفتن تمرکز صفحه
          * `COPY_PASTE` - کپی/چسباندن
          * `MULTIPLE_SESSION` - ورود هم‌زمان
          * `DISCONNECT` - قطع اتصال
          * `RECONNECT` - اتصال مجدد
          * `IP_CHANGE` - تغییر نشانی شبکه
          * `MANUAL_FLAG` - ثبت دستی مراقب
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: severity
        schema:
          type: string
          title: شدت
          enum:
          - CRITICAL
          - INFO
          - WARNING
        description: |-
          * `INFO` - اطلاعاتی
          * `WARNING` - هشدار
          * `CRITICAL` - بحرانی
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedProctorEventList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/proctor-events/{id}/:
    put:
      operationId: assessment_proctor_events_update
      description: |-
        جایگزینی کامل رخداد مراقبت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش کامل رخداد مراقبت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد مراقبت را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProctorEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProctorEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProctorEventRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProctorEvent'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_proctor_events_retrieve
      description: |-
        دریافت جزئیات یک رخداد مراقبت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رخداد مراقبت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات رخداد مراقبت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد مراقبت را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProctorEvent'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_proctor_events_partial_update
      description: |-
        ویرایش جزئی رخداد مراقبت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `exam.update`
      summary: ویرایش جزئی رخداد مراقبت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد مراقبت را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedProctorEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedProctorEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedProctorEventRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProctorEvent'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_proctor_events_destroy
      description: |-
        حذف نرم رخداد مراقبت.

        رخداد مراقبت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `exam.delete`
      summary: حذف رخداد مراقبت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد مراقبت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/grade-reviews/:
    get:
      operationId: assessment_grade_reviews_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده بازبینی‌های نمره.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `attempt_answer`، `review_type`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `exam.read`
      summary: بازبینی‌های نمره
      parameters:
      - in: query
        name: attempt_answer
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: review_type
        schema:
          type: string
          title: نوع بازبینی
          enum:
          - APPEAL
          - FIRST_PASS
          - MODERATION
          - SECOND_PASS
        description: |-
          * `FIRST_PASS` - تصحیح اول
          * `SECOND_PASS` - بازبینی دوم
          * `APPEAL` - بازتصحیح پس از اعتراض
          * `MODERATION` - تعدیل
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGradeReviewList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/grade-reviews/{id}/:
    get:
      operationId: assessment_grade_reviews_retrieve
      description: |-
        دریافت جزئیات یک بازبینی نمره با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر بازبینی نمره خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `exam.read`
      summary: جزئیات بازبینی نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این بازبینی نمره را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeReview'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/assessment/appeals/:
    post:
      operationId: assessment_appeals_create
      description: فقط تا پایان پنجره اعتراض آزمون امکان‌پذیر است.
      summary: ثبت اعتراض به نمره
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeAppealRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeAppealRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeAppealRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeAppeal'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_appeals_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اعتراض‌های نمره.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `attempt`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `appeal.read`
      summary: اعتراض‌های نمره
      parameters:
      - in: query
        name: attempt
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACCEPTED
          - REJECTED
          - SUBMITTED
          - UNDER_REVIEW
          - WITHDRAWN
        description: |-
          * `SUBMITTED` - ثبت‌شده
          * `UNDER_REVIEW` - در حال بررسی
          * `ACCEPTED` - پذیرفته‌شده
          * `REJECTED` - ردشده
          * `WITHDRAWN` - پس‌گرفته‌شده
      tags:
      - Assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGradeAppealList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/assessment/appeals/{id}/:
    put:
      operationId: assessment_appeals_update
      description: |-
        جایگزینی کامل اعتراض به نمره.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `appeal.update`
      summary: ویرایش کامل اعتراض به نمره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعتراض به نمره را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeAppealRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeAppealRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeAppealRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeAppeal'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: assessment_appeals_retrieve
      description: |-
        دریافت جزئیات یک اعتراض به نمره با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر اعتراض به نمره خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `appeal.read`
      summary: جزئیات اعتراض به نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعتراض به نمره را شناسایی میکند.
        required: true
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeAppeal'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: assessment_appeals_partial_update
      description: |-
        ویرایش جزئی اعتراض به نمره.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `appeal.update`
      summary: ویرایش جزئی اعتراض به نمره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعتراض به نمره را شناسایی میکند.
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedGradeAppealRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedGradeAppealRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedGradeAppealRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeAppeal'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: assessment_appeals_destroy
      description: |-
        حذف نرم اعتراض به نمره.

        اعتراض به نمره از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `appeal.delete`
      summary: حذف اعتراض به نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعتراض به نمره را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - assessment
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/assessment/appeals/{id}/resolve/:
    post:
      operationId: assessment_appeals_resolve_create
      description: با پذیرش اعتراض، تلاش به مرحله بازتصحیح می‌رود و نمره قبل/بعد برای
        ممیزی نگهداری می‌شود.
      summary: رسیدگی به اعتراض
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعتراض به نمره را شناسایی میکند.
        required: true
      tags:
      - Assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveAppealRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ResolveAppealRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ResolveAppealRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeAppeal'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/categories/:
    post:
      operationId: gradebook_categories_create
      description: مجموع وزن دسته‌های فعال هر درس باید دقیقاً ۱۰۰٪ باشد (بخش ۷.۷).
      summary: تعریف دسته ارزشیابی
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssessmentCategoryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssessmentCategoryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssessmentCategoryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentCategory'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_categories_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده دسته‌های ارزشیابی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `is_active`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `grade.read`
      summary: دسته‌های ارزشیابی
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAssessmentCategoryList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/categories/validate-weights/:
    get:
      operationId: gradebook_categories_validate_weights_retrieve
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: اعتبارسنجی مجموع وزن دسته‌ها
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
        description: شناسه ارائه درس
        required: true
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/categories/{id}/:
    put:
      operationId: gradebook_categories_update
      description: |-
        جایگزینی کامل دسته ارزشیابی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `grade.update`
      summary: ویرایش کامل دسته ارزشیابی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارزشیابی را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssessmentCategoryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssessmentCategoryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssessmentCategoryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentCategory'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_categories_retrieve
      description: |-
        دریافت جزئیات یک دسته ارزشیابی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر دسته ارزشیابی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `grade.read`
      summary: جزئیات دسته ارزشیابی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارزشیابی را شناسایی میکند.
        required: true
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentCategory'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: gradebook_categories_partial_update
      description: |-
        ویرایش جزئی دسته ارزشیابی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `grade.update`
      summary: ویرایش جزئی دسته ارزشیابی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارزشیابی را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAssessmentCategoryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAssessmentCategoryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAssessmentCategoryRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentCategory'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: gradebook_categories_destroy
      description: |-
        حذف نرم دسته ارزشیابی.

        دسته ارزشیابی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `grade.delete`
      summary: حذف دسته ارزشیابی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته ارزشیابی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/gradebook/grade-items/:
    post:
      operationId: gradebook_grade_items_create
      description: |-
        ایجاد قلم نمره تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `grade.create`
      summary: ایجاد قلم نمره
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeItem'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_grade_items_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام نمره.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `category`، `source_type`، `status`

        **جست‌وجو** — پارامتر `search` روی `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `grade.read`
      summary: اقلام نمره
      parameters:
      - in: query
        name: category
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: source_type
        schema:
          type: string
          title: نوع منبع
          enum:
          - ASSIGNMENT
          - CLASS_ACTIVITY
          - EXAM
          - MANUAL
          - PROJECT
        description: |-
          * `EXAM` - آزمون
          * `ASSIGNMENT` - تکلیف
          * `CLASS_ACTIVITY` - فعالیت کلاسی
          * `PROJECT` - پروژه
          * `MANUAL` - ثبت دستی
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - DRAFT
          - LOCKED
          - OPEN
          - PUBLISHED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `OPEN` - باز برای ثبت نمره
          * `LOCKED` - قفل‌شده
          * `PUBLISHED` - منتشرشده
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGradeItemList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/grade-items/{id}/:
    put:
      operationId: gradebook_grade_items_update
      description: |-
        جایگزینی کامل قلم نمره.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `grade.update`
      summary: ویرایش کامل قلم نمره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GradeItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GradeItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GradeItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeItem'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_grade_items_retrieve
      description: |-
        دریافت جزئیات یک قلم نمره با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم نمره خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `grade.read`
      summary: جزئیات قلم نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeItem'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: gradebook_grade_items_partial_update
      description: |-
        ویرایش جزئی قلم نمره.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `grade.update`
      summary: ویرایش جزئی قلم نمره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedGradeItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedGradeItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedGradeItemRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeItem'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: gradebook_grade_items_destroy
      description: |-
        حذف نرم قلم نمره.

        قلم نمره از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `grade.delete`
      summary: حذف قلم نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/gradebook/grade-items/{id}/scores/:
    post:
      operationId: gradebook_grade_items_scores_create
      description: |-
        برای هر دانش‌آموز، `status` را دقیق انتخاب کنید: `ABSENT` برای غایب، `EXEMPT` برای معاف و `RECORDED` با `rawScore = 0` برای نمره صفر. این سه حالت با هم یکسان نیستند (بخش ۷.۷).

        اگر قلم نمره قفل باشد، فیلد `reason` الزامی است.
      summary: ثبت گروهی نمرات یک قلم
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkScoreRequest'
            examples:
              ثبتگروهی:
                value:
                  rows:
                  - enrollment: 11111111-1111-1111-1111-111111111111
                    raw_score: 18.5
                    status: RECORDED
                  - enrollment: 22222222-2222-2222-2222-222222222222
                    status: ABSENT
                  - enrollment: 33333333-3333-3333-3333-333333333333
                    raw_score: 0
                    status: RECORDED
                    comment: پاسخ‌برگ سفید
                summary: ثبت گروهی
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BulkScoreRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BulkScoreRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/grade-items/{id}/lock/:
    post:
      operationId: gradebook_grade_items_lock_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: قفل قلم نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeItem'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/grade-items/{id}/unlock/:
    post:
      operationId: gradebook_grade_items_unlock_create
      description: نیازمند علت و مجوز `grade.unlock` (بخش ۷.۷).
      summary: بازگشایی قفل نمره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم نمره را شناسایی میکند.
        required: true
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnlockGradeItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UnlockGradeItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UnlockGradeItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GradeItem'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/scores/:
    post:
      operationId: gradebook_scores_create
      description: |-
        ایجاد نمره دانش‌آموز تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `grade.create`
      summary: ایجاد نمره دانش‌آموز
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentScoreRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentScoreRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentScoreRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentScore'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_scores_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نمرات دانش‌آموزان.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `grade_item`، `enrollment`، `status`، `course_offering`، `student`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `grade.read`
      summary: نمرات دانش‌آموزان
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
        description: ارائه درس
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_item
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ABSENT
          - EXCUSED
          - EXEMPT
          - NOT_RECORDED
          - PENDING_REVIEW
          - RECORDED
        description: |-
          * `NOT_RECORDED` - ثبت‌نشده
          * `RECORDED` - ثبت‌شده
          * `ABSENT` - غایب
          * `EXEMPT` - معاف
          * `EXCUSED` - غیبت موجه
          * `PENDING_REVIEW` - در انتظار بازبینی
      - in: query
        name: student
        schema:
          type: string
          format: uuid
        description: دانش‌آموز
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentScoreList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/scores/{id}/:
    put:
      operationId: gradebook_scores_update
      description: |-
        جایگزینی کامل نمره دانش‌آموز.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `grade.update`
      summary: ویرایش کامل نمره دانش‌آموز
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نمره دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentScoreRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentScoreRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentScoreRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentScore'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_scores_retrieve
      description: |-
        دریافت جزئیات یک نمره دانش‌آموز با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نمره دانش‌آموز خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `grade.read`
      summary: جزئیات نمره دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نمره دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentScore'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: gradebook_scores_partial_update
      description: |-
        ویرایش جزئی نمره دانش‌آموز.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `grade.update`
      summary: ویرایش جزئی نمره دانش‌آموز
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نمره دانش‌آموز را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudentScoreRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudentScoreRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudentScoreRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentScore'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: gradebook_scores_destroy
      description: |-
        حذف نرم نمره دانش‌آموز.

        نمره دانش‌آموز از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `grade.delete`
      summary: حذف نمره دانش‌آموز
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نمره دانش‌آموز را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/gradebook/scores/{id}/changes/:
    get:
      operationId: gradebook_scores_changes_list
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تاریخچه تغییرات نمره
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
        description: ارائه درس
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_item
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نمره دانش‌آموز را شناسایی میکند.
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ABSENT
          - EXCUSED
          - EXEMPT
          - NOT_RECORDED
          - PENDING_REVIEW
          - RECORDED
        description: |-
          * `NOT_RECORDED` - ثبت‌نشده
          * `RECORDED` - ثبت‌شده
          * `ABSENT` - غایب
          * `EXEMPT` - معاف
          * `EXCUSED` - غیبت موجه
          * `PENDING_REVIEW` - در انتظار بازبینی
      - in: query
        name: student
        schema:
          type: string
          format: uuid
        description: دانش‌آموز
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedScoreChangeList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/gradebook/:
    get:
      operationId: gradebook_gradebook_list
      description: |-
        جدول کامل دفتر نمره: دسته‌ها، ستون‌های نمره و ردیف دانش‌آموزان با نمرات ثبت‌شده. یک درخواست برای رندر کل صفحه دفتر نمره (بخش ۱۰.۱ سند فرانت).

        `weightsValid` نشان می‌دهد مجموع وزن دسته‌ها ۱۰۰٪ هست یا نه؛ اگر نه، فرانت باید هشدار نشان دهد و اجازه نهایی‌سازی ندهد.
      summary: دفتر نمره یک ارائه درس
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
        description: شناسه ارائه درس
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Gradebook'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/gradebook/{id}/:
    get:
      operationId: gradebook_gradebook_retrieve
      description: |-
        جدول کامل دفتر نمره: دسته‌ها، ستون‌های نمره و ردیف دانش‌آموزان با نمرات ثبت‌شده. یک درخواست برای رندر کل صفحه دفتر نمره (بخش ۱۰.۱ سند فرانت).

        `weightsValid` نشان می‌دهد مجموع وزن دسته‌ها ۱۰۰٪ هست یا نه؛ اگر نه، فرانت باید هشدار نشان دهد و اجازه نهایی‌سازی ندهد.
      summary: دفتر نمره یک ارائه درس
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
        description: شناسه ارائه درس
        required: true
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ارائه درس را شناسایی میکند.
        required: true
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Gradebook'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/course-results/:
    post:
      operationId: gradebook_course_results_create
      description: |-
        ایجاد نتیجه درس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `grade.create`
      summary: ایجاد نتیجه درس
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseResultRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CourseResultRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CourseResultRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseResult'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_course_results_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نتایج دروس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `course_offering`، `enrollment`، `result_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `grade.read`
      summary: نتایج دروس
      parameters:
      - in: query
        name: course_offering
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: result_status
        schema:
          type: string
          title: وضعیت نتیجه
          enum:
          - EXEMPT
          - FAILED
          - INCOMPLETE
          - IN_PROGRESS
          - PASSED
        description: |-
          * `IN_PROGRESS` - در جریان
          * `PASSED` - قبول
          * `FAILED` - مردود
          * `INCOMPLETE` - ناتمام
          * `EXEMPT` - معاف
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCourseResultList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/course-results/calculate/:
    post:
      operationId: gradebook_course_results_calculate_create
      description: |-
        بر اساس وزن دسته‌ها و نمرات ثبت‌شده محاسبه می‌شود. Snapshot ورودی‌های محاسبه در `calculationInputs` ذخیره و `calculationVersion` یک واحد جلو می‌رود (بخش ۱۱.۵).

        اگر `enrollment` ارسال نشود، برای همه دانش‌آموزان فعال کلاس محاسبه می‌شود.
      summary: محاسبه نتیجه درس
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalculateResultRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CalculateResultRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CalculateResultRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/course-results/{id}/:
    put:
      operationId: gradebook_course_results_update
      description: |-
        جایگزینی کامل نتیجه درس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `grade.update`
      summary: ویرایش کامل نتیجه درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نتیجه درس را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseResultRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CourseResultRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CourseResultRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseResult'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_course_results_retrieve
      description: |-
        دریافت جزئیات یک نتیجه درس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نتیجه درس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `grade.read`
      summary: جزئیات نتیجه درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نتیجه درس را شناسایی میکند.
        required: true
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseResult'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: gradebook_course_results_partial_update
      description: |-
        ویرایش جزئی نتیجه درس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `grade.update`
      summary: ویرایش جزئی نتیجه درس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نتیجه درس را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCourseResultRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCourseResultRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCourseResultRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseResult'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: gradebook_course_results_destroy
      description: |-
        حذف نرم نتیجه درس.

        نتیجه درس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `grade.delete`
      summary: حذف نتیجه درس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نتیجه درس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/gradebook/report-cards/:
    post:
      operationId: gradebook_report_cards_create
      description: |-
        ایجاد کارنامه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `report_card.create`
      summary: ایجاد کارنامه
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReportCardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReportCardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReportCardRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportCard'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: gradebook_report_cards_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده کارنامه‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `enrollment`، `term`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `report_card.read`
      summary: فهرست کارنامه‌ها
      parameters:
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - DRAFT
          - GENERATED
          - PUBLISHED
          - SUPERSEDED
          - UNDER_REVIEW
        description: |-
          * `DRAFT` - پیش‌نویس
          * `GENERATED` - تولیدشده
          * `UNDER_REVIEW` - در حال بازبینی
          * `PUBLISHED` - منتشرشده
          * `SUPERSEDED` - جایگزین‌شده با نسخه جدید
      - in: query
        name: term
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedReportCardList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/report-cards/bulk-generate/:
    post:
      operationId: gradebook_report_cards_bulk_generate_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تولید گروهی کارنامه یک کلاس
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkGenerateReportCardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BulkGenerateReportCardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BulkGenerateReportCardRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResult'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/report-cards/generate/:
    post:
      operationId: gradebook_report_cards_generate_create
      description: نسخه جدید کارنامه با Snapshot نتایج دروس ساخته می‌شود. نسخه منتشرشده
        قبلی به وضعیت «جایگزین‌شده» می‌رود و حذف نمی‌شود (بخش ۷.۷).
      summary: تولید کارنامه
      tags:
      - Gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateReportCardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GenerateReportCardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GenerateReportCardRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportCard'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/report-cards/{id}/:
    get:
      operationId: gradebook_report_cards_retrieve
      description: |-
        دریافت جزئیات یک کارنامه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر کارنامه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `report_card.read`
      summary: جزئیات کارنامه با اقلام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارنامه را شناسایی میکند.
        required: true
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportCard'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: gradebook_report_cards_partial_update
      description: |-
        ویرایش جزئی کارنامه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `report_card.update`
      summary: ویرایش جزئی کارنامه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارنامه را شناسایی میکند.
        required: true
      tags:
      - gradebook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedReportCardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedReportCardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedReportCardRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportCard'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/gradebook/report-cards/{id}/publish/:
    post:
      operationId: gradebook_report_cards_publish_create
      description: پس از انتشار، رویداد `ReportCardPublished` منتشر می‌شود (بخش ۱۳.۱).
      summary: انتشار کارنامه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کارنامه را شناسایی میکند.
        required: true
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportCard'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: قفل یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/gradebook/report-card-items/:
    get:
      operationId: gradebook_report_card_items_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام کارنامه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `report_card`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `report_card.read`
      summary: اقلام کارنامه
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: report_card
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedReportCardItemList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/gradebook/report-card-items/{id}/:
    get:
      operationId: gradebook_report_card_items_retrieve
      description: |-
        دریافت جزئیات یک قلم کارنامه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم کارنامه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `report_card.read`
      summary: جزئیات قلم کارنامه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم کارنامه را شناسایی میکند.
        required: true
      tags:
      - gradebook
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportCardItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/finance/fiscal-years/:
    post:
      operationId: finance_fiscal_years_create
      description: |-
        ایجاد سال مالی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `journal.create`
      summary: ایجاد سال مالی
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FiscalYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/FiscalYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FiscalYearRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalYear'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_fiscal_years_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده سال‌های مالی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `journal.read`
      summary: سال‌های مالی
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CLOSED
          - CLOSING
          - OPEN
        description: |-
          * `OPEN` - باز
          * `CLOSING` - در حال بستن
          * `CLOSED` - بسته
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedFiscalYearList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/fiscal-years/{id}/:
    put:
      operationId: finance_fiscal_years_update
      description: |-
        جایگزینی کامل سال مالی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `journal.update`
      summary: ویرایش کامل سال مالی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال مالی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FiscalYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/FiscalYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FiscalYearRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_fiscal_years_retrieve
      description: |-
        دریافت جزئیات یک سال مالی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سال مالی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `journal.read`
      summary: جزئیات سال مالی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال مالی را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_fiscal_years_partial_update
      description: |-
        ویرایش جزئی سال مالی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `journal.update`
      summary: ویرایش جزئی سال مالی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال مالی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedFiscalYearRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedFiscalYearRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedFiscalYearRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_fiscal_years_destroy
      description: |-
        حذف نرم سال مالی.

        سال مالی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `journal.delete`
      summary: حذف سال مالی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال مالی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/fiscal-years/{id}/close/:
    post:
      operationId: finance_fiscal_years_close_create
      description: پیش از بستن، وجود اسناد پیش‌نویس بررسی می‌شود. پس از بستن، ثبت
        یا تغییر سند در آن دوره ممنوع است و اصلاح فقط با سند برگشتی در دوره باز انجام
        می‌شود (بخش ۷.۸).
      summary: بستن دوره مالی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سال مالی را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FiscalYear'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/accounts/:
    post:
      operationId: finance_accounts_create
      description: |-
        ایجاد حساب تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `journal.create`
      summary: ایجاد حساب
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AccountRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_accounts_list
      description: حساب‌های گروه (`allowsPosting = false`) قابل ثبت سند مستقیم نیستند.
      summary: کدینگ حساب‌ها
      parameters:
      - in: query
        name: account_type
        schema:
          type: string
          title: نوع حساب
          enum:
          - ASSET
          - EQUITY
          - EXPENSE
          - LIABILITY
          - REVENUE
        description: |-
          * `ASSET` - دارایی
          * `LIABILITY` - بدهی
          * `EQUITY` - حقوق صاحبان سرمایه
          * `REVENUE` - درآمد
          * `EXPENSE` - هزینه
      - in: query
        name: allows_posting
        schema:
          type: boolean
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: parent
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAccountList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/accounts/{id}/:
    put:
      operationId: finance_accounts_update
      description: |-
        جایگزینی کامل حساب.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `journal.update`
      summary: ویرایش کامل حساب
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AccountRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_accounts_retrieve
      description: |-
        دریافت جزئیات یک حساب با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر حساب خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `journal.read`
      summary: جزئیات حساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_accounts_partial_update
      description: |-
        ویرایش جزئی حساب.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `journal.update`
      summary: ویرایش جزئی حساب
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAccountRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_accounts_destroy
      description: |-
        حذف نرم حساب.

        حساب از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `journal.delete`
      summary: حذف حساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/accounts/{id}/ledger/:
    get:
      operationId: finance_accounts_ledger_retrieve
      description: ریز گردش یک حساب در بازه مشخص با مانده تجمعی — برای صفحه «گردش
        حساب» (بخش ۱۲.۲).
      summary: گردش حساب
      parameters:
      - in: query
        name: date_from
        schema:
          type: string
        description: از تاریخ (YYYY-MM-DD)
      - in: query
        name: date_to
        schema:
          type: string
        description: تا تاریخ (YYYY-MM-DD)
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountLedger'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/cost-centers/:
    post:
      operationId: finance_cost_centers_create
      description: |-
        ایجاد مرکز هزینه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `journal.create`
      summary: ایجاد مرکز هزینه
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CostCenterRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CostCenterRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CostCenterRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostCenter'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_cost_centers_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده مراکز هزینه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `is_active`

        **جست‌وجو** — پارامتر `search` روی `code`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `journal.read`
      summary: مراکز هزینه
      parameters:
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCostCenterList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/cost-centers/{id}/:
    put:
      operationId: finance_cost_centers_update
      description: |-
        جایگزینی کامل مرکز هزینه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `journal.update`
      summary: ویرایش کامل مرکز هزینه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مرکز هزینه را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CostCenterRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CostCenterRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CostCenterRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostCenter'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_cost_centers_retrieve
      description: |-
        دریافت جزئیات یک مرکز هزینه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر مرکز هزینه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `journal.read`
      summary: جزئیات مرکز هزینه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مرکز هزینه را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostCenter'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_cost_centers_partial_update
      description: |-
        ویرایش جزئی مرکز هزینه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `journal.update`
      summary: ویرایش جزئی مرکز هزینه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مرکز هزینه را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCostCenterRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCostCenterRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCostCenterRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostCenter'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_cost_centers_destroy
      description: |-
        حذف نرم مرکز هزینه.

        مرکز هزینه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `journal.delete`
      summary: حذف مرکز هزینه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مرکز هزینه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/fee-plans/:
    post:
      operationId: finance_fee_plans_create
      description: |-
        ایجاد الگوی شهریه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `fee_plan.create`
      summary: ایجاد الگوی شهریه
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeePlanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/FeePlanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FeePlanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlan'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_fee_plans_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده الگوهای شهریه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `academic_year`، `grade_level`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `fee_plan.read`
      summary: الگوهای شهریه
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: grade_level
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedFeePlanList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/fee-plans/{id}/:
    put:
      operationId: finance_fee_plans_update
      description: |-
        جایگزینی کامل الگوی شهریه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `fee_plan.update`
      summary: ویرایش کامل الگوی شهریه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این الگوی شهریه را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeePlanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/FeePlanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FeePlanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_fee_plans_retrieve
      description: |-
        دریافت جزئیات یک الگوی شهریه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر الگوی شهریه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `fee_plan.read`
      summary: جزئیات الگوی شهریه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این الگوی شهریه را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_fee_plans_partial_update
      description: |-
        ویرایش جزئی الگوی شهریه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `fee_plan.update`
      summary: ویرایش جزئی الگوی شهریه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این الگوی شهریه را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedFeePlanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedFeePlanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedFeePlanRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_fee_plans_destroy
      description: |-
        حذف نرم الگوی شهریه.

        الگوی شهریه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `fee_plan.delete`
      summary: حذف الگوی شهریه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این الگوی شهریه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/fee-plan-items/:
    post:
      operationId: finance_fee_plan_items_create
      description: |-
        ایجاد قلم الگوی شهریه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `fee_plan.create`
      summary: ایجاد قلم الگوی شهریه
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeePlanItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/FeePlanItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FeePlanItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlanItem'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_fee_plan_items_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام الگوی شهریه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `fee_plan`، `fee_type`، `is_mandatory`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `fee_plan.read`
      summary: اقلام الگوی شهریه
      parameters:
      - in: query
        name: fee_plan
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: fee_type
        schema:
          type: string
          title: نوع شهریه
          enum:
          - BOOK
          - EXAM
          - FIELD_TRIP
          - LATE_FEE
          - MEAL
          - OPTIONAL_SERVICE
          - OTHER
          - REGISTRATION
          - TRANSPORT
          - TUITION
        description: |-
          * `REGISTRATION` - ثبت‌نام
          * `TUITION` - شهریه آموزشی
          * `TRANSPORT` - سرویس
          * `MEAL` - غذا
          * `BOOK` - کتاب و لوازم
          * `FIELD_TRIP` - اردو
          * `EXAM` - آزمون
          * `OPTIONAL_SERVICE` - خدمات اختیاری
          * `LATE_FEE` - جریمه دیرکرد
          * `OTHER` - سایر
      - in: query
        name: is_mandatory
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedFeePlanItemList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/fee-plan-items/{id}/:
    put:
      operationId: finance_fee_plan_items_update
      description: |-
        جایگزینی کامل قلم الگوی شهریه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `fee_plan.update`
      summary: ویرایش کامل قلم الگوی شهریه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم الگوی شهریه را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeePlanItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/FeePlanItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FeePlanItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlanItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_fee_plan_items_retrieve
      description: |-
        دریافت جزئیات یک قلم الگوی شهریه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم الگوی شهریه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `fee_plan.read`
      summary: جزئیات قلم الگوی شهریه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم الگوی شهریه را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlanItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_fee_plan_items_partial_update
      description: |-
        ویرایش جزئی قلم الگوی شهریه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `fee_plan.update`
      summary: ویرایش جزئی قلم الگوی شهریه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم الگوی شهریه را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedFeePlanItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedFeePlanItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedFeePlanItemRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeePlanItem'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_fee_plan_items_destroy
      description: |-
        حذف نرم قلم الگوی شهریه.

        قلم الگوی شهریه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `fee_plan.delete`
      summary: حذف قلم الگوی شهریه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم الگوی شهریه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/agreements/:
    post:
      operationId: finance_agreements_create
      description: |-
        ایجاد قرارداد مالی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `invoice.create`
      summary: ایجاد قرارداد مالی
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentFinancialAgreementRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentFinancialAgreementRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentFinancialAgreementRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentFinancialAgreement'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_agreements_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده قراردادهای مالی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `enrollment`، `fee_plan`، `responsible_guardian`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `invoice.read`
      summary: قراردادهای مالی دانش‌آموزان
      parameters:
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: fee_plan
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: responsible_guardian
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CANCELLED
          - DRAFT
          - SETTLED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - جاری
          * `SETTLED` - تسویه‌شده
          * `CANCELLED` - لغوشده
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentFinancialAgreementList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/agreements/{id}/:
    put:
      operationId: finance_agreements_update
      description: |-
        جایگزینی کامل قرارداد مالی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `invoice.update`
      summary: ویرایش کامل قرارداد مالی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentFinancialAgreementRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentFinancialAgreementRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentFinancialAgreementRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentFinancialAgreement'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_agreements_retrieve
      description: شامل مبلغ توافق‌شده، تخفیف‌ها، جمع صورتحساب، پرداخت و مانده.
      summary: جزئیات قرارداد مالی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentFinancialAgreement'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_agreements_partial_update
      description: |-
        ویرایش جزئی قرارداد مالی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `invoice.update`
      summary: ویرایش جزئی قرارداد مالی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudentFinancialAgreementRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudentFinancialAgreementRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudentFinancialAgreementRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentFinancialAgreement'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_agreements_destroy
      description: |-
        حذف نرم قرارداد مالی.

        قرارداد مالی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `invoice.delete`
      summary: حذف قرارداد مالی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/agreements/{id}/generate-installments/:
    post:
      operationId: finance_agreements_generate_installments_create
      description: بر اساس `installmentCount` قرارداد، صورتحساب‌های قسطی در وضعیت
        پیش‌نویس ساخته می‌شوند. باقیمانده تقسیم به قسط آخر اضافه می‌شود تا جمع اقساط
        دقیقاً برابر مبلغ توافق‌شده بماند.
      summary: تولید صورتحساب اقساط
      parameters:
      - in: query
        name: enrollment
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: fee_plan
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: responsible_guardian
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - CANCELLED
          - DRAFT
          - SETTLED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ACTIVE` - جاری
          * `SETTLED` - تسویه‌شده
          * `CANCELLED` - لغوشده
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateInstallmentsRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GenerateInstallmentsRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GenerateInstallmentsRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedInvoiceList'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/agreements/{id}/recalculate/:
    post:
      operationId: finance_agreements_recalculate_create
      description: پس از تأیید یا تغییر تخفیف‌ها فراخوانی می‌شود.
      summary: محاسبه مجدد مبلغ توافق‌شده
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentFinancialAgreement'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/discounts/:
    post:
      operationId: finance_discounts_create
      description: |-
        ایجاد تخفیف تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `invoice.create`
      summary: ثبت تخفیف
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiscountAwardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DiscountAwardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DiscountAwardRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscountAward'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_discounts_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تخفیف‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `agreement`، `discount_type`، `approval_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `invoice.read`
      summary: تخفیف‌ها
      parameters:
      - in: query
        name: agreement
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: approval_status
        schema:
          type: string
          title: وضعیت تأیید
          enum:
          - APPROVED
          - PENDING
          - REJECTED
        description: |-
          * `PENDING` - در انتظار تأیید
          * `APPROVED` - تأییدشده
          * `REJECTED` - ردشده
      - in: query
        name: discount_type
        schema:
          type: string
          title: نوع تخفیف
          enum:
          - EARLY_PAYMENT
          - FINANCIAL_AID
          - SCHOLARSHIP
          - SIBLING
          - SPECIAL
          - STAFF_CHILD
        description: |-
          * `SIBLING` - تخفیف خواهر/برادر
          * `SCHOLARSHIP` - بورسیه
          * `STAFF_CHILD` - فرزند کارکنان
          * `FINANCIAL_AID` - کمک‌هزینه
          * `EARLY_PAYMENT` - تخفیف پرداخت زودهنگام
          * `SPECIAL` - تخفیف موردی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedDiscountAwardList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/discounts/{id}/:
    put:
      operationId: finance_discounts_update
      description: |-
        جایگزینی کامل تخفیف.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `invoice.update`
      summary: ویرایش کامل تخفیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تخفیف را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiscountAwardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DiscountAwardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DiscountAwardRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscountAward'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_discounts_retrieve
      description: |-
        دریافت جزئیات یک تخفیف با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تخفیف خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `invoice.read`
      summary: جزئیات تخفیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تخفیف را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscountAward'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_discounts_partial_update
      description: |-
        ویرایش جزئی تخفیف.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `invoice.update`
      summary: ویرایش جزئی تخفیف
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تخفیف را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedDiscountAwardRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedDiscountAwardRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedDiscountAwardRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscountAward'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_discounts_destroy
      description: |-
        حذف نرم تخفیف.

        تخفیف از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `invoice.delete`
      summary: حذف تخفیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تخفیف را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/discounts/{id}/approve/:
    post:
      operationId: finance_discounts_approve_create
      description: پس از تأیید، مبلغ توافق‌شده قرارداد مجدداً محاسبه می‌شود.
      summary: تأیید تخفیف
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تخفیف را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscountAward'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/invoices/:
    post:
      operationId: finance_invoices_create
      description: |-
        ایجاد صورتحساب تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `invoice.create`
      summary: ایجاد صورتحساب پیش‌نویس
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvoiceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/InvoiceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/InvoiceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_invoices_list
      description: برای داشبورد مطالبات و پرونده مالی دانش‌آموز (بخش ۱۲.۱ و ۱۲.۲ سند
        فرانت).
      summary: فهرست صورتحساب‌ها
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
        description: سال تحصیلی
      - in: query
        name: agreement
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: due_from
        schema:
          type: string
          format: date
      - in: query
        name: due_to
        schema:
          type: string
          format: date
      - in: query
        name: guardian
        schema:
          type: string
          format: uuid
        description: مسئول مالی
      - in: query
        name: installment_no
        schema:
          type: integer
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - CREDITED
          - DISPUTED
          - DRAFT
          - ISSUED
          - OVERDUE
          - PAID
          - PARTIALLY_PAID
          - REFUNDED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ISSUED` - صادرشده
          * `PARTIALLY_PAID` - پرداخت جزئی
          * `PAID` - تسویه‌شده
          * `OVERDUE` - سررسید گذشته
          * `DISPUTED` - مورد اعتراض
          * `CREDITED` - یادداشت بستانکار صادر شد
          * `REFUNDED` - مسترد شده
          * `CANCELLED` - لغوشده
      - in: query
        name: student
        schema:
          type: string
        description: شناسه دانش‌آموز
      - in: query
        name: unpaid
        schema:
          type: boolean
        description: فقط صورتحساب‌های دارای مانده
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedInvoiceList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/invoices/aging/:
    get:
      operationId: finance_invoices_aging_list
      description: توزیع مانده مطالبات در بازه‌های سررسید (بخش ۱۴.۳).
      summary: گزارش سنی مطالبات
      parameters:
      - in: query
        name: academic_year
        schema:
          type: string
          format: uuid
        description: سال تحصیلی
      - in: query
        name: agreement
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: due_from
        schema:
          type: string
          format: date
      - in: query
        name: due_to
        schema:
          type: string
          format: date
      - in: query
        name: guardian
        schema:
          type: string
          format: uuid
        description: مسئول مالی
      - in: query
        name: installment_no
        schema:
          type: integer
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - CREDITED
          - DISPUTED
          - DRAFT
          - ISSUED
          - OVERDUE
          - PAID
          - PARTIALLY_PAID
          - REFUNDED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ISSUED` - صادرشده
          * `PARTIALLY_PAID` - پرداخت جزئی
          * `PAID` - تسویه‌شده
          * `OVERDUE` - سررسید گذشته
          * `DISPUTED` - مورد اعتراض
          * `CREDITED` - یادداشت بستانکار صادر شد
          * `REFUNDED` - مسترد شده
          * `CANCELLED` - لغوشده
      - in: query
        name: student
        schema:
          type: string
          format: uuid
        description: دانش‌آموز
      - in: query
        name: unpaid
        schema:
          type: boolean
        description: فقط دارای مانده
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedReceivablesAgingList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/invoices/{id}/:
    put:
      operationId: finance_invoices_update
      description: |-
        جایگزینی کامل صورتحساب.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `invoice.update`
      summary: ویرایش کامل صورتحساب
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صورتحساب را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvoiceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/InvoiceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/InvoiceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_invoices_retrieve
      description: |-
        دریافت جزئیات یک صورتحساب با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر صورتحساب خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `invoice.read`
      summary: جزئیات صورتحساب با اقلام
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صورتحساب را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_invoices_partial_update
      description: |-
        ویرایش جزئی صورتحساب.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `invoice.update`
      summary: ویرایش جزئی صورتحساب
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صورتحساب را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedInvoiceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedInvoiceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedInvoiceRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_invoices_destroy
      description: |-
        حذف نرم صورتحساب.

        صورتحساب از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `invoice.delete`
      summary: حذف صورتحساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صورتحساب را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/invoices/{id}/cancel/:
    post:
      operationId: finance_invoices_cancel_create
      description: فقط صورتحساب پیش‌نویس قابل لغو است؛ صورتحساب صادرشده با یادداشت
        بستانکار اصلاح می‌شود.
      summary: لغو صورتحساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صورتحساب را شناسایی میکند.
        required: true
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/invoices/{id}/issue/:
    post:
      operationId: finance_invoices_issue_create
      description: صورتحساب از پیش‌نویس به «صادرشده» می‌رود و سند حسابداری تعهدی (بدهکار
        حساب دریافتنی / بستانکار درآمد) ثبت می‌شود. رویداد `InvoiceIssued` منتشر می‌گردد.
      summary: صدور صورتحساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این صورتحساب را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/invoice-lines/:
    post:
      operationId: finance_invoice_lines_create
      description: |-
        ایجاد قلم صورتحساب تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `invoice.create`
      summary: ایجاد قلم صورتحساب
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvoiceLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/InvoiceLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/InvoiceLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceLine'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_invoice_lines_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام صورتحساب.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `invoice`، `fee_type`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `invoice.read`
      summary: اقلام صورتحساب
      parameters:
      - in: query
        name: fee_type
        schema:
          type: string
          title: نوع شهریه
          enum:
          - BOOK
          - EXAM
          - FIELD_TRIP
          - LATE_FEE
          - MEAL
          - OPTIONAL_SERVICE
          - OTHER
          - REGISTRATION
          - TRANSPORT
          - TUITION
        description: |-
          * `REGISTRATION` - ثبت‌نام
          * `TUITION` - شهریه آموزشی
          * `TRANSPORT` - سرویس
          * `MEAL` - غذا
          * `BOOK` - کتاب و لوازم
          * `FIELD_TRIP` - اردو
          * `EXAM` - آزمون
          * `OPTIONAL_SERVICE` - خدمات اختیاری
          * `LATE_FEE` - جریمه دیرکرد
          * `OTHER` - سایر
      - in: query
        name: invoice
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedInvoiceLineList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/invoice-lines/{id}/:
    put:
      operationId: finance_invoice_lines_update
      description: |-
        جایگزینی کامل قلم صورتحساب.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `invoice.update`
      summary: ویرایش کامل قلم صورتحساب
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم صورتحساب را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvoiceLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/InvoiceLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/InvoiceLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_invoice_lines_retrieve
      description: |-
        دریافت جزئیات یک قلم صورتحساب با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم صورتحساب خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `invoice.read`
      summary: جزئیات قلم صورتحساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم صورتحساب را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_invoice_lines_partial_update
      description: |-
        ویرایش جزئی قلم صورتحساب.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `invoice.update`
      summary: ویرایش جزئی قلم صورتحساب
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم صورتحساب را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedInvoiceLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedInvoiceLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedInvoiceLineRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_invoice_lines_destroy
      description: |-
        حذف نرم قلم صورتحساب.

        قلم صورتحساب از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `invoice.delete`
      summary: حذف قلم صورتحساب
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم صورتحساب را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/payments/:
    post:
      operationId: finance_payments_create
      description: |-
        دریافت در وضعیت «در انتظار» ساخته می‌شود. برای قطعی‌کردن، `post` را صدا بزنید.

        **Idempotent:** هدر `Idempotency-Key` را برای پرداخت درگاه بفرستید تا Callback تکراری، دریافت دوم نسازد (بخش ۷.۸).
      summary: ثبت دریافت
      parameters:
      - in: header
        name: Idempotency-Key
        schema:
          type: string
        description: کلید یکتای عملیات
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_payments_list
      description: برای صندوق و مغایرت‌گیری (بخش ۱۲.۳ سند فرانت).
      summary: فهرست دریافت‌ها
      parameters:
      - in: query
        name: bank_account
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: has_unallocated
        schema:
          type: boolean
        description: دارای مبلغ تخصیص‌نیافته
      - in: query
        name: method
        schema:
          type: string
          title: روش
          enum:
          - CASH
          - CHEQUE
          - MIXED
          - ONLINE_GATEWAY
          - POS
          - TRANSFER
          - WALLET
        description: |-
          * `CASH` - نقدی
          * `POS` - کارت‌خوان
          * `TRANSFER` - انتقال بانکی
          * `CHEQUE` - چک
          * `ONLINE_GATEWAY` - درگاه آنلاین
          * `WALLET` - کیف پول
          * `MIXED` - ترکیبی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: payer_person
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: received_from
        schema:
          type: string
          format: date-time
      - in: query
        name: received_to
        schema:
          type: string
          format: date-time
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - FAILED
          - PARTIALLY_REFUNDED
          - PENDING
          - REFUNDED
          - SUCCEEDED
          - VOIDED
        description: |-
          * `PENDING` - در انتظار
          * `SUCCEEDED` - موفق
          * `FAILED` - ناموفق
          * `VOIDED` - ابطال‌شده
          * `REFUNDED` - مسترد شده
          * `PARTIALLY_REFUNDED` - استرداد جزئی
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPaymentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/payments/{id}/:
    put:
      operationId: finance_payments_update
      description: |-
        جایگزینی کامل دریافت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `payment.update`
      summary: ویرایش کامل دریافت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_payments_retrieve
      description: |-
        دریافت جزئیات یک دریافت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر دریافت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `payment.read`
      summary: جزئیات دریافت با تخصیص‌ها
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_payments_partial_update
      description: |-
        ویرایش جزئی دریافت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `payment.update`
      summary: ویرایش جزئی دریافت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPaymentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPaymentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPaymentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_payments_destroy
      description: |-
        حذف نرم دریافت.

        دریافت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `payment.delete`
      summary: حذف دریافت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/payments/{id}/allocate/:
    post:
      operationId: finance_payments_allocate_create
      description: یک دریافت می‌تواند به چند صورتحساب تخصیص یابد. مجموع تخصیص از مبلغ
        تخصیص‌نیافته دریافت و از مانده هر صورتحساب فراتر نمی‌رود (بخش ۷.۸).
      summary: تخصیص دریافت به صورتحساب‌ها
      parameters:
      - in: query
        name: bank_account
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: has_unallocated
        schema:
          type: boolean
        description: دارای مبلغ تخصیص‌نیافته
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      - in: query
        name: method
        schema:
          type: string
          title: روش
          enum:
          - CASH
          - CHEQUE
          - MIXED
          - ONLINE_GATEWAY
          - POS
          - TRANSFER
          - WALLET
        description: |-
          * `CASH` - نقدی
          * `POS` - کارت‌خوان
          * `TRANSFER` - انتقال بانکی
          * `CHEQUE` - چک
          * `ONLINE_GATEWAY` - درگاه آنلاین
          * `WALLET` - کیف پول
          * `MIXED` - ترکیبی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: payer_person
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: received_from
        schema:
          type: string
          format: date-time
      - in: query
        name: received_to
        schema:
          type: string
          format: date-time
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - FAILED
          - PARTIALLY_REFUNDED
          - PENDING
          - REFUNDED
          - SUCCEEDED
          - VOIDED
        description: |-
          * `PENDING` - در انتظار
          * `SUCCEEDED` - موفق
          * `FAILED` - ناموفق
          * `VOIDED` - ابطال‌شده
          * `REFUNDED` - مسترد شده
          * `PARTIALLY_REFUNDED` - استرداد جزئی
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AllocatePaymentRequest'
            examples:
              تخصیصبهدوقسط:
                value:
                  allocations:
                  - invoice: aaaaaaaa-1111-2222-3333-444444444444
                    amount: 5000000
                  - invoice: bbbbbbbb-1111-2222-3333-444444444444
                    amount: 3000000
                summary: تخصیص به دو قسط
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AllocatePaymentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AllocatePaymentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPaymentAllocationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                خطایتجاوزازمانده:
                  value:
                    code: ALLOCATION_EXCEEDS_INVOICE_BALANCE
                    message: مبلغ تخصیص (5,000,000) از مانده صورتحساب INV-202608-000012
                      (3,000,000) بیشتر است.
                    correlationId: 1d4e7a2b
                    fieldErrors:
                    - field: invoice
                      reason: exceeds_balance
                    retryable: false
                  summary: خطای تجاوز از مانده
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/payments/{id}/post/:
    post:
      operationId: finance_payments_post_create
      description: وضعیت را به «موفق» می‌برد و سند حسابداری دریافت را ثبت می‌کند.
        رویداد `PaymentPosted` منتشر می‌شود.
      summary: قطعی‌کردن دریافت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/payments/{id}/void/:
    post:
      operationId: finance_payments_void_create
      description: تخصیص‌ها برداشته می‌شوند، وضعیت صورتحساب‌ها به‌روز می‌شود و سند
        برگشتی ثبت می‌گردد. ابطال با استرداد یکی نیست (بخش ۷.۸).
      summary: ابطال دریافت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دریافت را شناسایی میکند.
        required: true
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/allocations/:
    get:
      operationId: finance_allocations_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تخصیص‌های پرداخت.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `payment`، `invoice`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `payment.read`
      summary: تخصیص‌های پرداخت
      parameters:
      - in: query
        name: invoice
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: payment
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPaymentAllocationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/allocations/{id}/:
    get:
      operationId: finance_allocations_retrieve
      description: |-
        دریافت جزئیات یک تخصیص پرداخت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تخصیص پرداخت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `payment.read`
      summary: جزئیات تخصیص پرداخت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تخصیص پرداخت را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentAllocation'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/finance/refunds/:
    post:
      operationId: finance_refunds_create
      description: |-
        ایجاد استرداد تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `refund.create`
      summary: درخواست استرداد
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefundRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RefundRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RefundRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_refunds_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده استردادها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `payment`، `status`، `approval_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `refund.read`
      summary: استردادها
      parameters:
      - in: query
        name: approval_status
        schema:
          type: string
          title: وضعیت تأیید
          enum:
          - APPROVED
          - PENDING
          - REJECTED
        description: |-
          * `PENDING` - در انتظار تأیید
          * `APPROVED` - تأییدشده
          * `REJECTED` - ردشده
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: payment
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - COMPLETED
          - FAILED
          - PROCESSING
          - REJECTED
          - REQUESTED
        description: |-
          * `REQUESTED` - درخواست‌شده
          * `APPROVED` - تأییدشده
          * `PROCESSING` - در حال پردازش
          * `COMPLETED` - انجام‌شده
          * `REJECTED` - ردشده
          * `FAILED` - ناموفق
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRefundList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/refunds/{id}/:
    put:
      operationId: finance_refunds_update
      description: |-
        جایگزینی کامل استرداد.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `refund.update`
      summary: ویرایش کامل استرداد
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این استرداد را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefundRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RefundRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RefundRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_refunds_retrieve
      description: |-
        دریافت جزئیات یک استرداد با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر استرداد خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `refund.read`
      summary: جزئیات استرداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این استرداد را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_refunds_partial_update
      description: |-
        ویرایش جزئی استرداد.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `refund.update`
      summary: ویرایش جزئی استرداد
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این استرداد را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedRefundRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedRefundRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedRefundRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_refunds_destroy
      description: |-
        حذف نرم استرداد.

        استرداد از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `refund.delete`
      summary: حذف استرداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این استرداد را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/refunds/{id}/approve/:
    post:
      operationId: finance_refunds_approve_create
      description: '**تفکیک وظایف:** درخواست‌دهنده نمی‌تواند تأییدکننده باشد (بخش
        ۳.۲ و ۱۶.۲). در این حالت خطای `SEGREGATION_OF_DUTIES` با کد ۴۰۳ برمی‌گردد.'
      summary: تأیید استرداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این استرداد را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/refunds/{id}/complete/:
    post:
      operationId: finance_refunds_complete_create
      description: سند حسابداری معکوس ثبت و رویداد `RefundCompleted` منتشر می‌شود.
      summary: تکمیل استرداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این استرداد را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Refund'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/journal-entries/:
    post:
      operationId: finance_journal_entries_create
      description: |-
        ایجاد سند حسابداری تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `journal.create`
      summary: ایجاد سند حسابداری
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/JournalEntryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/JournalEntryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/JournalEntryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JournalEntry'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_journal_entries_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اسناد حسابداری.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `fiscal_year`، `status`، `source_type`، `source_id`، `date_from`، `date_to`

        **جست‌وجو** — پارامتر `search` روی `entry_no`، `description`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `journal.read`
      summary: فهرست اسناد حسابداری
      parameters:
      - in: query
        name: date_from
        schema:
          type: string
          format: date
      - in: query
        name: date_to
        schema:
          type: string
          format: date
      - in: query
        name: fiscal_year
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: source_id
        schema:
          type: string
          format: uuid
      - in: query
        name: source_type
        schema:
          type: string
          title: نوع منبع
          enum:
          - DEPRECIATION
          - INVOICE
          - MANUAL
          - PAYMENT
          - PAYROLL
          - PURCHASE
          - REFUND
          - STOCK
        description: |-
          * `MANUAL` - دستی
          * `INVOICE` - صورتحساب
          * `PAYMENT` - دریافت
          * `REFUND` - استرداد
          * `PAYROLL` - حقوق
          * `PURCHASE` - خرید
          * `STOCK` - انبار
          * `DEPRECIATION` - استهلاک
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - DRAFT
          - POSTED
          - REVERSED
          - VALIDATED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `VALIDATED` - کنترل‌شده
          * `POSTED` - قطعی
          * `REVERSED` - برگشت‌خورده
          * `CANCELLED` - لغوشده
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedJournalEntryList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/journal-entries/create-entry/:
    post:
      operationId: finance_journal_entries_create_entry_create
      description: |-
        خطوط سند باید متوازن باشند. هر خط دقیقاً یکی از «بدهکار» یا «بستانکار» را دارد. حساب گروه قابل ثبت نیست.

        با `postImmediately = true` سند بلافاصله قطعی می‌شود.
      summary: ایجاد سند حسابداری دستی
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateJournalEntryRequest'
            examples:
              سنددستیمتوازن:
                value:
                  fiscal_year: cccccccc-1111-2222-3333-444444444444
                  entry_date: '2026-08-25'
                  description: بابت هزینه نوشت‌افزار اداری
                  lines:
                  - account: dddddddd-1111-2222-3333-444444444444
                    debit: 12000000
                    credit: 0
                    description: هزینه اداری
                  - account: eeeeeeee-1111-2222-3333-444444444444
                    debit: 0
                    credit: 12000000
                    description: پرداخت از صندوق
                  post_immediately: true
                summary: سند دستی متوازن
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CreateJournalEntryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CreateJournalEntryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JournalEntry'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                خطایعدمتوازن:
                  value:
                    code: JOURNAL_NOT_BALANCED
                    message: 'سند متوازن نیست: جمع بدهکار 12,000,000 و جمع بستانکار
                      10,000,000 است.'
                    correlationId: 5e8b3c1d
                    fieldErrors:
                    - field: lines
                      reason: not_balanced
                    retryable: false
                  summary: خطای عدم توازن
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/journal-entries/{id}/:
    get:
      operationId: finance_journal_entries_retrieve
      description: |-
        دریافت جزئیات یک سند حسابداری با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سند حسابداری خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `journal.read`
      summary: جزئیات سند با خطوط
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند حسابداری را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JournalEntry'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/finance/journal-entries/{id}/post/:
    post:
      operationId: finance_journal_entries_post_create
      description: پس از قطعی‌شدن، سند غیرقابل ویرایش و حذف است؛ اصلاح فقط با سند
        برگشتی انجام می‌شود (بخش ۱۰.۶).
      summary: قطعی‌کردن سند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند حسابداری را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JournalEntry'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/journal-entries/{id}/reverse/:
    post:
      operationId: finance_journal_entries_reverse_create
      description: سند جدیدی با خطوط معکوس ساخته و قطعی می‌شود؛ سند اصلی به وضعیت
        «برگشت‌خورده» می‌رود و حذف نمی‌شود.
      summary: برگشت سند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند حسابداری را شناسایی میکند.
        required: true
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReverseJournalRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReverseJournalRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReverseJournalRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JournalEntry'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/journal-lines/:
    get:
      operationId: finance_journal_lines_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده خطوط سند.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `journal_entry`، `account`، `cost_center`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `journal.read`
      summary: خطوط اسناد
      parameters:
      - in: query
        name: account
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: cost_center
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: journal_entry
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedJournalLineList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/journal-lines/{id}/:
    get:
      operationId: finance_journal_lines_retrieve
      description: |-
        دریافت جزئیات یک خط سند با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر خط سند خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `journal.read`
      summary: جزئیات خط سند
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این خط سند را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JournalLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/finance/bank-accounts/:
    post:
      operationId: finance_bank_accounts_create
      description: |-
        ایجاد حساب بانکی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `bank.create`
      summary: ایجاد حساب بانکی
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BankAccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BankAccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BankAccountRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccount'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_bank_accounts_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده حساب‌های بانکی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `bank.read`
      summary: حساب‌های بانکی
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedBankAccountList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/bank-accounts/{id}/:
    put:
      operationId: finance_bank_accounts_update
      description: |-
        جایگزینی کامل حساب بانکی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `bank.update`
      summary: ویرایش کامل حساب بانکی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب بانکی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BankAccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BankAccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BankAccountRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccount'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_bank_accounts_retrieve
      description: |-
        دریافت جزئیات یک حساب بانکی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر حساب بانکی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `bank.read`
      summary: جزئیات حساب بانکی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب بانکی را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccount'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_bank_accounts_partial_update
      description: |-
        ویرایش جزئی حساب بانکی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `bank.update`
      summary: ویرایش جزئی حساب بانکی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب بانکی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedBankAccountRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedBankAccountRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedBankAccountRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccount'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_bank_accounts_destroy
      description: |-
        حذف نرم حساب بانکی.

        حساب بانکی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `bank.delete`
      summary: حذف حساب بانکی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حساب بانکی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/reconciliations/:
    post:
      operationId: finance_reconciliations_create
      description: '`difference` خودکار از تفاضل مانده بانک و مانده دفتر محاسبه می‌شود.'
      summary: ثبت مغایرت‌گیری
      tags:
      - Finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BankReconciliationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BankReconciliationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BankReconciliationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankReconciliation'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_reconciliations_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده مغایرت‌های بانکی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `bank_account`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `bank.read`
      summary: مغایرت‌های بانکی
      parameters:
      - in: query
        name: bank_account
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CLOSED
          - DISCREPANCY
          - MATCHED
          - OPEN
        description: |-
          * `OPEN` - باز
          * `MATCHED` - تطبیق‌شده
          * `DISCREPANCY` - دارای مغایرت
          * `CLOSED` - بسته
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedBankReconciliationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/finance/reconciliations/{id}/:
    put:
      operationId: finance_reconciliations_update
      description: |-
        جایگزینی کامل مغایرت بانکی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `bank.update`
      summary: ویرایش کامل مغایرت بانکی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مغایرت بانکی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BankReconciliationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BankReconciliationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BankReconciliationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankReconciliation'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: finance_reconciliations_retrieve
      description: |-
        دریافت جزئیات یک مغایرت بانکی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر مغایرت بانکی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `bank.read`
      summary: جزئیات مغایرت بانکی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مغایرت بانکی را شناسایی میکند.
        required: true
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankReconciliation'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: finance_reconciliations_partial_update
      description: |-
        ویرایش جزئی مغایرت بانکی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `bank.update`
      summary: ویرایش جزئی مغایرت بانکی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مغایرت بانکی را شناسایی میکند.
        required: true
      tags:
      - finance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedBankReconciliationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedBankReconciliationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedBankReconciliationRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankReconciliation'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: finance_reconciliations_destroy
      description: |-
        حذف نرم مغایرت بانکی.

        مغایرت بانکی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `bank.delete`
      summary: حذف مغایرت بانکی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مغایرت بانکی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/finance/family-balance/:
    get:
      operationId: finance_family_balance_list
      description: |-
        مانده تجمیعی همه دانش‌آموزان تحت مسئولیت مالی یک ولی، به‌همراه ریز هر دانش‌آموز و اعتبار تخصیص‌نیافته (بخش ۱۲.۲ سند تحلیل).

        این نما پایه صفحه «پرونده مالی خانواده» در پرتال ولی است.
      summary: مانده مالی خانواده
      parameters:
      - in: query
        name: guardian
        schema:
          type: string
        description: شناسه ولی
        required: true
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FamilyBalance'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/finance/family-balance/{id}/:
    get:
      operationId: finance_family_balance_retrieve
      description: |-
        مانده تجمیعی همه دانش‌آموزان تحت مسئولیت مالی یک ولی، به‌همراه ریز هر دانش‌آموز و اعتبار تخصیص‌نیافته (بخش ۱۲.۲ سند تحلیل).

        این نما پایه صفحه «پرونده مالی خانواده» در پرتال ولی است.
      summary: مانده مالی خانواده
      parameters:
      - in: query
        name: guardian
        schema:
          type: string
        description: شناسه ولی
        required: true
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قرارداد مالی را شناسایی میکند.
        required: true
      tags:
      - Finance
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FamilyBalance'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز یا تفکیک وظایف
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: دوره بسته یا تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/vendors/:
    post:
      operationId: inventory_vendors_create
      description: |-
        ایجاد تأمین‌کننده تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `vendor.create`
      summary: ایجاد تأمین‌کننده
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VendorRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/VendorRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/VendorRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vendor'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_vendors_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تأمین‌کنندگان.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `status`

        **جست‌وجو** — پارامتر `search` روی `code`، `legal_name`، `tax_id`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `vendor.read`
      summary: فهرست تأمین‌کنندگان
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - BLACKLISTED
          - INACTIVE
          - SUSPENDED
        description: |-
          * `ACTIVE` - فعال
          * `SUSPENDED` - تعلیق
          * `BLACKLISTED` - لیست سیاه
          * `INACTIVE` - غیرفعال
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedVendorList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/vendors/{id}/:
    put:
      operationId: inventory_vendors_update
      description: |-
        جایگزینی کامل تأمین‌کننده.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `vendor.update`
      summary: ویرایش کامل تأمین‌کننده
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تأمین‌کننده را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VendorRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/VendorRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/VendorRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vendor'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_vendors_retrieve
      description: |-
        دریافت جزئیات یک تأمین‌کننده با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تأمین‌کننده خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `vendor.read`
      summary: جزئیات تأمین‌کننده
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تأمین‌کننده را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vendor'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_vendors_partial_update
      description: |-
        ویرایش جزئی تأمین‌کننده.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `vendor.update`
      summary: ویرایش جزئی تأمین‌کننده
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تأمین‌کننده را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedVendorRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedVendorRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedVendorRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vendor'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_vendors_destroy
      description: |-
        حذف نرم تأمین‌کننده.

        تأمین‌کننده از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `vendor.delete`
      summary: حذف تأمین‌کننده
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تأمین‌کننده را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/uoms/:
    post:
      operationId: inventory_uoms_create
      description: |-
        ایجاد واحد سنجش تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `item.create`
      summary: ایجاد واحد سنجش
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnitOfMeasureRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UnitOfMeasureRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UnitOfMeasureRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitOfMeasure'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_uoms_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده واحدهای سنجش.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **جست‌وجو** — پارامتر `search` روی `code`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `item.read`
      summary: واحدهای سنجش
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUnitOfMeasureList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/uoms/{id}/:
    put:
      operationId: inventory_uoms_update
      description: |-
        جایگزینی کامل واحد سنجش.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `item.update`
      summary: ویرایش کامل واحد سنجش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سنجش را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnitOfMeasureRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UnitOfMeasureRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UnitOfMeasureRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitOfMeasure'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_uoms_retrieve
      description: |-
        دریافت جزئیات یک واحد سنجش با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر واحد سنجش خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `item.read`
      summary: جزئیات واحد سنجش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سنجش را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitOfMeasure'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_uoms_partial_update
      description: |-
        ویرایش جزئی واحد سنجش.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `item.update`
      summary: ویرایش جزئی واحد سنجش
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سنجش را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedUnitOfMeasureRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedUnitOfMeasureRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedUnitOfMeasureRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitOfMeasure'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_uoms_destroy
      description: |-
        حذف نرم واحد سنجش.

        واحد سنجش از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `item.delete`
      summary: حذف واحد سنجش
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این واحد سنجش را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/item-categories/:
    post:
      operationId: inventory_item_categories_create
      description: |-
        ایجاد دسته کالا تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `item.create`
      summary: ایجاد دسته کالا
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ItemCategoryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ItemCategoryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ItemCategoryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ItemCategory'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_item_categories_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده دسته‌های کالا.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `school`، `parent`

        **جست‌وجو** — پارامتر `search` روی `code`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `item.read`
      summary: دسته‌های کالا
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: parent
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: school
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedItemCategoryList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/item-categories/{id}/:
    put:
      operationId: inventory_item_categories_update
      description: |-
        جایگزینی کامل دسته کالا.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `item.update`
      summary: ویرایش کامل دسته کالا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ItemCategoryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ItemCategoryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ItemCategoryRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ItemCategory'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_item_categories_retrieve
      description: |-
        دریافت جزئیات یک دسته کالا با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر دسته کالا خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `item.read`
      summary: جزئیات دسته کالا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ItemCategory'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_item_categories_partial_update
      description: |-
        ویرایش جزئی دسته کالا.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `item.update`
      summary: ویرایش جزئی دسته کالا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedItemCategoryRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedItemCategoryRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedItemCategoryRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ItemCategory'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_item_categories_destroy
      description: |-
        حذف نرم دسته کالا.

        دسته کالا از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `item.delete`
      summary: حذف دسته کالا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دسته کالا را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/items/:
    post:
      operationId: inventory_items_create
      description: |-
        ایجاد کالا تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `item.create`
      summary: تعریف کالا
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Item'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_items_list
      description: '`totalOnHand` جمع موجودی همه انبارها را برمی‌گرداند.'
      summary: فهرست کالاها
      parameters:
      - in: query
        name: below_reorder
        schema:
          type: boolean
        description: فقط اقلام زیر نقطه سفارش
      - in: query
        name: category
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: is_capital_asset
        schema:
          type: boolean
      - in: query
        name: lot_tracked
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: serial_tracked
        schema:
          type: boolean
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedItemList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/items/{id}/:
    put:
      operationId: inventory_items_update
      description: |-
        جایگزینی کامل کالا.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `item.update`
      summary: ویرایش کامل کالا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ItemRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Item'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_items_retrieve
      description: |-
        دریافت جزئیات یک کالا با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر کالا خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `item.read`
      summary: جزئیات کالا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Item'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_items_partial_update
      description: |-
        ویرایش جزئی کالا.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `item.update`
      summary: ویرایش جزئی کالا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedItemRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedItemRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedItemRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Item'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_items_destroy
      description: |-
        حذف نرم کالا.

        کالا از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `item.delete`
      summary: حذف کالا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کالا را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/items/{id}/kardex/:
    get:
      operationId: inventory_items_kardex_retrieve
      description: ریز حرکات ورود و خروج با موجودی تجمعی — برای صفحه «کاردکس کالا»
        (بخش ۱۳.۵ سند فرانت).
      summary: کاردکس کالا
      parameters:
      - in: query
        name: date_from
        schema:
          type: string
        description: از تاریخ
      - in: query
        name: date_to
        schema:
          type: string
        description: تا تاریخ
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این کالا را شناسایی میکند.
        required: true
      - in: query
        name: warehouse
        schema:
          type: string
        description: محدود به یک انبار
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Kardex'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/warehouses/:
    post:
      operationId: inventory_warehouses_create
      description: |-
        ایجاد انبار تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `stock.create`
      summary: ایجاد انبار
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WarehouseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WarehouseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WarehouseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Warehouse'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_warehouses_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده انبارها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `campus`، `status`

        **جست‌وجو** — پارامتر `search` روی `code`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `stock.read`
      summary: انبارها
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedWarehouseList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/warehouses/{id}/:
    put:
      operationId: inventory_warehouses_update
      description: |-
        جایگزینی کامل انبار.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `stock.update`
      summary: ویرایش کامل انبار
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WarehouseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WarehouseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WarehouseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Warehouse'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_warehouses_retrieve
      description: |-
        دریافت جزئیات یک انبار با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر انبار خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `stock.read`
      summary: جزئیات انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Warehouse'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_warehouses_partial_update
      description: |-
        ویرایش جزئی انبار.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `stock.update`
      summary: ویرایش جزئی انبار
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedWarehouseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedWarehouseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedWarehouseRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Warehouse'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_warehouses_destroy
      description: |-
        حذف نرم انبار.

        انبار از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `stock.delete`
      summary: حذف انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انبار را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/balances/:
    get:
      operationId: inventory_balances_list
      description: موجودی خلاصه به تفکیک انبار/کالا/بچ. این جدول از حرکات موجودی ساخته
        می‌شود و مستقیماً قابل ویرایش نیست (بخش ۷.۹).
      summary: موجودی انبارها
      parameters:
      - in: query
        name: below_reorder
        schema:
          type: boolean
        description: فقط زیر نقطه سفارش
      - in: query
        name: expiring_before
        schema:
          type: string
        description: تاریخ انقضای قبل از
      - in: query
        name: item
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: lot_no
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: warehouse
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStockBalanceList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/balances/{id}/:
    get:
      operationId: inventory_balances_retrieve
      description: |-
        دریافت جزئیات یک موجودی انبار با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر موجودی انبار خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `stock.read`
      summary: جزئیات موجودی انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این موجودی انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockBalance'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/inventory/stock-documents/:
    post:
      operationId: inventory_stock_documents_create
      description: پس از افزودن اقلام، با `confirm` سند قطعی و موجودی به‌روز می‌شود.
      summary: ایجاد سند انبار (پیش‌نویس)
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockDocumentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StockDocumentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StockDocumentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocument'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_stock_documents_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اسناد انبار.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `warehouse`، `document_type`، `status`، `source_type`، `source_id`، `date_from`، `date_to`

        **جست‌وجو** — پارامتر `search` روی `document_no`، `description`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `stock.read`
      summary: اسناد انبار
      parameters:
      - in: query
        name: date_from
        schema:
          type: string
          format: date-time
      - in: query
        name: date_to
        schema:
          type: string
          format: date-time
      - in: query
        name: document_type
        schema:
          type: string
          title: نوع سند
          enum:
          - ADJUSTMENT_IN
          - ADJUSTMENT_OUT
          - COUNT
          - ISSUE
          - RECEIPT
          - RETURN_FROM_USE
          - RETURN_TO_VENDOR
          - TRANSFER_IN
          - TRANSFER_OUT
        description: |-
          * `RECEIPT` - رسید
          * `ISSUE` - حواله مصرف
          * `TRANSFER_OUT` - انتقال خروج
          * `TRANSFER_IN` - انتقال ورود
          * `RETURN_TO_VENDOR` - برگشت به فروشنده
          * `RETURN_FROM_USE` - برگشت از مصرف
          * `ADJUSTMENT_IN` - تعدیل مثبت
          * `ADJUSTMENT_OUT` - تعدیل منفی
          * `COUNT` - انبارگردانی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: source_id
        schema:
          type: string
          format: uuid
      - in: query
        name: source_type
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - CONFIRMED
          - DRAFT
          - REVERSED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `CONFIRMED` - قطعی
          * `CANCELLED` - لغوشده
          * `REVERSED` - برگشت‌خورده
      - in: query
        name: warehouse
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStockDocumentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/stock-documents/{id}/:
    put:
      operationId: inventory_stock_documents_update
      description: |-
        جایگزینی کامل سند انبار.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `stock.update`
      summary: ویرایش کامل سند انبار
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockDocumentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StockDocumentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StockDocumentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_stock_documents_retrieve
      description: |-
        دریافت جزئیات یک سند انبار با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سند انبار خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `stock.read`
      summary: جزئیات سند انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند انبار را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_stock_documents_partial_update
      description: |-
        ویرایش جزئی سند انبار.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `stock.update`
      summary: ویرایش جزئی سند انبار
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStockDocumentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStockDocumentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStockDocumentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_stock_documents_destroy
      description: |-
        حذف نرم سند انبار.

        سند انبار از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `stock.delete`
      summary: حذف سند انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند انبار را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/stock-documents/{id}/confirm/:
    post:
      operationId: inventory_stock_documents_confirm_create
      description: حرکات موجودی ثبت و موجودی خلاصه به‌روز می‌شود. برای خروج، کفایت
        موجودی، سریال و بچ کنترل می‌گردد.
      summary: قطعی‌کردن سند انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند انبار را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocument'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                خطایکمبودموجودی:
                  value:
                    code: INSUFFICIENT_STOCK
                    message: موجودی قابل‌دسترس «کاغذ A4» در انبار W01 برابر 12 است
                      و کمتر از 50 درخواستی است.
                    correlationId: 6b2f9d4a
                    fieldErrors:
                    - field: quantity
                      reason: insufficient_stock
                    retryable: false
                  summary: خطای کمبود موجودی
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/stock-documents/{id}/reverse/:
    post:
      operationId: inventory_stock_documents_reverse_create
      description: سند قطعی ویرایش نمی‌شود؛ سند معکوس ساخته و قطعی می‌شود (بخش ۷.۹).
      summary: برگشت سند انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سند انبار را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocument'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/stock-document-lines/:
    post:
      operationId: inventory_stock_document_lines_create
      description: |-
        ایجاد قلم سند انبار تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `stock.create`
      summary: ایجاد قلم سند انبار
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockDocumentLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StockDocumentLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StockDocumentLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocumentLine'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_stock_document_lines_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام سند انبار.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `stock_document`، `item`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `stock.read`
      summary: اقلام سند انبار
      parameters:
      - in: query
        name: item
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: stock_document
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStockDocumentLineList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/stock-document-lines/{id}/:
    put:
      operationId: inventory_stock_document_lines_update
      description: |-
        جایگزینی کامل قلم سند انبار.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `stock.update`
      summary: ویرایش کامل قلم سند انبار
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سند انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockDocumentLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StockDocumentLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StockDocumentLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocumentLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_stock_document_lines_retrieve
      description: |-
        دریافت جزئیات یک قلم سند انبار با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم سند انبار خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `stock.read`
      summary: جزئیات قلم سند انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سند انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocumentLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_stock_document_lines_partial_update
      description: |-
        ویرایش جزئی قلم سند انبار.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `stock.update`
      summary: ویرایش جزئی قلم سند انبار
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سند انبار را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStockDocumentLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStockDocumentLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStockDocumentLineRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockDocumentLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_stock_document_lines_destroy
      description: |-
        حذف نرم قلم سند انبار.

        قلم سند انبار از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `stock.delete`
      summary: حذف قلم سند انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سند انبار را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/movements/:
    get:
      operationId: inventory_movements_list
      description: تنها منبع تغییر موجودی؛ رکوردها تغییرناپذیرند (بخش ۵ و ۷.۹).
      summary: حرکات موجودی
      parameters:
      - in: query
        name: item
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: lot_no
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: serial_no
        schema:
          type: string
      - in: query
        name: warehouse
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStockMovementList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/movements/{id}/:
    get:
      operationId: inventory_movements_retrieve
      description: |-
        دریافت جزئیات یک حرکت موجودی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر حرکت موجودی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `stock.read`
      summary: جزئیات حرکت موجودی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این حرکت موجودی را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockMovement'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/inventory/purchase-requests/:
    post:
      operationId: inventory_purchase_requests_create
      description: |-
        ایجاد درخواست خرید تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `purchase_request.create`
      summary: ثبت درخواست خرید
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_requests_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده درخواست‌های خرید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `campus`، `status`، `requester_user_id`

        **جست‌وجو** — پارامتر `search` روی `request_no`، `title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `purchase_request.read`
      summary: درخواست‌های خرید
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: requester_user_id
        schema:
          type: string
          format: uuid
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVAL
          - APPROVED
          - BUDGET_CHECK
          - CANCELLED
          - CLOSED
          - DRAFT
          - ORDERED
          - PARTIALLY_RECEIVED
          - RECEIVED
          - REJECTED
          - SOURCING
          - SUBMITTED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `SUBMITTED` - ارسال‌شده
          * `BUDGET_CHECK` - کنترل بودجه
          * `APPROVAL` - در انتظار تأیید
          * `APPROVED` - تأییدشده
          * `REJECTED` - ردشده
          * `SOURCING` - در حال استعلام
          * `ORDERED` - سفارش‌شده
          * `PARTIALLY_RECEIVED` - دریافت جزئی
          * `RECEIVED` - دریافت کامل
          * `CLOSED` - بسته
          * `CANCELLED` - لغوشده
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPurchaseRequestList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/purchase-requests/{id}/:
    put:
      operationId: inventory_purchase_requests_update
      description: |-
        جایگزینی کامل درخواست خرید.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `purchase_request.update`
      summary: ویرایش کامل درخواست خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_requests_retrieve
      description: |-
        دریافت جزئیات یک درخواست خرید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر درخواست خرید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `purchase_request.read`
      summary: جزئیات درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_purchase_requests_partial_update
      description: |-
        ویرایش جزئی درخواست خرید.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `purchase_request.update`
      summary: ویرایش جزئی درخواست خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseRequestRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_purchase_requests_destroy
      description: |-
        حذف نرم درخواست خرید.

        درخواست خرید از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `purchase_request.delete`
      summary: حذف درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/purchase-requests/{id}/approve/:
    post:
      operationId: inventory_purchase_requests_approve_create
      description: '**تفکیک وظایف:** درخواست‌دهنده نمی‌تواند تأییدکننده باشد (بخش
        ۱۶.۲).'
      summary: تأیید درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/purchase-requests/{id}/reject/:
    post:
      operationId: inventory_purchase_requests_reject_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: رد درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/purchase-requests/{id}/submit/:
    post:
      operationId: inventory_purchase_requests_submit_create
      description: درخواست پس از ارسال به مرحله کنترل بودجه می‌رود (بخش ۱۰.۷).
      summary: ارسال درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/purchase-request-lines/:
    post:
      operationId: inventory_purchase_request_lines_create
      description: |-
        ایجاد قلم درخواست خرید تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `purchase_request.create`
      summary: ایجاد قلم درخواست خرید
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseRequestLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseRequestLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseRequestLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestLine'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_request_lines_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام درخواست خرید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `request`، `item`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `purchase_request.read`
      summary: اقلام درخواست خرید
      parameters:
      - in: query
        name: item
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: request
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPurchaseRequestLineList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/purchase-request-lines/{id}/:
    put:
      operationId: inventory_purchase_request_lines_update
      description: |-
        جایگزینی کامل قلم درخواست خرید.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `purchase_request.update`
      summary: ویرایش کامل قلم درخواست خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseRequestLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseRequestLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseRequestLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_request_lines_retrieve
      description: |-
        دریافت جزئیات یک قلم درخواست خرید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم درخواست خرید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `purchase_request.read`
      summary: جزئیات قلم درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_purchase_request_lines_partial_update
      description: |-
        ویرایش جزئی قلم درخواست خرید.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `purchase_request.update`
      summary: ویرایش جزئی قلم درخواست خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم درخواست خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseRequestLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseRequestLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseRequestLineRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseRequestLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_purchase_request_lines_destroy
      description: |-
        حذف نرم قلم درخواست خرید.

        قلم درخواست خرید از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `purchase_request.delete`
      summary: حذف قلم درخواست خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم درخواست خرید را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/purchase-orders/:
    post:
      operationId: inventory_purchase_orders_create
      description: |-
        ایجاد سفارش خرید تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `purchase_order.create`
      summary: ایجاد سفارش خرید
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseOrderRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseOrderRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_orders_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده سفارش‌های خرید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `vendor`، `status`، `purchase_request`

        **جست‌وجو** — پارامتر `search` روی `order_no`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `purchase_order.read`
      summary: سفارش‌های خرید
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: purchase_request
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - CLOSED
          - DRAFT
          - ISSUED
          - PARTIALLY_RECEIVED
          - RECEIVED
        description: |-
          * `DRAFT` - پیش‌نویس
          * `ISSUED` - صادرشده
          * `PARTIALLY_RECEIVED` - دریافت جزئی
          * `RECEIVED` - دریافت کامل
          * `CLOSED` - بسته
          * `CANCELLED` - لغوشده
      - in: query
        name: vendor
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPurchaseOrderList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/purchase-orders/{id}/:
    put:
      operationId: inventory_purchase_orders_update
      description: |-
        جایگزینی کامل سفارش خرید.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `purchase_order.update`
      summary: ویرایش کامل سفارش خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseOrderRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseOrderRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_orders_retrieve
      description: |-
        دریافت جزئیات یک سفارش خرید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر سفارش خرید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `purchase_order.read`
      summary: جزئیات سفارش خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_purchase_orders_partial_update
      description: |-
        ویرایش جزئی سفارش خرید.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `purchase_order.update`
      summary: ویرایش جزئی سفارش خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseOrderRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseOrderRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseOrderRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_purchase_orders_destroy
      description: |-
        حذف نرم سفارش خرید.

        سفارش خرید از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `purchase_order.delete`
      summary: حذف سفارش خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/purchase-orders/{id}/close/:
    post:
      operationId: inventory_purchase_orders_close_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بستن سفارش خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/purchase-orders/{id}/issue/:
    post:
      operationId: inventory_purchase_orders_issue_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: صدور سفارش خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/purchase-orders/{id}/receive/:
    post:
      operationId: inventory_purchase_orders_receive_create
      description: |-
        سند انبار از نوع «رسید» ساخته و قطعی می‌شود، مقدار دریافتی سفارش به‌روز می‌گردد و رویداد `GoodsReceived` منتشر می‌شود.

        مقدار هر قلم نمی‌تواند از باقیمانده سفارش بیشتر باشد.
      summary: ثبت رسید کالا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReceiveGoodsRequest'
            examples:
              رسیدجزئی:
                value:
                  warehouse: 7a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                  vendor_invoice_no: F-140501-882
                  vendor_invoice_amount: 48000000
                  lines:
                  - order_line: 8b2c3d4e-5f6a-7b8c-9d0e-1f2a3b4c5d6e
                    quantity: 100
                    unit_cost: 480000
                summary: رسید جزئی
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReceiveGoodsRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReceiveGoodsRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceipt'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/purchase-order-lines/:
    post:
      operationId: inventory_purchase_order_lines_create
      description: |-
        ایجاد قلم سفارش خرید تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `purchase_order.create`
      summary: ایجاد قلم سفارش خرید
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseOrderLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseOrderLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderLine'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_order_lines_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقلام سفارش خرید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `order`، `item`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `purchase_order.read`
      summary: اقلام سفارش خرید
      parameters:
      - in: query
        name: item
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: order
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPurchaseOrderLineList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/purchase-order-lines/{id}/:
    put:
      operationId: inventory_purchase_order_lines_update
      description: |-
        جایگزینی کامل قلم سفارش خرید.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `purchase_order.update`
      summary: ویرایش کامل قلم سفارش خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PurchaseOrderLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PurchaseOrderLineRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_purchase_order_lines_retrieve
      description: |-
        دریافت جزئیات یک قلم سفارش خرید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قلم سفارش خرید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `purchase_order.read`
      summary: جزئیات قلم سفارش خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_purchase_order_lines_partial_update
      description: |-
        ویرایش جزئی قلم سفارش خرید.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `purchase_order.update`
      summary: ویرایش جزئی قلم سفارش خرید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سفارش خرید را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseOrderLineRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseOrderLineRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedPurchaseOrderLineRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PurchaseOrderLine'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_purchase_order_lines_destroy
      description: |-
        حذف نرم قلم سفارش خرید.

        قلم سفارش خرید از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `purchase_order.delete`
      summary: حذف قلم سفارش خرید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قلم سفارش خرید را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/goods-receipts/:
    post:
      operationId: inventory_goods_receipts_create
      description: |-
        ایجاد رسید کالا تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `stock.create`
      summary: ایجاد رسید کالا
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoodsReceiptRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/GoodsReceiptRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/GoodsReceiptRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceipt'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_goods_receipts_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده رسیدهای کالا.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `order`، `quality_status`، `status`، `three_way_matched`

        **جست‌وجو** — پارامتر `search` روی `receipt_no`، `vendor_invoice_no`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `stock.read`
      summary: رسیدهای کالا
      parameters:
      - in: query
        name: order
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: quality_status
        schema:
          type: string
          title: وضعیت کیفی
          enum:
          - ACCEPTED
          - PARTIALLY_ACCEPTED
          - PENDING
          - REJECTED
        description: |-
          * `PENDING` - در انتظار کنترل کیفیت
          * `ACCEPTED` - پذیرفته‌شده
          * `PARTIALLY_ACCEPTED` - پذیرش جزئی
          * `REJECTED` - مردود
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - FINAL
          - PROVISIONAL
        description: |-
          * `PROVISIONAL` - رسید موقت
          * `FINAL` - رسید قطعی
          * `CANCELLED` - لغوشده
      - in: query
        name: three_way_matched
        schema:
          type: boolean
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedGoodsReceiptList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/goods-receipts/{id}/:
    get:
      operationId: inventory_goods_receipts_retrieve
      description: |-
        دریافت جزئیات یک رسید کالا با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رسید کالا خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `stock.read`
      summary: جزئیات رسید کالا
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رسید کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceipt'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_goods_receipts_partial_update
      description: |-
        ویرایش جزئی رسید کالا.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `stock.update`
      summary: ویرایش جزئی رسید کالا
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رسید کالا را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedGoodsReceiptRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedGoodsReceiptRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedGoodsReceiptRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoodsReceipt'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/inventory/goods-receipts/{id}/three-way-match/:
    post:
      operationId: inventory_goods_receipts_three_way_match_create
      description: مقایسه مبلغ سفارش، ارزش کالای دریافتی و فاکتور تأمین‌کننده با تلورانس
        قابل تنظیم. پیش از پرداخت به تأمین‌کننده انجام می‌شود (بخش ۷.۹).
      summary: تطبیق سه‌طرفه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رسید کالا را شناسایی میکند.
        required: true
      - in: query
        name: tolerance_percent
        schema:
          type: number
          format: double
        description: درصد تلورانس (پیش‌فرض ۲)
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeWayMatch'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/assets/:
    post:
      operationId: inventory_assets_create
      description: |-
        ایجاد مال سرمایه‌ای تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `asset.create`
      summary: ثبت مال سرمایه‌ای
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssetRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssetRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_assets_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اموال.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `item`، `lifecycle_status`، `condition_status`، `current_room`

        **جست‌وجو** — پارامتر `search` روی `asset_tag`، `title`، `serial_no`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `asset.read`
      summary: فهرست اموال
      parameters:
      - in: query
        name: condition_status
        schema:
          type: string
          title: وضعیت فنی
          enum:
          - BROKEN
          - FAIR
          - GOOD
          - NEW
          - POOR
        description: |-
          * `NEW` - نو
          * `GOOD` - سالم
          * `FAIR` - قابل قبول
          * `POOR` - نامناسب
          * `BROKEN` - خراب
      - in: query
        name: current_room
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: item
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: lifecycle_status
        schema:
          type: string
          title: وضعیت چرخه عمر
          enum:
          - ASSIGNED
          - DAMAGED
          - DISPOSED
          - IN_STOCK
          - LOST
          - RECOVERED
          - REGISTERED
          - RETIRED
          - UNDER_MAINTENANCE
        description: |-
          * `REGISTERED` - ثبت‌شده
          * `IN_STOCK` - موجود در انبار
          * `ASSIGNED` - تحویل‌شده
          * `UNDER_MAINTENANCE` - در تعمیر
          * `DAMAGED` - آسیب‌دیده
          * `LOST` - مفقود
          * `RECOVERED` - بازیافت‌شده
          * `RETIRED` - بازنشسته
          * `DISPOSED` - اسقاط‌شده
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAssetList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/assets/{id}/:
    put:
      operationId: inventory_assets_update
      description: |-
        جایگزینی کامل مال سرمایه‌ای.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `asset.update`
      summary: ویرایش کامل مال سرمایه‌ای
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssetRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssetRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_assets_retrieve
      description: |-
        دریافت جزئیات یک مال سرمایه‌ای با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر مال سرمایه‌ای خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `asset.read`
      summary: جزئیات مال سرمایه‌ای
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_assets_partial_update
      description: |-
        ویرایش جزئی مال سرمایه‌ای.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `asset.update`
      summary: ویرایش جزئی مال سرمایه‌ای
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAssetRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAssetRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAssetRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_assets_destroy
      description: |-
        حذف نرم مال سرمایه‌ای.

        مال سرمایه‌ای از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `asset.delete`
      summary: حذف مال سرمایه‌ای
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/assets/{id}/accept/:
    post:
      operationId: inventory_assets_accept_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: پذیرش مال و ورود به انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/assets/{id}/assign/:
    post:
      operationId: inventory_assets_assign_create
      description: مال به کارمند، اتاق، واحد سازمانی یا کلاس تحویل می‌شود و رویداد
        `AssetAssigned` منتشر می‌گردد (بخش ۱۳.۱).
      summary: تحویل مال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignAssetRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssignAssetRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssignAssetRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetAssignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/assets/{id}/retire/:
    post:
      operationId: inventory_assets_retire_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بازنشستگی مال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReasonRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/assets/{id}/return/:
    post:
      operationId: inventory_assets_return_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بازگشت مال به انبار
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مال سرمایه‌ای را شناسایی میکند.
        required: true
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReturnAssetRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReturnAssetRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReturnAssetRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Asset'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/inventory/asset-assignments/:
    post:
      operationId: inventory_asset_assignments_create
      description: |-
        ایجاد تحویل اموال تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `asset.create`
      summary: ایجاد تحویل اموال
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssetAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssetAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetAssignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_asset_assignments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تحویل‌های اموال.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `asset`، `assignee_type`، `assignee_id`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `asset.read`
      summary: تحویل‌های اموال
      parameters:
      - in: query
        name: asset
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: assignee_id
        schema:
          type: string
          format: uuid
      - in: query
        name: assignee_type
        schema:
          type: string
          title: نوع تحویل‌گیرنده
          enum:
          - CLASS_GROUP
          - EMPLOYEE
          - ORG_UNIT
          - ROOM
        description: |-
          * `EMPLOYEE` - کارمند
          * `ROOM` - اتاق
          * `ORG_UNIT` - واحد سازمانی
          * `CLASS_GROUP` - کلاس
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAssetAssignmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/asset-assignments/{id}/:
    put:
      operationId: inventory_asset_assignments_update
      description: |-
        جایگزینی کامل تحویل اموال.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `asset.update`
      summary: ویرایش کامل تحویل اموال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل اموال را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssetAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssetAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_asset_assignments_retrieve
      description: |-
        دریافت جزئیات یک تحویل اموال با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تحویل اموال خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `asset.read`
      summary: جزئیات تحویل اموال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل اموال را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_asset_assignments_partial_update
      description: |-
        ویرایش جزئی تحویل اموال.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `asset.update`
      summary: ویرایش جزئی تحویل اموال
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل اموال را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAssetAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAssetAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAssetAssignmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssetAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_asset_assignments_destroy
      description: |-
        حذف نرم تحویل اموال.

        تحویل اموال از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `asset.delete`
      summary: حذف تحویل اموال
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تحویل اموال را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/maintenance-orders/:
    post:
      operationId: inventory_maintenance_orders_create
      description: |-
        ایجاد دستور تعمیر تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `asset.create`
      summary: ثبت دستور تعمیر
      tags:
      - Inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaintenanceOrderRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/MaintenanceOrderRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MaintenanceOrderRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceOrder'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_maintenance_orders_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده دستورهای تعمیر.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `asset`، `maintenance_type`، `status`، `vendor`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `asset.read`
      summary: دستورهای تعمیر
      parameters:
      - in: query
        name: asset
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: maintenance_type
        schema:
          type: string
          title: نوع نگهداشت
          enum:
          - CORRECTIVE
          - EMERGENCY
          - INSPECTION
          - PREVENTIVE
        description: |-
          * `PREVENTIVE` - پیشگیرانه
          * `CORRECTIVE` - اصلاحی
          * `EMERGENCY` - اضطراری
          * `INSPECTION` - بازرسی
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - COMPLETED
          - IN_PROGRESS
          - OPEN
        description: |-
          * `OPEN` - باز
          * `IN_PROGRESS` - در حال انجام
          * `COMPLETED` - انجام‌شده
          * `CANCELLED` - لغوشده
      - in: query
        name: vendor
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMaintenanceOrderList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/inventory/maintenance-orders/{id}/:
    put:
      operationId: inventory_maintenance_orders_update
      description: |-
        جایگزینی کامل دستور تعمیر.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `asset.update`
      summary: ویرایش کامل دستور تعمیر
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دستور تعمیر را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaintenanceOrderRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/MaintenanceOrderRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/MaintenanceOrderRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: inventory_maintenance_orders_retrieve
      description: |-
        دریافت جزئیات یک دستور تعمیر با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر دستور تعمیر خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `asset.read`
      summary: جزئیات دستور تعمیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دستور تعمیر را شناسایی میکند.
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: inventory_maintenance_orders_partial_update
      description: |-
        ویرایش جزئی دستور تعمیر.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `asset.update`
      summary: ویرایش جزئی دستور تعمیر
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دستور تعمیر را شناسایی میکند.
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedMaintenanceOrderRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedMaintenanceOrderRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedMaintenanceOrderRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: inventory_maintenance_orders_destroy
      description: |-
        حذف نرم دستور تعمیر.

        دستور تعمیر از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `asset.delete`
      summary: حذف دستور تعمیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دستور تعمیر را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/inventory/maintenance-orders/{id}/complete/:
    post:
      operationId: inventory_maintenance_orders_complete_create
      description: مال از وضعیت «در تعمیر» به «موجود در انبار» بازمی‌گردد.
      summary: تکمیل دستور تعمیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این دستور تعمیر را شناسایی میکند.
        required: true
      tags:
      - Inventory
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceOrder'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: گذار وضعیت نامعتبر
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/health-profiles/:
    post:
      operationId: welfare_health_profiles_create
      description: |-
        ایجاد پرونده سلامت تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `health.create`
      summary: ایجاد پرونده سلامت
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthProfileRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/HealthProfileRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/HealthProfileRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthProfile'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_health_profiles_list
      description: داده سلامت «بسیار حساس» است و فقط با مجوز `health.read` در دسترس
        قرار می‌گیرد (بخش ۱۵.۲).
      summary: پرونده‌های سلامت
      parameters:
      - in: query
        name: blood_type
        schema:
          type: string
          title: گروه خونی
          enum:
          - A+
          - A-
          - AB+
          - AB-
          - B+
          - B-
          - O+
          - O-
          - UNKNOWN
        description: |-
          * `A+` - A+
          * `A-` - A-
          * `B+` - B+
          * `B-` - B-
          * `AB+` - AB+
          * `AB-` - AB-
          * `O+` - O+
          * `O-` - O-
          * `UNKNOWN` - نامشخص
      - in: query
        name: confidentiality_level
        schema:
          type: string
          title: سطح محرمانگی
          enum:
          - RESTRICTED
          - SENSITIVE
          - STANDARD
        description: |-
          * `STANDARD` - عادی
          * `SENSITIVE` - حساس
          * `RESTRICTED` - بسیار محرمانه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedHealthProfileList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/health-profiles/{id}/:
    put:
      operationId: welfare_health_profiles_update
      description: |-
        جایگزینی کامل پرونده سلامت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `health.update`
      summary: ویرایش کامل پرونده سلامت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthProfileRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/HealthProfileRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/HealthProfileRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthProfile'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_health_profiles_retrieve
      description: |-
        دریافت جزئیات یک پرونده سلامت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پرونده سلامت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `health.read`
      summary: جزئیات پرونده سلامت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthProfile'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_health_profiles_partial_update
      description: |-
        ویرایش جزئی پرونده سلامت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `health.update`
      summary: ویرایش جزئی پرونده سلامت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedHealthProfileRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedHealthProfileRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedHealthProfileRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthProfile'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_health_profiles_destroy
      description: |-
        حذف نرم پرونده سلامت.

        پرونده سلامت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `health.delete`
      summary: حذف پرونده سلامت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده سلامت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/health-alerts/:
    post:
      operationId: welfare_health_alerts_create
      description: |-
        ایجاد هشدار سلامت تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `health.create`
      summary: ثبت هشدار سلامت
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthAlertRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/HealthAlertRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/HealthAlertRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthAlert'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_health_alerts_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده هشدارهای سلامت.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `health_profile`، `alert_type`، `severity`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `health.read`
      summary: هشدارهای سلامت
      parameters:
      - in: query
        name: alert_type
        schema:
          type: string
          title: نوع هشدار
          enum:
          - ALLERGY
          - CHRONIC_CONDITION
          - DIETARY
          - MEDICATION
          - MOBILITY
          - OTHER
          - VISION_HEARING
        description: |-
          * `ALLERGY` - حساسیت
          * `CHRONIC_CONDITION` - بیماری مزمن
          * `MEDICATION` - داروی مصرفی
          * `DIETARY` - محدودیت غذایی
          * `MOBILITY` - محدودیت حرکتی
          * `VISION_HEARING` - بینایی/شنوایی
          * `OTHER` - سایر
      - in: query
        name: health_profile
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: severity
        schema:
          type: string
          title: شدت
          enum:
          - HIGH
          - LIFE_THREATENING
          - LOW
          - MEDIUM
        description: |-
          * `LOW` - کم
          * `MEDIUM` - متوسط
          * `HIGH` - زیاد
          * `LIFE_THREATENING` - تهدیدکننده حیات
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - EXPIRED
          - RESOLVED
        description: |-
          * `ACTIVE` - فعال
          * `RESOLVED` - رفع‌شده
          * `EXPIRED` - منقضی
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedHealthAlertList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/health-alerts/for-class/:
    get:
      operationId: welfare_health_alerts_for_class_list
      description: برای معلم و مسئول اردو. فقط `safeSummary` برمی‌گردد و `instructions`
        (جزئیات پزشکی) حذف می‌شود — بخش ۷.۱۰.
      summary: هشدارهای سلامت یک کلاس (نمای ایمن)
      parameters:
      - in: query
        name: alert_type
        schema:
          type: string
          title: نوع هشدار
          enum:
          - ALLERGY
          - CHRONIC_CONDITION
          - DIETARY
          - MEDICATION
          - MOBILITY
          - OTHER
          - VISION_HEARING
        description: |-
          * `ALLERGY` - حساسیت
          * `CHRONIC_CONDITION` - بیماری مزمن
          * `MEDICATION` - داروی مصرفی
          * `DIETARY` - محدودیت غذایی
          * `MOBILITY` - محدودیت حرکتی
          * `VISION_HEARING` - بینایی/شنوایی
          * `OTHER` - سایر
      - in: query
        name: class_group
        schema:
          type: string
        description: شناسه کلاس
        required: true
      - in: query
        name: health_profile
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: severity
        schema:
          type: string
          title: شدت
          enum:
          - HIGH
          - LIFE_THREATENING
          - LOW
          - MEDIUM
        description: |-
          * `LOW` - کم
          * `MEDIUM` - متوسط
          * `HIGH` - زیاد
          * `LIFE_THREATENING` - تهدیدکننده حیات
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - EXPIRED
          - RESOLVED
        description: |-
          * `ACTIVE` - فعال
          * `RESOLVED` - رفع‌شده
          * `EXPIRED` - منقضی
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedHealthAlertSafeList'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/health-alerts/{id}/:
    put:
      operationId: welfare_health_alerts_update
      description: |-
        جایگزینی کامل هشدار سلامت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `health.update`
      summary: ویرایش کامل هشدار سلامت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این هشدار سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthAlertRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/HealthAlertRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/HealthAlertRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthAlert'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_health_alerts_retrieve
      description: |-
        دریافت جزئیات یک هشدار سلامت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر هشدار سلامت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `health.read`
      summary: جزئیات هشدار سلامت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این هشدار سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthAlert'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_health_alerts_partial_update
      description: |-
        ویرایش جزئی هشدار سلامت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `health.update`
      summary: ویرایش جزئی هشدار سلامت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این هشدار سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedHealthAlertRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedHealthAlertRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedHealthAlertRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthAlert'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_health_alerts_destroy
      description: |-
        حذف نرم هشدار سلامت.

        هشدار سلامت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `health.delete`
      summary: حذف هشدار سلامت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این هشدار سلامت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/health-incidents/:
    post:
      operationId: welfare_health_incidents_create
      description: پس از ثبت، اولیای مجاز با پیام حداقلی مطلع می‌شوند (بخش ۱۱.۴).
      summary: ثبت رخداد سلامت
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthIncidentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/HealthIncidentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/HealthIncidentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthIncident'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_health_incidents_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده رخدادهای سلامت.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `student`، `outcome`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `health.read`
      summary: رخدادهای سلامت
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - in: query
        name: outcome
        schema:
          type: string
          title: نتیجه
          enum:
          - EMERGENCY
          - REFERRED_CLINIC
          - RESOLVED_ON_SITE
          - SENT_HOME
          - UNDER_OBSERVATION
        description: |-
          * `RESOLVED_ON_SITE` - رفع در محل
          * `SENT_HOME` - ارسال به منزل
          * `REFERRED_CLINIC` - ارجاع به درمانگاه
          * `EMERGENCY` - اورژانس
          * `UNDER_OBSERVATION` - تحت نظر
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedHealthIncidentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/health-incidents/{id}/:
    put:
      operationId: welfare_health_incidents_update
      description: |-
        جایگزینی کامل رخداد سلامت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `health.update`
      summary: ویرایش کامل رخداد سلامت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthIncidentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/HealthIncidentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/HealthIncidentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_health_incidents_retrieve
      description: |-
        دریافت جزئیات یک رخداد سلامت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رخداد سلامت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `health.read`
      summary: جزئیات رخداد سلامت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_health_incidents_partial_update
      description: |-
        ویرایش جزئی رخداد سلامت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `health.update`
      summary: ویرایش جزئی رخداد سلامت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سلامت را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedHealthIncidentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedHealthIncidentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedHealthIncidentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_health_incidents_destroy
      description: |-
        حذف نرم رخداد سلامت.

        رخداد سلامت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `health.delete`
      summary: حذف رخداد سلامت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سلامت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/counseling-cases/:
    post:
      operationId: welfare_counseling_cases_create
      description: |-
        ایجاد پرونده مشاوره تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `counseling.create`
      summary: ایجاد پرونده مشاوره
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CounselingCaseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CounselingCaseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CounselingCaseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingCase'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_counseling_cases_list
      description: داده مشاوره «بسیار حساس» است و دسترسی آن محدود است (بخش ۱۵.۲).
      summary: پرونده‌های مشاوره
      parameters:
      - in: query
        name: counselor_employee_id
        schema:
          type: string
          format: uuid
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: priority
        schema:
          type: string
          title: اولویت
          enum:
          - HIGH
          - LOW
          - NORMAL
          - URGENT
        description: |-
          * `LOW` - کم
          * `NORMAL` - عادی
          * `HIGH` - زیاد
          * `URGENT` - فوری
      - in: query
        name: referral_source
        schema:
          type: string
          title: منبع ارجاع
          enum:
          - DISCIPLINE
          - GUARDIAN
          - HEALTH
          - PRINCIPAL
          - SELF
          - TEACHER
        description: |-
          * `TEACHER` - معلم
          * `GUARDIAN` - ولی
          * `SELF` - خود دانش‌آموز
          * `PRINCIPAL` - مدیر
          * `HEALTH` - مربی بهداشت
          * `DISCIPLINE` - مسئول انضباط
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CLOSED
          - IN_PROGRESS
          - ON_HOLD
          - OPEN
          - REFERRED_OUT
        description: |-
          * `OPEN` - باز
          * `IN_PROGRESS` - در حال پیگیری
          * `ON_HOLD` - در انتظار
          * `CLOSED` - بسته
          * `REFERRED_OUT` - ارجاع به بیرون
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCounselingCaseList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/counseling-cases/{id}/:
    put:
      operationId: welfare_counseling_cases_update
      description: |-
        جایگزینی کامل پرونده مشاوره.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `counseling.update`
      summary: ویرایش کامل پرونده مشاوره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده مشاوره را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CounselingCaseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CounselingCaseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CounselingCaseRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingCase'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_counseling_cases_retrieve
      description: |-
        دریافت جزئیات یک پرونده مشاوره با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پرونده مشاوره خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `counseling.read`
      summary: جزئیات پرونده مشاوره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده مشاوره را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingCase'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_counseling_cases_partial_update
      description: |-
        ویرایش جزئی پرونده مشاوره.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `counseling.update`
      summary: ویرایش جزئی پرونده مشاوره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده مشاوره را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCounselingCaseRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCounselingCaseRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCounselingCaseRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingCase'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_counseling_cases_destroy
      description: |-
        حذف نرم پرونده مشاوره.

        پرونده مشاوره از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `counseling.delete`
      summary: حذف پرونده مشاوره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده مشاوره را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/counseling-cases/{id}/close/:
    post:
      operationId: welfare_counseling_cases_close_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بستن پرونده مشاوره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پرونده مشاوره را شناسایی میکند.
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingCase'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/counseling-sessions/:
    post:
      operationId: welfare_counseling_sessions_create
      description: |-
        ایجاد جلسه مشاوره تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `counseling.create`
      summary: ایجاد جلسه مشاوره
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CounselingSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CounselingSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CounselingSessionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingSession'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_counseling_sessions_list
      description: '`protectedNote` فقط برای مشاور صاحب پرونده و نقش‌های دارای مجوز
        `counseling.update` پر می‌شود؛ برای بقیه رشته خالی برمی‌گردد.'
      summary: جلسات مشاوره
      parameters:
      - in: query
        name: case
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCounselingSessionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/counseling-sessions/{id}/:
    put:
      operationId: welfare_counseling_sessions_update
      description: |-
        جایگزینی کامل جلسه مشاوره.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `counseling.update`
      summary: ویرایش کامل جلسه مشاوره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه مشاوره را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CounselingSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CounselingSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CounselingSessionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_counseling_sessions_retrieve
      description: |-
        دریافت جزئیات یک جلسه مشاوره با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر جلسه مشاوره خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `counseling.read`
      summary: جزئیات جلسه مشاوره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه مشاوره را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_counseling_sessions_partial_update
      description: |-
        ویرایش جزئی جلسه مشاوره.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `counseling.update`
      summary: ویرایش جزئی جلسه مشاوره
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه مشاوره را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCounselingSessionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCounselingSessionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCounselingSessionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounselingSession'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_counseling_sessions_destroy
      description: |-
        حذف نرم جلسه مشاوره.

        جلسه مشاوره از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `counseling.delete`
      summary: حذف جلسه مشاوره
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این جلسه مشاوره را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/behavior-incidents/:
    post:
      operationId: welfare_behavior_incidents_create
      description: |-
        ایجاد رخداد رفتاری تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `behavior.create`
      summary: ثبت رخداد رفتاری
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorIncidentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BehaviorIncidentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BehaviorIncidentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorIncident'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_behavior_incidents_list
      description: شامل تخلف و تشویق. رخداد در وضعیت «گزارش‌شده» ثبت می‌شود و تا پیش
        از تصمیم، اتهام قطعی محسوب نمی‌شود (بخش ۷.۱۰).
      summary: رخدادهای رفتاری
      parameters:
      - in: query
        name: class_group
        schema:
          type: string
          format: uuid
        description: کلاس
      - in: query
        name: date_from
        schema:
          type: string
          format: date-time
      - in: query
        name: date_to
        schema:
          type: string
          format: date-time
      - in: query
        name: incident_type
        schema:
          type: string
          title: نوع رخداد
          enum:
          - ACADEMIC_DISHONESTY
          - BULLYING
          - COMMENDATION
          - DISRUPTION
          - DRESS_CODE
          - OTHER
          - PROPERTY_DAMAGE
          - TARDINESS
        description: |-
          * `DISRUPTION` - اخلال در کلاس
          * `TARDINESS` - تأخیر مکرر
          * `DRESS_CODE` - عدم رعایت پوشش
          * `BULLYING` - آزار همکلاسی
          * `PROPERTY_DAMAGE` - تخریب اموال
          * `ACADEMIC_DISHONESTY` - تقلب
          * `COMMENDATION` - تشویق
          * `OTHER` - سایر
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: severity
        schema:
          type: string
          title: شدت
          enum:
          - MAJOR
          - MINOR
          - MODERATE
          - SEVERE
        description: |-
          * `MINOR` - جزئی
          * `MODERATE` - متوسط
          * `MAJOR` - جدی
          * `SEVERE` - بسیار جدی
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTION_TAKEN
          - CLOSED
          - REPORTED
          - SUBSTANTIATED
          - UNDER_APPEAL
          - UNDER_INVESTIGATION
          - UNSUBSTANTIATED
        description: |-
          * `REPORTED` - گزارش‌شده
          * `UNDER_INVESTIGATION` - در حال بررسی
          * `SUBSTANTIATED` - تأییدشده
          * `UNSUBSTANTIATED` - رد شده
          * `ACTION_TAKEN` - اقدام انجام شد
          * `UNDER_APPEAL` - در حال اعتراض
          * `CLOSED` - بسته
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedBehaviorIncidentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/behavior-incidents/summary/:
    get:
      operationId: welfare_behavior_incidents_summary_retrieve
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: خلاصه انضباطی دانش‌آموز
      parameters:
      - in: query
        name: student
        schema:
          type: string
        description: شناسه دانش‌آموز
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentBehaviorSummary'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/behavior-incidents/{id}/:
    put:
      operationId: welfare_behavior_incidents_update
      description: |-
        جایگزینی کامل رخداد رفتاری.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `behavior.update`
      summary: ویرایش کامل رخداد رفتاری
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد رفتاری را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorIncidentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BehaviorIncidentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BehaviorIncidentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_behavior_incidents_retrieve
      description: |-
        دریافت جزئیات یک رخداد رفتاری با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رخداد رفتاری خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `behavior.read`
      summary: جزئیات رخداد رفتاری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد رفتاری را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_behavior_incidents_partial_update
      description: |-
        ویرایش جزئی رخداد رفتاری.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `behavior.update`
      summary: ویرایش جزئی رخداد رفتاری
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد رفتاری را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedBehaviorIncidentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedBehaviorIncidentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedBehaviorIncidentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_behavior_incidents_destroy
      description: |-
        حذف نرم رخداد رفتاری.

        رخداد رفتاری از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `behavior.delete`
      summary: حذف رخداد رفتاری
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد رفتاری را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/behavior-incidents/{id}/decide/:
    post:
      operationId: welfare_behavior_incidents_decide_create
      description: با `substantiated = true` رخداد تأیید و امتیاز اعمال می‌شود؛ با
        `false` رخداد رد می‌گردد. مسیر اعتراض مستقل است (بخش ۷.۱۰).
      summary: تصمیم درباره رخداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد رفتاری را شناسایی میکند.
        required: true
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorDecisionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BehaviorDecisionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BehaviorDecisionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/behavior-incidents/{id}/investigate/:
    post:
      operationId: welfare_behavior_incidents_investigate_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: شروع بررسی رخداد
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد رفتاری را شناسایی میکند.
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorIncident'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/behavior-actions/:
    post:
      operationId: welfare_behavior_actions_create
      description: |-
        ایجاد اقدام انضباطی تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `behavior.create`
      summary: ثبت اقدام انضباطی
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorActionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BehaviorActionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BehaviorActionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorAction'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_behavior_actions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اقدامات انضباطی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `incident`، `action_type`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `behavior.read`
      summary: اقدامات انضباطی
      parameters:
      - in: query
        name: action_type
        schema:
          type: string
          title: نوع عمل
          enum:
          - COMMENDATION
          - COUNSELING_REFERRAL
          - DETENTION
          - GUARDIAN_MEETING
          - RESTITUTION
          - SUSPENSION
          - VERBAL_WARNING
          - WRITTEN_WARNING
        description: |-
          * `VERBAL_WARNING` - تذکر شفاهی
          * `WRITTEN_WARNING` - اخطار کتبی
          * `GUARDIAN_MEETING` - جلسه با ولی
          * `COUNSELING_REFERRAL` - ارجاع به مشاور
          * `DETENTION` - ماندن پس از ساعت درسی
          * `SUSPENSION` - تعلیق موقت
          * `COMMENDATION` - تشویق و امتیاز
          * `RESTITUTION` - جبران خسارت
      - in: query
        name: incident
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedBehaviorActionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/behavior-actions/{id}/:
    put:
      operationId: welfare_behavior_actions_update
      description: |-
        جایگزینی کامل اقدام انضباطی.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `behavior.update`
      summary: ویرایش کامل اقدام انضباطی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اقدام انضباطی را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorActionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BehaviorActionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BehaviorActionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorAction'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_behavior_actions_retrieve
      description: |-
        دریافت جزئیات یک اقدام انضباطی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر اقدام انضباطی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `behavior.read`
      summary: جزئیات اقدام انضباطی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اقدام انضباطی را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorAction'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_behavior_actions_partial_update
      description: |-
        ویرایش جزئی اقدام انضباطی.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `behavior.update`
      summary: ویرایش جزئی اقدام انضباطی
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اقدام انضباطی را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedBehaviorActionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedBehaviorActionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedBehaviorActionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BehaviorAction'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_behavior_actions_destroy
      description: |-
        حذف نرم اقدام انضباطی.

        اقدام انضباطی از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `behavior.delete`
      summary: حذف اقدام انضباطی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اقدام انضباطی را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/library-titles/:
    post:
      operationId: welfare_library_titles_create
      description: |-
        ایجاد عنوان کتابخانه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `library.create`
      summary: ثبت عنوان جدید
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LibraryTitleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LibraryTitleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LibraryTitleRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryTitle'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_library_titles_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده عناوین کتابخانه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `material_type`، `language`، `classification`

        **جست‌وجو** — پارامتر `search` روی `title`، `author`، `isbn`، `subject`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `library.read`
      summary: عناوین کتابخانه
      parameters:
      - in: query
        name: classification
        schema:
          type: string
      - in: query
        name: language
        schema:
          type: string
      - in: query
        name: material_type
        schema:
          type: string
          title: نوع ماده
          enum:
          - AUDIO
          - BOOK
          - DIGITAL
          - EQUIPMENT
          - MAGAZINE
          - VIDEO
        description: |-
          * `BOOK` - کتاب
          * `MAGAZINE` - مجله
          * `AUDIO` - صوتی
          * `VIDEO` - تصویری
          * `DIGITAL` - دیجیتال
          * `EQUIPMENT` - تجهیزات
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedLibraryTitleList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/library-titles/{id}/:
    put:
      operationId: welfare_library_titles_update
      description: |-
        جایگزینی کامل عنوان کتابخانه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `library.update`
      summary: ویرایش کامل عنوان کتابخانه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عنوان کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LibraryTitleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LibraryTitleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LibraryTitleRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryTitle'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_library_titles_retrieve
      description: |-
        دریافت جزئیات یک عنوان کتابخانه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر عنوان کتابخانه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `library.read`
      summary: جزئیات عنوان کتابخانه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عنوان کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryTitle'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_library_titles_partial_update
      description: |-
        ویرایش جزئی عنوان کتابخانه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `library.update`
      summary: ویرایش جزئی عنوان کتابخانه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عنوان کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLibraryTitleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLibraryTitleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLibraryTitleRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryTitle'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_library_titles_destroy
      description: |-
        حذف نرم عنوان کتابخانه.

        عنوان کتابخانه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `library.delete`
      summary: حذف عنوان کتابخانه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این عنوان کتابخانه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/library-copies/:
    post:
      operationId: welfare_library_copies_create
      description: |-
        ایجاد نسخه کتابخانه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `library.create`
      summary: ایجاد نسخه کتابخانه
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LibraryCopyRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LibraryCopyRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LibraryCopyRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryCopy'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_library_copies_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده نسخه‌های کتابخانه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `title_ref`، `status`، `is_loanable`

        **جست‌وجو** — پارامتر `search` روی `barcode`، `title_ref__title`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `library.read`
      summary: نسخه‌های کتابخانه
      parameters:
      - in: query
        name: is_loanable
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - AVAILABLE
          - DAMAGED
          - LOST
          - ON_LOAN
          - RESERVED
          - WITHDRAWN
        description: |-
          * `AVAILABLE` - موجود
          * `ON_LOAN` - امانت داده‌شده
          * `RESERVED` - رزروشده
          * `LOST` - مفقود
          * `DAMAGED` - آسیب‌دیده
          * `WITHDRAWN` - خارج از رده
      - in: query
        name: title_ref
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedLibraryCopyList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/library-copies/{id}/:
    put:
      operationId: welfare_library_copies_update
      description: |-
        جایگزینی کامل نسخه کتابخانه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `library.update`
      summary: ویرایش کامل نسخه کتابخانه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LibraryCopyRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LibraryCopyRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LibraryCopyRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryCopy'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_library_copies_retrieve
      description: |-
        دریافت جزئیات یک نسخه کتابخانه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر نسخه کتابخانه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `library.read`
      summary: جزئیات نسخه کتابخانه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryCopy'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_library_copies_partial_update
      description: |-
        ویرایش جزئی نسخه کتابخانه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `library.update`
      summary: ویرایش جزئی نسخه کتابخانه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLibraryCopyRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLibraryCopyRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLibraryCopyRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryCopy'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_library_copies_destroy
      description: |-
        حذف نرم نسخه کتابخانه.

        نسخه کتابخانه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `library.delete`
      summary: حذف نسخه کتابخانه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این نسخه کتابخانه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/library-loans/:
    post:
      operationId: welfare_library_loans_create
      description: |-
        ایجاد امانت کتابخانه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `library.create`
      summary: ایجاد امانت کتابخانه
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LibraryLoanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LibraryLoanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LibraryLoanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryLoan'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_library_loans_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده امانت‌های کتابخانه.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `copy`، `borrower_person`، `status`، `fine_paid`، `overdue`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `library.read`
      summary: امانت‌های کتابخانه
      parameters:
      - in: query
        name: borrower_person
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: copy
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: fine_paid
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - in: query
        name: overdue
        schema:
          type: boolean
        description: فقط امانت‌های سررسید گذشته
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - ACTIVE
          - LOST
          - OVERDUE
          - RENEWED
          - RETURNED
        description: |-
          * `ACTIVE` - در امانت
          * `RETURNED` - بازگردانده‌شده
          * `OVERDUE` - سررسید گذشته
          * `LOST` - مفقود
          * `RENEWED` - تمدیدشده
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedLibraryLoanList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/library-loans/create-loan/:
    post:
      operationId: welfare_library_loans_create_loan_create
      description: نسخه باید «موجود» و «قابل امانت» باشد. با ثبت امانت، وضعیت نسخه
        به «امانت داده‌شده» تغییر می‌کند (بخش ۷.۱۰).
      summary: ثبت امانت
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLoanRequest'
            examples:
              امانت۱۴روزه:
                value:
                  copy: 1c2d3e4f-5a6b-7c8d-9e0f-1a2b3c4d5e6f
                  borrower_person: 2d3e4f5a-6b7c-8d9e-0f1a-2b3c4d5e6f7a
                  loan_days: 14
                summary: امانت ۱۴ روزه
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CreateLoanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CreateLoanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryLoan'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/library-loans/{id}/:
    get:
      operationId: welfare_library_loans_retrieve
      description: |-
        دریافت جزئیات یک امانت کتابخانه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر امانت کتابخانه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `library.read`
      summary: جزئیات امانت کتابخانه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این امانت کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryLoan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_library_loans_partial_update
      description: |-
        ویرایش جزئی امانت کتابخانه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `library.update`
      summary: ویرایش جزئی امانت کتابخانه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این امانت کتابخانه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLibraryLoanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLibraryLoanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLibraryLoanRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryLoan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/welfare/library-loans/{id}/renew/:
    post:
      operationId: welfare_library_loans_renew_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: تمدید امانت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این امانت کتابخانه را شناسایی میکند.
        required: true
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLoanRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CreateLoanRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CreateLoanRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryLoan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/library-loans/{id}/return/:
    post:
      operationId: welfare_library_loans_return_create
      description: در صورت تأخیر، جریمه محاسبه و ثبت می‌شود.
      summary: بازگشت امانت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این امانت کتابخانه را شناسایی میکند.
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LibraryLoan'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/vehicles/:
    post:
      operationId: welfare_vehicles_create
      description: |-
        ایجاد خودرو تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `transport.create`
      summary: ایجاد خودرو
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VehicleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/VehicleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/VehicleRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vehicle'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_vehicles_list
      description: '`documentsValid` نشان می‌دهد معاینه فنی و بیمه معتبرند یا نه.'
      summary: خودروها
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedVehicleList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/vehicles/{id}/:
    put:
      operationId: welfare_vehicles_update
      description: |-
        جایگزینی کامل خودرو.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش کامل خودرو
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این خودرو را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VehicleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/VehicleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/VehicleRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vehicle'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_vehicles_retrieve
      description: |-
        دریافت جزئیات یک خودرو با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر خودرو خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `transport.read`
      summary: جزئیات خودرو
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این خودرو را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vehicle'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_vehicles_partial_update
      description: |-
        ویرایش جزئی خودرو.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش جزئی خودرو
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این خودرو را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedVehicleRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedVehicleRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedVehicleRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vehicle'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_vehicles_destroy
      description: |-
        حذف نرم خودرو.

        خودرو از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `transport.delete`
      summary: حذف خودرو
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این خودرو را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/routes/:
    post:
      operationId: welfare_routes_create
      description: |-
        ایجاد مسیر سرویس تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `transport.create`
      summary: تعریف مسیر سرویس
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransportRouteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TransportRouteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TransportRouteRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransportRoute'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_routes_list
      description: '`activeRiderCount` برای کنترل ظرفیت خودرو استفاده می‌شود.'
      summary: مسیرهای سرویس
      parameters:
      - in: query
        name: campus
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: direction
        schema:
          type: string
          title: جهت
          enum:
          - AFTERNOON
          - BOTH
          - MORNING
        description: |-
          * `MORNING` - رفت (صبح)
          * `AFTERNOON` - برگشت (بعدازظهر)
          * `BOTH` - رفت و برگشت
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTransportRouteList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/routes/{id}/:
    put:
      operationId: welfare_routes_update
      description: |-
        جایگزینی کامل مسیر سرویس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش کامل مسیر سرویس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مسیر سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransportRouteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TransportRouteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TransportRouteRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransportRoute'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_routes_retrieve
      description: |-
        دریافت جزئیات یک مسیر سرویس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر مسیر سرویس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `transport.read`
      summary: جزئیات مسیر سرویس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مسیر سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransportRoute'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_routes_partial_update
      description: |-
        ویرایش جزئی مسیر سرویس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش جزئی مسیر سرویس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مسیر سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTransportRouteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTransportRouteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTransportRouteRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransportRoute'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_routes_destroy
      description: |-
        حذف نرم مسیر سرویس.

        مسیر سرویس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `transport.delete`
      summary: حذف مسیر سرویس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این مسیر سرویس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/route-stops/:
    post:
      operationId: welfare_route_stops_create
      description: |-
        ایجاد ایستگاه تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `transport.create`
      summary: ایجاد ایستگاه
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteStopRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RouteStopRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RouteStopRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteStop'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_route_stops_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده ایستگاه‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `route`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `transport.read`
      summary: ایستگاه‌های مسیر
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: route
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRouteStopList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/route-stops/{id}/:
    put:
      operationId: welfare_route_stops_update
      description: |-
        جایگزینی کامل ایستگاه.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `transport.update`
      summary: ویرایش کامل ایستگاه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ایستگاه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteStopRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RouteStopRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RouteStopRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteStop'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_route_stops_retrieve
      description: |-
        دریافت جزئیات یک ایستگاه با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ایستگاه خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `transport.read`
      summary: جزئیات ایستگاه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ایستگاه را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteStop'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_route_stops_partial_update
      description: |-
        ویرایش جزئی ایستگاه.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `transport.update`
      summary: ویرایش جزئی ایستگاه
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ایستگاه را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedRouteStopRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedRouteStopRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedRouteStopRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteStop'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_route_stops_destroy
      description: |-
        حذف نرم ایستگاه.

        ایستگاه از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `transport.delete`
      summary: حذف ایستگاه
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ایستگاه را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/route-assignments/:
    post:
      operationId: welfare_route_assignments_create
      description: تعداد دانش‌آموزان فعال مسیر نباید از ظرفیت خودرو بیشتر شود و مدارک
        خودرو باید معتبر باشد (بخش ۷.۱۰).
      summary: انتساب دانش‌آموز به مسیر
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentRouteAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentRouteAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentRouteAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentRouteAssignment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
    get:
      operationId: welfare_route_assignments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده انتساب‌های سرویس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `student`، `route`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `transport.read`
      summary: انتساب‌های سرویس
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: route
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedStudentRouteAssignmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/route-assignments/{id}/:
    put:
      operationId: welfare_route_assignments_update
      description: |-
        جایگزینی کامل انتساب سرویس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش کامل انتساب سرویس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentRouteAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentRouteAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentRouteAssignmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentRouteAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_route_assignments_retrieve
      description: |-
        دریافت جزئیات یک انتساب سرویس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر انتساب سرویس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `transport.read`
      summary: جزئیات انتساب سرویس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentRouteAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_route_assignments_partial_update
      description: |-
        ویرایش جزئی انتساب سرویس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش جزئی انتساب سرویس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudentRouteAssignmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudentRouteAssignmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudentRouteAssignmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentRouteAssignment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_route_assignments_destroy
      description: |-
        حذف نرم انتساب سرویس.

        انتساب سرویس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `transport.delete`
      summary: حذف انتساب سرویس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این انتساب سرویس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/route-runs/:
    post:
      operationId: welfare_route_runs_create
      description: |-
        ایجاد اجرای مسیر تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `transport.create`
      summary: ثبت اجرای مسیر
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteRunRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RouteRunRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RouteRunRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteRun'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_route_runs_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده اجراهای مسیر.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `route`، `vehicle`، `run_date`، `direction`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `transport.read`
      summary: اجراهای مسیر
      parameters:
      - in: query
        name: direction
        schema:
          type: string
          title: جهت
          enum:
          - AFTERNOON
          - BOTH
          - MORNING
        description: |-
          * `MORNING` - رفت (صبح)
          * `AFTERNOON` - برگشت (بعدازظهر)
          * `BOTH` - رفت و برگشت
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: route
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: run_date
        schema:
          type: string
          format: date
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - COMPLETED
          - IN_PROGRESS
          - PLANNED
        description: |-
          * `PLANNED` - برنامه‌ریزی‌شده
          * `IN_PROGRESS` - در حال اجرا
          * `COMPLETED` - پایان‌یافته
          * `CANCELLED` - لغوشده
      - in: query
        name: vehicle
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRouteRunList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/route-runs/{id}/:
    put:
      operationId: welfare_route_runs_update
      description: |-
        جایگزینی کامل اجرای مسیر.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش کامل اجرای مسیر
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteRunRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RouteRunRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RouteRunRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_route_runs_retrieve
      description: |-
        دریافت جزئیات یک اجرای مسیر با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر اجرای مسیر خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `transport.read`
      summary: جزئیات اجرای مسیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_route_runs_partial_update
      description: |-
        ویرایش جزئی اجرای مسیر.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `transport.update`
      summary: ویرایش جزئی اجرای مسیر
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedRouteRunRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedRouteRunRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedRouteRunRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_route_runs_destroy
      description: |-
        حذف نرم اجرای مسیر.

        اجرای مسیر از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `transport.delete`
      summary: حذف اجرای مسیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/welfare/route-runs/{id}/complete/:
    post:
      operationId: welfare_route_runs_complete_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: پایان اجرای مسیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/route-runs/{id}/depart/:
    post:
      operationId: welfare_route_runs_depart_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: اعلام حرکت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteRun'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/route-runs/{id}/manifest/:
    get:
      operationId: welfare_route_runs_manifest_retrieve
      description: دانش‌آموزان منتسب به مسیر با آخرین رخداد سوار/پیاده‌شدن — برای
        کنسول راننده و مسئول سرویس (بخش ۱۴.۵ سند فرانت).
      summary: فهرست مسافران اجرای مسیر
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اجرای مسیر را شناسایی میکند.
        required: true
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteManifest'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/welfare/ridership-events/:
    post:
      operationId: welfare_ridership_events_create
      description: 'پس از ثبت، اولیای مجاز مطلع می‌شوند. توجه: نبود رخداد دستگاه به‌تنهایی
        اثبات غیبت نیست (بخش ۷.۱۰).'
      summary: ثبت سوار/پیاده شدن
      tags:
      - Welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RidershipEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RidershipEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RidershipEventRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RidershipEvent'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_ridership_events_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده رخدادهای سرویس.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `route_run`، `student`، `event_type`، `source`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `transport.read`
      summary: رخدادهای سرویس
      parameters:
      - in: query
        name: event_type
        schema:
          type: string
          title: نوع رویداد
          enum:
          - ALIGHTED
          - BOARDED
          - NO_SHOW
        description: |-
          * `BOARDED` - سوار شد
          * `ALIGHTED` - پیاده شد
          * `NO_SHOW` - حاضر نشد
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: route_run
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: source
        schema:
          type: string
          title: منبع
          enum:
          - DEVICE
          - DRIVER
          - MANUAL
          - SUPERVISOR
        description: |-
          * `DEVICE` - دستگاه
          * `DRIVER` - ثبت راننده
          * `SUPERVISOR` - ثبت مسئول
          * `MANUAL` - دستی
      - in: query
        name: student
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedRidershipEventList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/welfare/ridership-events/{id}/:
    put:
      operationId: welfare_ridership_events_update
      description: |-
        جایگزینی کامل رخداد سرویس.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `transport.update`
      summary: ویرایش کامل رخداد سرویس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RidershipEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RidershipEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RidershipEventRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RidershipEvent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: welfare_ridership_events_retrieve
      description: |-
        دریافت جزئیات یک رخداد سرویس با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر رخداد سرویس خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `transport.read`
      summary: جزئیات رخداد سرویس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RidershipEvent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: welfare_ridership_events_partial_update
      description: |-
        ویرایش جزئی رخداد سرویس.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `transport.update`
      summary: ویرایش جزئی رخداد سرویس
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سرویس را شناسایی میکند.
        required: true
      tags:
      - welfare
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedRidershipEventRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedRidershipEventRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedRidershipEventRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RidershipEvent'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: welfare_ridership_events_destroy
      description: |-
        حذف نرم رخداد سرویس.

        رخداد سرویس از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `transport.delete`
      summary: حذف رخداد سرویس
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رخداد سرویس را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - welfare
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/workflow-definitions/:
    post:
      operationId: workflow_workflow_definitions_create
      description: |-
        `stepsDefinition` آرایه‌ای از گام‌هاست، مثلاً:
        `[{"sequence": 1, "roleCode": "ACCOUNTANT"}, {"sequence": 2, "roleCode": "PRINCIPAL"}]`
      summary: تعریف گردش تأیید
      tags:
      - Workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowDefinitionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WorkflowDefinitionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WorkflowDefinitionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinition'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_workflow_definitions_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تعاریف گردش تأیید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `code`، `subject_type`، `is_active`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `approval.read`
      summary: تعاریف گردش تأیید
      parameters:
      - in: query
        name: code
        schema:
          type: string
      - in: query
        name: is_active
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: subject_type
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedWorkflowDefinitionList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/workflow-definitions/{id}/:
    put:
      operationId: workflow_workflow_definitions_update
      description: |-
        جایگزینی کامل تعریف گردش تأیید.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `approval.update`
      summary: ویرایش کامل تعریف گردش تأیید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تعریف گردش تأیید را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkflowDefinitionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WorkflowDefinitionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WorkflowDefinitionRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinition'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_workflow_definitions_retrieve
      description: |-
        دریافت جزئیات یک تعریف گردش تأیید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تعریف گردش تأیید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `approval.read`
      summary: جزئیات تعریف گردش تأیید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تعریف گردش تأیید را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinition'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_workflow_definitions_partial_update
      description: |-
        ویرایش جزئی تعریف گردش تأیید.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `approval.update`
      summary: ویرایش جزئی تعریف گردش تأیید
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تعریف گردش تأیید را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedWorkflowDefinitionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedWorkflowDefinitionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedWorkflowDefinitionRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkflowDefinition'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_workflow_definitions_destroy
      description: |-
        حذف نرم تعریف گردش تأیید.

        تعریف گردش تأیید از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `approval.delete`
      summary: حذف تعریف گردش تأیید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تعریف گردش تأیید را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/approvals/:
    post:
      operationId: workflow_approvals_create
      description: |-
        ایجاد درخواست تأیید تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `approval.create`
      summary: ایجاد درخواست تأیید
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ApprovalRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ApprovalRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequest'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_approvals_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده درخواست‌های تأیید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `subject_type`، `subject_id`، `workflow_code`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `approval.read`
      summary: درخواست‌های تأیید
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - APPROVED
          - CANCELLED
          - EXPIRED
          - PENDING
          - REJECTED
        description: |-
          * `PENDING` - در جریان
          * `APPROVED` - تأییدشده
          * `REJECTED` - ردشده
          * `CANCELLED` - لغوشده
          * `EXPIRED` - منقضی
      - in: query
        name: subject_id
        schema:
          type: string
          format: uuid
      - in: query
        name: subject_type
        schema:
          type: string
      - in: query
        name: workflow_code
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedApprovalRequestList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/approvals/start/:
    post:
      operationId: workflow_approvals_start_create
      description: نسخه فعال تعریف گردش Snapshot می‌شود و گام‌ها ساخته می‌شوند (بخش
        ۷.۱۱).
      summary: شروع گردش تأیید
      tags:
      - Workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartApprovalRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StartApprovalRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StartApprovalRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequest'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/workflow/approvals/{id}/:
    get:
      operationId: workflow_approvals_retrieve
      description: |-
        دریافت جزئیات یک درخواست تأیید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر درخواست تأیید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `approval.read`
      summary: جزئیات درخواست تأیید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست تأیید را شناسایی میکند.
        required: true
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow/approvals/{id}/cancel/:
    post:
      operationId: workflow_approvals_cancel_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: لغو درخواست تأیید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این درخواست تأیید را شناسایی میکند.
        required: true
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/workflow/approval-steps/:
    post:
      operationId: workflow_approval_steps_create
      description: |-
        ایجاد گام تأیید تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `approval.create`
      summary: ایجاد گام تأیید
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalStepRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ApprovalStepRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ApprovalStepRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalStep'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_approval_steps_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده گام‌های تأیید.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `request`، `decision`، `approver_role_code`، `approver_user_id`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `approval.read`
      summary: گام‌های تأیید
      parameters:
      - in: query
        name: approver_role_code
        schema:
          type: string
      - in: query
        name: approver_user_id
        schema:
          type: string
          format: uuid
      - in: query
        name: decision
        schema:
          type: string
          title: تصمیم
          enum:
          - APPROVED
          - DELEGATED
          - PENDING
          - REJECTED
          - RETURNED
        description: |-
          * `PENDING` - در انتظار
          * `APPROVED` - تأیید
          * `REJECTED` - رد
          * `RETURNED` - بازگشت برای اصلاح
          * `DELEGATED` - ارجاع به جانشین
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: request
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedApprovalStepList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/approval-steps/{id}/:
    get:
      operationId: workflow_approval_steps_retrieve
      description: |-
        دریافت جزئیات یک گام تأیید با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر گام تأیید خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `approval.read`
      summary: جزئیات گام تأیید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این گام تأیید را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalStep'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow/approval-steps/{id}/decide/:
    post:
      operationId: workflow_approval_steps_decide_create
      description: گام‌ها به ترتیب `sequenceNo` تصمیم‌گیری می‌شوند. رد یک گام، کل
        درخواست را رد می‌کند. با تکمیل همه گام‌ها رویداد `ApprovalCompleted` منتشر
        می‌شود.
      summary: ثبت تصمیم گام تأیید
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این گام تأیید را شناسایی میکند.
        required: true
      tags:
      - Workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DecideStepRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DecideStepRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DecideStepRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalRequest'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/workflow/notification-templates/:
    post:
      operationId: workflow_notification_templates_create
      description: |-
        ایجاد قالب اعلان تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `notification.create`
      summary: ایجاد قالب اعلان
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationTemplateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NotificationTemplateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NotificationTemplateRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationTemplate'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_notification_templates_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده قالب‌های اعلان.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `code`، `channel`، `locale`، `is_active`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `notification.read`
      summary: قالب‌های اعلان
      parameters:
      - in: query
        name: channel
        schema:
          type: string
          title: کانال
          enum:
          - EMAIL
          - IN_APP
          - PUSH
          - SMS
        description: |-
          * `IN_APP` - درون‌برنامه‌ای
          * `SMS` - پیامک
          * `EMAIL` - ایمیل
          * `PUSH` - اعلان موبایل
      - in: query
        name: code
        schema:
          type: string
      - in: query
        name: is_active
        schema:
          type: boolean
      - in: query
        name: locale
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedNotificationTemplateList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/notification-templates/{id}/:
    put:
      operationId: workflow_notification_templates_update
      description: |-
        جایگزینی کامل قالب اعلان.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `notification.update`
      summary: ویرایش کامل قالب اعلان
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قالب اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationTemplateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NotificationTemplateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NotificationTemplateRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationTemplate'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_notification_templates_retrieve
      description: |-
        دریافت جزئیات یک قالب اعلان با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر قالب اعلان خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `notification.read`
      summary: جزئیات قالب اعلان
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قالب اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationTemplate'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_notification_templates_partial_update
      description: |-
        ویرایش جزئی قالب اعلان.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `notification.update`
      summary: ویرایش جزئی قالب اعلان
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قالب اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedNotificationTemplateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedNotificationTemplateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedNotificationTemplateRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationTemplate'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_notification_templates_destroy
      description: |-
        حذف نرم قالب اعلان.

        قالب اعلان از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `notification.delete`
      summary: حذف قالب اعلان
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این قالب اعلان را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/notifications/:
    post:
      operationId: workflow_notifications_create
      description: |-
        ایجاد اعلان تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `notification.create`
      summary: ایجاد اعلان
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_notifications_list
      description: صفحه‌بندی Cursor؛ برای مرکز اعلان و زنگوله هدر.
      summary: فهرست اعلان‌ها
      parameters:
      - in: query
        name: channel
        schema:
          type: string
          title: کانال
          enum:
          - EMAIL
          - IN_APP
          - PUSH
          - SMS
        description: |-
          * `IN_APP` - درون‌برنامه‌ای
          * `SMS` - پیامک
          * `EMAIL` - ایمیل
          * `PUSH` - اعلان موبایل
      - name: cursor
        required: false
        in: query
        description: مقدار نشانگر صفحه‌بندی.
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: priority
        schema:
          type: string
          title: اولویت
          enum:
          - EMERGENCY
          - OPERATIONAL
          - PROMOTIONAL
          - TRANSACTIONAL
        description: |-
          * `TRANSACTIONAL` - تراکنشی
          * `OPERATIONAL` - عملیاتی
          * `PROMOTIONAL` - اطلاع‌رسانی عمومی
          * `EMERGENCY` - اضطراری
      - in: query
        name: recipient_person
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: related_type
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - DELIVERED
          - FAILED
          - QUEUED
          - READ
          - SENDING
          - SENT
        description: |-
          * `QUEUED` - در صف
          * `SENDING` - در حال ارسال
          * `SENT` - ارسال‌شده
          * `DELIVERED` - تحویل‌شده
          * `FAILED` - ناموفق
          * `CANCELLED` - لغوشده
          * `READ` - خوانده‌شده
      - in: query
        name: unread
        schema:
          type: boolean
        description: فقط خوانده‌نشده
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedNotificationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/notifications/broadcast/:
    post:
      operationId: workflow_notifications_broadcast_create
      description: 'دو مرحله‌ای است (بخش ۱۱.۴): با `confirm=false` فقط تعداد گیرندگان
        و نمونه پیام برمی‌گردد؛ با `confirm=true` ارسال انجام می‌شود.'
      summary: ارسال گروهی اعلان
      tags:
      - Workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BroadcastRequest'
            examples:
              پیش‌نمایش:
                value:
                  audience: CLASS_GUARDIANS
                  target_id: 0a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                  channel: SMS
                  body: جلسه اولیا و مربیان روز چهارشنبه ساعت ۱۶ برگزار می‌شود.
                  confirm: false
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BroadcastRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BroadcastRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BroadcastPreview'
          description: عملیات با موفقیت انجام شد.
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/workflow/notifications/mine/:
    get:
      operationId: workflow_notifications_mine_list
      description: اعلان‌های کاربر جاری بر اساس شخصِ متصل به حساب کاربری.
      summary: اعلان‌های من
      parameters:
      - in: query
        name: channel
        schema:
          type: string
          title: کانال
          enum:
          - EMAIL
          - IN_APP
          - PUSH
          - SMS
        description: |-
          * `IN_APP` - درون‌برنامه‌ای
          * `SMS` - پیامک
          * `EMAIL` - ایمیل
          * `PUSH` - اعلان موبایل
      - name: cursor
        required: false
        in: query
        description: مقدار نشانگر صفحه‌بندی.
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: priority
        schema:
          type: string
          title: اولویت
          enum:
          - EMERGENCY
          - OPERATIONAL
          - PROMOTIONAL
          - TRANSACTIONAL
        description: |-
          * `TRANSACTIONAL` - تراکنشی
          * `OPERATIONAL` - عملیاتی
          * `PROMOTIONAL` - اطلاع‌رسانی عمومی
          * `EMERGENCY` - اضطراری
      - in: query
        name: recipient_person
        schema:
          type: string
          format: uuid
          title: شناسه
      - in: query
        name: related_type
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CANCELLED
          - DELIVERED
          - FAILED
          - QUEUED
          - READ
          - SENDING
          - SENT
        description: |-
          * `QUEUED` - در صف
          * `SENDING` - در حال ارسال
          * `SENT` - ارسال‌شده
          * `DELIVERED` - تحویل‌شده
          * `FAILED` - ناموفق
          * `CANCELLED` - لغوشده
          * `READ` - خوانده‌شده
      - in: query
        name: unread
        schema:
          type: boolean
        description: فقط خوانده‌نشده
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedNotificationList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/notifications/{id}/:
    put:
      operationId: workflow_notifications_update
      description: |-
        جایگزینی کامل اعلان.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `notification.update`
      summary: ویرایش کامل اعلان
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NotificationRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_notifications_retrieve
      description: |-
        دریافت جزئیات یک اعلان با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر اعلان خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `notification.read`
      summary: جزئیات اعلان
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعلان را شناسایی میکند.
        required: true
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_notifications_partial_update
      description: |-
        ویرایش جزئی اعلان.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `notification.update`
      summary: ویرایش جزئی اعلان
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedNotificationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedNotificationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedNotificationRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_notifications_destroy
      description: |-
        حذف نرم اعلان.

        اعلان از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `notification.delete`
      summary: حذف اعلان
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعلان را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/notifications/{id}/mark-read/:
    post:
      operationId: workflow_notifications_mark_read_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: علامت‌گذاری به‌عنوان خوانده‌شده
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این اعلان را شناسایی میکند.
        required: true
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
  /api/v1/workflow/notification-preferences/:
    post:
      operationId: workflow_notification_preferences_create
      description: |-
        ایجاد ترجیح اعلان تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `notification.create`
      summary: ایجاد ترجیح اعلان
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationPreferenceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NotificationPreferenceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NotificationPreferenceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreference'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_notification_preferences_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده ترجیحات اعلان.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `person`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `notification.read`
      summary: ترجیحات اعلان
      parameters:
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: person
        schema:
          type: string
          format: uuid
          title: شناسه
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedNotificationPreferenceList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/notification-preferences/{id}/:
    put:
      operationId: workflow_notification_preferences_update
      description: |-
        جایگزینی کامل ترجیح اعلان.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `notification.update`
      summary: ویرایش کامل ترجیح اعلان
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترجیح اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationPreferenceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NotificationPreferenceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/NotificationPreferenceRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreference'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_notification_preferences_retrieve
      description: |-
        دریافت جزئیات یک ترجیح اعلان با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر ترجیح اعلان خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `notification.read`
      summary: جزئیات ترجیح اعلان
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترجیح اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreference'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_notification_preferences_partial_update
      description: |-
        ویرایش جزئی ترجیح اعلان.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `notification.update`
      summary: ویرایش جزئی ترجیح اعلان
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترجیح اعلان را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedNotificationPreferenceRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedNotificationPreferenceRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedNotificationPreferenceRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationPreference'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_notification_preferences_destroy
      description: |-
        حذف نرم ترجیح اعلان.

        ترجیح اعلان از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `notification.delete`
      summary: حذف ترجیح اعلان
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این ترجیح اعلان را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/attachments/:
    post:
      operationId: workflow_attachments_create
      description: فایل تا تکمیل اسکن بدافزار (`scanStatus = CLEAN`) در دسترس کاربر
        نهایی قرار نمی‌گیرد (بخش ۷.۱۱).
      summary: بارگذاری پیوست
      tags:
      - Workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AttachmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_attachments_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پیوست‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `owner_type`، `owner_id`، `classification`، `scan_status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `attachment.read`
      summary: پیوست‌ها
      parameters:
      - in: query
        name: classification
        schema:
          type: string
          title: طبقه‌بندی
          enum:
          - CONFIDENTIAL
          - INTERNAL
          - PUBLIC
          - RESTRICTED
        description: |-
          * `PUBLIC` - عمومی
          * `INTERNAL` - داخلی
          * `CONFIDENTIAL` - محرمانه
          * `RESTRICTED` - بسیار حساس
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - in: query
        name: owner_id
        schema:
          type: string
          format: uuid
      - in: query
        name: owner_type
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: scan_status
        schema:
          type: string
          title: وضعیت پویش بدافزار
          enum:
          - CLEAN
          - FAILED
          - INFECTED
          - PENDING
        description: |-
          * `PENDING` - در انتظار اسکن
          * `CLEAN` - سالم
          * `INFECTED` - آلوده
          * `FAILED` - اسکن ناموفق
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAttachmentList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/attachments/{id}/:
    put:
      operationId: workflow_attachments_update
      description: |-
        جایگزینی کامل پیوست.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `attachment.update`
      summary: ویرایش کامل پیوست
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیوست را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AttachmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_attachments_retrieve
      description: |-
        دریافت جزئیات یک پیوست با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پیوست خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `attachment.read`
      summary: جزئیات پیوست
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیوست را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_attachments_partial_update
      description: |-
        ویرایش جزئی پیوست.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `attachment.update`
      summary: ویرایش جزئی پیوست
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیوست را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAttachmentRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAttachmentRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAttachmentRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_attachments_destroy
      description: |-
        حذف نرم پیوست.

        پیوست از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `attachment.delete`
      summary: حذف پیوست
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیوست را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/outbox-events/:
    get:
      operationId: workflow_outbox_events_list
      description: برای پایش عملیاتی انتشار رویدادها و Dead Letter (بخش ۱۳.۲).
      summary: رویدادهای Outbox
      parameters:
      - in: query
        name: aggregate_id
        schema:
          type: string
          format: uuid
      - in: query
        name: aggregate_type
        schema:
          type: string
      - name: cursor
        required: false
        in: query
        description: مقدار نشانگر صفحه‌بندی.
        schema:
          type: string
      - in: query
        name: event_type
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedOutboxEventList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/outbox-events/{id}/:
    get:
      operationId: workflow_outbox_events_retrieve
      description: |-
        دریافت جزئیات یک رویداد Outbox با شناسه UUID.

        اگر رویداد Outbox خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `report.read`
      summary: جزئیات رویداد Outbox
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این رویداد Outbox را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OutboxEvent'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow/integration-messages/:
    get:
      operationId: workflow_integration_messages_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پیام‌های یکپارچه‌سازی.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `integration_code`، `direction`، `status`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `report.read`
      summary: پیام‌های یکپارچه‌سازی
      parameters:
      - in: query
        name: direction
        schema:
          type: string
          title: جهت
          enum:
          - INBOUND
          - OUTBOUND
        description: |-
          * `INBOUND` - ورودی
          * `OUTBOUND` - خروجی
      - in: query
        name: integration_code
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - DEAD_LETTER
          - FAILED
          - PROCESSED
          - RECEIVED
        description: |-
          * `RECEIVED` - دریافت‌شده
          * `PROCESSED` - پردازش‌شده
          * `FAILED` - ناموفق
          * `DEAD_LETTER` - Dead Letter
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedIntegrationMessageList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/integration-messages/{id}/:
    get:
      operationId: workflow_integration_messages_retrieve
      description: |-
        دریافت جزئیات یک پیام یکپارچه‌سازی با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پیام یکپارچه‌سازی خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `report.read`
      summary: جزئیات پیام یکپارچه‌سازی
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیام یکپارچه‌سازی را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationMessage'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/workflow/tickets/:
    post:
      operationId: workflow_tickets_create
      description: |-
        ایجاد تیکت تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `approval.create`
      summary: ثبت تیکت جدید
      tags:
      - Workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TicketRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TicketRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TicketRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ticket'
          description: رکورد با موفقیت ایجاد شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_tickets_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده تیکت‌ها.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `category`، `status`، `priority`، `assignee_user_id`

        **جست‌وجو** — پارامتر `search` روی `ticket_no`، `subject`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `approval.read`
      summary: فهرست تیکت‌ها
      parameters:
      - in: query
        name: assignee_user_id
        schema:
          type: string
          format: uuid
      - in: query
        name: category
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - in: query
        name: priority
        schema:
          type: string
          title: اولویت
          enum:
          - HIGH
          - LOW
          - NORMAL
          - URGENT
        description: |-
          * `LOW` - کم
          * `NORMAL` - عادی
          * `HIGH` - زیاد
          * `URGENT` - فوری
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: status
        schema:
          type: string
          title: وضعیت
          enum:
          - CLOSED
          - IN_PROGRESS
          - OPEN
          - RESOLVED
          - WAITING_REQUESTER
        description: |-
          * `OPEN` - باز
          * `IN_PROGRESS` - در حال بررسی
          * `WAITING_REQUESTER` - در انتظار پاسخ درخواست‌کننده
          * `RESOLVED` - حل‌شده
          * `CLOSED` - بسته
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTicketList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/tickets/{id}/:
    put:
      operationId: workflow_tickets_update
      description: |-
        جایگزینی کامل تیکت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `approval.update`
      summary: ویرایش کامل تیکت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تیکت را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TicketRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TicketRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TicketRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ticket'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_tickets_retrieve
      description: |-
        دریافت جزئیات یک تیکت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر تیکت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `approval.read`
      summary: جزئیات تیکت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تیکت را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ticket'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_tickets_partial_update
      description: |-
        ویرایش جزئی تیکت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **تغییر وضعیت** — فیلد `status` از این مسیر تغییر نمی‌کند؛ برای هر گذار، Endpoint اختصاصی خودش وجود دارد (بخش ۱۰ سند تحلیل).

        **مجوز لازم:** `approval.update`
      summary: ویرایش جزئی تیکت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تیکت را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTicketRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTicketRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTicketRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ticket'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_tickets_destroy
      description: |-
        حذف نرم تیکت.

        تیکت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `approval.delete`
      summary: حذف تیکت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تیکت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/tickets/{id}/resolve/:
    post:
      operationId: workflow_tickets_resolve_create
      description: ViewSet استاندارد منابع عملیاتی سامانه.
      summary: بستن تیکت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این تیکت را شناسایی میکند.
        required: true
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ticket'
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: داده ورودی معتبر نیست
        '403':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: بدون مجوز
        '404':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: یافت نشد
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: تعارض وضعیت
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: نقض قاعده کسب‌وکار
        '401':
          $ref: '#/components/responses/Unauthenticated'
  /api/v1/workflow/ticket-messages/:
    post:
      operationId: workflow_ticket_messages_create
      description: |-
        ایجاد پیام تیکت تازه.

        - فیلدهای `id`، `created_at`، `updated_at` و `version` فقط‌خواندنی‌اند و در بدنه پذیرفته نمی‌شوند.
        - سازمان (`tenant`) از Context کاربر جاری گرفته می‌شود؛ فرستادن آن در بدنه اثری ندارد.
        - پاسخ موفق `201` است و رکورد ساخته‌شده را کامل برمی‌گرداند.

        **مجوز لازم:** `approval.create`
      summary: ایجاد پیام تیکت
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TicketMessageRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TicketMessageRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TicketMessageRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketMessage'
          description: رکورد با موفقیت ایجاد شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_ticket_messages_list
      description: |-
        دریافت فهرست صفحه‌بندی‌شده پیام‌های تیکت.

        **صفحه‌بندی** — پارامترهای `page` و `page_size` (پیش‌فرض ۲۵، حداکثر ۲۰۰). پاسخ در پاکت `count`، `pageCount`، `page`، `pageSize`، `next`، `previous` و `results` برمی‌گردد.

        **فیلترها** — `ticket`، `is_internal`

        **مرتب‌سازی** — پارامتر `ordering`؛ برای ترتیب نزولی نام فیلد را با `-` شروع کنید.

        **دامنه دسترسی** — خروجی همیشه به سازمان و Context کاری کاربر جاری محدود است. این فیلتر در لایه Queryset اعمال می‌شود و با پارامتر Query قابل دور زدن نیست.

        **مجوز لازم:** `approval.read`
      summary: پیام‌های تیکت
      parameters:
      - in: query
        name: is_internal
        schema:
          type: boolean
      - name: ordering
        required: false
        in: query
        description: کدام فیلد باید هنگام مرتب‌سازی نتایج استفاده شود.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: یک شماره صفحه‌ در مجموعه نتایج صفحه‌بندی شده.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: تعداد نتایج برای نمایش در هر صفحه.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: یک عبارت جستجو.
        schema:
          type: string
      - in: query
        name: ticket
        schema:
          type: string
          format: uuid
          title: شناسه
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedTicketMessageList'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/workflow/ticket-messages/{id}/:
    put:
      operationId: workflow_ticket_messages_update
      description: |-
        جایگزینی کامل پیام تیکت.

        همه فیلدهای الزامی باید ارسال شوند؛ هر فیلد اختیاری که نیاید به مقدار پیش‌فرض بازمی‌گردد. برای تغییر چند فیلد از `PATCH` استفاده کنید.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `approval.update`
      summary: ویرایش کامل پیام تیکت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیام تیکت را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TicketMessageRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TicketMessageRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TicketMessageRequest'
        required: true
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketMessage'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    get:
      operationId: workflow_ticket_messages_retrieve
      description: |-
        دریافت جزئیات یک پیام تیکت با شناسه UUID.

        پاسخ شامل فیلد `version` است و همان مقدار در هدر `ETag` هم برمی‌گردد؛ برای ویرایش امن، آن را در هدر `If-Match` بازگردانید.

        اگر پیام تیکت خارج از دامنه دسترسی کاربر باشد پاسخ `404` است، نه `403` — تا وجود یا نبود رکورد افشا نشود.

        **مجوز لازم:** `approval.read`
      summary: جزئیات پیام تیکت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیام تیکت را شناسایی میکند.
        required: true
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketMessage'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: workflow_ticket_messages_partial_update
      description: |-
        ویرایش جزئی پیام تیکت.

        فقط فیلدهایی که در بدنه می‌آیند تغییر می‌کنند؛ بقیه دست‌نخورده می‌مانند.

        **کنترل هم‌زمانی** — مقدار `version` رکورد را در هدر `If-Match` بفرستید. اگر رکورد در این فاصله تغییر کرده باشد پاسخ `409` با کد `VERSION_CONFLICT` است و باید نسخه تازه را بخوانید و تغییر را دوباره اعمال کنید.

        **مجوز لازم:** `approval.update`
      summary: ویرایش جزئی پیام تیکت
      parameters:
      - in: header
        name: If-Match
        schema:
          type: string
        description: نسخه رکوردی که ویرایش می‌کنید، به شکل `"3"`. مقدار آن همان `version`
          پاسخ `GET` (و هدر `ETag`) است. اگر رکورد در این فاصله تغییر کرده باشد، پاسخ
          `409` با کد `VERSION_CONFLICT` است. ارسال‌نکردن این هدر یعنی صرف‌نظر از
          کنترل هم‌زمانی.
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیام تیکت را شناسایی میکند.
        required: true
      tags:
      - workflow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTicketMessageRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTicketMessageRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTicketMessageRequest'
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketMessage'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
        '422':
          $ref: '#/components/responses/BusinessRule'
    delete:
      operationId: workflow_ticket_messages_destroy
      description: |-
        حذف نرم پیام تیکت.

        پیام تیکت از فهرست‌ها و جست‌وجوها خارج می‌شود اما با `deleted_at` در پایگاه داده می‌ماند و در ممیزی قابل ردیابی است.

        - علت حذف را در پارامتر Query `reason` بفرستید.
        - پاسخ موفق `204` و بدون بدنه است.

        **مجوز لازم:** `approval.delete`
      summary: حذف پیام تیکت
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
          title: شناسه
        description: یک رشته UUID که این پیام تیکت را شناسایی میکند.
        required: true
      - in: query
        name: reason
        schema:
          type: string
        description: علت حذف؛ روی رکورد و در ممیزی ثبت می‌شود.
      tags:
      - workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/VersionConflict'
  /api/v1/workflow/my-tasks/:
    get:
      operationId: workflow_my_tasks_list
      description: 'همه کارهای در انتظار کاربر جاری در یک فهرست: تأییدهای منتظر تصمیم،
        تیکت‌های ارجاع‌شده و جلسات با حضور ثبت‌نشده (بخش ۶.۲ سند فرانت).'
      summary: کارهای من (Inbox یکپارچه)
      tags:
      - Workflow
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MyTask'
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/health/:
    get:
      operationId: health_retrieve
      description: برای Health Check زیرساخت و Load Balancer. نیازی به احراز هویت
        ندارد.
      summary: بررسی سلامت سرویس
      tags:
      - Reports
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  database:
                    type: string
                    example: ok
                  version:
                    type: string
                    example: 1.0.0
          description: عملیات با موفقیت انجام شد.
          headers:
            ETag:
              description: نسخه فعلی رکورد. همین مقدار را در هدر `If-Match` درخواست
                ویرایش بعدی بفرستید تا تغییرِ هم‌زمان کاربر دیگر بازنویسی نشود.
              schema:
                type: string
                example: '"3"'
        '400':
          $ref: '#/components/responses/ValidationError'
  /api/v1/meta/enums/:
    get:
      operationId: meta_enums_retrieve
      description: |-
        همه فهرست‌های مقادیر مجاز (وضعیت‌ها، انواع، دسته‌ها) را یک‌جا برمی‌گرداند تا فرانت‌اند بتواند Dropdownها و برچسب‌های فارسی را بدون Hardcode بسازد.

        هر آیتم شامل `value` (مقداری که به API ارسال می‌شود) و `label` (متن فارسی قابل نمایش) است.
      summary: کاتالوگ شمارش‌ها و وضعیت‌ها
      tags:
      - Reports
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: array
                  items:
                    type: object
                    properties:
                      value:
                        type: string
                      label:
                        type: string
              examples:
                نمونهپاسخ:
                  value:
                    core.Gender:
                    - value: MALE
                      label: مرد
                    - value: FEMALE
                      label: زن
                    students.EnrollmentStatus:
                    - value: PENDING_DOCUMENTS
                      label: در انتظار مدارک
                    - value: ACTIVE
                      label: فعال
                  summary: نمونه پاسخ
          description: عملیات با موفقیت انجام شد.
        '401':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
          description: احراز هویت نشده است.
        '400':
          $ref: '#/components/responses/ValidationError'
        '403':
          $ref: '#/components/responses/PermissionDenied'
  /api/v1/meta/modules/:
    get:
      operationId: meta_modules_retrieve
      description: 'نقشه سریع منابع سامانه برای توسعه‌دهنده فرانت: نام ماژول، تعداد
        موجودیت‌ها و پیشوند مسیر REST هر ماژول.'
      summary: فهرست ماژول‌ها و منابع API
      tags:
      - Reports
      security:
      - jwtAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    module:
                      type: string
                      title: شناسه ماژول
                    title:
                      type: string
                      title: عنوان فارسی ماژول
                    basePath:
                      type: string
                      title: پیشوند مسیر REST
                    entityCount:
                      type: integer
                      title: تعداد موجودیت‌ها
                    entities:
                      type: array
                      items:
                        type: string
                      title: نام مدل‌ها
              examples:
                نمونهپاسخ:
                  value:
                  - module: gradebook
                    title: دفتر نمره و کارنامه
                    basePath: /api/v1/gradebook/
                    entityCount: 7
                    entities:
                    - AssessmentCategory
                    - GradeItem
                    - StudentScore
                  summary: نمونه پاسخ
          description: عملیات با موفقیت انجام شد.
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '403':
          $ref: '#/components/responses/PermissionDenied'
components:
  schemas:
    AbsenceJustification:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        attendance:
          type: string
          format: uuid
          title: حضور و غیاب
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        session_date:
          type: string
          format: date-time
          readOnly: true
          title: تاریخ جلسه
        submitted_by_guardian_id:
          type: string
          format: uuid
          nullable: true
          title: ولی ارسال‌کننده
        reason:
          type: string
          title: دلیل غیبت
        evidence_file:
          type: string
          format: uri
          nullable: true
          title: فایل مستند
        decision:
          allOf:
          - $ref: '#/components/schemas/AbsenceJustificationDecisionEnum'
          readOnly: true
          title: تصمیم
        decided_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تصمیم
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - attendance
      - created_at
      - decided_at
      - decision
      - id
      - reason
      - session_date
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attendance: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        session_date: '2026-09-23T08:30:00+03:30'
        submitted_by_guardian_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        evidence_file: https://example.school/media/sample.pdf
        decision: PENDING
        decided_at: '2026-09-23T08:30:00+03:30'
        decision_note: توضیح نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AbsenceJustificationDecisionEnum:
      enum:
      - PENDING
      - APPROVED
      - REJECTED
      type: string
      description: |-
        * `PENDING` - در انتظار بررسی
        * `APPROVED` - پذیرفته‌شده
        * `REJECTED` - ردشده
    AbsenceJustificationRequest:
      type: object
      properties:
        attendance:
          type: string
          format: uuid
          title: حضور و غیاب
        submitted_by_guardian_id:
          type: string
          format: uuid
          nullable: true
          title: ولی ارسال‌کننده
        reason:
          type: string
          minLength: 1
          title: دلیل غیبت
        evidence_file:
          type: string
          format: binary
          nullable: true
          title: فایل مستند
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 300
      required:
      - attendance
      - reason
      example:
        attendance: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        submitted_by_guardian_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        evidence_file: sample.pdf
        decision_note: توضیح نمونه
    AcademicYear:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        school_name:
          type: string
          readOnly: true
          title: نام مدرسه
        title:
          type: string
          title: عنوان
          description: مثلاً ۱۴۰۵–۱۴۰۶
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          title: تاریخ پایان
        is_default:
          type: boolean
          readOnly: true
          title: سال پیش‌فرض عملیات روزانه
        status:
          allOf:
          - $ref: '#/components/schemas/AcademicYearStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        is_editable:
          type: boolean
          readOnly: true
          title: قابل ویرایش است
        closed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بستن
        close_note:
          type: string
          title: توضیح بستن
          maxLength: 400
        terms:
          type: array
          items:
            $ref: '#/components/schemas/Term'
          readOnly: true
          title: ترم‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - closed_at
      - created_at
      - ends_on
      - id
      - is_default
      - is_editable
      - school
      - school_name
      - starts_on
      - status
      - status_display
      - terms
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school_name: دبیرستان نمونه
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        is_default: true
        status: PLANNING
        status_display: در حال برنامه‌ریزی
        is_editable: true
        closed_at: '2027-03-20T12:00:00+03:30'
        close_note: توضیح نمونه
        terms:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          sequence_no: 3
          status: PLANNED
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AcademicYearRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          description: مثلاً ۱۴۰۵–۱۴۰۶
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          title: تاریخ پایان
        close_note:
          type: string
          title: توضیح بستن
          maxLength: 400
      required:
      - ends_on
      - school
      - starts_on
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        close_note: توضیح نمونه
    AcademicYearStatusEnum:
      enum:
      - PLANNING
      - ACTIVE
      - CLOSING
      - CLOSED
      - ARCHIVED
      type: string
      description: |-
        * `PLANNING` - در حال برنامه‌ریزی
        * `ACTIVE` - فعال
        * `CLOSING` - در حال بستن
        * `CLOSED` - بسته
        * `ARCHIVED` - بایگانی
    AccessReview:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        title:
          type: string
          title: عنوان دوره بازبینی
          maxLength: 200
        opened_at:
          type: string
          format: date-time
          title: زمان بازکردن
        closed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بستن
        status:
          allOf:
          - $ref: '#/components/schemas/AccessReviewStatusEnum'
          title: وضعیت
        scope_note:
          type: string
          title: توضیح دامنه
          maxLength: 300
        item_count:
          type: integer
          readOnly: true
          title: تعداد اقلام
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - closed_at
      - created_at
      - id
      - item_count
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        opened_at: '2026-09-23T08:30:00+03:30'
        closed_at: '2027-03-20T12:00:00+03:30'
        status: OPEN
        scope_note: توضیح نمونه
        item_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AccessReviewItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        review:
          type: string
          format: uuid
          title: بازبینی
        assignment:
          type: string
          format: uuid
          title: تکلیف
        assignment_detail:
          allOf:
          - $ref: '#/components/schemas/UserRoleAssignment'
          readOnly: true
          title: جزئیات تکلیف
        decision:
          allOf:
          - $ref: '#/components/schemas/AccessReviewItemDecisionEnum'
          title: تصمیم
        reviewed_by_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: بازبین
        reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بازبینی
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - assignment
      - assignment_detail
      - id
      - review
      - reviewed_at
      - reviewed_by_id
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        review: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignment_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          username: s.mohammadi
          role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          role_code: SAMPLE-001
          role_title: عنوان نمونه
          scope_type: TENANT
          scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: ACTIVE
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          is_currently_effective: true
          grant_reason: توضیح نمونه
          revoked_at: '2026-09-23T08:30:00+03:30'
          revoke_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        decision: PENDING
        reviewed_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reviewed_at: '2026-09-23T08:30:00+03:30'
        note: یادداشت داخلی
    AccessReviewItemDecisionEnum:
      enum:
      - PENDING
      - KEEP
      - REVOKE
      - MODIFY
      type: string
      description: |-
        * `PENDING` - در انتظار
        * `KEEP` - ابقا
        * `REVOKE` - لغو دسترسی
        * `MODIFY` - اصلاح دامنه
    AccessReviewItemRequest:
      type: object
      properties:
        review:
          type: string
          format: uuid
          title: بازبینی
        assignment:
          type: string
          format: uuid
          title: تکلیف
        decision:
          allOf:
          - $ref: '#/components/schemas/AccessReviewItemDecisionEnum'
          title: تصمیم
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - assignment
      - review
      example:
        review: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision: PENDING
        note: یادداشت داخلی
    AccessReviewRequest:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          title: عنوان دوره بازبینی
          maxLength: 200
        opened_at:
          type: string
          format: date-time
          title: زمان بازکردن
        status:
          allOf:
          - $ref: '#/components/schemas/AccessReviewStatusEnum'
          title: وضعیت
        scope_note:
          type: string
          title: توضیح دامنه
          maxLength: 300
      required:
      - title
      example:
        title: عنوان نمونه
        opened_at: '2026-09-23T08:30:00+03:30'
        status: OPEN
        scope_note: توضیح نمونه
    AccessReviewStatusEnum:
      enum:
      - OPEN
      - IN_PROGRESS
      - CLOSED
      type: string
      description: |-
        * `OPEN` - باز
        * `IN_PROGRESS` - در حال بررسی
        * `CLOSED` - بسته
    Account:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        parent_code:
          type: string
          readOnly: true
          title: کد والد
        code:
          type: string
          title: کد حساب
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 200
        account_type:
          allOf:
          - $ref: '#/components/schemas/AccountTypeEnum'
          title: نوع حساب
        account_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع حساب
        allows_posting:
          type: boolean
          title: قابل ثبت سند مستقیم
        is_active:
          type: boolean
          title: فعال
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - account_type
      - account_type_display
      - code
      - created_at
      - id
      - parent_code
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent_code: SAMPLE-001
        code: SAMPLE-001
        title: عنوان نمونه
        account_type: ASSET
        account_type_display: دارایی
        allows_posting: true
        is_active: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AccountLedger:
      type: object
      description: گردش حساب (بخش ۱۲.۲).
      properties:
        accountCode:
          type: string
          title: کد حساب
        accountTitle:
          type: string
          title: عنوان حساب
        openingBalance:
          type: integer
          title: مانده ابتدای دوره
        closingBalance:
          type: integer
          title: مانده پایان دوره
        rows:
          type: array
          items:
            $ref: '#/components/schemas/LedgerRow'
          title: سطرها
      required:
      - accountCode
      - accountTitle
      - closingBalance
      - openingBalance
      - rows
      example:
        accountCode: '1101'
        accountTitle: عنوان نمونه
        openingBalance: 3
        closingBalance: 3
        rows:
        - entryNo: JE-1405-000731
          entryDate: '2026-09-23'
          description: توضیح تکمیلی این رکورد
          costCenter: متن نمونه
          debit: 12500000
          credit: 0
          balance: 32500000
    AccountRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          minLength: 1
          title: کد حساب
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        account_type:
          allOf:
          - $ref: '#/components/schemas/AccountTypeEnum'
          title: نوع حساب
        allows_posting:
          type: boolean
          title: قابل ثبت سند مستقیم
        is_active:
          type: boolean
          title: فعال
      required:
      - account_type
      - code
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        account_type: ASSET
        allows_posting: true
        is_active: true
    AccountTypeEnum:
      enum:
      - ASSET
      - LIABILITY
      - EQUITY
      - REVENUE
      - EXPENSE
      type: string
      description: |-
        * `ASSET` - دارایی
        * `LIABILITY` - بدهی
        * `EQUITY` - حقوق صاحبان سرمایه
        * `REVENUE` - درآمد
        * `EXPENSE` - هزینه
    ActionEnum:
      enum:
      - CREATE
      - UPDATE
      - DELETE
      - READ_SENSITIVE
      - LOGIN
      - LOGIN_FAILED
      - LOGOUT
      - PERMISSION_CHANGE
      - STATE_TRANSITION
      - EXPORT
      - BREAK_GLASS
      type: string
      description: |-
        * `CREATE` - ایجاد
        * `UPDATE` - ویرایش
        * `DELETE` - حذف
        * `READ_SENSITIVE` - مشاهده داده حساس
        * `LOGIN` - ورود
        * `LOGIN_FAILED` - ورود ناموفق
        * `LOGOUT` - خروج
        * `PERMISSION_CHANGE` - تغییر مجوز
        * `STATE_TRANSITION` - تغییر وضعیت
        * `EXPORT` - خروجی‌گیری
        * `BREAK_GLASS` - دسترسی اضطراری
    ActionTypeEnum:
      enum:
      - VERBAL_WARNING
      - WRITTEN_WARNING
      - GUARDIAN_MEETING
      - COUNSELING_REFERRAL
      - DETENTION
      - SUSPENSION
      - COMMENDATION
      - RESTITUTION
      type: string
      description: |-
        * `VERBAL_WARNING` - تذکر شفاهی
        * `WRITTEN_WARNING` - اخطار کتبی
        * `GUARDIAN_MEETING` - جلسه با ولی
        * `COUNSELING_REFERRAL` - ارجاع به مشاور
        * `DETENTION` - ماندن پس از ساعت درسی
        * `SUSPENSION` - تعلیق موقت
        * `COMMENDATION` - تشویق و امتیاز
        * `RESTITUTION` - جبران خسارت
    Address:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        country:
          type: string
          title: کشور
          maxLength: 60
        province:
          type: string
          title: استان
          maxLength: 80
        city:
          type: string
          title: شهر
          maxLength: 80
        district:
          type: string
          title: منطقه
          maxLength: 80
        postal_code:
          type: string
          title: کد پستی
          maxLength: 20
        line:
          type: string
          title: نشانی
          maxLength: 400
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: عرض جغرافیایی
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: طول جغرافیایی
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - id
      - line
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        country: IR
        province: تهران
        city: تهران
        district: منطقه ۳
        postal_code: '1234567890'
        line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        latitude: '35.6892'
        longitude: '51.389'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AddressRequest:
      type: object
      properties:
        country:
          type: string
          minLength: 1
          title: کشور
          maxLength: 60
        province:
          type: string
          title: استان
          maxLength: 80
        city:
          type: string
          title: شهر
          maxLength: 80
        district:
          type: string
          title: منطقه
          maxLength: 80
        postal_code:
          type: string
          title: کد پستی
          maxLength: 20
        line:
          type: string
          minLength: 1
          title: نشانی
          maxLength: 400
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: عرض جغرافیایی
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: طول جغرافیایی
      required:
      - line
      example:
        country: IR
        province: تهران
        city: تهران
        district: منطقه ۳
        postal_code: '1234567890'
        line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        latitude: '35.6892'
        longitude: '51.389'
    AddressTypeEnum:
      enum:
      - HOME
      - WORK
      - BILLING
      - OTHER
      type: string
      description: |-
        * `HOME` - محل سکونت
        * `WORK` - محل کار
        * `BILLING` - نشانی صورتحساب
        * `OTHER` - سایر
    AdmissionApplication:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        applicant_name:
          type: string
          readOnly: true
          title: نام متقاضی
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        preferred_campus:
          type: string
          format: uuid
          title: شعبه ترجیحی
        campus_name:
          type: string
          readOnly: true
          title: نام شعبه
        preferred_grade_level:
          type: string
          format: uuid
          title: پایه ترجیحی
        grade_level_title:
          type: string
          readOnly: true
          title: عنوان پایه تحصیلی
        preferred_program:
          type: string
          format: uuid
          title: رشته ترجیحی
          nullable: true
        application_no:
          type: string
          title: شماره درخواست
          maxLength: 30
        status:
          allOf:
          - $ref: '#/components/schemas/AdmissionStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        submitted_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ارسال
        reviewer_id:
          type: string
          format: uuid
          nullable: true
          title: بررسی‌کننده
        interview_at:
          type: string
          format: date-time
          nullable: true
          title: زمان مصاحبه
        entrance_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره آزمون ورودی
        interview_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره مصاحبه
        final_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: امتیاز نهایی
        waitlist_rank:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: رتبه در فهرست انتظار
        conditions:
          type: string
          title: شرایط پذیرش مشروط
        decision_reason:
          type: string
          title: علت تصمیم
          maxLength: 400
        decided_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تصمیم
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - academic_year
      - applicant_name
      - application_no
      - campus_name
      - created_at
      - decided_at
      - grade_level_title
      - id
      - person
      - preferred_campus
      - preferred_grade_level
      - status
      - status_display
      - submitted_at
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        applicant_name: سارا محمدی
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus_name: شعبه مرکزی
        preferred_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level_title: پایه هفتم
        preferred_program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        application_no: SAMPLE-001
        status: DRAFT
        status_display: پیش‌نویس
        submitted_at: '2026-09-23T08:30:00+03:30'
        reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        interview_at: '2026-09-23T08:30:00+03:30'
        entrance_score: '18.50'
        interview_score: '18.50'
        final_score: '18.50'
        waitlist_rank: 3
        conditions: متن نمونه
        decision_reason: توضیح نمونه
        decided_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AdmissionApplicationRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        preferred_campus:
          type: string
          format: uuid
          title: شعبه ترجیحی
        preferred_grade_level:
          type: string
          format: uuid
          title: پایه ترجیحی
        preferred_program:
          type: string
          format: uuid
          title: رشته ترجیحی
          nullable: true
        application_no:
          type: string
          minLength: 1
          title: شماره درخواست
          maxLength: 30
        reviewer_id:
          type: string
          format: uuid
          nullable: true
          title: بررسی‌کننده
        interview_at:
          type: string
          format: date-time
          nullable: true
          title: زمان مصاحبه
        entrance_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره آزمون ورودی
        interview_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره مصاحبه
        final_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: امتیاز نهایی
        waitlist_rank:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: رتبه در فهرست انتظار
        conditions:
          type: string
          title: شرایط پذیرش مشروط
        decision_reason:
          type: string
          title: علت تصمیم
          maxLength: 400
      required:
      - academic_year
      - application_no
      - person
      - preferred_campus
      - preferred_grade_level
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        application_no: SAMPLE-001
        reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        interview_at: '2026-09-23T08:30:00+03:30'
        entrance_score: '18.50'
        interview_score: '18.50'
        final_score: '18.50'
        waitlist_rank: 3
        conditions: متن نمونه
        decision_reason: توضیح نمونه
    AdmissionDecisionRequest:
      type: object
      properties:
        reason:
          type: string
          maxLength: 400
          title: علت
        conditions:
          type: string
          description: فقط برای پذیرش مشروط
          maxLength: 1000
        final_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره نهایی
      example:
        reason: اصلاح اطلاعات پس از بررسی پرونده
        conditions: متن نمونه
        final_score: '18.50'
    AdmissionStatusEnum:
      enum:
      - DRAFT
      - SUBMITTED
      - INCOMPLETE
      - UNDER_REVIEW
      - WAITLISTED
      - CONDITIONALLY_ACCEPTED
      - ACCEPTED
      - REJECTED
      - CONVERTED
      - WITHDRAWN
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `SUBMITTED` - ارسال‌شده
        * `INCOMPLETE` - نقص مدارک
        * `UNDER_REVIEW` - در حال بررسی
        * `WAITLISTED` - فهرست انتظار
        * `CONDITIONALLY_ACCEPTED` - پذیرش مشروط
        * `ACCEPTED` - پذیرفته‌شده
        * `REJECTED` - ردشده
        * `CONVERTED` - تبدیل به ثبت‌نام
        * `WITHDRAWN` - انصراف
    AgreementStatusEnum:
      enum:
      - DRAFT
      - ACTIVE
      - SETTLED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `ACTIVE` - جاری
        * `SETTLED` - تسویه‌شده
        * `CANCELLED` - لغوشده
    AlertSeverityEnum:
      enum:
      - LOW
      - MEDIUM
      - HIGH
      - LIFE_THREATENING
      type: string
      description: |-
        * `LOW` - کم
        * `MEDIUM` - متوسط
        * `HIGH` - زیاد
        * `LIFE_THREATENING` - تهدیدکننده حیات
    AlertTypeEnum:
      enum:
      - ALLERGY
      - CHRONIC_CONDITION
      - MEDICATION
      - DIETARY
      - MOBILITY
      - VISION_HEARING
      - OTHER
      type: string
      description: |-
        * `ALLERGY` - حساسیت
        * `CHRONIC_CONDITION` - بیماری مزمن
        * `MEDICATION` - داروی مصرفی
        * `DIETARY` - محدودیت غذایی
        * `MOBILITY` - محدودیت حرکتی
        * `VISION_HEARING` - بینایی/شنوایی
        * `OTHER` - سایر
    AllocatePaymentRequest:
      type: object
      description: |-
        تخصیص یک پرداخت به چند صورتحساب.

        مجموع تخصیص نباید از مبلغ تخصیص‌نیافته پرداخت یا مانده هر صورتحساب بیشتر
        شود (بخش ۷.۸).
      properties:
        allocations:
          type: array
          items:
            $ref: '#/components/schemas/AllocationRowRequest'
          title: تخصیص‌ها
      required:
      - allocations
      example:
        allocations:
        - invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          amount: 12500000
    AllocationRowRequest:
      type: object
      properties:
        invoice:
          type: string
          format: uuid
          title: صورتحساب
        amount:
          type: integer
          minimum: 1
          description: مبلغ به ریال
      required:
      - amount
      - invoice
      example:
        invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        amount: 12500000
    AmendAttendanceRequest:
      type: object
      description: اصلاح حضور پس از نهایی‌سازی — نیازمند علت (بخش ۷.۵).
      properties:
        attendance_status:
          type: string
          minLength: 1
          title: وضعیت حضور
        reason:
          type: string
          minLength: 1
          maxLength: 400
          title: علت
      required:
      - attendance_status
      - reason
      example:
        attendance_status: متن نمونه
        reason: اصلاح اطلاعات پس از بررسی پرونده
    AppealStatusEnum:
      enum:
      - SUBMITTED
      - UNDER_REVIEW
      - ACCEPTED
      - REJECTED
      - WITHDRAWN
      type: string
      description: |-
        * `SUBMITTED` - ثبت‌شده
        * `UNDER_REVIEW` - در حال بررسی
        * `ACCEPTED` - پذیرفته‌شده
        * `REJECTED` - ردشده
        * `WITHDRAWN` - پس‌گرفته‌شده
    ApprovalDecisionEnum:
      enum:
      - PENDING
      - APPROVED
      - REJECTED
      - RETURNED
      - DELEGATED
      type: string
      description: |-
        * `PENDING` - در انتظار
        * `APPROVED` - تأیید
        * `REJECTED` - رد
        * `RETURNED` - بازگشت برای اصلاح
        * `DELEGATED` - ارجاع به جانشین
    ApprovalRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        subject_type:
          type: string
          title: نوع موضوع
          maxLength: 80
        subject_id:
          type: string
          format: uuid
          title: شناسه موضوع
        subject_label:
          type: string
          title: عنوان موضوع
          maxLength: 250
        workflow_code:
          type: string
          title: کد گردش کار
          maxLength: 60
        workflow_version:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: نسخه گردش کار
        workflow_snapshot:
          readOnly: true
          title: Snapshot تعریف گردش در زمان شروع
        requested_by_id:
          type: string
          format: uuid
          nullable: true
          title: درخواست‌دهنده
        requested_at:
          type: string
          format: date-time
          title: زمان درخواست
        status:
          allOf:
          - $ref: '#/components/schemas/WorkflowApprovalStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        completed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تکمیل
        note:
          type: string
          title: یادداشت
          maxLength: 400
        current_step:
          type: object
          additionalProperties: {}
          nullable: true
          readOnly: true
          title: گام جاری
        steps:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalStep'
          readOnly: true
          title: گام‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - completed_at
      - created_at
      - current_step
      - id
      - status
      - status_display
      - steps
      - subject_id
      - subject_type
      - updated_at
      - version
      - workflow_code
      - workflow_snapshot
      example:
        subject_type: متن نمونه
        subject_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        subject_label: متن نمونه
        workflow_code: SAMPLE-001
        workflow_version: 3
        requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        requested_at: '2026-09-23T08:30:00+03:30'
        note: یادداشت داخلی
    ApprovalRequestRequest:
      type: object
      properties:
        subject_type:
          type: string
          minLength: 1
          title: نوع موضوع
          maxLength: 80
        subject_id:
          type: string
          format: uuid
          title: شناسه موضوع
        subject_label:
          type: string
          title: عنوان موضوع
          maxLength: 250
        workflow_code:
          type: string
          minLength: 1
          title: کد گردش کار
          maxLength: 60
        workflow_version:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: نسخه گردش کار
        requested_by_id:
          type: string
          format: uuid
          nullable: true
          title: درخواست‌دهنده
        requested_at:
          type: string
          format: date-time
          title: زمان درخواست
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - subject_id
      - subject_type
      - workflow_code
      example:
        subject_type: متن نمونه
        subject_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        subject_label: متن نمونه
        workflow_code: SAMPLE-001
        workflow_version: 3
        requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        requested_at: '2026-09-23T08:30:00+03:30'
        note: یادداشت داخلی
    ApprovalStep:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        request:
          type: string
          format: uuid
          title: درخواست
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره ترتیب
        approver_type:
          type: string
          title: نوع تأییدکننده
          description: ROLE یا USER
          maxLength: 20
        approver_role_code:
          type: string
          title: کد نقش تأییدکننده
          maxLength: 60
        approver_user_id:
          type: string
          format: uuid
          nullable: true
          title: کاربر تأییدکننده
        decision:
          allOf:
          - $ref: '#/components/schemas/ApprovalDecisionEnum'
          readOnly: true
          title: تصمیم
        decision_display:
          type: string
          readOnly: true
          title: برچسب فارسی تصمیم
        decided_by_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: تصمیم‌گیرنده واقعی
        decided_on_behalf_of_id:
          type: string
          format: uuid
          nullable: true
          title: به نیابت از
        decided_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تصمیم
        comment:
          type: string
          title: توضیح
        due_at:
          type: string
          format: date-time
          nullable: true
          title: مهلت
      required:
      - decided_at
      - decided_by_id
      - decision
      - decision_display
      - id
      - request
      - sequence_no
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sequence_no: 3
        approver_type: متن نمونه
        approver_role_code: SAMPLE-001
        approver_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision: PENDING
        decision_display: در انتظار
        decided_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decided_on_behalf_of_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decided_at: '2026-09-23T08:30:00+03:30'
        comment: توضیح کوتاه ثبت‌کننده
        due_at: '2027-03-20T12:00:00+03:30'
    ApprovalStepRequest:
      type: object
      properties:
        request:
          type: string
          format: uuid
          title: درخواست
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره ترتیب
        approver_type:
          type: string
          minLength: 1
          title: نوع تأییدکننده
          description: ROLE یا USER
          maxLength: 20
        approver_role_code:
          type: string
          title: کد نقش تأییدکننده
          maxLength: 60
        approver_user_id:
          type: string
          format: uuid
          nullable: true
          title: کاربر تأییدکننده
        decided_on_behalf_of_id:
          type: string
          format: uuid
          nullable: true
          title: به نیابت از
        comment:
          type: string
          title: توضیح
        due_at:
          type: string
          format: date-time
          nullable: true
          title: مهلت
      required:
      - request
      - sequence_no
      example:
        request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sequence_no: 3
        approver_type: متن نمونه
        approver_role_code: SAMPLE-001
        approver_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decided_on_behalf_of_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        comment: توضیح کوتاه ثبت‌کننده
        due_at: '2027-03-20T12:00:00+03:30'
    AssessmentCategory:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        title:
          type: string
          title: عنوان دسته
          maxLength: 150
        weight_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: وزن درصدی
        drop_policy:
          allOf:
          - $ref: '#/components/schemas/DropPolicyEnum'
          title: سیاست حذف
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        is_active:
          type: boolean
          title: فعال
        item_count:
          type: integer
          readOnly: true
          title: تعداد اقلام
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - course_offering
      - created_at
      - id
      - item_count
      - title
      - updated_at
      - version
      - weight_percent
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        weight_percent: '18.50'
        drop_policy: NONE
        display_order: 1
        is_active: true
        item_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AssessmentCategoryRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        title:
          type: string
          minLength: 1
          title: عنوان دسته
          maxLength: 150
        weight_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: وزن درصدی
        drop_policy:
          allOf:
          - $ref: '#/components/schemas/DropPolicyEnum'
          title: سیاست حذف
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        is_active:
          type: boolean
          title: فعال
      required:
      - course_offering
      - title
      - weight_percent
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        weight_percent: '18.50'
        drop_policy: NONE
        display_order: 1
        is_active: true
    AssessmentSchemeEnum:
      enum:
      - NUMERIC
      - DESCRIPTIVE
      - LETTER
      - COMPETENCY
      - MIXED
      type: string
      description: |-
        * `NUMERIC` - عددی
        * `DESCRIPTIVE` - توصیفی
        * `LETTER` - حرفی
        * `COMPETENCY` - شایستگی‌محور
        * `MIXED` - ترکیبی
    Asset:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        item:
          type: string
          format: uuid
          title: کالا
          nullable: true
        item_title:
          type: string
          readOnly: true
          title: عنوان قلم
        asset_tag:
          type: string
          title: پلاک اموال
          maxLength: 40
        title:
          type: string
          title: عنوان
          maxLength: 200
        serial_no:
          type: string
          title: شماره سریال
          maxLength: 80
        acquired_on:
          type: string
          format: date
          title: تاریخ تملک
        acquisition_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای تملک
        useful_life_months:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: عمر مفید (ماه)
        accumulated_depreciation:
          type: integer
          readOnly: true
          title: استهلاک انباشته
        book_value:
          type: integer
          readOnly: true
          title: ارزش دفتری (ریال)
        warranty_until:
          type: string
          format: date
          nullable: true
          title: گارانتی تا
        insurance_policy_no:
          type: string
          title: شماره بیمه‌نامه
          maxLength: 60
        condition_status:
          allOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          title: وضعیت فنی
        lifecycle_status:
          allOf:
          - $ref: '#/components/schemas/AssetLifecycleStatusEnum'
          readOnly: true
          title: وضعیت چرخه عمر
        lifecycle_status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت چرخه عمر
        current_room:
          type: string
          format: uuid
          title: اتاق فعلی
          nullable: true
        current_assignee:
          type: object
          additionalProperties: {}
          nullable: true
          readOnly: true
          title: تحویل‌گیرنده فعلی
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - accumulated_depreciation
      - acquired_on
      - asset_tag
      - book_value
      - created_at
      - current_assignee
      - id
      - item_title
      - lifecycle_status
      - lifecycle_status_display
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_title: عنوان نمونه
        asset_tag: AST-004512
        title: عنوان نمونه
        serial_no: SN-88213047
        acquired_on: '2024-05-11'
        acquisition_cost: 2500000
        useful_life_months: 3
        accumulated_depreciation: 3
        book_value: 3
        warranty_until: '2027-03-20'
        insurance_policy_no: SAMPLE-001
        condition_status: NEW
        lifecycle_status: REGISTERED
        lifecycle_status_display: ثبت‌نام‌شده
        current_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        current_assignee: {}
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AssetAssignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        asset:
          type: string
          format: uuid
          title: دارایی
        asset_tag:
          type: string
          readOnly: true
          title: پلاک اموال
        assignee_type:
          allOf:
          - $ref: '#/components/schemas/AssetAssignmentAssigneeTypeEnum'
          title: نوع تحویل‌گیرنده
        assignee_id:
          type: string
          format: uuid
          title: شناسه تحویل‌گیرنده
        location_room:
          type: string
          format: uuid
          title: اتاق محل استقرار
          nullable: true
        assigned_at:
          type: string
          format: date-time
          title: زمان تخصیص
        returned_at:
          type: string
          format: date-time
          nullable: true
          title: زمان بازگشت
        condition_on_assign:
          title: وضعیت هنگام تحویل
          oneOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          - $ref: '#/components/schemas/BlankEnum'
        condition_on_return:
          title: وضعیت هنگام بازگشت
          oneOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          - $ref: '#/components/schemas/BlankEnum'
        status:
          type: string
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - asset
      - asset_tag
      - assigned_at
      - assignee_id
      - assignee_type
      - created_at
      - id
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        asset_tag: AST-004512
        assignee_type: EMPLOYEE
        assignee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        location_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assigned_at: '2026-09-23T08:30:00+03:30'
        returned_at: '2026-09-23T08:30:00+03:30'
        condition_on_assign: NEW
        condition_on_return: NEW
        status: متن نمونه
        note: یادداشت داخلی
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AssetAssignmentAssigneeTypeEnum:
      enum:
      - EMPLOYEE
      - ROOM
      - ORG_UNIT
      - CLASS_GROUP
      type: string
      description: |-
        * `EMPLOYEE` - کارمند
        * `ROOM` - اتاق
        * `ORG_UNIT` - واحد سازمانی
        * `CLASS_GROUP` - کلاس
    AssetAssignmentRequest:
      type: object
      properties:
        asset:
          type: string
          format: uuid
          title: دارایی
        assignee_type:
          allOf:
          - $ref: '#/components/schemas/AssetAssignmentAssigneeTypeEnum'
          title: نوع تحویل‌گیرنده
        assignee_id:
          type: string
          format: uuid
          title: شناسه تحویل‌گیرنده
        location_room:
          type: string
          format: uuid
          title: اتاق محل استقرار
          nullable: true
        assigned_at:
          type: string
          format: date-time
          title: زمان تخصیص
        returned_at:
          type: string
          format: date-time
          nullable: true
          title: زمان بازگشت
        condition_on_assign:
          title: وضعیت هنگام تحویل
          oneOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          - $ref: '#/components/schemas/BlankEnum'
        condition_on_return:
          title: وضعیت هنگام بازگشت
          oneOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          - $ref: '#/components/schemas/BlankEnum'
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
      required:
      - asset
      - assigned_at
      - assignee_id
      - assignee_type
      example:
        asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignee_type: EMPLOYEE
        assignee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        location_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assigned_at: '2026-09-23T08:30:00+03:30'
        returned_at: '2026-09-23T08:30:00+03:30'
        condition_on_assign: NEW
        condition_on_return: NEW
        status: متن نمونه
        note: یادداشت داخلی
    AssetConditionEnum:
      enum:
      - NEW
      - GOOD
      - FAIR
      - POOR
      - BROKEN
      type: string
      description: |-
        * `NEW` - نو
        * `GOOD` - سالم
        * `FAIR` - قابل قبول
        * `POOR` - نامناسب
        * `BROKEN` - خراب
    AssetLifecycleStatusEnum:
      enum:
      - REGISTERED
      - IN_STOCK
      - ASSIGNED
      - UNDER_MAINTENANCE
      - DAMAGED
      - LOST
      - RECOVERED
      - RETIRED
      - DISPOSED
      type: string
      description: |-
        * `REGISTERED` - ثبت‌شده
        * `IN_STOCK` - موجود در انبار
        * `ASSIGNED` - تحویل‌شده
        * `UNDER_MAINTENANCE` - در تعمیر
        * `DAMAGED` - آسیب‌دیده
        * `LOST` - مفقود
        * `RECOVERED` - بازیافت‌شده
        * `RETIRED` - بازنشسته
        * `DISPOSED` - اسقاط‌شده
    AssetRequest:
      type: object
      properties:
        item:
          type: string
          format: uuid
          title: کالا
          nullable: true
        asset_tag:
          type: string
          minLength: 1
          title: پلاک اموال
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        serial_no:
          type: string
          title: شماره سریال
          maxLength: 80
        acquired_on:
          type: string
          format: date
          title: تاریخ تملک
        acquisition_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای تملک
        useful_life_months:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: عمر مفید (ماه)
        warranty_until:
          type: string
          format: date
          nullable: true
          title: گارانتی تا
        insurance_policy_no:
          type: string
          title: شماره بیمه‌نامه
          maxLength: 60
        condition_status:
          allOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          title: وضعیت فنی
        current_room:
          type: string
          format: uuid
          title: اتاق فعلی
          nullable: true
      required:
      - acquired_on
      - asset_tag
      - title
      example:
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        asset_tag: AST-004512
        title: عنوان نمونه
        serial_no: SN-88213047
        acquired_on: '2024-05-11'
        acquisition_cost: 2500000
        useful_life_months: 3
        warranty_until: '2027-03-20'
        insurance_policy_no: SAMPLE-001
        condition_status: NEW
        current_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    AssignAssetAssigneeTypeEnum:
      enum:
      - EMPLOYEE
      - ROOM
      - ORG_UNIT
      - CLASS_GROUP
      type: string
      description: |-
        * `EMPLOYEE` - EMPLOYEE
        * `ROOM` - ROOM
        * `ORG_UNIT` - ORG_UNIT
        * `CLASS_GROUP` - CLASS_GROUP
    AssignAssetRequest:
      type: object
      properties:
        assignee_type:
          $ref: '#/components/schemas/AssignAssetAssigneeTypeEnum'
          title: نوع تحویل‌گیرنده
        assignee_id:
          type: string
          format: uuid
          title: شناسه تحویل‌گیرنده
        location_room:
          type: string
          format: uuid
          nullable: true
          title: اتاق یا محل استقرار
        condition_on_assign:
          type: string
          default: ''
          title: وضعیت هنگام تحویل
        note:
          type: string
          default: ''
          title: یادداشت
      required:
      - assignee_id
      - assignee_type
      example:
        assignee_type: EMPLOYEE
        assignee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        location_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        condition_on_assign: ''
        note: ''
    Assignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        title:
          type: string
          title: عنوان تکلیف
          maxLength: 200
        description:
          type: string
          title: شرح
        opens_at:
          type: string
          format: date-time
          title: زمان بازشدن
        due_at:
          type: string
          format: date-time
          title: مهلت تحویل
        close_at:
          type: string
          format: date-time
          nullable: true
          title: پایان پذیرش با تأخیر
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم
        allow_late_submission:
          type: boolean
          title: پذیرش تحویل با تأخیر
        late_penalty_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد جریمه تأخیر
        max_attempts:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: حداکثر تلاش
        status:
          allOf:
          - $ref: '#/components/schemas/AssignmentStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار
        submission_stats:
          type: object
          additionalProperties: {}
          readOnly: true
          title: آمار تحویل تکلیف
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - class_group_code
      - course_offering
      - course_title
      - created_at
      - due_at
      - id
      - opens_at
      - published_at
      - status
      - status_display
      - submission_stats
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        class_group_code: G7-A
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        opens_at: '2026-09-23T08:30:00+03:30'
        due_at: '2027-03-20T12:00:00+03:30'
        close_at: '2026-09-23T08:30:00+03:30'
        max_score: '18.50'
        allow_late_submission: true
        late_penalty_percent: '18.50'
        max_attempts: 3
        status: DRAFT
        status_display: پیش‌نویس
        published_at: '2026-09-23T08:30:00+03:30'
        submission_stats: {}
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AssignmentRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        title:
          type: string
          minLength: 1
          title: عنوان تکلیف
          maxLength: 200
        description:
          type: string
          title: شرح
        opens_at:
          type: string
          format: date-time
          title: زمان بازشدن
        due_at:
          type: string
          format: date-time
          title: مهلت تحویل
        close_at:
          type: string
          format: date-time
          nullable: true
          title: پایان پذیرش با تأخیر
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم
        allow_late_submission:
          type: boolean
          title: پذیرش تحویل با تأخیر
        late_penalty_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد جریمه تأخیر
        max_attempts:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: حداکثر تلاش
      required:
      - course_offering
      - due_at
      - opens_at
      - title
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        opens_at: '2026-09-23T08:30:00+03:30'
        due_at: '2027-03-20T12:00:00+03:30'
        close_at: '2026-09-23T08:30:00+03:30'
        max_score: '18.50'
        allow_late_submission: true
        late_penalty_percent: '18.50'
        max_attempts: 3
    AssignmentStatusEnum:
      enum:
      - DRAFT
      - PUBLISHED
      - CLOSED
      - ARCHIVED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `PUBLISHED` - منتشرشده
        * `CLOSED` - بسته
        * `ARCHIVED` - بایگانی
    AssignmentSubmission:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        assignment:
          type: string
          format: uuid
          title: تکلیف
        assignment_title:
          type: string
          readOnly: true
          title: عنوان تکلیف
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        attempt_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          default: 1
          title: شماره تلاش
        submitted_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ارسال
        content:
          type: string
          title: محتوا
        attachment:
          type: string
          format: uri
          nullable: true
          title: پیوست
        status:
          allOf:
          - $ref: '#/components/schemas/SubmissionStatusEnum'
          readOnly: true
          title: وضعیت
        is_late:
          type: boolean
          readOnly: true
          title: با تأخیر
        late_minutes:
          type: integer
          readOnly: true
          title: دقیقه تأخیر
        policy_snapshot:
          readOnly: true
          title: Snapshot سیاست تأخیر در زمان تحویل
        feedbacks:
          type: array
          items:
            $ref: '#/components/schemas/SubmissionFeedback'
          readOnly: true
          title: بازخوردها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - assignment
      - assignment_title
      - created_at
      - enrollment
      - feedbacks
      - id
      - is_late
      - late_minutes
      - policy_snapshot
      - status
      - student_name
      - student_no
      - submitted_at
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignment_title: تمرین سری سوم
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        attempt_no: 1
        submitted_at: '2026-09-23T08:30:00+03:30'
        content: متن نمونه
        attachment: https://example.school/media/sample.pdf
        status: DRAFT
        is_late: true
        late_minutes: 45
        feedbacks:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          submission: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          score: '18.50'
          feedback: متن نمونه
          reviewed_at: '2026-09-23T08:30:00+03:30'
          is_final: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AssignmentSubmissionRequest:
      type: object
      properties:
        assignment:
          type: string
          format: uuid
          title: تکلیف
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        attempt_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          default: 1
          title: شماره تلاش
        content:
          type: string
          title: محتوا
        attachment:
          type: string
          format: binary
          nullable: true
          title: پیوست
      required:
      - assignment
      - enrollment
      example:
        assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt_no: 1
        content: متن نمونه
        attachment: sample.pdf
    Attachment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        owner_type:
          type: string
          title: نوع مالک
          maxLength: 80
        owner_id:
          type: string
          format: uuid
          title: شناسه مالک
        file:
          type: string
          format: uri
          title: فایل
        original_name:
          type: string
          title: نام اصلی فایل
          maxLength: 250
        media_type:
          type: string
          title: نوع محتوا
          maxLength: 100
        size_bytes:
          type: integer
          readOnly: true
          title: حجم (بایت)
        sha256:
          type: string
          readOnly: true
          title: چکیده SHA-256
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        scan_status:
          allOf:
          - $ref: '#/components/schemas/ScanStatusEnum'
          readOnly: true
          title: وضعیت پویش بدافزار
        scanned_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان پویش
        is_available:
          type: boolean
          readOnly: true
          title: در دسترس است
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - file
      - id
      - is_available
      - owner_id
      - owner_type
      - scan_status
      - scanned_at
      - sha256
      - size_bytes
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        owner_type: متن نمونه
        owner_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        file: https://example.school/media/sample.pdf
        original_name: sample.pdf
        media_type: application/pdf
        size_bytes: 248576
        sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
        classification: PUBLIC
        scan_status: PENDING
        scanned_at: '2026-09-23T08:30:00+03:30'
        is_available: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AttachmentRequest:
      type: object
      properties:
        owner_type:
          type: string
          minLength: 1
          title: نوع مالک
          maxLength: 80
        owner_id:
          type: string
          format: uuid
          title: شناسه مالک
        file:
          type: string
          format: binary
          title: فایل
        original_name:
          type: string
          title: نام اصلی فایل
          maxLength: 250
        media_type:
          type: string
          title: نوع محتوا
          maxLength: 100
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
      required:
      - file
      - owner_id
      - owner_type
      example:
        owner_type: متن نمونه
        owner_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        file: sample.pdf
        original_name: sample.pdf
        media_type: application/pdf
        classification: PUBLIC
    AttemptAnswer:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        attempt:
          type: string
          format: uuid
          title: تلاش
        exam_question:
          type: string
          format: uuid
          title: سؤال آزمون
        response_payload:
          title: پاسخ
          description: 'مثلاً {"selectedKeys": ["B"]} یا {"text": "..."}'
        attachment:
          type: string
          format: uri
          nullable: true
          title: پیوست
        saved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ذخیره
        save_revision:
          type: integer
          readOnly: true
          title: شماره بازنگری ذخیره
        awarded_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره اعطاشده
        grading_status:
          allOf:
          - $ref: '#/components/schemas/GradingStatusEnum'
          readOnly: true
          title: وضعیت تصحیح
        time_spent_seconds:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: زمان صرف‌شده (ثانیه)
      required:
      - attempt
      - awarded_score
      - exam_question
      - grading_status
      - id
      - save_revision
      - saved_at
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        exam_question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attachment: https://example.school/media/sample.pdf
        saved_at: '2026-09-23T08:30:00+03:30'
        save_revision: 3
        awarded_score: '18.50'
        grading_status: PENDING
        time_spent_seconds: 120
    AttemptAnswerRequest:
      type: object
      properties:
        attempt:
          type: string
          format: uuid
          title: تلاش
        exam_question:
          type: string
          format: uuid
          title: سؤال آزمون
        response_payload:
          title: پاسخ
          description: 'مثلاً {"selectedKeys": ["B"]} یا {"text": "..."}'
        attachment:
          type: string
          format: binary
          nullable: true
          title: پیوست
        time_spent_seconds:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: زمان صرف‌شده (ثانیه)
      required:
      - attempt
      - exam_question
      example:
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        exam_question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attachment: sample.pdf
        time_spent_seconds: 120
    AttemptPaper:
      type: object
      description: برگه آزمون در حال اجرا (پوسته ایزوله آزمون — بخش ۹.۶ سند فرانت).
      properties:
        attemptId:
          type: string
          format: uuid
          title: شناسه تلاش
        examTitle:
          type: string
          title: عنوان آزمون
        instructions:
          type: string
          title: دستورالعمل
        maxScore:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        allowBacktrack:
          type: boolean
          title: اجازه بازگشت به سؤال قبلی
        expiresAt:
          type: string
          format: date-time
          nullable: true
          title: زمان انقضا
        remainingSeconds:
          type: integer
          nullable: true
          title: ثانیه‌های باقی‌مانده
        questions:
          type: array
          items:
            $ref: '#/components/schemas/AttemptQuestion'
          title: سؤالات
      required:
      - allowBacktrack
      - attemptId
      - examTitle
      - expiresAt
      - instructions
      - maxScore
      - questions
      - remainingSeconds
      example:
        attemptId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        examTitle: آزمون میان‌ترم ریاضی
        instructions: متن نمونه
        maxScore: '18.50'
        allowBacktrack: true
        expiresAt: '2027-03-20T12:00:00+03:30'
        remainingSeconds: 120
        questions:
        - examQuestionId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          sectionTitle: عنوان نمونه
          questionType: متن نمونه
          body: متن نمونه
          score: '18.50'
          displayOrder: 1
          isRequired: true
          options: []
    AttemptQuestion:
      type: object
      description: سؤال در نمای دانش‌آموز — بدون کلید پاسخ.
      properties:
        examQuestionId:
          type: string
          format: uuid
          title: شناسه سؤال آزمون
        sectionTitle:
          type: string
          title: عنوان بخش
        questionType:
          type: string
          title: نوع سؤال
        body:
          type: string
          title: متن
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره
        displayOrder:
          type: integer
          title: ترتیب نمایش
        isRequired:
          type: boolean
          title: الزامی است
        options:
          type: array
          items:
            $ref: '#/components/schemas/QuestionOptionPublic'
          title: گزینه‌ها
        savedAnswer:
          nullable: true
          title: پاسخ ذخیره‌شده
      required:
      - body
      - displayOrder
      - examQuestionId
      - isRequired
      - options
      - questionType
      - savedAnswer
      - score
      - sectionTitle
      example:
        examQuestionId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sectionTitle: عنوان نمونه
        questionType: متن نمونه
        body: متن نمونه
        score: '18.50'
        displayOrder: 1
        isRequired: true
        options:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          option_key: متن نمونه
          body: متن نمونه
          display_order: 1
    AttemptStatusEnum:
      enum:
      - CREATED
      - IN_PROGRESS
      - INTERRUPTED
      - SUBMITTED
      - AUTO_SUBMITTED
      - EXPIRED
      - GRADING
      - GRADED
      - UNDER_APPEAL
      - REGRADED
      - FINALIZED
      type: string
      description: |-
        * `CREATED` - ایجادشده
        * `IN_PROGRESS` - در حال پاسخ‌دهی
        * `INTERRUPTED` - قطع اتصال
        * `SUBMITTED` - تحویل‌شده
        * `AUTO_SUBMITTED` - تحویل خودکار (پایان زمان)
        * `EXPIRED` - منقضی (شروع نشد)
        * `GRADING` - در حال تصحیح
        * `GRADED` - تصحیح‌شده
        * `UNDER_APPEAL` - در حال رسیدگی به اعتراض
        * `REGRADED` - بازتصحیح‌شده
        * `FINALIZED` - نهایی‌شده
    AttendanceMonitorRow:
      type: object
      description: ردیف پایش حضور مدرسه (بخش ۸.۵ سند فرانت).
      properties:
        classGroupId:
          type: string
          format: uuid
          title: شناسه کلاس
        classGroupCode:
          type: string
          title: کد کلاس
        gradeLevel:
          type: string
          title: پایه تحصیلی
        totalSessions:
          type: integer
          title: تعداد کل جلسات
        finalizedSessions:
          type: integer
          title: جلسات قطعی‌شده
        pendingSessions:
          type: integer
          title: جلسات در انتظار قطعی‌سازی
        absentToday:
          type: integer
          title: تعداد غایبان امروز
      required:
      - absentToday
      - classGroupCode
      - classGroupId
      - finalizedSessions
      - gradeLevel
      - pendingSessions
      - totalSessions
      example:
        classGroupId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        classGroupCode: G7-A
        gradeLevel: هفتم
        totalSessions: 12
        finalizedSessions: 3
        pendingSessions: 3
        absentToday: 3
    AttendanceRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        session:
          type: string
          format: uuid
          title: جلسه
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        attendance_status:
          allOf:
          - $ref: '#/components/schemas/AttendanceStatusEnum'
          title: وضعیت حضور
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        late_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه تأخیر
        early_leave_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه خروج زودهنگام
        reason_code:
          type: string
          title: کد علت
          maxLength: 60
        note:
          type: string
          title: یادداشت
          maxLength: 300
        finalization_status:
          allOf:
          - $ref: '#/components/schemas/FinalizationStatusEnum'
          readOnly: true
          title: وضعیت قطعی‌سازی
        amended_reason:
          type: string
          title: علت اصلاح
          maxLength: 300
        has_justification:
          type: boolean
          readOnly: true
          title: دارای توجیه است
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - enrollment
      - finalization_status
      - has_justification
      - id
      - session
      - status_display
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        attendance_status: PRESENT
        status_display: برچسب فارسی مقدار
        late_minutes: 45
        early_leave_minutes: 45
        reason_code: SAMPLE-001
        note: یادداشت داخلی
        finalization_status: DRAFT
        amended_reason: توضیح نمونه
        has_justification: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    AttendanceRecordRequest:
      type: object
      properties:
        session:
          type: string
          format: uuid
          title: جلسه
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        attendance_status:
          allOf:
          - $ref: '#/components/schemas/AttendanceStatusEnum'
          title: وضعیت حضور
        late_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه تأخیر
        early_leave_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه خروج زودهنگام
        reason_code:
          type: string
          title: کد علت
          maxLength: 60
        note:
          type: string
          title: یادداشت
          maxLength: 300
        amended_reason:
          type: string
          title: علت اصلاح
          maxLength: 300
      required:
      - enrollment
      - session
      example:
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attendance_status: PRESENT
        late_minutes: 45
        early_leave_minutes: 45
        reason_code: SAMPLE-001
        note: یادداشت داخلی
        amended_reason: توضیح نمونه
    AttendanceRoster:
      type: object
      description: |-
        فهرست حضور یک جلسه.

        خروجی GET برای رندر فرم ثبت حضور و ورودی POST برای ثبت گروهی
        (بخش ۸.۴ سند فرانت).
      properties:
        sessionId:
          type: string
          format: uuid
          readOnly: true
          title: شناسه جلسه
        classGroupCode:
          type: string
          readOnly: true
          title: کد کلاس
        courseTitle:
          type: string
          readOnly: true
          title: عنوان درس
        startsAt:
          type: string
          format: date-time
          readOnly: true
          title: زمان شروع
        finalizationStatus:
          type: string
          readOnly: true
          title: وضعیت قطعی‌سازی
        rows:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceRosterRow'
          title: سطرها
      required:
      - classGroupCode
      - courseTitle
      - finalizationStatus
      - rows
      - sessionId
      - startsAt
      example:
        sessionId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        classGroupCode: G7-A
        courseTitle: ریاضی ۷
        startsAt: '2026-09-23T08:30:00+03:30'
        finalizationStatus: متن نمونه
        rows:
        - enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          studentNo: '14030127'
          studentName: سارا محمدی
          attendanceStatus: متن نمونه
          lateMinutes: 0
          earlyLeaveMinutes: 0
          reasonCode: ''
          note: ''
    AttendanceRosterRow:
      type: object
      description: یک ردیف فهرست حضور برای ثبت گروهی.
      properties:
        enrollmentId:
          type: string
          format: uuid
          title: شناسه ثبت‌نام
        studentNo:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        studentName:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        attendanceStatus:
          type: string
          title: وضعیت حضور
        lateMinutes:
          type: integer
          default: 0
          title: دقایق تأخیر
        earlyLeaveMinutes:
          type: integer
          default: 0
          title: دقایق خروج زودهنگام
        reasonCode:
          type: string
          default: ''
          title: کد علت
        note:
          type: string
          default: ''
          title: یادداشت
      required:
      - attendanceStatus
      - enrollmentId
      - studentName
      - studentNo
      example:
        enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        studentNo: '14030127'
        studentName: سارا محمدی
        attendanceStatus: متن نمونه
        lateMinutes: 0
        earlyLeaveMinutes: 0
        reasonCode: ''
        note: ''
    AttendanceRosterRowRequest:
      type: object
      description: یک ردیف فهرست حضور برای ثبت گروهی.
      properties:
        enrollmentId:
          type: string
          format: uuid
          title: شناسه ثبت‌نام
        attendanceStatus:
          type: string
          minLength: 1
          title: وضعیت حضور
        lateMinutes:
          type: integer
          default: 0
          title: دقایق تأخیر
        earlyLeaveMinutes:
          type: integer
          default: 0
          title: دقایق خروج زودهنگام
        reasonCode:
          type: string
          default: ''
          title: کد علت
        note:
          type: string
          default: ''
          title: یادداشت
      required:
      - attendanceStatus
      - enrollmentId
      example:
        enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attendanceStatus: متن نمونه
        lateMinutes: 0
        earlyLeaveMinutes: 0
        reasonCode: ''
        note: ''
    AttendanceStatusEnum:
      enum:
      - PRESENT
      - ABSENT
      - LATE
      - EXCUSED
      - SCHOOL_ACTIVITY
      - REMOTE
      - EARLY_LEAVE
      type: string
      description: |-
        * `PRESENT` - حاضر
        * `ABSENT` - غایب
        * `LATE` - تأخیر
        * `EXCUSED` - غیبت موجه
        * `SCHOOL_ACTIVITY` - مأموریت مدرسه
        * `REMOTE` - حضور مجازی
        * `EARLY_LEAVE` - خروج زودهنگام
    AudienceEnum:
      enum:
      - CLASS_GUARDIANS
      - GRADE_GUARDIANS
      - ALL_STAFF
      - CLASS_STUDENTS
      type: string
      description: |-
        * `CLASS_GUARDIANS` - اولیای یک کلاس
        * `GRADE_GUARDIANS` - اولیای یک پایه
        * `ALL_STAFF` - همه کارکنان
        * `CLASS_STUDENTS` - دانش‌آموزان یک کلاس
    AuditLog:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        actor_user_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: کاربر عامل
        actor_username:
          type: string
          readOnly: true
          title: نام کاربری عامل
        action:
          allOf:
          - $ref: '#/components/schemas/ActionEnum'
          readOnly: true
          title: عمل
        action_display:
          type: string
          readOnly: true
          title: برچسب فارسی عمل
        entity_type:
          type: string
          readOnly: true
          title: نوع موجودیت
        entity_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: شناسه موجودیت
        entity_label:
          type: string
          readOnly: true
          title: عنوان موجودیت
        reason:
          type: string
          readOnly: true
          title: علت
        correlation_id:
          type: string
          readOnly: true
          title: شناسه ردیابی
        changes:
          readOnly: true
          title: تغییرات (Diff)
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          readOnly: true
          title: طبقه‌بندی
        client_ip:
          type: string
          readOnly: true
          nullable: true
          title: نشانی IP کلاینت
        occurred_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان رخداد
      required:
      - action
      - action_display
      - actor_user_id
      - actor_username
      - changes
      - classification
      - client_ip
      - correlation_id
      - entity_id
      - entity_label
      - entity_type
      - id
      - occurred_at
      - reason
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        actor_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        actor_username: متن نمونه
        action: CREATE
        action_display: ایجاد
        entity_type: متن نمونه
        entity_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        entity_label: متن نمونه
        reason: اصلاح اطلاعات پس از بررسی پرونده
        correlation_id: 8c4a1f2e
        classification: PUBLIC
        client_ip: متن نمونه
        occurred_at: '2026-09-23T08:30:00+03:30'
    BankAccount:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          title: عنوان
          maxLength: 200
        bank_name:
          type: string
          title: نام بانک
          maxLength: 120
        iban_masked:
          type: string
          title: شبا (ماسک‌شده)
          description: 'بخش ۱۵.۲: شماره کامل حساب در Log و پاسخ API ذخیره نمی‌شود.'
          maxLength: 40
        ledger_account:
          type: string
          format: uuid
          title: حساب دفتر کل
          nullable: true
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        status:
          type: string
          title: وضعیت
          maxLength: 20
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - id
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        bank_name: نام نمونه
        iban_masked: متن نمونه
        ledger_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        currency: IRR
        status: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    BankAccountRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        bank_name:
          type: string
          title: نام بانک
          maxLength: 120
        iban_masked:
          type: string
          title: شبا (ماسک‌شده)
          description: 'بخش ۱۵.۲: شماره کامل حساب در Log و پاسخ API ذخیره نمی‌شود.'
          maxLength: 40
        ledger_account:
          type: string
          format: uuid
          title: حساب دفتر کل
          nullable: true
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        bank_name: نام نمونه
        iban_masked: متن نمونه
        ledger_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        currency: IRR
        status: متن نمونه
    BankReconciliation:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        bank_account:
          type: string
          format: uuid
          title: حساب بانکی
        bank_account_title:
          type: string
          readOnly: true
          title: عنوان حساب بانکی
        period_start:
          type: string
          format: date
          title: آغاز دوره
        period_end:
          type: string
          format: date
          title: پایان دوره
        statement_balance:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مانده صورتحساب بانک
        ledger_balance:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مانده دفتر
        difference:
          type: integer
          readOnly: true
          title: اختلاف
        status:
          allOf:
          - $ref: '#/components/schemas/ReconciliationStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
        reconciled_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تطبیق
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - bank_account
      - bank_account_title
      - created_at
      - difference
      - id
      - ledger_balance
      - period_end
      - period_start
      - reconciled_at
      - statement_balance
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        bank_account_title: عنوان نمونه
        period_start: '2026-09-23'
        period_end: '2027-03-20'
        statement_balance: 3
        ledger_balance: 3
        difference: 3
        status: OPEN
        note: یادداشت داخلی
        reconciled_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    BankReconciliationRequest:
      type: object
      properties:
        bank_account:
          type: string
          format: uuid
          title: حساب بانکی
        period_start:
          type: string
          format: date
          title: آغاز دوره
        period_end:
          type: string
          format: date
          title: پایان دوره
        statement_balance:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مانده صورتحساب بانک
        ledger_balance:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مانده دفتر
        status:
          allOf:
          - $ref: '#/components/schemas/ReconciliationStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
      required:
      - bank_account
      - ledger_balance
      - period_end
      - period_start
      - statement_balance
      example:
        bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        period_start: '2026-09-23'
        period_end: '2027-03-20'
        statement_balance: 3
        ledger_balance: 3
        status: OPEN
        note: یادداشت داخلی
    BehaviorAction:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        incident:
          type: string
          format: uuid
          title: رخداد
        action_type:
          allOf:
          - $ref: '#/components/schemas/ActionTypeEnum'
          title: نوع عمل
        action_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع اقدام
        details:
          type: string
          title: جزئیات
        effective_from:
          type: string
          format: date
          nullable: true
          title: معتبر از
        effective_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          type: string
          title: وضعیت
          maxLength: 20
        completed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان تکمیل
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - action_type
      - action_type_display
      - created_at
      - id
      - incident
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        incident: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        action_type: VERBAL_WARNING
        action_type_display: تذکر شفاهی
        details: متن نمونه
        effective_from: '2026-09-23'
        effective_until: '2027-03-20'
        status: متن نمونه
        completed_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    BehaviorActionRequest:
      type: object
      properties:
        incident:
          type: string
          format: uuid
          title: رخداد
        action_type:
          allOf:
          - $ref: '#/components/schemas/ActionTypeEnum'
          title: نوع عمل
        details:
          type: string
          title: جزئیات
        effective_from:
          type: string
          format: date
          nullable: true
          title: معتبر از
        effective_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        completed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان تکمیل
      required:
      - action_type
      - incident
      example:
        incident: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        action_type: VERBAL_WARNING
        details: متن نمونه
        effective_from: '2026-09-23'
        effective_until: '2027-03-20'
        status: متن نمونه
        completed_at: '2026-09-23T08:30:00+03:30'
    BehaviorDecisionRequest:
      type: object
      description: تصمیم درباره یک رخداد رفتاری.
      properties:
        substantiated:
          type: boolean
          description: 'آیا تخلف تأیید شد؟ (بخش ۷.۱۰: رخداد اتهام قطعی نیست)'
        investigation_note:
          type: string
          maxLength: 2000
          title: یادداشت بررسی تخلف
        points:
          type: integer
          default: 0
          description: منفی برای تخلف، مثبت برای تشویق
      required:
      - substantiated
      example:
        substantiated: true
        investigation_note: توضیح نمونه
        points: 0
    BehaviorIncident:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        location:
          type: string
          title: محل
          maxLength: 150
        incident_type:
          allOf:
          - $ref: '#/components/schemas/IncidentTypeEnum'
          title: نوع رخداد
        incident_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع رخداد انضباطی
        severity:
          allOf:
          - $ref: '#/components/schemas/BehaviorSeverityEnum'
          title: شدت
        description:
          type: string
          title: شرح
        witnesses:
          type: string
          title: شهود
          maxLength: 400
        student_statement:
          type: string
          title: توضیح دانش‌آموز
        status:
          allOf:
          - $ref: '#/components/schemas/BehaviorIncidentStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        investigation_note:
          type: string
          title: توضیح بررسی
        decided_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تصمیم
        points:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: امتیاز
          description: منفی برای تخلف، مثبت برای تشویق
        guardian_notified_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان اطلاع به ولی
        actions:
          type: array
          items:
            $ref: '#/components/schemas/BehaviorAction'
          readOnly: true
          title: اقدامات
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - actions
      - created_at
      - decided_at
      - description
      - guardian_notified_at
      - id
      - incident_type
      - incident_type_display
      - occurred_at
      - status
      - status_display
      - student
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        occurred_at: '2026-09-23T08:30:00+03:30'
        location: متن نمونه
        incident_type: DISRUPTION
        incident_type_display: اخلال در کلاس
        severity: MINOR
        description: توضیح تکمیلی این رکورد
        witnesses: متن نمونه
        student_statement: متن نمونه
        status: REPORTED
        status_display: گزارش‌شده
        investigation_note: توضیح نمونه
        decided_at: '2026-09-23T08:30:00+03:30'
        points: 3
        guardian_notified_at: '2026-09-23T08:30:00+03:30'
        actions:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          incident: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          action_type: VERBAL_WARNING
          action_type_display: تذکر شفاهی
          details: متن نمونه
          effective_from: '2026-09-23'
          effective_until: '2027-03-20'
          status: متن نمونه
          completed_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    BehaviorIncidentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        location:
          type: string
          title: محل
          maxLength: 150
        incident_type:
          allOf:
          - $ref: '#/components/schemas/IncidentTypeEnum'
          title: نوع رخداد
        severity:
          allOf:
          - $ref: '#/components/schemas/BehaviorSeverityEnum'
          title: شدت
        description:
          type: string
          minLength: 1
          title: شرح
        witnesses:
          type: string
          title: شهود
          maxLength: 400
        student_statement:
          type: string
          title: توضیح دانش‌آموز
        investigation_note:
          type: string
          title: توضیح بررسی
        points:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: امتیاز
          description: منفی برای تخلف، مثبت برای تشویق
      required:
      - description
      - incident_type
      - occurred_at
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        occurred_at: '2026-09-23T08:30:00+03:30'
        location: متن نمونه
        incident_type: DISRUPTION
        severity: MINOR
        description: توضیح تکمیلی این رکورد
        witnesses: متن نمونه
        student_statement: متن نمونه
        investigation_note: توضیح نمونه
        points: 3
    BehaviorIncidentStatusEnum:
      enum:
      - REPORTED
      - UNDER_INVESTIGATION
      - SUBSTANTIATED
      - UNSUBSTANTIATED
      - ACTION_TAKEN
      - UNDER_APPEAL
      - CLOSED
      type: string
      description: |-
        * `REPORTED` - گزارش‌شده
        * `UNDER_INVESTIGATION` - در حال بررسی
        * `SUBSTANTIATED` - تأییدشده
        * `UNSUBSTANTIATED` - رد شده
        * `ACTION_TAKEN` - اقدام انجام شد
        * `UNDER_APPEAL` - در حال اعتراض
        * `CLOSED` - بسته
    BehaviorSeverityEnum:
      enum:
      - MINOR
      - MODERATE
      - MAJOR
      - SEVERE
      type: string
      description: |-
        * `MINOR` - جزئی
        * `MODERATE` - متوسط
        * `MAJOR` - جدی
        * `SEVERE` - بسیار جدی
    BlankEnum:
      enum:
      - ''
    BloodTypeEnum:
      enum:
      - A+
      - A-
      - B+
      - B-
      - AB+
      - AB-
      - O+
      - O-
      - UNKNOWN
      type: string
      description: |-
        * `A+` - A+
        * `A-` - A-
        * `B+` - B+
        * `B-` - B-
        * `AB+` - AB+
        * `AB-` - AB-
        * `O+` - O+
        * `O-` - O-
        * `UNKNOWN` - نامشخص
    BroadcastChannelEnum:
      enum:
      - IN_APP
      - SMS
      - EMAIL
      - PUSH
      type: string
      description: |-
        * `IN_APP` - IN_APP
        * `SMS` - SMS
        * `EMAIL` - EMAIL
        * `PUSH` - PUSH
    BroadcastPreview:
      type: object
      properties:
        recipientCount:
          type: integer
          title: تعداد گیرندگان
        sampleMessage:
          type: string
          title: نمونه پیام تولیدشده
        channel:
          type: string
          title: کانال ارسال
        sent:
          type: boolean
          title: ارسال شد
      required:
      - channel
      - recipientCount
      - sampleMessage
      - sent
      example:
        recipientCount: 12
        sampleMessage: متن نمونه
        channel: متن نمونه
        sent: true
    BroadcastPriorityEnum:
      enum:
      - TRANSACTIONAL
      - OPERATIONAL
      - PROMOTIONAL
      - EMERGENCY
      type: string
      description: |-
        * `TRANSACTIONAL` - TRANSACTIONAL
        * `OPERATIONAL` - OPERATIONAL
        * `PROMOTIONAL` - PROMOTIONAL
        * `EMERGENCY` - EMERGENCY
    BroadcastRequest:
      type: object
      description: |-
        ارسال گروهی اعلان.

        بخش ۱۱.۴: «ارسال انبوه دارای پیش‌نمایش تعداد گیرندگان، نمونه پیام، تأیید
        و امکان توقف است.» بنابراین ابتدا `preview` و سپس `confirm=true`.
      properties:
        audience:
          $ref: '#/components/schemas/AudienceEnum'
          title: مخاطبان
        target_id:
          type: string
          format: uuid
          nullable: true
          description: شناسه کلاس یا پایه
        channel:
          allOf:
          - $ref: '#/components/schemas/BroadcastChannelEnum'
          default: IN_APP
          title: کانال ارسال
        subject:
          type: string
          maxLength: 300
          title: موضوع
        body:
          type: string
          minLength: 1
          maxLength: 2000
          title: متن
        priority:
          allOf:
          - $ref: '#/components/schemas/BroadcastPriorityEnum'
          default: OPERATIONAL
          title: اولویت
        confirm:
          type: boolean
          default: false
          description: در حالت false فقط پیش‌نمایش تعداد گیرندگان برمی‌گردد.
      required:
      - audience
      - body
      example:
        audience: CLASS_GUARDIANS
        target_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        channel: IN_APP
        subject: متن نمونه
        body: متن نمونه
        priority: TRANSACTIONAL
        confirm: false
    BulkAttendanceRequest:
      type: object
      description: بدنه ثبت گروهی حضور.
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceRosterRowRequest'
          title: سطرها
        finalize:
          type: boolean
          default: false
          description: اگر true باشد، پس از ثبت، حضور جلسه نهایی می‌شود.
        reason:
          type: string
          default: ''
          description: 'علت اصلاح. اگر حضور جلسه قبلاً نهایی شده باشد، این فیلد الزامی
            است (بخش ۷.۵: اصلاح پس از نهایی‌شدن نیازمند علت و مجوز است).'
          maxLength: 400
      required:
      - rows
      example:
        rows:
        - enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attendanceStatus: متن نمونه
          lateMinutes: 0
          earlyLeaveMinutes: 0
          reasonCode: ''
          note: ''
        finalize: false
        reason: ''
    BulkGenerateReportCardRequest:
      type: object
      description: تولید گروهی کارنامه برای یک کلاس.
      properties:
        class_group:
          type: string
          format: uuid
          title: کلاس
        term:
          type: string
          format: uuid
          title: ترم
      required:
      - class_group
      - term
      example:
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    BulkScoreRequest:
      type: object
      description: |-
        ثبت گروهی نمرات یک قلم نمره.

        «غیبت»، «معاف»، «ثبت‌نشده» و «صفر» چهار حالت متفاوت‌اند و باید با
        `status` مناسب ارسال شوند، نه با نمره صفر (بخش ۷.۷).
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/BulkScoreRowRequest'
          title: سطرها
        reason:
          type: string
          description: در صورت اصلاح نمره پس از قفل، الزامی است.
          maxLength: 400
      required:
      - rows
      example:
        rows:
        - enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          raw_score: '18.50'
          status: RECORDED
          comment: ''
        reason: اصلاح اطلاعات پس از بررسی پرونده
    BulkScoreRowRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        raw_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره خام
        status:
          allOf:
          - $ref: '#/components/schemas/BulkScoreRowStatusEnum'
          default: RECORDED
          title: وضعیت
        comment:
          type: string
          default: ''
          title: توضیح
      required:
      - enrollment
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        raw_score: '18.50'
        status: RECORDED
        comment: ''
    BulkScoreRowStatusEnum:
      enum:
      - RECORDED
      - ABSENT
      - EXEMPT
      - EXCUSED
      - NOT_RECORDED
      - PENDING_REVIEW
      type: string
      description: |-
        * `RECORDED` - RECORDED
        * `ABSENT` - ABSENT
        * `EXEMPT` - EXEMPT
        * `EXCUSED` - EXCUSED
        * `NOT_RECORDED` - NOT_RECORDED
        * `PENDING_REVIEW` - PENDING_REVIEW
    CalculateResultRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        enrollment:
          type: string
          format: uuid
          nullable: true
          description: در صورت خالی بودن، برای همه دانش‌آموزان کلاس محاسبه می‌شود.
      required:
      - course_offering
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    CalendarEvent:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        campus:
          type: string
          format: uuid
          title: شعبه
          nullable: true
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        title:
          type: string
          title: عنوان رویداد
          maxLength: 200
        event_type:
          allOf:
          - $ref: '#/components/schemas/CalendarEventEventTypeEnum'
          title: نوع رویداد
        event_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع رخداد
        starts_on:
          type: string
          format: date
          title: از تاریخ
        ends_on:
          type: string
          format: date
          title: تا تاریخ
        is_working_day:
          type: boolean
          title: روز کاری محسوب می‌شود
        description:
          type: string
          title: شرح
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - academic_year
      - created_at
      - ends_on
      - event_type_display
      - id
      - school
      - starts_on
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        event_type: HOLIDAY
        event_type_display: تعطیلی
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        is_working_day: true
        description: توضیح تکمیلی این رکورد
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CalendarEventEventTypeEnum:
      enum:
      - HOLIDAY
      - EXAM_PERIOD
      - PARENT_MEETING
      - CEREMONY
      - FIELD_TRIP
      - OTHER
      type: string
      description: |-
        * `HOLIDAY` - تعطیلی
        * `EXAM_PERIOD` - بازه امتحانات
        * `PARENT_MEETING` - جلسه اولیا
        * `CEREMONY` - مراسم
        * `FIELD_TRIP` - اردو
        * `OTHER` - سایر
    CalendarEventRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        campus:
          type: string
          format: uuid
          title: شعبه
          nullable: true
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        title:
          type: string
          minLength: 1
          title: عنوان رویداد
          maxLength: 200
        event_type:
          allOf:
          - $ref: '#/components/schemas/CalendarEventEventTypeEnum'
          title: نوع رویداد
        starts_on:
          type: string
          format: date
          title: از تاریخ
        ends_on:
          type: string
          format: date
          title: تا تاریخ
        is_working_day:
          type: boolean
          title: روز کاری محسوب می‌شود
        description:
          type: string
          title: شرح
      required:
      - academic_year
      - ends_on
      - school
      - starts_on
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        event_type: HOLIDAY
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        is_working_day: true
        description: توضیح تکمیلی این رکورد
    Campus:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        school_name:
          type: string
          readOnly: true
          title: نام مدرسه
        code:
          type: string
          title: کد شعبه
          maxLength: 30
        name:
          type: string
          title: نام شعبه
          maxLength: 200
        address_line:
          type: string
          title: نشانی
          maxLength: 400
        phone:
          type: string
          title: تلفن
          maxLength: 30
        timezone:
          type: string
          title: منطقه زمانی
          maxLength: 64
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - name
      - school
      - school_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school_name: دبیرستان نمونه
        code: SAMPLE-001
        name: نام نمونه
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        phone: 09121234567
        timezone: Asia/Tehran
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CampusRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد شعبه
          maxLength: 30
        name:
          type: string
          minLength: 1
          title: نام شعبه
          maxLength: 200
        address_line:
          type: string
          title: نشانی
          maxLength: 400
        phone:
          type: string
          title: تلفن
          maxLength: 30
        timezone:
          type: string
          minLength: 1
          title: منطقه زمانی
          maxLength: 64
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      required:
      - code
      - name
      - school
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        name: نام نمونه
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        phone: 09121234567
        timezone: Asia/Tehran
        status: DRAFT
    ClassGroup:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        campus:
          type: string
          format: uuid
          title: شعبه
        campus_name:
          type: string
          readOnly: true
          title: نام شعبه
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        grade_level_title:
          type: string
          readOnly: true
          title: عنوان پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        program_title:
          type: string
          readOnly: true
          title: عنوان دوره تحصیلی
        home_room:
          type: string
          format: uuid
          title: اتاق اصلی
          nullable: true
        home_room_code:
          type: string
          readOnly: true
          title: کد کلاس‌درس ثابت
        homeroom_teacher_id:
          type: string
          format: uuid
          nullable: true
          title: معلم راهنما
        code:
          type: string
          title: کد کلاس
          maxLength: 30
        title:
          type: string
          title: عنوان کلاس
          maxLength: 100
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت مصوب
        capacity_override_reason:
          type: string
          title: علت افزایش ظرفیت
          description: افزایش ظرفیت نیازمند مجوز و علت ثبت‌شده است (بخش ۱۱.۳)
          maxLength: 300
        occupied_seats:
          type: integer
          readOnly: true
          title: صندلی‌های اشغال‌شده
        available_seats:
          type: integer
          readOnly: true
          title: صندلی‌های خالی
        status:
          allOf:
          - $ref: '#/components/schemas/ClassGroupStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - academic_year
      - available_seats
      - campus
      - campus_name
      - code
      - created_at
      - grade_level
      - grade_level_title
      - home_room_code
      - id
      - occupied_seats
      - program_title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus_name: شعبه مرکزی
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        grade_level_title: پایه هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        program_title: عنوان نمونه
        home_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        home_room_code: SAMPLE-001
        homeroom_teacher_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        capacity: 3
        capacity_override_reason: توضیح نمونه
        occupied_seats: 3
        available_seats: 3
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ClassGroupList:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد کلاس
          maxLength: 30
        title:
          type: string
          title: عنوان کلاس
          maxLength: 100
        campus:
          type: string
          format: uuid
          title: شعبه
        campus_name:
          type: string
          readOnly: true
          title: نام شعبه
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        grade_level_title:
          type: string
          readOnly: true
          title: عنوان پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        program_title:
          type: string
          readOnly: true
          title: عنوان دوره تحصیلی
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت مصوب
        occupied_seats:
          type: integer
          readOnly: true
          title: صندلی‌های اشغال‌شده
        available_seats:
          type: integer
          readOnly: true
          title: صندلی‌های خالی
        status:
          allOf:
          - $ref: '#/components/schemas/ClassGroupStatusEnum'
          title: وضعیت
      required:
      - academic_year
      - available_seats
      - campus
      - campus_name
      - code
      - grade_level
      - grade_level_title
      - id
      - occupied_seats
      - program_title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus_name: شعبه مرکزی
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        grade_level_title: پایه هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        program_title: عنوان نمونه
        capacity: 3
        occupied_seats: 3
        available_seats: 3
        status: DRAFT
    ClassGroupRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        home_room:
          type: string
          format: uuid
          title: اتاق اصلی
          nullable: true
        homeroom_teacher_id:
          type: string
          format: uuid
          nullable: true
          title: معلم راهنما
        code:
          type: string
          minLength: 1
          title: کد کلاس
          maxLength: 30
        title:
          type: string
          title: عنوان کلاس
          maxLength: 100
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت مصوب
        capacity_override_reason:
          type: string
          title: علت افزایش ظرفیت
          description: افزایش ظرفیت نیازمند مجوز و علت ثبت‌شده است (بخش ۱۱.۳)
          maxLength: 300
        status:
          allOf:
          - $ref: '#/components/schemas/ClassGroupStatusEnum'
          title: وضعیت
      required:
      - academic_year
      - campus
      - code
      - grade_level
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        home_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        homeroom_teacher_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        capacity: 3
        capacity_override_reason: توضیح نمونه
        status: DRAFT
    ClassGroupStatusEnum:
      enum:
      - DRAFT
      - ACTIVE
      - FULL
      - CLOSED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `ACTIVE` - فعال
        * `FULL` - تکمیل ظرفیت
        * `CLOSED` - بسته
    ClassMembership:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        class_group:
          type: string
          format: uuid
          title: کلاس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        is_primary:
          type: boolean
          title: عضویت اصلی
        status:
          allOf:
          - $ref: '#/components/schemas/ClassMembershipStatusEnum'
          title: وضعیت
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        exit_reason:
          type: string
          title: علت خروج
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - class_group
      - class_group_code
      - created_at
      - effective_from
      - enrollment
      - id
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        class_group_code: G7-A
        is_primary: true
        status: ACTIVE
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        exit_reason: توضیح نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ClassMembershipRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        class_group:
          type: string
          format: uuid
          title: کلاس
        is_primary:
          type: boolean
          title: عضویت اصلی
        status:
          allOf:
          - $ref: '#/components/schemas/ClassMembershipStatusEnum'
          title: وضعیت
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        exit_reason:
          type: string
          title: علت خروج
          maxLength: 300
      required:
      - class_group
      - effective_from
      - enrollment
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        is_primary: true
        status: ACTIVE
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        exit_reason: توضیح نمونه
    ClassMembershipStatusEnum:
      enum:
      - ACTIVE
      - TRANSFERRED
      - ENDED
      type: string
      description: |-
        * `ACTIVE` - فعال
        * `TRANSFERRED` - منتقل‌شده
        * `ENDED` - پایان‌یافته
    CloneYearRequest:
      type: object
      properties:
        target_academic_year:
          type: string
          format: uuid
          description: شناسه سال تحصیلی مقصد که ساختار در آن ایجاد می‌شود
      required:
      - target_academic_year
      example:
        target_academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    ConfidentialityLevelEnum:
      enum:
      - STANDARD
      - SENSITIVE
      - RESTRICTED
      type: string
      description: |-
        * `STANDARD` - عادی
        * `SENSITIVE` - حساس
        * `RESTRICTED` - بسیار محرمانه
    Consent:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        guardian:
          type: string
          format: uuid
          title: ولی
          nullable: true
        guardian_name:
          type: string
          readOnly: true
          title: نام ولی
        consent_type:
          allOf:
          - $ref: '#/components/schemas/ConsentTypeEnum'
          title: نوع رضایت
        consent_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع رضایت‌نامه
        status:
          allOf:
          - $ref: '#/components/schemas/ConsentStatusEnum'
          readOnly: true
          title: وضعیت
        granted_at:
          type: string
          format: date-time
          title: زمان اعطا
        revoked_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ابطال
        revoke_reason:
          type: string
          title: علت ابطال
          maxLength: 300
        expires_at:
          type: string
          format: date
          nullable: true
          title: زمان انقضا
        policy_version:
          type: string
          title: نسخه متن سیاست
          maxLength: 30
        policy_text_snapshot:
          type: string
          title: متن سیاست در زمان اخذ رضایت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - consent_type
      - consent_type_display
      - created_at
      - granted_at
      - guardian_name
      - id
      - revoked_at
      - status
      - student
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian_name: علی محمدی
        consent_type: PHOTO
        consent_type_display: انتشار تصویر
        status: GRANTED
        granted_at: '2026-09-23T08:30:00+03:30'
        revoked_at: '2026-09-23T08:30:00+03:30'
        revoke_reason: توضیح نمونه
        expires_at: '2027-03-20'
        policy_version: متن نمونه
        policy_text_snapshot: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ConsentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        guardian:
          type: string
          format: uuid
          title: ولی
          nullable: true
        consent_type:
          allOf:
          - $ref: '#/components/schemas/ConsentTypeEnum'
          title: نوع رضایت
        granted_at:
          type: string
          format: date-time
          title: زمان اعطا
        revoke_reason:
          type: string
          title: علت ابطال
          maxLength: 300
        expires_at:
          type: string
          format: date
          nullable: true
          title: زمان انقضا
        policy_version:
          type: string
          minLength: 1
          title: نسخه متن سیاست
          maxLength: 30
        policy_text_snapshot:
          type: string
          title: متن سیاست در زمان اخذ رضایت
      required:
      - consent_type
      - granted_at
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        consent_type: PHOTO
        granted_at: '2026-09-23T08:30:00+03:30'
        revoke_reason: توضیح نمونه
        expires_at: '2027-03-20'
        policy_version: متن نمونه
        policy_text_snapshot: متن نمونه
    ConsentStatusEnum:
      enum:
      - GRANTED
      - REVOKED
      - EXPIRED
      type: string
      description: |-
        * `GRANTED` - اعطاشده
        * `REVOKED` - لغوشده
        * `EXPIRED` - منقضی
    ConsentTypeEnum:
      enum:
      - PHOTO
      - FIELD_TRIP
      - EMERGENCY_MEDICAL
      - CONTENT_PUBLISH
      - TRANSPORT
      - DATA_PROCESSING
      type: string
      description: |-
        * `PHOTO` - انتشار تصویر
        * `FIELD_TRIP` - سفر و اردو
        * `EMERGENCY_MEDICAL` - درمان اضطراری
        * `CONTENT_PUBLISH` - انتشار محتوا
        * `TRANSPORT` - استفاده از سرویس
        * `DATA_PROCESSING` - پردازش داده
    ContactPoint:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        contact_type:
          allOf:
          - $ref: '#/components/schemas/ContactTypeEnum'
          title: نوع تماس
        contact_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع راه تماس
        value:
          type: string
          title: مقدار
          maxLength: 200
        is_primary:
          type: boolean
          title: تماس اصلی
        is_verified:
          type: boolean
          readOnly: true
          title: تأییدشده
        verified_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید هویت
        label:
          type: string
          title: برچسب
          maxLength: 60
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - contact_type
      - contact_type_display
      - created_at
      - id
      - is_verified
      - person
      - updated_at
      - value
      - verified_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        contact_type: MOBILE
        contact_type_display: تلفن همراه
        value: متن نمونه
        is_primary: true
        is_verified: true
        verified_at: '2026-09-23T08:30:00+03:30'
        label: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ContactPointRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        contact_type:
          allOf:
          - $ref: '#/components/schemas/ContactTypeEnum'
          title: نوع تماس
        value:
          type: string
          minLength: 1
          title: مقدار
          maxLength: 200
        is_primary:
          type: boolean
          title: تماس اصلی
        label:
          type: string
          title: برچسب
          maxLength: 60
      required:
      - contact_type
      - person
      - value
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        contact_type: MOBILE
        value: متن نمونه
        is_primary: true
        label: متن نمونه
    ContactTypeEnum:
      enum:
      - MOBILE
      - PHONE
      - EMAIL
      - EMERGENCY
      type: string
      description: |-
        * `MOBILE` - تلفن همراه
        * `PHONE` - تلفن ثابت
        * `EMAIL` - پست الکترونیک
        * `EMERGENCY` - تماس اضطراری
    ContractStatusEnum:
      enum:
      - DRAFT
      - ACTIVE
      - EXPIRED
      - TERMINATED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `ACTIVE` - جاری
        * `EXPIRED` - منقضی
        * `TERMINATED` - فسخ‌شده
    ContractTypeEnum:
      enum:
      - PERMANENT
      - FIXED_TERM
      - HOURLY
      - CONTRACTOR
      - INTERN
      type: string
      description: |-
        * `PERMANENT` - رسمی/دائم
        * `FIXED_TERM` - مدت معین
        * `HOURLY` - ساعتی/حق‌التدریس
        * `CONTRACTOR` - پیمانکاری
        * `INTERN` - کارآموز
    ConvertAdmissionRequest:
      type: object
      properties:
        enrolled_on:
          type: string
          format: date
          title: تاریخ ثبت‌نام
      example:
        enrolled_on: '2026-09-23'
    CopyStatusEnum:
      enum:
      - AVAILABLE
      - ON_LOAN
      - RESERVED
      - LOST
      - DAMAGED
      - WITHDRAWN
      type: string
      description: |-
        * `AVAILABLE` - موجود
        * `ON_LOAN` - امانت داده‌شده
        * `RESERVED` - رزروشده
        * `LOST` - مفقود
        * `DAMAGED` - آسیب‌دیده
        * `WITHDRAWN` - خارج از رده
    CostCenter:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          title: کد
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 200
        is_active:
          type: boolean
          title: فعال
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        is_active: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CostCenterRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        is_active:
          type: boolean
          title: فعال
      required:
      - code
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        is_active: true
    CounselingCase:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        counselor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: مشاور
        referral_source:
          allOf:
          - $ref: '#/components/schemas/ReferralSourceEnum'
          title: منبع ارجاع
        subject:
          type: string
          title: موضوع
          maxLength: 200
        priority:
          allOf:
          - $ref: '#/components/schemas/PriorityLevelEnum'
          title: اولویت
        status:
          allOf:
          - $ref: '#/components/schemas/CounselingCaseStatusEnum'
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        opened_on:
          type: string
          format: date
          title: تاریخ گشایش
        closed_on:
          type: string
          format: date
          nullable: true
          title: تاریخ بستن
        action_plan:
          type: string
          title: برنامه اقدام
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
        session_count:
          type: integer
          readOnly: true
          title: تعداد جلسه
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - id
      - opened_on
      - referral_source
      - session_count
      - status_display
      - student
      - student_name
      - student_no
      - subject
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        counselor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        referral_source: TEACHER
        subject: متن نمونه
        priority: LOW
        status: OPEN
        status_display: باز
        opened_on: '2026-09-23'
        closed_on: '2027-03-20'
        action_plan: متن نمونه
        confidentiality_level: STANDARD
        session_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CounselingCaseRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        counselor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: مشاور
        referral_source:
          allOf:
          - $ref: '#/components/schemas/ReferralSourceEnum'
          title: منبع ارجاع
        subject:
          type: string
          minLength: 1
          title: موضوع
          maxLength: 200
        priority:
          allOf:
          - $ref: '#/components/schemas/PriorityLevelEnum'
          title: اولویت
        status:
          allOf:
          - $ref: '#/components/schemas/CounselingCaseStatusEnum'
          title: وضعیت
        opened_on:
          type: string
          format: date
          title: تاریخ گشایش
        closed_on:
          type: string
          format: date
          nullable: true
          title: تاریخ بستن
        action_plan:
          type: string
          title: برنامه اقدام
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
      required:
      - opened_on
      - referral_source
      - student
      - subject
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        counselor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        referral_source: TEACHER
        subject: متن نمونه
        priority: LOW
        status: OPEN
        opened_on: '2026-09-23'
        closed_on: '2027-03-20'
        action_plan: متن نمونه
        confidentiality_level: STANDARD
    CounselingCaseStatusEnum:
      enum:
      - OPEN
      - IN_PROGRESS
      - ON_HOLD
      - CLOSED
      - REFERRED_OUT
      type: string
      description: |-
        * `OPEN` - باز
        * `IN_PROGRESS` - در حال پیگیری
        * `ON_HOLD` - در انتظار
        * `CLOSED` - بسته
        * `REFERRED_OUT` - ارجاع به بیرون
    CounselingSession:
      type: object
      description: |-
        جلسه مشاوره.

        `protectedNote` فقط برای مشاور صاحب پرونده و نقش‌های دارای مجوز فیلدی
        برگردانده می‌شود؛ در غیر این صورت رشته خالی است (بخش ۷.۱۰).
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        case:
          type: string
          format: uuid
          title: پرونده
        held_at:
          type: string
          format: date-time
          title: زمان برگزاری
        duration_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مدت (دقیقه)
        summary:
          type: string
          title: خلاصه قابل مشاهده برای نقش‌های مجاز
        protected_note:
          type: string
          title: یادداشت محرمانه مشاور
        next_followup_at:
          type: string
          format: date-time
          nullable: true
          title: زمان پیگیری بعدی
        attendees:
          type: string
          title: حاضران
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - case
      - created_at
      - held_at
      - id
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        case: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        held_at: '2026-09-23T08:30:00+03:30'
        duration_minutes: 45
        summary: متن نمونه
        protected_note: توضیح نمونه
        next_followup_at: '2026-09-23T08:30:00+03:30'
        attendees: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CounselingSessionRequest:
      type: object
      description: |-
        جلسه مشاوره.

        `protectedNote` فقط برای مشاور صاحب پرونده و نقش‌های دارای مجوز فیلدی
        برگردانده می‌شود؛ در غیر این صورت رشته خالی است (بخش ۷.۱۰).
      properties:
        case:
          type: string
          format: uuid
          title: پرونده
        held_at:
          type: string
          format: date-time
          title: زمان برگزاری
        duration_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مدت (دقیقه)
        summary:
          type: string
          title: خلاصه قابل مشاهده برای نقش‌های مجاز
        protected_note:
          type: string
          title: یادداشت محرمانه مشاور
        next_followup_at:
          type: string
          format: date-time
          nullable: true
          title: زمان پیگیری بعدی
        attendees:
          type: string
          title: حاضران
          maxLength: 300
      required:
      - case
      - held_at
      example:
        case: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        held_at: '2026-09-23T08:30:00+03:30'
        duration_minutes: 45
        summary: متن نمونه
        protected_note: توضیح نمونه
        next_followup_at: '2026-09-23T08:30:00+03:30'
        attendees: متن نمونه
    Course:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          title: کد درس
          maxLength: 30
        title:
          type: string
          title: عنوان درس
          maxLength: 150
        credit:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: واحد
        assessment_scheme:
          allOf:
          - $ref: '#/components/schemas/AssessmentSchemeEnum'
          title: طرح ارزشیابی
        assessment_scheme_display:
          type: string
          readOnly: true
          title: برچسب فارسی نظام ارزشیابی
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        prerequisites:
          type: array
          items:
            type: string
            format: uuid
            title: پیش‌نیازها
          title: پیش‌نیازها
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - assessment_scheme_display
      - code
      - created_at
      - id
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        credit: '0'
        assessment_scheme: NUMERIC
        assessment_scheme_display: عددی
        max_score: '18.50'
        prerequisites:
        - 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CourseOffering:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        class_group:
          type: string
          format: uuid
          title: کلاس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        term:
          type: string
          format: uuid
          title: ترم
        term_title:
          type: string
          readOnly: true
          title: عنوان ترم
        course:
          type: string
          format: uuid
          title: درس
        course_code:
          type: string
          readOnly: true
          title: کد درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        weekly_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه در هفته
        status:
          allOf:
          - $ref: '#/components/schemas/CourseOfferingStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - class_group
      - class_group_code
      - course
      - course_code
      - course_title
      - created_at
      - id
      - term
      - term_title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        class_group_code: G7-A
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term_title: نیم‌سال اول
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_code: SAMPLE-001
        course_title: ریاضی ۷
        weekly_minutes: 45
        status: PLANNED
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CourseOfferingRequest:
      type: object
      properties:
        class_group:
          type: string
          format: uuid
          title: کلاس
        term:
          type: string
          format: uuid
          title: ترم
        course:
          type: string
          format: uuid
          title: درس
        weekly_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه در هفته
        status:
          allOf:
          - $ref: '#/components/schemas/CourseOfferingStatusEnum'
          title: وضعیت
      required:
      - class_group
      - course
      - term
      example:
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekly_minutes: 45
        status: PLANNED
    CourseOfferingStatusEnum:
      enum:
      - PLANNED
      - ACTIVE
      - COMPLETED
      - CANCELLED
      type: string
      description: |-
        * `PLANNED` - برنامه‌ریزی‌شده
        * `ACTIVE` - جاری
        * `COMPLETED` - پایان‌یافته
        * `CANCELLED` - لغوشده
    CourseRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد درس
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان درس
          maxLength: 150
        credit:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: واحد
        assessment_scheme:
          allOf:
          - $ref: '#/components/schemas/AssessmentSchemeEnum'
          title: طرح ارزشیابی
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        prerequisites:
          type: array
          items:
            type: string
            format: uuid
            title: پیش‌نیازها
          title: پیش‌نیازها
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      required:
      - code
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        credit: '0'
        assessment_scheme: NUMERIC
        max_score: '18.50'
        prerequisites:
        - 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: DRAFT
    CourseResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        continuous_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره مستمر
        final_exam_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره پایانی
        final_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره نهایی
        letter_grade:
          type: string
          title: نمره حرفی
          maxLength: 5
        qualitative_level:
          title: سطح کیفی
          oneOf:
          - $ref: '#/components/schemas/QualitativeLevelEnum'
          - $ref: '#/components/schemas/BlankEnum'
        result_status:
          allOf:
          - $ref: '#/components/schemas/CourseResultStatusEnum'
          title: وضعیت نتیجه
        result_status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت نتیجه
        calculation_version:
          type: integer
          readOnly: true
          title: نسخه محاسبه
        calculated_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان محاسبه
        calculation_inputs:
          readOnly: true
          title: Snapshot ورودی‌های محاسبه
          description: 'بخش ۱۱.۵: هر محاسبه مهم Snapshot ورودی‌های ضروری دارد.'
        teacher_comment:
          type: string
          title: نظر معلم
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - calculated_at
      - calculation_inputs
      - calculation_version
      - course_offering
      - course_title
      - created_at
      - enrollment
      - final_score
      - id
      - result_status_display
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        continuous_score: '18.50'
        final_exam_score: '18.50'
        final_score: '18.50'
        letter_grade: A
        qualitative_level: EXCELLENT
        result_status: IN_PROGRESS
        result_status_display: در حال بررسی
        calculation_version: 3
        calculated_at: '2026-09-23T08:30:00+03:30'
        teacher_comment: توضیح نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    CourseResultRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        continuous_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره مستمر
        final_exam_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره پایانی
        letter_grade:
          type: string
          title: نمره حرفی
          maxLength: 5
        qualitative_level:
          title: سطح کیفی
          oneOf:
          - $ref: '#/components/schemas/QualitativeLevelEnum'
          - $ref: '#/components/schemas/BlankEnum'
        result_status:
          allOf:
          - $ref: '#/components/schemas/CourseResultStatusEnum'
          title: وضعیت نتیجه
        teacher_comment:
          type: string
          title: نظر معلم
      required:
      - course_offering
      - enrollment
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        continuous_score: '18.50'
        final_exam_score: '18.50'
        letter_grade: A
        qualitative_level: EXCELLENT
        result_status: IN_PROGRESS
        teacher_comment: توضیح نمونه
    CourseResultStatusEnum:
      enum:
      - IN_PROGRESS
      - PASSED
      - FAILED
      - INCOMPLETE
      - EXEMPT
      type: string
      description: |-
        * `IN_PROGRESS` - در جریان
        * `PASSED` - قبول
        * `FAILED` - مردود
        * `INCOMPLETE` - ناتمام
        * `EXEMPT` - معاف
    CreateJournalEntryRequest:
      type: object
      description: |-
        ایجاد سند حسابداری دستی.

        مجموع بدهکار و بستانکار باید برابر و بزرگ‌تر از صفر باشد؛ در غیر این صورت
        قطعی‌سازی سند با خطای `JOURNAL_NOT_BALANCED` رد می‌شود.
      properties:
        fiscal_year:
          type: string
          format: uuid
          title: سال مالی
        entry_date:
          type: string
          format: date
          title: تاریخ ثبت
        description:
          type: string
          minLength: 1
          maxLength: 400
          title: توضیح
        lines:
          type: array
          items:
            $ref: '#/components/schemas/JournalLineInputRequest'
          title: سطرهای سند
        post_immediately:
          type: boolean
          default: false
          description: اگر true باشد، سند بلافاصله قطعی می‌شود.
      required:
      - description
      - entry_date
      - fiscal_year
      - lines
      example:
        fiscal_year: '1405'
        entry_date: '2026-09-23'
        description: توضیح تکمیلی این رکورد
        lines:
        - account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          debit: 0
          credit: 0
          description: ''
        post_immediately: false
    CreateLoanRequest:
      type: object
      description: |-
        ثبت امانت.

        امانت فقط برای نسخه «موجود» و «قابل امانت» مجاز است (بخش ۷.۱۰).
      properties:
        copy:
          type: string
          format: uuid
          title: شناسه نسخه
        borrower_person:
          type: string
          format: uuid
          title: شناسه شخص امانت‌گیرنده
        loan_days:
          type: integer
          maximum: 180
          minimum: 1
          default: 14
          title: مدت امانت (روز)
      required:
      - borrower_person
      - copy
      example:
        copy: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        borrower_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        loan_days: 14
    CurrencyEnum:
      enum:
      - IRR
      - IRT
      - USD
      - EUR
      type: string
      description: |-
        * `IRR` - ریال ایران
        * `IRT` - تومان ایران
        * `USD` - دلار آمریکا
        * `EUR` - یورو
    CurrentUser:
      type: object
      description: پروفایل کاربر جاری به همراه مجوزها و محیط‌های کاری مجاز.
      properties:
        id:
          type: string
          format: uuid
          title: شناسه
        username:
          type: string
          title: نام کاربری
        displayName:
          type: string
          title: نام نمایشی
        email:
          type: string
          title: رایانامه
        mobile:
          type: string
          title: تلفن همراه
        personId:
          type: string
          format: uuid
          nullable: true
          title: شناسه شخص
        tenantId:
          type: string
          format: uuid
          nullable: true
          title: شناسه سازمان
        status:
          type: string
          title: وضعیت
        mfaEnabled:
          type: boolean
          title: احراز هویت دومرحله‌ای فعال است
        mustChangePassword:
          type: boolean
          title: تغییر اجباری گذرواژه در ورود بعدی
        isSuperuser:
          type: boolean
          title: کاربر ارشد سامانه
        roles:
          type: array
          items:
            type: string
          title: نقش‌ها
        permissions:
          type: array
          items:
            type: string
          title: مجوزها
        contexts:
          type: array
          items:
            $ref: '#/components/schemas/ScopeContext'
          title: Contextهای کاری در دسترس
      required:
      - contexts
      - displayName
      - email
      - id
      - isSuperuser
      - mfaEnabled
      - mobile
      - mustChangePassword
      - permissions
      - personId
      - roles
      - status
      - tenantId
      - username
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        username: s.mohammadi
        displayName: سارا محمدی
        email: user@example.school
        mobile: 09121234567
        personId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        tenantId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
        mfaEnabled: true
        mustChangePassword: true
        isSuperuser: true
        roles:
        - متن نمونه
        permissions:
        - متن نمونه
        contexts:
        - roleCode: SAMPLE-001
          roleTitle: عنوان نمونه
          scopeType: متن نمونه
          scopeId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    DataClassificationEnum:
      enum:
      - PUBLIC
      - INTERNAL
      - CONFIDENTIAL
      - RESTRICTED
      type: string
      description: |-
        * `PUBLIC` - عمومی
        * `INTERNAL` - داخلی
        * `CONFIDENTIAL` - محرمانه
        * `RESTRICTED` - بسیار حساس
    DecideStepDecisionEnum:
      enum:
      - APPROVED
      - REJECTED
      - RETURNED
      - DELEGATED
      type: string
      description: |-
        * `APPROVED` - APPROVED
        * `REJECTED` - REJECTED
        * `RETURNED` - RETURNED
        * `DELEGATED` - DELEGATED
    DecideStepRequest:
      type: object
      properties:
        decision:
          $ref: '#/components/schemas/DecideStepDecisionEnum'
          title: تصمیم
        comment:
          type: string
          maxLength: 1000
          title: توضیح
        on_behalf_of_id:
          type: string
          format: uuid
          nullable: true
          description: در صورت تصمیم به نیابت
      required:
      - decision
      example:
        decision: APPROVED
        comment: توضیح کوتاه ثبت‌کننده
        on_behalf_of_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    DeliveryAttempt:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        notification:
          type: string
          format: uuid
          title: اعلان
          readOnly: true
        attempt_no:
          type: integer
          readOnly: true
          title: شماره تلاش
        provider:
          type: string
          readOnly: true
          title: ارائه‌دهنده
        provider_reference:
          type: string
          readOnly: true
          title: ارجاع ارائه‌دهنده
        result:
          allOf:
          - $ref: '#/components/schemas/ResultEnum'
          readOnly: true
          title: نتیجه
        error_message:
          type: string
          readOnly: true
          title: پیام خطا
        attempted_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان تلاش
      required:
      - attempt_no
      - attempted_at
      - error_message
      - id
      - notification
      - provider
      - provider_reference
      - result
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        notification: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt_no: 3
        provider: متن نمونه
        provider_reference: متن نمونه
        result: SUCCESS
        error_message: متن نمونه
        attempted_at: '2026-09-23T08:30:00+03:30'
    DifficultyEnum:
      enum:
      - VERY_EASY
      - EASY
      - MEDIUM
      - HARD
      - VERY_HARD
      type: string
      description: |-
        * `VERY_EASY` - خیلی آسان
        * `EASY` - آسان
        * `MEDIUM` - متوسط
        * `HARD` - دشوار
        * `VERY_HARD` - خیلی دشوار
    DiscountAward:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        agreement:
          type: string
          format: uuid
          title: توافق‌نامه
        discount_type:
          allOf:
          - $ref: '#/components/schemas/DiscountTypeEnum'
          title: نوع تخفیف
        discount_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع تخفیف
        percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد تخفیف
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ ثابت تخفیف
        reason:
          type: string
          title: علت
          maxLength: 400
        approval_status:
          allOf:
          - $ref: '#/components/schemas/PendingApprovalStateEnum'
          readOnly: true
          title: وضعیت تأیید
        approved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - agreement
      - approval_status
      - approved_at
      - created_at
      - discount_type
      - discount_type_display
      - id
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        discount_type: SIBLING
        discount_type_display: خواهر/برادر
        percent: '18.50'
        amount: 12500000
        reason: اصلاح اطلاعات پس از بررسی پرونده
        approval_status: PENDING
        approved_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    DiscountAwardRequest:
      type: object
      properties:
        agreement:
          type: string
          format: uuid
          title: توافق‌نامه
        discount_type:
          allOf:
          - $ref: '#/components/schemas/DiscountTypeEnum'
          title: نوع تخفیف
        percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد تخفیف
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ ثابت تخفیف
        reason:
          type: string
          title: علت
          maxLength: 400
      required:
      - agreement
      - discount_type
      example:
        agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        discount_type: SIBLING
        percent: '18.50'
        amount: 12500000
        reason: اصلاح اطلاعات پس از بررسی پرونده
    DiscountTypeEnum:
      enum:
      - SIBLING
      - SCHOLARSHIP
      - STAFF_CHILD
      - FINANCIAL_AID
      - EARLY_PAYMENT
      - SPECIAL
      type: string
      description: |-
        * `SIBLING` - تخفیف خواهر/برادر
        * `SCHOLARSHIP` - بورسیه
        * `STAFF_CHILD` - فرزند کارکنان
        * `FINANCIAL_AID` - کمک‌هزینه
        * `EARLY_PAYMENT` - تخفیف پرداخت زودهنگام
        * `SPECIAL` - تخفیف موردی
    DocumentTypeEnum:
      enum:
      - RECEIPT
      - ISSUE
      - TRANSFER_OUT
      - TRANSFER_IN
      - RETURN_TO_VENDOR
      - RETURN_FROM_USE
      - ADJUSTMENT_IN
      - ADJUSTMENT_OUT
      - COUNT
      type: string
      description: |-
        * `RECEIPT` - رسید
        * `ISSUE` - حواله مصرف
        * `TRANSFER_OUT` - انتقال خروج
        * `TRANSFER_IN` - انتقال ورود
        * `RETURN_TO_VENDOR` - برگشت به فروشنده
        * `RETURN_FROM_USE` - برگشت از مصرف
        * `ADJUSTMENT_IN` - تعدیل مثبت
        * `ADJUSTMENT_OUT` - تعدیل منفی
        * `COUNT` - انبارگردانی
    DropPolicyEnum:
      enum:
      - NONE
      - DROP_LOWEST
      - DROP_LOWEST_TWO
      type: string
      description: |-
        * `NONE` - بدون حذف
        * `DROP_LOWEST` - حذف پایین‌ترین نمره
        * `DROP_LOWEST_TWO` - حذف دو نمره پایین
    DutyEnum:
      enum:
      - TEACHER
      - ASSISTANT
      - SUBSTITUTE
      - OBSERVER
      type: string
      description: |-
        * `TEACHER` - مدرس
        * `ASSISTANT` - دستیار
        * `SUBSTITUTE` - جانشین
        * `OBSERVER` - ناظر
    Employee:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        person_detail:
          allOf:
          - $ref: '#/components/schemas/PersonList'
          readOnly: true
          title: مشخصات شخص
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        employee_no:
          type: string
          title: شماره پرسنلی
          maxLength: 30
        hired_on:
          type: string
          format: date
          title: تاریخ استخدام
        terminated_on:
          type: string
          format: date
          nullable: true
          title: تاریخ پایان همکاری
        status:
          allOf:
          - $ref: '#/components/schemas/EmployeeStatusEnum'
          title: وضعیت
        bank_account_masked:
          type: string
          title: شماره حساب (ماسک‌شده)
          maxLength: 40
        emergency_contact:
          type: string
          title: تماس اضطراری
          maxLength: 200
        contracts:
          type: array
          items:
            $ref: '#/components/schemas/EmploymentContract'
          readOnly: true
          title: قراردادها
        assignments:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeAssignment'
          readOnly: true
          title: انتساب‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - assignments
      - contracts
      - created_at
      - employee_no
      - full_name
      - hired_on
      - id
      - person
      - person_detail
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          first_name: سارا
          last_name: محمدی
          full_name: سارا محمدی
          national_id: 0012345678
          birth_date: '2012-09-26'
          gender: MALE
          gender_display: مرد
          status: ACTIVE
        full_name: سارا محمدی
        employee_no: SAMPLE-001
        hired_on: '2026-09-23'
        terminated_on: '2026-09-23'
        status: ONBOARDING
        bank_account_masked: متن نمونه
        emergency_contact: متن نمونه
        contracts:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee_name: مریم رضایی
          contract_no: HR-1405-0042
          contract_type: PERMANENT
          contract_type_display: رسمی/دائم
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          workload_ratio: '18.50'
          base_salary_amount: 2500000
          currency: IRR
          status: DRAFT
          termination_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        assignments:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          position: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          position_title: عنوان نمونه
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus_name: شعبه مرکزی
          allocation_percent: '18.50'
          is_primary: true
          cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    EmployeeAssignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        employee:
          type: string
          format: uuid
          title: کارمند
        position:
          type: string
          format: uuid
          title: سِمت
        position_title:
          type: string
          readOnly: true
          title: عنوان سمت
        campus:
          type: string
          format: uuid
          title: شعبه
        campus_name:
          type: string
          readOnly: true
          title: نام شعبه
        allocation_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد تخصیص
        is_primary:
          type: boolean
          title: انتساب اصلی
        cost_center_id:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus
      - campus_name
      - created_at
      - effective_from
      - employee
      - id
      - position
      - position_title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        position: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        position_title: عنوان نمونه
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus_name: شعبه مرکزی
        allocation_percent: '18.50'
        is_primary: true
        cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    EmployeeAssignmentRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        position:
          type: string
          format: uuid
          title: سِمت
        campus:
          type: string
          format: uuid
          title: شعبه
        allocation_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد تخصیص
        is_primary:
          type: boolean
          title: انتساب اصلی
        cost_center_id:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      required:
      - campus
      - effective_from
      - employee
      - position
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        position: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        allocation_percent: '18.50'
        is_primary: true
        cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    EmployeeAttendance:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        employee:
          type: string
          format: uuid
          title: کارمند
        employee_name:
          type: string
          readOnly: true
          title: نام کارمند
        shift:
          type: string
          format: uuid
          title: شیفت
          nullable: true
        work_date:
          type: string
          format: date
          title: تاریخ کاری
        check_in_at:
          type: string
          format: date-time
          nullable: true
          title: زمان ورود
        check_out_at:
          type: string
          format: date-time
          nullable: true
          title: زمان خروج
        worked_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه کارکرد
        overtime_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه اضافه‌کاری
        late_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه تأخیر
        source:
          allOf:
          - $ref: '#/components/schemas/EmployeeAttendanceSourceEnum'
          title: منبع
        status:
          type: string
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - employee
      - employee_name
      - id
      - updated_at
      - version
      - work_date
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_name: مریم رضایی
        shift: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        work_date: '2026-09-23'
        check_in_at: '2026-09-23T08:30:00+03:30'
        check_out_at: '2026-09-23T08:30:00+03:30'
        worked_minutes: 45
        overtime_minutes: 45
        late_minutes: 45
        source: DEVICE
        status: متن نمونه
        note: یادداشت داخلی
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    EmployeeAttendanceRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        shift:
          type: string
          format: uuid
          title: شیفت
          nullable: true
        work_date:
          type: string
          format: date
          title: تاریخ کاری
        check_in_at:
          type: string
          format: date-time
          nullable: true
          title: زمان ورود
        check_out_at:
          type: string
          format: date-time
          nullable: true
          title: زمان خروج
        worked_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه کارکرد
        overtime_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه اضافه‌کاری
        late_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه تأخیر
        source:
          allOf:
          - $ref: '#/components/schemas/EmployeeAttendanceSourceEnum'
          title: منبع
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
      required:
      - employee
      - work_date
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        shift: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        work_date: '2026-09-23'
        check_in_at: '2026-09-23T08:30:00+03:30'
        check_out_at: '2026-09-23T08:30:00+03:30'
        worked_minutes: 45
        overtime_minutes: 45
        late_minutes: 45
        source: DEVICE
        status: متن نمونه
        note: یادداشت داخلی
    EmployeeAttendanceSourceEnum:
      enum:
      - DEVICE
      - MANUAL
      - MOBILE
      - IMPORT
      type: string
      description: |-
        * `DEVICE` - دستگاه تردد
        * `MANUAL` - ثبت دستی
        * `MOBILE` - اپلیکیشن موبایل
        * `IMPORT` - ورود گروهی
    EmployeeList:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        employee_no:
          type: string
          title: شماره پرسنلی
          maxLength: 30
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        national_id:
          type: string
          readOnly: true
          title: کد ملی
        hired_on:
          type: string
          format: date
          title: تاریخ استخدام
        status:
          allOf:
          - $ref: '#/components/schemas/EmployeeStatusEnum'
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        primary_position:
          type: string
          nullable: true
          readOnly: true
          title: سمت اصلی
        is_teacher:
          type: boolean
          readOnly: true
          title: معلم است
      required:
      - employee_no
      - full_name
      - hired_on
      - id
      - is_teacher
      - national_id
      - primary_position
      - status_display
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_no: SAMPLE-001
        full_name: سارا محمدی
        national_id: 0012345678
        hired_on: '2026-09-23'
        status: ONBOARDING
        status_display: در حال جذب
        primary_position: متن نمونه
        is_teacher: true
    EmployeeRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        employee_no:
          type: string
          minLength: 1
          title: شماره پرسنلی
          maxLength: 30
        hired_on:
          type: string
          format: date
          title: تاریخ استخدام
        terminated_on:
          type: string
          format: date
          nullable: true
          title: تاریخ پایان همکاری
        status:
          allOf:
          - $ref: '#/components/schemas/EmployeeStatusEnum'
          title: وضعیت
        bank_account_masked:
          type: string
          title: شماره حساب (ماسک‌شده)
          maxLength: 40
        emergency_contact:
          type: string
          title: تماس اضطراری
          maxLength: 200
      required:
      - employee_no
      - hired_on
      - person
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_no: SAMPLE-001
        hired_on: '2026-09-23'
        terminated_on: '2026-09-23'
        status: ONBOARDING
        bank_account_masked: متن نمونه
        emergency_contact: متن نمونه
    EmployeeStatusEnum:
      enum:
      - ONBOARDING
      - ACTIVE
      - ON_LEAVE
      - SUSPENDED
      - TERMINATED
      type: string
      description: |-
        * `ONBOARDING` - در حال جذب
        * `ACTIVE` - شاغل
        * `ON_LEAVE` - در مرخصی
        * `SUSPENDED` - تعلیق
        * `TERMINATED` - پایان همکاری
    EmploymentContract:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        employee:
          type: string
          format: uuid
          title: کارمند
        employee_name:
          type: string
          readOnly: true
          title: نام کارمند
        contract_no:
          type: string
          title: شماره قرارداد
          maxLength: 40
        contract_type:
          allOf:
          - $ref: '#/components/schemas/ContractTypeEnum'
          title: نوع قرارداد
        contract_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع قرارداد
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          nullable: true
          title: تاریخ پایان
        workload_ratio:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: ضریب اشتغال
          description: ۱ یعنی تمام‌وقت
        base_salary_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: حقوق پایه
          description: به کوچک‌ترین واحد پولی (ریال)
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        status:
          allOf:
          - $ref: '#/components/schemas/ContractStatusEnum'
          title: وضعیت
        termination_reason:
          type: string
          title: علت خاتمه
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - contract_no
      - contract_type
      - contract_type_display
      - created_at
      - employee
      - employee_name
      - id
      - starts_on
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_name: مریم رضایی
        contract_no: HR-1405-0042
        contract_type: PERMANENT
        contract_type_display: رسمی/دائم
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        workload_ratio: '18.50'
        base_salary_amount: 2500000
        currency: IRR
        status: DRAFT
        termination_reason: توضیح نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    EmploymentContractRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        contract_no:
          type: string
          minLength: 1
          title: شماره قرارداد
          maxLength: 40
        contract_type:
          allOf:
          - $ref: '#/components/schemas/ContractTypeEnum'
          title: نوع قرارداد
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          nullable: true
          title: تاریخ پایان
        workload_ratio:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: ضریب اشتغال
          description: ۱ یعنی تمام‌وقت
        base_salary_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: حقوق پایه
          description: به کوچک‌ترین واحد پولی (ریال)
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        status:
          allOf:
          - $ref: '#/components/schemas/ContractStatusEnum'
          title: وضعیت
        termination_reason:
          type: string
          title: علت خاتمه
          maxLength: 300
      required:
      - contract_no
      - contract_type
      - employee
      - starts_on
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        contract_no: HR-1405-0042
        contract_type: PERMANENT
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        workload_ratio: '18.50'
        base_salary_amount: 2500000
        currency: IRR
        status: DRAFT
        termination_reason: توضیح نمونه
    Enrollment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        academic_year_title:
          type: string
          readOnly: true
          title: عنوان سال تحصیلی
        campus:
          type: string
          format: uuid
          title: شعبه
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        grade_level_title:
          type: string
          readOnly: true
          title: عنوان پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        admission_application:
          type: string
          format: uuid
          title: درخواست پذیرش
          nullable: true
        enrollment_no:
          type: string
          readOnly: true
          title: شماره ثبت‌نام
        enrolled_on:
          type: string
          format: date
          title: تاریخ ثبت‌نام
        status:
          allOf:
          - $ref: '#/components/schemas/EnrollmentStatusEnum'
          readOnly: true
          default: PENDING_DOCUMENTS
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        current_class_code:
          type: string
          nullable: true
          readOnly: true
          title: کد کلاس فعلی
        exit_date:
          type: string
          format: date
          nullable: true
          title: تاریخ خروج
        exit_reason:
          type: string
          title: علت خروج
          maxLength: 300
        class_memberships:
          type: array
          items:
            $ref: '#/components/schemas/ClassMembership'
          readOnly: true
          title: عضویت‌های کلاسی
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - academic_year
      - academic_year_title
      - campus
      - class_memberships
      - created_at
      - current_class_code
      - enrolled_on
      - enrollment_no
      - grade_level
      - grade_level_title
      - id
      - status
      - status_display
      - student
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year_title: عنوان نمونه
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        grade_level_title: پایه هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        admission_application: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment_no: SAMPLE-001
        enrolled_on: '2026-09-23'
        status: PENDING_DOCUMENTS
        status_display: در انتظار مدارک
        current_class_code: SAMPLE-001
        exit_date: '2027-03-20'
        exit_reason: توضیح نمونه
        class_memberships:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          class_group_code: G7-A
          is_primary: true
          status: ACTIVE
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          exit_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    EnrollmentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        campus:
          type: string
          format: uuid
          title: شعبه
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        admission_application:
          type: string
          format: uuid
          title: درخواست پذیرش
          nullable: true
        enrolled_on:
          type: string
          format: date
          title: تاریخ ثبت‌نام
        exit_date:
          type: string
          format: date
          nullable: true
          title: تاریخ خروج
        exit_reason:
          type: string
          title: علت خروج
          maxLength: 300
      required:
      - academic_year
      - campus
      - enrolled_on
      - grade_level
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        admission_application: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrolled_on: '2026-09-23'
        exit_date: '2027-03-20'
        exit_reason: توضیح نمونه
    EnrollmentStatusEnum:
      enum:
      - PENDING_DOCUMENTS
      - PENDING_FINANCE
      - PENDING_PLACEMENT
      - ACTIVE
      - SUSPENDED
      - TRANSFERRED
      - WITHDRAWN
      - COMPLETED
      - GRADUATED
      - CANCELLED
      type: string
      description: |-
        * `PENDING_DOCUMENTS` - در انتظار مدارک
        * `PENDING_FINANCE` - در انتظار تسویه مالی
        * `PENDING_PLACEMENT` - در انتظار تخصیص کلاس
        * `ACTIVE` - فعال
        * `SUSPENDED` - تعلیق
        * `TRANSFERRED` - انتقالی
        * `WITHDRAWN` - ترک تحصیل
        * `COMPLETED` - پایان سال
        * `GRADUATED` - فارغ‌التحصیل
        * `CANCELLED` - لغوشده
    ErrorResponse:
      type: object
      description: قالب استاندارد خطای همه Endpointها.
      properties:
        code:
          type: string
          description: کد معنایی خطا؛ فرانت باید روی این مقدار تصمیم بگیرد نه روی
            متن پیام.
        message:
          type: string
          description: پیام قابل نمایش به کاربر (فارسی)
        correlationId:
          type: string
          description: شناسه ردیابی درخواست؛ در گزارش خطا به پشتیبانی ارسال شود.
        fieldErrors:
          type: array
          items:
            $ref: '#/components/schemas/FieldError'
          title: خطاهای فیلدی
        retryable:
          type: boolean
          description: آیا تکرار همان درخواست می‌تواند موفق شود؟
      required:
      - code
      - correlationId
      - message
      - retryable
      example:
        code: SAMPLE-001
        message: عملیات با موفقیت انجام شد.
        correlationId: 8c4a1f2e
        fieldErrors:
        - field: متن نمونه
          reason: اصلاح اطلاعات پس از بررسی پرونده
        retryable: true
    Exam:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        code:
          type: string
          title: کد
          maxLength: 40
        title:
          type: string
          title: عنوان
          maxLength: 200
        mode:
          allOf:
          - $ref: '#/components/schemas/ModeEnum'
          title: حالت
        mode_display:
          type: string
          readOnly: true
          title: برچسب فارسی حالت
        purpose:
          allOf:
          - $ref: '#/components/schemas/PurposeEnum'
          title: هدف
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        total_question_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          title: مجموع بارم سؤالات
        instructions:
          type: string
          title: دستورالعمل
        shuffle_questions:
          type: boolean
          title: ترتیب تصادفی سؤالات
        shuffle_options:
          type: boolean
          title: ترتیب تصادفی گزینه‌ها
        allow_backtrack:
          type: boolean
          title: امکان بازگشت به سؤال قبلی
        show_result_immediately:
          type: boolean
          title: نمایش فوری نتیجه
        status:
          allOf:
          - $ref: '#/components/schemas/ExamStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار
        results_published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار نتایج
        appeal_deadline:
          type: string
          format: date-time
          nullable: true
          title: مهلت اعتراض
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
        section_count:
          type: integer
          readOnly: true
          title: تعداد بخش‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - class_group_code
      - code
      - course_offering
      - course_title
      - created_at
      - id
      - mode
      - mode_display
      - published_at
      - results_published_at
      - section_count
      - status
      - status_display
      - title
      - total_question_score
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        class_group_code: G7-A
        code: SAMPLE-001
        title: عنوان نمونه
        mode: ONLINE
        mode_display: آنلاین
        purpose: FORMATIVE
        max_score: '18.50'
        total_question_score: '18.50'
        instructions: متن نمونه
        shuffle_questions: true
        shuffle_options: true
        allow_backtrack: true
        show_result_immediately: true
        status: DRAFT
        status_display: پیش‌نویس
        published_at: '2026-09-23T08:30:00+03:30'
        results_published_at: '2026-09-23T08:30:00+03:30'
        appeal_deadline: '2027-03-20T12:00:00+03:30'
        cancel_reason: توضیح نمونه
        section_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ExamAttempt:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        registration:
          type: string
          format: uuid
          title: ثبت‌نام آزمون
          readOnly: true
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        exam_title:
          type: string
          readOnly: true
          title: عنوان آزمون
        attempt_no:
          type: integer
          readOnly: true
          default: 1
          title: شماره تلاش
        started_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان شروع
        submitted_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ارسال
        last_saved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان آخرین ذخیره
        expires_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان پایان مجاز
        remaining_seconds:
          type: integer
          nullable: true
          readOnly: true
          title: ثانیه‌های باقی‌مانده
        auto_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره خودکار
        manual_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره دستی
        final_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره نهایی
        status:
          allOf:
          - $ref: '#/components/schemas/AttemptStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        calculation_version:
          type: integer
          readOnly: true
          title: نسخه محاسبه
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - attempt_no
      - auto_score
      - calculation_version
      - created_at
      - exam_title
      - expires_at
      - final_score
      - id
      - last_saved_at
      - manual_score
      - registration
      - remaining_seconds
      - started_at
      - status
      - status_display
      - student_name
      - submitted_at
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        registration: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        exam_title: آزمون میان‌ترم ریاضی
        attempt_no: 1
        started_at: '2026-09-23T08:30:00+03:30'
        submitted_at: '2026-09-23T08:30:00+03:30'
        last_saved_at: '2026-09-23T08:30:00+03:30'
        expires_at: '2027-03-20T12:00:00+03:30'
        remaining_seconds: 120
        auto_score: '18.50'
        manual_score: '18.50'
        final_score: '18.50'
        status: CREATED
        status_display: ایجادشده
        calculation_version: 3
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ExamDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        code:
          type: string
          title: کد
          maxLength: 40
        title:
          type: string
          title: عنوان
          maxLength: 200
        mode:
          allOf:
          - $ref: '#/components/schemas/ModeEnum'
          title: حالت
        mode_display:
          type: string
          readOnly: true
          title: برچسب فارسی حالت
        purpose:
          allOf:
          - $ref: '#/components/schemas/PurposeEnum'
          title: هدف
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        total_question_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          title: مجموع بارم سؤالات
        instructions:
          type: string
          title: دستورالعمل
        shuffle_questions:
          type: boolean
          title: ترتیب تصادفی سؤالات
        shuffle_options:
          type: boolean
          title: ترتیب تصادفی گزینه‌ها
        allow_backtrack:
          type: boolean
          title: امکان بازگشت به سؤال قبلی
        show_result_immediately:
          type: boolean
          title: نمایش فوری نتیجه
        status:
          allOf:
          - $ref: '#/components/schemas/ExamStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار
        results_published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار نتایج
        appeal_deadline:
          type: string
          format: date-time
          nullable: true
          title: مهلت اعتراض
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
        section_count:
          type: integer
          readOnly: true
          title: تعداد بخش‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
        sections:
          type: array
          items:
            $ref: '#/components/schemas/ExamSection'
          readOnly: true
          title: بخش‌ها
        sessions:
          type: array
          items:
            $ref: '#/components/schemas/ExamSession'
          readOnly: true
          title: جلسات
      required:
      - class_group_code
      - code
      - course_offering
      - course_title
      - created_at
      - id
      - mode
      - mode_display
      - published_at
      - results_published_at
      - section_count
      - sections
      - sessions
      - status
      - status_display
      - title
      - total_question_score
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        class_group_code: G7-A
        code: SAMPLE-001
        title: عنوان نمونه
        mode: ONLINE
        mode_display: آنلاین
        purpose: FORMATIVE
        max_score: '18.50'
        total_question_score: '18.50'
        instructions: متن نمونه
        shuffle_questions: true
        shuffle_options: true
        allow_backtrack: true
        show_result_immediately: true
        status: DRAFT
        status_display: پیش‌نویس
        published_at: '2026-09-23T08:30:00+03:30'
        results_published_at: '2026-09-23T08:30:00+03:30'
        appeal_deadline: '2027-03-20T12:00:00+03:30'
        cancel_reason: توضیح نمونه
        section_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
        sections:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          instructions: متن نمونه
          display_order: 1
          time_limit_minutes: 45
          question_count: 12
          questions: []
        sessions:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam_title: آزمون میان‌ترم ریاضی
          room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          room_code: R-204
          title: عنوان نمونه
          opens_at: '2026-09-23T08:30:00+03:30'
          closes_at: '2026-09-23T08:30:00+03:30'
          duration_minutes: 45
          attempt_limit: 3
          proctor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: PLANNED
          registration_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    ExamQuestion:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        section:
          type: string
          format: uuid
          title: بخش
        question_version:
          type: string
          format: uuid
          title: نسخه سؤال
        question_body:
          type: string
          readOnly: true
          title: متن سؤال
        question_type:
          type: string
          readOnly: true
          title: نوع سؤال
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        is_required:
          type: boolean
          title: الزامی
      required:
      - id
      - question_body
      - question_type
      - question_version
      - section
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        section: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_body: متن نمونه
        question_type: متن نمونه
        score: '18.50'
        display_order: 1
        is_required: true
    ExamQuestionRequest:
      type: object
      properties:
        section:
          type: string
          format: uuid
          title: بخش
        question_version:
          type: string
          format: uuid
          title: نسخه سؤال
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        is_required:
          type: boolean
          title: الزامی
      required:
      - question_version
      - section
      example:
        section: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        score: '18.50'
        display_order: 1
        is_required: true
    ExamRegistration:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        exam_session:
          type: string
          format: uuid
          title: جلسه آزمون
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        seat_no:
          type: string
          title: شماره صندلی
          maxLength: 20
        extra_time_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: زمان اضافه فردی
        registration_status:
          allOf:
          - $ref: '#/components/schemas/RegistrationStatusEnum'
          title: وضعیت ثبت‌نام
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - enrollment
      - exam_session
      - id
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        exam_session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        seat_no: SAMPLE-001
        extra_time_minutes: 45
        registration_status: REGISTERED
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ExamRegistrationRequest:
      type: object
      properties:
        exam_session:
          type: string
          format: uuid
          title: جلسه آزمون
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        seat_no:
          type: string
          title: شماره صندلی
          maxLength: 20
        extra_time_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: زمان اضافه فردی
        registration_status:
          allOf:
          - $ref: '#/components/schemas/RegistrationStatusEnum'
          title: وضعیت ثبت‌نام
      required:
      - enrollment
      - exam_session
      example:
        exam_session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        seat_no: SAMPLE-001
        extra_time_minutes: 45
        registration_status: REGISTERED
    ExamRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        mode:
          allOf:
          - $ref: '#/components/schemas/ModeEnum'
          title: حالت
        purpose:
          allOf:
          - $ref: '#/components/schemas/PurposeEnum'
          title: هدف
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        instructions:
          type: string
          title: دستورالعمل
        shuffle_questions:
          type: boolean
          title: ترتیب تصادفی سؤالات
        shuffle_options:
          type: boolean
          title: ترتیب تصادفی گزینه‌ها
        allow_backtrack:
          type: boolean
          title: امکان بازگشت به سؤال قبلی
        show_result_immediately:
          type: boolean
          title: نمایش فوری نتیجه
        appeal_deadline:
          type: string
          format: date-time
          nullable: true
          title: مهلت اعتراض
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
      required:
      - code
      - course_offering
      - mode
      - title
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        mode: ONLINE
        purpose: FORMATIVE
        max_score: '18.50'
        instructions: متن نمونه
        shuffle_questions: true
        shuffle_options: true
        allow_backtrack: true
        show_result_immediately: true
        appeal_deadline: '2027-03-20T12:00:00+03:30'
        cancel_reason: توضیح نمونه
    ExamSection:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        exam:
          type: string
          format: uuid
          title: آزمون
        title:
          type: string
          title: عنوان
          maxLength: 150
        instructions:
          type: string
          title: دستورالعمل
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        time_limit_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: محدودیت زمان (دقیقه)
        question_count:
          type: integer
          readOnly: true
          title: تعداد سؤالات
        questions:
          type: array
          items:
            $ref: '#/components/schemas/ExamQuestion'
          readOnly: true
          title: سؤالات
      required:
      - exam
      - id
      - question_count
      - questions
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        instructions: متن نمونه
        display_order: 1
        time_limit_minutes: 45
        question_count: 12
        questions:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          section: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_body: متن نمونه
          question_type: متن نمونه
          score: '18.50'
          display_order: 1
          is_required: true
    ExamSectionRequest:
      type: object
      properties:
        exam:
          type: string
          format: uuid
          title: آزمون
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        instructions:
          type: string
          title: دستورالعمل
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        time_limit_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: محدودیت زمان (دقیقه)
      required:
      - exam
      - title
      example:
        exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        instructions: متن نمونه
        display_order: 1
        time_limit_minutes: 45
    ExamSession:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        exam:
          type: string
          format: uuid
          title: آزمون
        exam_title:
          type: string
          readOnly: true
          title: عنوان آزمون
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        room_code:
          type: string
          readOnly: true
          title: کد اتاق
        title:
          type: string
          title: عنوان
          maxLength: 150
        opens_at:
          type: string
          format: date-time
          title: شروع پنجره
        closes_at:
          type: string
          format: date-time
          title: پایان پنجره
        duration_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مدت آزمون
        attempt_limit:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: سقف تلاش
        proctor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: مراقب آزمون
        status:
          allOf:
          - $ref: '#/components/schemas/ExamSessionStatusEnum'
          title: وضعیت
        registration_count:
          type: integer
          readOnly: true
          title: تعداد ثبت‌نام‌شدگان
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - closes_at
      - created_at
      - duration_minutes
      - exam
      - exam_title
      - id
      - opens_at
      - registration_count
      - room_code
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        exam_title: آزمون میان‌ترم ریاضی
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room_code: R-204
        title: عنوان نمونه
        opens_at: '2026-09-23T08:30:00+03:30'
        closes_at: '2026-09-23T08:30:00+03:30'
        duration_minutes: 45
        attempt_limit: 3
        proctor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: PLANNED
        registration_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ExamSessionRequest:
      type: object
      properties:
        exam:
          type: string
          format: uuid
          title: آزمون
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        title:
          type: string
          title: عنوان
          maxLength: 150
        opens_at:
          type: string
          format: date-time
          title: شروع پنجره
        closes_at:
          type: string
          format: date-time
          title: پایان پنجره
        duration_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مدت آزمون
        attempt_limit:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: سقف تلاش
        proctor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: مراقب آزمون
        status:
          allOf:
          - $ref: '#/components/schemas/ExamSessionStatusEnum'
          title: وضعیت
      required:
      - closes_at
      - duration_minutes
      - exam
      - opens_at
      example:
        exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        opens_at: '2026-09-23T08:30:00+03:30'
        closes_at: '2026-09-23T08:30:00+03:30'
        duration_minutes: 45
        attempt_limit: 3
        proctor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: PLANNED
    ExamSessionStatusEnum:
      enum:
      - PLANNED
      - OPEN
      - CLOSED
      - CANCELLED
      type: string
      description: |-
        * `PLANNED` - برنامه‌ریزی‌شده
        * `OPEN` - باز
        * `CLOSED` - بسته
        * `CANCELLED` - لغوشده
    ExamStatusEnum:
      enum:
      - DRAFT
      - UNDER_REVIEW
      - APPROVED
      - SCHEDULED
      - PUBLISHED
      - IN_PROGRESS
      - SUBMISSION_CLOSED
      - GRADING
      - MODERATION
      - RESULTS_READY
      - PUBLISHED_RESULTS
      - ARCHIVED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `UNDER_REVIEW` - در حال بازبینی
        * `APPROVED` - تأییدشده
        * `SCHEDULED` - زمان‌بندی‌شده
        * `PUBLISHED` - منتشرشده
        * `IN_PROGRESS` - در حال اجرا
        * `SUBMISSION_CLOSED` - پایان مهلت تحویل
        * `GRADING` - در حال تصحیح
        * `MODERATION` - بازبینی نمرات
        * `RESULTS_READY` - نتایج آماده
        * `PUBLISHED_RESULTS` - نتایج منتشرشده
        * `ARCHIVED` - بایگانی
        * `CANCELLED` - لغوشده
    FamilyBalance:
      type: object
      description: |-
        مانده خانواده (بخش ۱۲.۲).

        همه دانش‌آموزان مرتبط با یک مسئول مالی و مانده هر کدام.
      properties:
        guardianId:
          type: string
          format: uuid
          title: شناسه ولی
        guardianName:
          type: string
          title: نام ولی
        currency:
          type: string
          title: واحد پول (ISO 4217)
        totalInvoiced:
          type: integer
          title: جمع صورتحساب‌ها (ریال)
        totalPaid:
          type: integer
          title: جمع پرداختی‌ها (ریال)
        totalBalance:
          type: integer
          title: مجموع مانده
        unallocatedCredit:
          type: integer
          description: پیش‌دریافت تخصیص‌نیافته
        students:
          type: array
          items:
            type: object
            additionalProperties: {}
          title: دانش‌آموزان
      required:
      - currency
      - guardianId
      - guardianName
      - students
      - totalBalance
      - totalInvoiced
      - totalPaid
      - unallocatedCredit
      example:
        guardianId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardianName: علی محمدی
        currency: IRR
        totalInvoiced: 12
        totalPaid: 12
        totalBalance: 12
        unallocatedCredit: 3
        students:
        - {}
    FeePlan:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        title:
          type: string
          title: عنوان
          maxLength: 200
        currency:
          allOf:
          - $ref: '#/components/schemas/CurrencyEnum'
          title: واحد پول
        status:
          type: string
          title: وضعیت
          maxLength: 20
        total_amount:
          type: integer
          readOnly: true
          title: مبلغ کل (ریال)
        items:
          type: array
          items:
            $ref: '#/components/schemas/FeePlanItem'
          readOnly: true
          title: اقلام
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - academic_year
      - created_at
      - id
      - items
      - title
      - total_amount
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        title: عنوان نمونه
        currency: IRR
        status: متن نمونه
        total_amount: 45000000
        items:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_type: REGISTRATION
          fee_type_display: ثبت‌نام
          title: عنوان نمونه
          amount: 12500000
          recurrence: ONE_TIME
          is_mandatory: true
          revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    FeePlanItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        fee_plan:
          type: string
          format: uuid
          title: طرح شهریه
        fee_type:
          allOf:
          - $ref: '#/components/schemas/FeeTypeEnum'
          title: نوع شهریه
        fee_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع تعرفه
        title:
          type: string
          title: عنوان
          maxLength: 200
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ (ریال)
        recurrence:
          allOf:
          - $ref: '#/components/schemas/RecurrenceEnum'
          title: تکرار
        is_mandatory:
          type: boolean
          title: اجباری
        revenue_account:
          type: string
          format: uuid
          title: حساب درآمد
          nullable: true
      required:
      - amount
      - fee_plan
      - fee_type
      - fee_type_display
      - id
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_type: REGISTRATION
        fee_type_display: ثبت‌نام
        title: عنوان نمونه
        amount: 12500000
        recurrence: ONE_TIME
        is_mandatory: true
        revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    FeePlanItemRequest:
      type: object
      properties:
        fee_plan:
          type: string
          format: uuid
          title: طرح شهریه
        fee_type:
          allOf:
          - $ref: '#/components/schemas/FeeTypeEnum'
          title: نوع شهریه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ (ریال)
        recurrence:
          allOf:
          - $ref: '#/components/schemas/RecurrenceEnum'
          title: تکرار
        is_mandatory:
          type: boolean
          title: اجباری
        revenue_account:
          type: string
          format: uuid
          title: حساب درآمد
          nullable: true
      required:
      - amount
      - fee_plan
      - fee_type
      - title
      example:
        fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_type: REGISTRATION
        title: عنوان نمونه
        amount: 12500000
        recurrence: ONE_TIME
        is_mandatory: true
        revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    FeePlanRequest:
      type: object
      properties:
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        currency:
          allOf:
          - $ref: '#/components/schemas/CurrencyEnum'
          title: واحد پول
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - academic_year
      - title
      example:
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        title: عنوان نمونه
        currency: IRR
        status: متن نمونه
    FeeTypeEnum:
      enum:
      - REGISTRATION
      - TUITION
      - TRANSPORT
      - MEAL
      - BOOK
      - FIELD_TRIP
      - EXAM
      - OPTIONAL_SERVICE
      - LATE_FEE
      - OTHER
      type: string
      description: |-
        * `REGISTRATION` - ثبت‌نام
        * `TUITION` - شهریه آموزشی
        * `TRANSPORT` - سرویس
        * `MEAL` - غذا
        * `BOOK` - کتاب و لوازم
        * `FIELD_TRIP` - اردو
        * `EXAM` - آزمون
        * `OPTIONAL_SERVICE` - خدمات اختیاری
        * `LATE_FEE` - جریمه دیرکرد
        * `OTHER` - سایر
    FieldError:
      type: object
      description: یک خطای فیلدی در قالب پاسخ خطا (بخش ۱۲.۳).
      properties:
        field:
          type: string
          description: نام فیلد دارای خطا، مثلاً `classGroupId`
        reason:
          type: string
          description: علت خطا به‌صورت کد یا متن کوتاه
      required:
      - field
      - reason
      example:
        field: متن نمونه
        reason: اصلاح اطلاعات پس از بررسی پرونده
    FinalizationStatusEnum:
      enum:
      - DRAFT
      - FINALIZED
      - AMENDED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `FINALIZED` - نهایی‌شده
        * `AMENDED` - اصلاح‌شده پس از نهایی‌سازی
    FiscalYear:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          title: عنوان
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          title: تاریخ پایان
        status:
          allOf:
          - $ref: '#/components/schemas/FiscalYearStatusEnum'
          readOnly: true
          title: وضعیت
        is_open:
          type: boolean
          readOnly: true
          title: باز است
        closed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بستن
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - closed_at
      - created_at
      - ends_on
      - id
      - is_open
      - school
      - starts_on
      - status
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        status: OPEN
        is_open: true
        closed_at: '2027-03-20T12:00:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    FiscalYearRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          title: تاریخ پایان
      required:
      - ends_on
      - school
      - starts_on
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
    FiscalYearStatusEnum:
      enum:
      - OPEN
      - CLOSING
      - CLOSED
      type: string
      description: |-
        * `OPEN` - باز
        * `CLOSING` - در حال بستن
        * `CLOSED` - بسته
    GenderEnum:
      enum:
      - MALE
      - FEMALE
      - OTHER
      - UNDISCLOSED
      type: string
      description: |-
        * `MALE` - مرد
        * `FEMALE` - زن
        * `OTHER` - سایر
        * `UNDISCLOSED` - اعلام‌نشده
    GenerateInstallmentsRequest:
      type: object
      properties:
        first_due_date:
          type: string
          format: date
          title: سررسید قسط اول
        interval_days:
          type: integer
          minimum: 1
          default: 30
          title: فاصله اقساط (روز)
      required:
      - first_due_date
      example:
        first_due_date: '2026-09-23'
        interval_days: 30
    GenerateReportCardRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        term:
          type: string
          format: uuid
          title: ترم
      required:
      - enrollment
      - term
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    GoodsReceipt:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        order:
          type: string
          format: uuid
          title: سفارش
        order_no:
          type: string
          readOnly: true
          title: شماره سفارش
        vendor_name:
          type: string
          readOnly: true
          title: نام تأمین‌کننده
        stock_document:
          type: string
          format: uuid
          title: سند انبار
          readOnly: true
          nullable: true
        receipt_no:
          type: string
          readOnly: true
          title: شماره رسید
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        quality_status:
          allOf:
          - $ref: '#/components/schemas/QualityStatusEnum'
          title: وضعیت کیفی
        status:
          allOf:
          - $ref: '#/components/schemas/ReceiptStatusEnum'
          title: وضعیت
        vendor_invoice_no:
          type: string
          title: شماره فاکتور تأمین‌کننده
          maxLength: 60
        vendor_invoice_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ فاکتور تأمین‌کننده
        three_way_matched:
          type: boolean
          readOnly: true
          title: تطبیق سه‌طرفه انجام شد
          description: 'بخش ۷.۹: تطبیق سفارش، رسید و فاکتور پیش از پرداخت'
        note:
          type: string
          title: یادداشت
          maxLength: 400
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - id
      - order
      - order_no
      - receipt_no
      - received_at
      - stock_document
      - three_way_matched
      - updated_at
      - vendor_name
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        order_no: PO-1405-000027
        vendor_name: نام نمونه
        stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        receipt_no: GRN-1405-000058
        received_at: '2026-09-23T08:30:00+03:30'
        quality_status: PENDING
        status: PROVISIONAL
        vendor_invoice_no: SAMPLE-001
        vendor_invoice_amount: 2500000
        three_way_matched: true
        note: یادداشت داخلی
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    GoodsReceiptRequest:
      type: object
      properties:
        order:
          type: string
          format: uuid
          title: سفارش
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        quality_status:
          allOf:
          - $ref: '#/components/schemas/QualityStatusEnum'
          title: وضعیت کیفی
        status:
          allOf:
          - $ref: '#/components/schemas/ReceiptStatusEnum'
          title: وضعیت
        vendor_invoice_no:
          type: string
          title: شماره فاکتور تأمین‌کننده
          maxLength: 60
        vendor_invoice_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ فاکتور تأمین‌کننده
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - order
      - received_at
      example:
        order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        received_at: '2026-09-23T08:30:00+03:30'
        quality_status: PENDING
        status: PROVISIONAL
        vendor_invoice_no: SAMPLE-001
        vendor_invoice_amount: 2500000
        note: یادداشت داخلی
    GradeAppeal:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        attempt:
          type: string
          format: uuid
          title: تلاش
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        submitted_by_id:
          type: string
          format: uuid
          nullable: true
          title: ارسال‌کننده
        reason:
          type: string
          title: دلیل اعتراض
        status:
          allOf:
          - $ref: '#/components/schemas/AppealStatusEnum'
          readOnly: true
          title: وضعیت
        resolution:
          type: string
          title: نتیجه رسیدگی
        resolved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان رسیدگی
        score_before:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره پیش از تغییر
        score_after:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره پس از تغییر
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - attempt
      - created_at
      - id
      - reason
      - resolved_at
      - score_after
      - score_before
      - status
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        submitted_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        status: SUBMITTED
        resolution: متن نمونه
        resolved_at: '2026-09-23T08:30:00+03:30'
        score_before: '18.50'
        score_after: '18.50'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    GradeAppealRequest:
      type: object
      properties:
        attempt:
          type: string
          format: uuid
          title: تلاش
        submitted_by_id:
          type: string
          format: uuid
          nullable: true
          title: ارسال‌کننده
        reason:
          type: string
          minLength: 1
          title: دلیل اعتراض
        resolution:
          type: string
          title: نتیجه رسیدگی
      required:
      - attempt
      - reason
      example:
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        submitted_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        resolution: متن نمونه
    GradeItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        category:
          type: string
          format: uuid
          title: دسته
        category_title:
          type: string
          readOnly: true
          title: عنوان دسته ارزشیابی
        source_type:
          allOf:
          - $ref: '#/components/schemas/GradeItemSourceTypeEnum'
          title: نوع منبع
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
          description: شناسه آزمون یا تکلیف مرتبط
        title:
          type: string
          title: عنوان
          maxLength: 200
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم
        weight:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: ضریب داخل دسته
        due_on:
          type: string
          format: date
          nullable: true
          title: سررسید
        status:
          allOf:
          - $ref: '#/components/schemas/GradeItemStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        locked_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان قفل
        recorded_count:
          type: integer
          readOnly: true
          title: تعداد نمرات ثبت‌شده
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - category
      - category_title
      - created_at
      - id
      - locked_at
      - recorded_count
      - status
      - status_display
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category_title: عنوان نمونه
        source_type: EXAM
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        max_score: '18.50'
        weight: '18.50'
        due_on: '2027-03-20'
        status: DRAFT
        status_display: پیش‌نویس
        locked_at: '2026-09-23T08:30:00+03:30'
        recorded_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    GradeItemRequest:
      type: object
      properties:
        category:
          type: string
          format: uuid
          title: دسته
        source_type:
          allOf:
          - $ref: '#/components/schemas/GradeItemSourceTypeEnum'
          title: نوع منبع
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
          description: شناسه آزمون یا تکلیف مرتبط
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم
        weight:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: ضریب داخل دسته
        due_on:
          type: string
          format: date
          nullable: true
          title: سررسید
      required:
      - category
      - title
      example:
        category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        source_type: EXAM
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        max_score: '18.50'
        weight: '18.50'
        due_on: '2027-03-20'
    GradeItemSourceTypeEnum:
      enum:
      - EXAM
      - ASSIGNMENT
      - CLASS_ACTIVITY
      - PROJECT
      - MANUAL
      type: string
      description: |-
        * `EXAM` - آزمون
        * `ASSIGNMENT` - تکلیف
        * `CLASS_ACTIVITY` - فعالیت کلاسی
        * `PROJECT` - پروژه
        * `MANUAL` - ثبت دستی
    GradeItemStatusEnum:
      enum:
      - DRAFT
      - OPEN
      - LOCKED
      - PUBLISHED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `OPEN` - باز برای ثبت نمره
        * `LOCKED` - قفل‌شده
        * `PUBLISHED` - منتشرشده
    GradeLevel:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          title: کد پایه
          maxLength: 30
        title:
          type: string
          title: عنوان پایه
          maxLength: 100
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب
          description: برای منطق ارتقای پایه استفاده می‌شود
        stage:
          type: string
          title: مقطع
          maxLength: 40
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - school
      - sequence_no
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        sequence_no: 3
        stage: متن نمونه
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    GradeLevelRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد پایه
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان پایه
          maxLength: 100
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب
          description: برای منطق ارتقای پایه استفاده می‌شود
        stage:
          type: string
          title: مقطع
          maxLength: 40
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      required:
      - code
      - school
      - sequence_no
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        sequence_no: 3
        stage: متن نمونه
        status: DRAFT
    GradeReview:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        attempt_answer:
          type: string
          format: uuid
          title: پاسخ تلاش
        reviewer_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: شناسه بازبین
        awarded_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          title: نمره اعطاشده
        feedback:
          type: string
          title: بازخورد
        review_type:
          allOf:
          - $ref: '#/components/schemas/GradeReviewReviewTypeEnum'
          title: نوع بازبینی
        is_anonymous:
          type: boolean
          title: ناشناس‌سازی مصحح
        reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بازبینی
      required:
      - attempt_answer
      - awarded_score
      - id
      - reviewed_at
      - reviewer_id
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt_answer: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        awarded_score: '18.50'
        feedback: متن نمونه
        review_type: FIRST_PASS
        is_anonymous: true
        reviewed_at: '2026-09-23T08:30:00+03:30'
    GradeReviewReviewTypeEnum:
      enum:
      - FIRST_PASS
      - SECOND_PASS
      - APPEAL
      - MODERATION
      type: string
      description: |-
        * `FIRST_PASS` - تصحیح اول
        * `SECOND_PASS` - بازبینی دوم
        * `APPEAL` - بازتصحیح پس از اعتراض
        * `MODERATION` - تعدیل
    GradeSubmissionRequest:
      type: object
      properties:
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره
        feedback:
          type: string
          maxLength: 2000
          title: بازخورد
      required:
      - score
      example:
        score: '18.50'
        feedback: متن نمونه
    Gradebook:
      type: object
      description: |-
        نمای کامل دفتر نمره یک ارائه درس.

        ساختار ستون/ردیف طوری است که فرانت بتواند مستقیماً جدول را رندر کند
        (بخش ۱۰.۱ سند فرانت).
      properties:
        courseOfferingId:
          type: string
          format: uuid
          title: شناسه ارائه درس
        courseTitle:
          type: string
          title: عنوان درس
        classGroupCode:
          type: string
          title: کد کلاس
        termTitle:
          type: string
          title: عنوان ترم
        categories:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentCategory'
          title: دسته‌ها
        columns:
          type: array
          items:
            $ref: '#/components/schemas/GradebookColumn'
          title: ستون‌ها
        rows:
          type: array
          items:
            $ref: '#/components/schemas/GradebookRow'
          title: سطرها
        weightsValid:
          type: boolean
          description: آیا مجموع وزن دسته‌های فعال دقیقاً ۱۰۰٪ است؟
      required:
      - categories
      - classGroupCode
      - columns
      - courseOfferingId
      - courseTitle
      - rows
      - termTitle
      - weightsValid
      example:
        courseOfferingId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        courseTitle: ریاضی ۷
        classGroupCode: G7-A
        termTitle: نیم‌سال اول
        categories:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          weight_percent: '18.50'
          drop_policy: NONE
          display_order: 1
          is_active: true
          item_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        columns:
        - gradeItemId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          categoryId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          categoryTitle: عنوان نمونه
          maxScore: '18.50'
          weight: '18.50'
          status: متن نمونه
          isLocked: true
        rows:
        - enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          studentNo: '14030127'
          studentName: سارا محمدی
          scores: {}
          categoryAverages: {}
          finalScore: 18.5
        weightsValid: true
    GradebookColumn:
      type: object
      description: یک ستون (قلم نمره) در جدول دفتر نمره.
      properties:
        gradeItemId:
          type: string
          format: uuid
          title: شناسه قلم نمره
        title:
          type: string
          title: عنوان
        categoryId:
          type: string
          format: uuid
          title: شناسه دسته
        categoryTitle:
          type: string
          title: عنوان دسته ارزشیابی
        maxScore:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        weight:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: وزن
        status:
          type: string
          title: وضعیت
        isLocked:
          type: boolean
          title: قفل است
      required:
      - categoryId
      - categoryTitle
      - gradeItemId
      - isLocked
      - maxScore
      - status
      - title
      - weight
      example:
        gradeItemId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        categoryId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        categoryTitle: عنوان نمونه
        maxScore: '18.50'
        weight: '18.50'
        status: متن نمونه
        isLocked: true
    GradebookRow:
      type: object
      description: یک ردیف (دانش‌آموز) در جدول دفتر نمره.
      properties:
        enrollmentId:
          type: string
          format: uuid
          title: شناسه ثبت‌نام
        studentNo:
          type: string
          title: شماره دانش‌آموزی
        studentName:
          type: string
          title: نام دانش‌آموز
        scores:
          type: object
          additionalProperties:
            type: object
            additionalProperties: {}
          description: نگاشت gradeItemId → {rawScore, status, comment}
        categoryAverages:
          type: object
          additionalProperties:
            type: number
            format: double
            nullable: true
          title: میانگین هر دسته
        finalScore:
          type: number
          format: double
          nullable: true
          title: نمره نهایی
      required:
      - enrollmentId
      - finalScore
      - scores
      - studentName
      - studentNo
      example:
        enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        studentNo: '14030127'
        studentName: سارا محمدی
        scores: {}
        categoryAverages: {}
        finalScore: 18.5
    GradingQueueRow:
      type: object
      description: یک قلم در صف تصحیح تشریحی (بخش ۹.۸ سند فرانت).
      properties:
        attemptAnswerId:
          type: string
          format: uuid
          title: شناسه پاسخ تلاش
        attemptId:
          type: string
          format: uuid
          title: شناسه تلاش
        examTitle:
          type: string
          title: عنوان آزمون
        questionBody:
          type: string
          title: متن سؤال
        responseText:
          type: string
          title: متن پاسخ
        maxScore:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        gradingRubric:
          type: string
          title: راهنمای تصحیح
        studentLabel:
          type: string
          description: در حالت ناشناس‌سازی، به‌جای نام، شناسه مستعار برمی‌گردد.
      required:
      - attemptAnswerId
      - attemptId
      - examTitle
      - gradingRubric
      - maxScore
      - questionBody
      - responseText
      - studentLabel
      example:
        attemptAnswerId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attemptId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        examTitle: آزمون میان‌ترم ریاضی
        questionBody: متن نمونه
        responseText: متن نمونه
        maxScore: '18.50'
        gradingRubric: متن نمونه
        studentLabel: متن نمونه
    GradingStatusEnum:
      enum:
      - PENDING
      - AUTO_GRADED
      - MANUAL_GRADED
      - NEEDS_REVIEW
      type: string
      description: |-
        * `PENDING` - در انتظار تصحیح
        * `AUTO_GRADED` - تصحیح خودکار
        * `MANUAL_GRADED` - تصحیح دستی
        * `NEEDS_REVIEW` - نیازمند بازبینی
    Guardian:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        person_detail:
          allOf:
          - $ref: '#/components/schemas/PersonList'
          readOnly: true
          title: مشخصات شخص
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        occupation:
          type: string
          title: شغل
          maxLength: 120
        education_level:
          type: string
          title: تحصیلات
          maxLength: 80
        workplace:
          type: string
          title: محل کار
          maxLength: 200
        status:
          type: string
          title: وضعیت
          maxLength: 20
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - full_name
      - id
      - person
      - person_detail
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          first_name: سارا
          last_name: محمدی
          full_name: سارا محمدی
          national_id: 0012345678
          birth_date: '2012-09-26'
          gender: MALE
          gender_display: مرد
          status: ACTIVE
        full_name: سارا محمدی
        occupation: متن نمونه
        education_level: متن نمونه
        workplace: متن نمونه
        status: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    GuardianRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        occupation:
          type: string
          title: شغل
          maxLength: 120
        education_level:
          type: string
          title: تحصیلات
          maxLength: 80
        workplace:
          type: string
          title: محل کار
          maxLength: 200
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - person
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        occupation: متن نمونه
        education_level: متن نمونه
        workplace: متن نمونه
        status: متن نمونه
    HealthAlert:
      type: object
      description: نمای کامل هشدار — فقط برای نقش‌های دارای مجوز `health.read`.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        health_profile:
          type: string
          format: uuid
          title: پرونده سلامت
        alert_type:
          allOf:
          - $ref: '#/components/schemas/AlertTypeEnum'
          title: نوع هشدار
        alert_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع هشدار
        title:
          type: string
          title: عنوان
          maxLength: 200
        safe_summary:
          type: string
          title: خلاصه قابل نمایش به معلم
          description: بدون جزئیات پزشکی غیرضروری
          maxLength: 300
        instructions:
          type: string
          title: دستورالعمل اقدام (محرمانه)
        severity:
          allOf:
          - $ref: '#/components/schemas/AlertSeverityEnum'
          title: شدت
        severity_display:
          type: string
          readOnly: true
          title: برچسب فارسی شدت
        valid_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/HealthAlertStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - alert_type
      - alert_type_display
      - created_at
      - health_profile
      - id
      - safe_summary
      - severity_display
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        health_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        alert_type: ALLERGY
        alert_type_display: حساسیت
        title: عنوان نمونه
        safe_summary: متن نمونه
        instructions: متن نمونه
        severity: LOW
        severity_display: کم
        valid_until: '2027-03-20'
        status: ACTIVE
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    HealthAlertRequest:
      type: object
      description: نمای کامل هشدار — فقط برای نقش‌های دارای مجوز `health.read`.
      properties:
        health_profile:
          type: string
          format: uuid
          title: پرونده سلامت
        alert_type:
          allOf:
          - $ref: '#/components/schemas/AlertTypeEnum'
          title: نوع هشدار
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        safe_summary:
          type: string
          minLength: 1
          title: خلاصه قابل نمایش به معلم
          description: بدون جزئیات پزشکی غیرضروری
          maxLength: 300
        instructions:
          type: string
          title: دستورالعمل اقدام (محرمانه)
        severity:
          allOf:
          - $ref: '#/components/schemas/AlertSeverityEnum'
          title: شدت
        valid_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/HealthAlertStatusEnum'
          title: وضعیت
      required:
      - alert_type
      - health_profile
      - safe_summary
      - title
      example:
        health_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        alert_type: ALLERGY
        title: عنوان نمونه
        safe_summary: متن نمونه
        instructions: متن نمونه
        severity: LOW
        valid_until: '2027-03-20'
        status: ACTIVE
    HealthAlertSafe:
      type: object
      description: |-
        نمای محدود هشدار برای معلم و مسئول اردو.

        بخش ۷.۱۰: «Alert سلامت فقط حداقل اطلاعات لازم برای اقدام ایمن را نشان
        می‌دهد.» بنابراین `instructions` در این نما برنمی‌گردد.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        alert_type:
          allOf:
          - $ref: '#/components/schemas/AlertTypeEnum'
          readOnly: true
          title: نوع هشدار
        alert_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع هشدار
        title:
          type: string
          readOnly: true
          title: عنوان
        safe_summary:
          type: string
          readOnly: true
          title: خلاصه قابل نمایش به معلم
          description: بدون جزئیات پزشکی غیرضروری
        severity:
          allOf:
          - $ref: '#/components/schemas/AlertSeverityEnum'
          readOnly: true
          title: شدت
      required:
      - alert_type
      - alert_type_display
      - id
      - safe_summary
      - severity
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        alert_type: ALLERGY
        alert_type_display: حساسیت
        title: عنوان نمونه
        safe_summary: متن نمونه
        severity: LOW
    HealthAlertStatusEnum:
      enum:
      - ACTIVE
      - RESOLVED
      - EXPIRED
      type: string
      description: |-
        * `ACTIVE` - فعال
        * `RESOLVED` - رفع‌شده
        * `EXPIRED` - منقضی
    HealthIncident:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        location:
          type: string
          title: محل
          maxLength: 150
        description:
          type: string
          title: شرح
        action_taken:
          type: string
          title: اقدام انجام‌شده
        outcome:
          allOf:
          - $ref: '#/components/schemas/OutcomeEnum'
          title: نتیجه
        outcome_display:
          type: string
          readOnly: true
          title: برچسب فارسی نتیجه
        guardian_notified_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان اطلاع به ولی
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - description
      - guardian_notified_at
      - id
      - occurred_at
      - outcome_display
      - student
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        occurred_at: '2026-09-23T08:30:00+03:30'
        location: متن نمونه
        description: توضیح تکمیلی این رکورد
        action_taken: متن نمونه
        outcome: RESOLVED_ON_SITE
        outcome_display: رفع در محل
        guardian_notified_at: '2026-09-23T08:30:00+03:30'
        confidentiality_level: STANDARD
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    HealthIncidentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        location:
          type: string
          title: محل
          maxLength: 150
        description:
          type: string
          minLength: 1
          title: شرح
        action_taken:
          type: string
          title: اقدام انجام‌شده
        outcome:
          allOf:
          - $ref: '#/components/schemas/OutcomeEnum'
          title: نتیجه
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
      required:
      - description
      - occurred_at
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        occurred_at: '2026-09-23T08:30:00+03:30'
        location: متن نمونه
        description: توضیح تکمیلی این رکورد
        action_taken: متن نمونه
        outcome: RESOLVED_ON_SITE
        confidentiality_level: STANDARD
    HealthProfile:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        blood_type:
          allOf:
          - $ref: '#/components/schemas/BloodTypeEnum'
          title: گروه خونی
        height_cm:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: قد (سانتی‌متر)
        weight_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: وزن (کیلوگرم)
        accessibility_needs:
          type: string
          title: نیازهای دسترس‌پذیری
        emergency_contact_name:
          type: string
          title: نام تماس اضطراری
          maxLength: 150
        emergency_contact_phone:
          type: string
          title: تلفن تماس اضطراری
          maxLength: 30
        family_physician:
          type: string
          title: پزشک خانواده
          maxLength: 150
        insurance_no:
          type: string
          title: شماره بیمه
          maxLength: 60
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
        alerts:
          type: array
          items:
            $ref: '#/components/schemas/HealthAlert'
          readOnly: true
          title: هشدارها
        active_alert_count:
          type: integer
          readOnly: true
          title: تعداد هشدارهای فعال
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - active_alert_count
      - alerts
      - created_at
      - id
      - student
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        blood_type: A+
        height_cm: 3
        weight_kg: 3
        accessibility_needs: متن نمونه
        emergency_contact_name: نام نمونه
        emergency_contact_phone: متن نمونه
        family_physician: متن نمونه
        insurance_no: SAMPLE-001
        confidentiality_level: STANDARD
        alerts:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          health_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          alert_type: ALLERGY
          alert_type_display: حساسیت
          title: عنوان نمونه
          safe_summary: متن نمونه
          instructions: متن نمونه
          severity: LOW
          severity_display: کم
          valid_until: '2027-03-20'
          status: ACTIVE
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        active_alert_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    HealthProfileRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        blood_type:
          allOf:
          - $ref: '#/components/schemas/BloodTypeEnum'
          title: گروه خونی
        height_cm:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: قد (سانتی‌متر)
        weight_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: وزن (کیلوگرم)
        accessibility_needs:
          type: string
          title: نیازهای دسترس‌پذیری
        emergency_contact_name:
          type: string
          title: نام تماس اضطراری
          maxLength: 150
        emergency_contact_phone:
          type: string
          title: تلفن تماس اضطراری
          maxLength: 30
        family_physician:
          type: string
          title: پزشک خانواده
          maxLength: 150
        insurance_no:
          type: string
          title: شماره بیمه
          maxLength: 60
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
      required:
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        blood_type: A+
        height_cm: 3
        weight_kg: 3
        accessibility_needs: متن نمونه
        emergency_contact_name: نام نمونه
        emergency_contact_phone: متن نمونه
        family_physician: متن نمونه
        insurance_no: SAMPLE-001
        confidentiality_level: STANDARD
    IncidentTypeEnum:
      enum:
      - DISRUPTION
      - TARDINESS
      - DRESS_CODE
      - BULLYING
      - PROPERTY_DAMAGE
      - ACADEMIC_DISHONESTY
      - COMMENDATION
      - OTHER
      type: string
      description: |-
        * `DISRUPTION` - اخلال در کلاس
        * `TARDINESS` - تأخیر مکرر
        * `DRESS_CODE` - عدم رعایت پوشش
        * `BULLYING` - آزار همکلاسی
        * `PROPERTY_DAMAGE` - تخریب اموال
        * `ACADEMIC_DISHONESTY` - تقلب
        * `COMMENDATION` - تشویق
        * `OTHER` - سایر
    IntegrationDirectionEnum:
      enum:
      - INBOUND
      - OUTBOUND
      type: string
      description: |-
        * `INBOUND` - ورودی
        * `OUTBOUND` - خروجی
    IntegrationMessage:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        integration_code:
          type: string
          title: کد یکپارچه‌سازی
          maxLength: 60
        direction:
          allOf:
          - $ref: '#/components/schemas/IntegrationDirectionEnum'
          title: جهت
        external_message_id:
          type: string
          title: شناسه پیام بیرونی
          maxLength: 200
        correlation_id:
          type: string
          title: شناسه ردیابی
          maxLength: 64
        status:
          allOf:
          - $ref: '#/components/schemas/IntegrationStatusEnum'
          title: وضعیت
        error_message:
          type: string
          title: پیام خطا
          maxLength: 400
        received_or_sent_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان دریافت یا ارسال
      required:
      - direction
      - id
      - integration_code
      - received_or_sent_at
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        integration_code: SAMPLE-001
        direction: INBOUND
        external_message_id: متن نمونه
        correlation_id: 8c4a1f2e
        status: RECEIVED
        error_message: متن نمونه
        received_or_sent_at: '2026-09-23T08:30:00+03:30'
    IntegrationStatusEnum:
      enum:
      - RECEIVED
      - PROCESSED
      - FAILED
      - DEAD_LETTER
      type: string
      description: |-
        * `RECEIVED` - دریافت‌شده
        * `PROCESSED` - پردازش‌شده
        * `FAILED` - ناموفق
        * `DEAD_LETTER` - Dead Letter
    Invoice:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        agreement:
          type: string
          format: uuid
          title: توافق‌نامه
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        invoice_no:
          type: string
          readOnly: true
          title: شماره صورتحساب
        issue_date:
          type: string
          format: date
          title: تاریخ صدور
        due_date:
          type: string
          format: date
          title: سررسید
        installment_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: شماره قسط
        total_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ کل
        discount_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ تخفیف
        paid_amount:
          type: integer
          readOnly: true
          title: مبلغ پرداخت‌شده
        balance:
          type: integer
          readOnly: true
          title: مانده
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        status:
          allOf:
          - $ref: '#/components/schemas/InvoiceStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        description:
          type: string
          title: شرح
          maxLength: 400
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
        lines:
          type: array
          items:
            $ref: '#/components/schemas/InvoiceLine'
          readOnly: true
          title: سطرهای سند
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - agreement
      - balance
      - created_at
      - due_date
      - id
      - invoice_no
      - issue_date
      - lines
      - paid_amount
      - status
      - status_display
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        invoice_no: INV-1405-000137
        issue_date: '2026-09-23'
        due_date: '2027-03-20'
        installment_no: 3
        total_amount: 45000000
        discount_amount: 5000000
        paid_amount: 2500000
        balance: 32500000
        currency: IRR
        status: DRAFT
        status_display: پیش‌نویس
        description: توضیح تکمیلی این رکورد
        cancel_reason: توضیح نمونه
        lines:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_type: REGISTRATION
          fee_type_display: ثبت‌نام
          description: توضیح تکمیلی این رکورد
          quantity: 3
          unit_amount: 2500000
          discount_amount: 5000000
          net_amount: 2500000
          revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    InvoiceLine:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        invoice:
          type: string
          format: uuid
          title: صورتحساب
        fee_type:
          allOf:
          - $ref: '#/components/schemas/FeeTypeEnum'
          title: نوع شهریه
        fee_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع تعرفه
        description:
          type: string
          title: شرح
          maxLength: 300
        quantity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد
        unit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ واحد
        discount_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ تخفیف
        net_amount:
          type: integer
          readOnly: true
          title: مبلغ خالص
        revenue_account:
          type: string
          format: uuid
          title: حساب درآمد
          nullable: true
      required:
      - description
      - fee_type
      - fee_type_display
      - id
      - invoice
      - net_amount
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_type: REGISTRATION
        fee_type_display: ثبت‌نام
        description: توضیح تکمیلی این رکورد
        quantity: 3
        unit_amount: 2500000
        discount_amount: 5000000
        net_amount: 2500000
        revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    InvoiceLineRequest:
      type: object
      properties:
        invoice:
          type: string
          format: uuid
          title: صورتحساب
        fee_type:
          allOf:
          - $ref: '#/components/schemas/FeeTypeEnum'
          title: نوع شهریه
        description:
          type: string
          minLength: 1
          title: شرح
          maxLength: 300
        quantity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد
        unit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ واحد
        discount_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ تخفیف
        revenue_account:
          type: string
          format: uuid
          title: حساب درآمد
          nullable: true
      required:
      - description
      - fee_type
      - invoice
      example:
        invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_type: REGISTRATION
        description: توضیح تکمیلی این رکورد
        quantity: 3
        unit_amount: 2500000
        discount_amount: 5000000
        revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    InvoiceRequest:
      type: object
      properties:
        agreement:
          type: string
          format: uuid
          title: توافق‌نامه
        issue_date:
          type: string
          format: date
          title: تاریخ صدور
        due_date:
          type: string
          format: date
          title: سررسید
        installment_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: شماره قسط
        total_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ کل
        discount_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ تخفیف
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        description:
          type: string
          title: شرح
          maxLength: 400
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
      required:
      - agreement
      - due_date
      - issue_date
      example:
        agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        issue_date: '2026-09-23'
        due_date: '2027-03-20'
        installment_no: 3
        total_amount: 45000000
        discount_amount: 5000000
        currency: IRR
        description: توضیح تکمیلی این رکورد
        cancel_reason: توضیح نمونه
    InvoiceStatusEnum:
      enum:
      - DRAFT
      - ISSUED
      - PARTIALLY_PAID
      - PAID
      - OVERDUE
      - DISPUTED
      - CREDITED
      - REFUNDED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `ISSUED` - صادرشده
        * `PARTIALLY_PAID` - پرداخت جزئی
        * `PAID` - تسویه‌شده
        * `OVERDUE` - سررسید گذشته
        * `DISPUTED` - مورد اعتراض
        * `CREDITED` - یادداشت بستانکار صادر شد
        * `REFUNDED` - مسترد شده
        * `CANCELLED` - لغوشده
    Item:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        category:
          type: string
          format: uuid
          title: دسته
        category_title:
          type: string
          readOnly: true
          title: عنوان دسته ارزشیابی
        base_uom:
          type: string
          format: uuid
          title: واحد سنجش پایه
        uom_title:
          type: string
          readOnly: true
          title: عنوان واحد شمارش
        sku:
          type: string
          title: کد کالا
          maxLength: 40
        title:
          type: string
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
          maxLength: 400
        barcode:
          type: string
          title: بارکد
          maxLength: 60
        lot_tracked:
          type: boolean
          title: ردیابی بچ
        serial_tracked:
          type: boolean
          title: ردیابی سریال
        expiry_tracked:
          type: boolean
          title: ردیابی انقضا
        is_capital_asset:
          type: boolean
          title: مال سرمایه‌ای (نه مصرفی)
        reorder_point:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: نقطه سفارش
        min_stock:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: حداقل موجودی
        max_stock:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: حداکثر موجودی
        allow_negative_stock:
          type: boolean
          title: اجازه موجودی منفی
          description: 'بخش ۷.۹: منفی‌شدن فقط برای اقلام مجاز با تأیید'
        expense_account_id:
          type: string
          format: uuid
          nullable: true
          title: حساب هزینه
        status:
          type: string
          title: وضعیت
          maxLength: 20
        total_on_hand:
          type: number
          format: double
          readOnly: true
          title: موجودی کل
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - base_uom
      - category
      - category_title
      - created_at
      - id
      - sku
      - title
      - total_on_hand
      - uom_title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category_title: عنوان نمونه
        base_uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        uom_title: عنوان نمونه
        sku: STA-A4-80
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        barcode: '6260123456789'
        lot_tracked: true
        serial_tracked: true
        expiry_tracked: true
        is_capital_asset: true
        reorder_point: '18.50'
        min_stock: '18.50'
        max_stock: '18.50'
        allow_negative_stock: true
        expense_account_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
        total_on_hand: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ItemCategory:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          title: کد
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 150
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ItemCategoryRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
      required:
      - code
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
    ItemRequest:
      type: object
      properties:
        category:
          type: string
          format: uuid
          title: دسته
        base_uom:
          type: string
          format: uuid
          title: واحد سنجش پایه
        sku:
          type: string
          minLength: 1
          title: کد کالا
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
          maxLength: 400
        barcode:
          type: string
          title: بارکد
          maxLength: 60
        lot_tracked:
          type: boolean
          title: ردیابی بچ
        serial_tracked:
          type: boolean
          title: ردیابی سریال
        expiry_tracked:
          type: boolean
          title: ردیابی انقضا
        is_capital_asset:
          type: boolean
          title: مال سرمایه‌ای (نه مصرفی)
        reorder_point:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: نقطه سفارش
        min_stock:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: حداقل موجودی
        max_stock:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: حداکثر موجودی
        allow_negative_stock:
          type: boolean
          title: اجازه موجودی منفی
          description: 'بخش ۷.۹: منفی‌شدن فقط برای اقلام مجاز با تأیید'
        expense_account_id:
          type: string
          format: uuid
          nullable: true
          title: حساب هزینه
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - base_uom
      - category
      - sku
      - title
      example:
        category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        base_uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sku: STA-A4-80
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        barcode: '6260123456789'
        lot_tracked: true
        serial_tracked: true
        expiry_tracked: true
        is_capital_asset: true
        reorder_point: '18.50'
        min_stock: '18.50'
        max_stock: '18.50'
        allow_negative_stock: true
        expense_account_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
    ItemTypeEnum:
      enum:
      - EARNING
      - DEDUCTION
      - EMPLOYER_COST
      type: string
      description: |-
        * `EARNING` - مزایا
        * `DEDUCTION` - کسورات
        * `EMPLOYER_COST` - سهم کارفرما
    JournalEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        fiscal_year:
          type: string
          format: uuid
          title: سال مالی
        entry_no:
          type: string
          readOnly: true
          title: شماره سند
        entry_date:
          type: string
          format: date
          title: تاریخ سند
        source_type:
          allOf:
          - $ref: '#/components/schemas/JournalEntrySourceTypeEnum'
          title: نوع منبع
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
        description:
          type: string
          title: شرح
          maxLength: 400
        status:
          allOf:
          - $ref: '#/components/schemas/JournalStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        posted_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان قطعی‌سازی
        reversal_of:
          type: string
          format: uuid
          title: سند برگشتی برای
          readOnly: true
          nullable: true
        total_debit:
          type: integer
          readOnly: true
          title: مجموع بدهکار
        total_credit:
          type: integer
          readOnly: true
          title: مجموع بستانکار
        is_balanced:
          type: boolean
          readOnly: true
          title: سند تراز است
        lines:
          type: array
          items:
            $ref: '#/components/schemas/JournalLine'
          readOnly: true
          title: سطرهای سند
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - description
      - entry_date
      - entry_no
      - fiscal_year
      - id
      - is_balanced
      - lines
      - posted_at
      - reversal_of
      - status
      - status_display
      - total_credit
      - total_debit
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fiscal_year: '1405'
        entry_no: JE-1405-000731
        entry_date: '2026-09-23'
        source_type: MANUAL
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        description: توضیح تکمیلی این رکورد
        status: DRAFT
        status_display: پیش‌نویس
        posted_at: '2026-09-23T08:30:00+03:30'
        reversal_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        total_debit: 12
        total_credit: 12
        is_balanced: true
        lines:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          journal_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          account_code: '1101'
          account_title: عنوان نمونه
          cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          cost_center_title: عنوان نمونه
          debit_amount: 2500000
          credit_amount: 2500000
          description: توضیح تکمیلی این رکورد
          line_no: 3
        created_at: '2026-09-23T08:30:00+03:30'
        version: 1
    JournalEntryRequest:
      type: object
      properties:
        fiscal_year:
          type: string
          format: uuid
          title: سال مالی
        entry_date:
          type: string
          format: date
          title: تاریخ سند
        source_type:
          allOf:
          - $ref: '#/components/schemas/JournalEntrySourceTypeEnum'
          title: نوع منبع
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
        description:
          type: string
          minLength: 1
          title: شرح
          maxLength: 400
      required:
      - description
      - entry_date
      - fiscal_year
      example:
        fiscal_year: '1405'
        entry_date: '2026-09-23'
        source_type: MANUAL
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        description: توضیح تکمیلی این رکورد
    JournalEntrySourceTypeEnum:
      enum:
      - MANUAL
      - INVOICE
      - PAYMENT
      - REFUND
      - PAYROLL
      - PURCHASE
      - STOCK
      - DEPRECIATION
      type: string
      description: |-
        * `MANUAL` - دستی
        * `INVOICE` - صورتحساب
        * `PAYMENT` - دریافت
        * `REFUND` - استرداد
        * `PAYROLL` - حقوق
        * `PURCHASE` - خرید
        * `STOCK` - انبار
        * `DEPRECIATION` - استهلاک
    JournalLine:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        journal_entry:
          type: string
          format: uuid
          title: سند حسابداری
        account:
          type: string
          format: uuid
          title: حساب
        account_code:
          type: string
          readOnly: true
          title: کد حساب
        account_title:
          type: string
          readOnly: true
          title: عنوان حساب
        cost_center:
          type: string
          format: uuid
          title: مرکز هزینه
          nullable: true
        cost_center_title:
          type: string
          readOnly: true
          title: عنوان مرکز هزینه
        debit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بدهکار
        credit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بستانکار
        description:
          type: string
          title: شرح
          maxLength: 300
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      required:
      - account
      - account_code
      - account_title
      - cost_center_title
      - id
      - journal_entry
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        journal_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        account_code: '1101'
        account_title: عنوان نمونه
        cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        cost_center_title: عنوان نمونه
        debit_amount: 2500000
        credit_amount: 2500000
        description: توضیح تکمیلی این رکورد
        line_no: 3
    JournalLineInputRequest:
      type: object
      properties:
        account:
          type: string
          format: uuid
          title: حساب
        cost_center:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        debit:
          type: integer
          minimum: 0
          default: 0
          title: بدهکار
        credit:
          type: integer
          minimum: 0
          default: 0
          title: بستانکار
        description:
          type: string
          default: ''
          maxLength: 300
          title: توضیح
      required:
      - account
      example:
        account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        debit: 0
        credit: 0
        description: ''
    JournalLineRequest:
      type: object
      properties:
        journal_entry:
          type: string
          format: uuid
          title: سند حسابداری
        account:
          type: string
          format: uuid
          title: حساب
        cost_center:
          type: string
          format: uuid
          title: مرکز هزینه
          nullable: true
        debit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بدهکار
        credit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بستانکار
        description:
          type: string
          title: شرح
          maxLength: 300
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      required:
      - account
      - journal_entry
      example:
        journal_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        debit_amount: 2500000
        credit_amount: 2500000
        description: توضیح تکمیلی این رکورد
        line_no: 3
    JournalStatusEnum:
      enum:
      - DRAFT
      - VALIDATED
      - POSTED
      - REVERSED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `VALIDATED` - کنترل‌شده
        * `POSTED` - قطعی
        * `REVERSED` - برگشت‌خورده
        * `CANCELLED` - لغوشده
    Kardex:
      type: object
      description: کاردکس کالا (بخش ۱۳.۵ سند فرانت).
      properties:
        itemId:
          type: string
          format: uuid
          title: شناسه کالا
        sku:
          type: string
          title: کد کالا (SKU)
        title:
          type: string
          title: عنوان
        closingBalance:
          type: number
          format: double
          title: مانده پایان دوره
        rows:
          type: array
          items:
            $ref: '#/components/schemas/KardexRow'
          title: سطرها
      required:
      - closingBalance
      - itemId
      - rows
      - sku
      - title
      example:
        itemId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sku: STA-A4-80
        title: عنوان نمونه
        closingBalance: 18.5
        rows:
        - occurredAt: '2026-09-23T08:30:00+03:30'
          documentNo: STK-1405-000094
          documentType: متن نمونه
          warehouse: متن نمونه
          lotNo: SAMPLE-001
          serialNo: SN-88213047
          quantity: 18.5
          unitCost: 2500000
          balance: 32500000
    KardexRow:
      type: object
      properties:
        occurredAt:
          type: string
          format: date-time
          title: زمان رخداد
        documentNo:
          type: string
          title: شماره سند انبار
        documentType:
          type: string
          title: نوع مدرک
        warehouse:
          type: string
          title: انبار
        lotNo:
          type: string
          title: شماره بچ
        serialNo:
          type: string
          title: شماره سریال
        quantity:
          type: number
          format: double
          title: تعداد
        unitCost:
          type: integer
          title: بهای واحد (ریال)
        balance:
          type: number
          format: double
          title: مانده
      required:
      - balance
      - documentNo
      - documentType
      - lotNo
      - occurredAt
      - quantity
      - serialNo
      - unitCost
      - warehouse
      example:
        occurredAt: '2026-09-23T08:30:00+03:30'
        documentNo: STK-1405-000094
        documentType: متن نمونه
        warehouse: متن نمونه
        lotNo: SAMPLE-001
        serialNo: SN-88213047
        quantity: 18.5
        unitCost: 2500000
        balance: 32500000
    LearningResource:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        resource_type:
          allOf:
          - $ref: '#/components/schemas/ResourceTypeEnum'
          title: نوع منبع
        title:
          type: string
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
        file:
          type: string
          format: uri
          nullable: true
          title: فایل
        url:
          title: نشانی اینترنتی
          oneOf:
          - type: string
            format: uri
            maxLength: 200
          - type: string
            maxLength: 0
        visibility:
          allOf:
          - $ref: '#/components/schemas/LearningResourceVisibilityEnum'
          title: سطح نمایش
        published_at:
          type: string
          format: date-time
          nullable: true
          title: زمان انتشار
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - course_offering
      - created_at
      - id
      - resource_type
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        resource_type: FILE
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        file: https://example.school/media/sample.pdf
        url: https://example.school/resources/sample.pdf
        visibility: CLASS_ONLY
        published_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    LearningResourceRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        resource_type:
          allOf:
          - $ref: '#/components/schemas/ResourceTypeEnum'
          title: نوع منبع
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
        file:
          type: string
          format: binary
          nullable: true
          title: فایل
        url:
          title: نشانی اینترنتی
          oneOf:
          - type: string
            format: uri
            maxLength: 200
          - type: string
            maxLength: 0
        visibility:
          allOf:
          - $ref: '#/components/schemas/LearningResourceVisibilityEnum'
          title: سطح نمایش
        published_at:
          type: string
          format: date-time
          nullable: true
          title: زمان انتشار
      required:
      - course_offering
      - resource_type
      - title
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        resource_type: FILE
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        file: sample.pdf
        url: https://example.school/resources/sample.pdf
        visibility: CLASS_ONLY
        published_at: '2026-09-23T08:30:00+03:30'
    LearningResourceVisibilityEnum:
      enum:
      - CLASS_ONLY
      - GRADE
      - SCHOOL
      - PRIVATE
      type: string
      description: |-
        * `CLASS_ONLY` - فقط کلاس
        * `GRADE` - کل پایه
        * `SCHOOL` - کل مدرسه
        * `PRIVATE` - فقط معلم
    LeaveDecisionRequest:
      type: object
      properties:
        note:
          type: string
          maxLength: 400
          title: یادداشت
        substitute_employee:
          type: string
          format: uuid
          nullable: true
          description: کارمند جانشین در بازه مرخصی
      example:
        note: یادداشت داخلی
        substitute_employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    LeaveRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        employee:
          type: string
          format: uuid
          title: کارمند
        employee_name:
          type: string
          readOnly: true
          title: نام کارمند
        leave_type:
          allOf:
          - $ref: '#/components/schemas/LeaveTypeEnum'
          title: نوع مرخصی
        leave_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع مرخصی
        starts_at:
          type: string
          format: date-time
          title: زمان شروع
        ends_at:
          type: string
          format: date-time
          title: زمان پایان
        requested_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه درخواستی
        reason:
          type: string
          title: علت
          maxLength: 400
        substitute_employee:
          type: string
          format: uuid
          title: جانشین
          nullable: true
        substitute_name:
          type: string
          readOnly: true
          title: نام جانشین
        status:
          allOf:
          - $ref: '#/components/schemas/LeaveStatusEnum'
          readOnly: true
          title: وضعیت
        decided_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تصمیم
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 400
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - decided_at
      - employee
      - employee_name
      - ends_at
      - id
      - leave_type
      - leave_type_display
      - starts_at
      - status
      - substitute_name
      - updated_at
      - version
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        leave_type: ANNUAL
        starts_at: '2026-09-23T08:30:00+03:30'
        ends_at: '2027-03-20T12:00:00+03:30'
        requested_minutes: 45
        reason: اصلاح اطلاعات پس از بررسی پرونده
        substitute_employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision_note: توضیح نمونه
    LeaveRequestRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        leave_type:
          allOf:
          - $ref: '#/components/schemas/LeaveTypeEnum'
          title: نوع مرخصی
        starts_at:
          type: string
          format: date-time
          title: زمان شروع
        ends_at:
          type: string
          format: date-time
          title: زمان پایان
        requested_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه درخواستی
        reason:
          type: string
          title: علت
          maxLength: 400
        substitute_employee:
          type: string
          format: uuid
          title: جانشین
          nullable: true
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 400
      required:
      - employee
      - ends_at
      - leave_type
      - starts_at
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        leave_type: ANNUAL
        starts_at: '2026-09-23T08:30:00+03:30'
        ends_at: '2027-03-20T12:00:00+03:30'
        requested_minutes: 45
        reason: اصلاح اطلاعات پس از بررسی پرونده
        substitute_employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision_note: توضیح نمونه
    LeaveStatusEnum:
      enum:
      - DRAFT
      - SUBMITTED
      - APPROVED
      - REJECTED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `SUBMITTED` - ارسال‌شده
        * `APPROVED` - تأییدشده
        * `REJECTED` - ردشده
        * `CANCELLED` - لغوشده
    LeaveTypeEnum:
      enum:
      - ANNUAL
      - SICK
      - UNPAID
      - MISSION
      - HOURLY
      - MATERNITY
      - BEREAVEMENT
      type: string
      description: |-
        * `ANNUAL` - استحقاقی
        * `SICK` - استعلاجی
        * `UNPAID` - بدون حقوق
        * `MISSION` - مأموریت
        * `HOURLY` - ساعتی
        * `MATERNITY` - زایمان
        * `BEREAVEMENT` - فوت بستگان
    LedgerRow:
      type: object
      properties:
        entryNo:
          type: string
          title: شماره سند
        entryDate:
          type: string
          format: date
          title: تاریخ ثبت
        description:
          type: string
          title: توضیح
        costCenter:
          type: string
          nullable: true
          title: مرکز هزینه
        debit:
          type: integer
          title: بدهکار
        credit:
          type: integer
          title: بستانکار
        balance:
          type: integer
          title: مانده
      required:
      - balance
      - costCenter
      - credit
      - debit
      - description
      - entryDate
      - entryNo
      example:
        entryNo: JE-1405-000731
        entryDate: '2026-09-23'
        description: توضیح تکمیلی این رکورد
        costCenter: متن نمونه
        debit: 12500000
        credit: 0
        balance: 32500000
    LessonPlan:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        session:
          type: string
          format: uuid
          title: جلسه
          nullable: true
        title:
          type: string
          title: عنوان
          maxLength: 200
        objectives:
          type: string
          title: اهداف یادگیری
        content:
          type: string
          title: محتوا
        planned_for:
          type: string
          format: date
          nullable: true
          title: برنامه‌ریزی‌شده برای
        status:
          allOf:
          - $ref: '#/components/schemas/LessonPlanStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - course_offering
      - created_at
      - id
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        objectives: متن نمونه
        content: متن نمونه
        planned_for: '2026-09-23'
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    LessonPlanRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        session:
          type: string
          format: uuid
          title: جلسه
          nullable: true
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        objectives:
          type: string
          title: اهداف یادگیری
        content:
          type: string
          title: محتوا
        planned_for:
          type: string
          format: date
          nullable: true
          title: برنامه‌ریزی‌شده برای
        status:
          allOf:
          - $ref: '#/components/schemas/LessonPlanStatusEnum'
          title: وضعیت
      required:
      - course_offering
      - title
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        objectives: متن نمونه
        content: متن نمونه
        planned_for: '2026-09-23'
        status: DRAFT
    LessonPlanStatusEnum:
      enum:
      - DRAFT
      - APPROVED
      - DELIVERED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `APPROVED` - تأییدشده
        * `DELIVERED` - اجراشده
    LibraryCopy:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        title_ref:
          type: string
          format: uuid
          title: عنوان کتاب
        title_name:
          type: string
          readOnly: true
          title: عنوان
        barcode:
          type: string
          title: بارکد
          maxLength: 60
        location:
          type: string
          title: محل قفسه
          maxLength: 100
        is_loanable:
          type: boolean
          title: قابل امانت
        acquisition_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای تملک
        status:
          allOf:
          - $ref: '#/components/schemas/CopyStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - barcode
      - created_at
      - id
      - title_name
      - title_ref
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title_ref: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title_name: نام نمونه
        barcode: '6260123456789'
        location: متن نمونه
        is_loanable: true
        acquisition_cost: 2500000
        status: AVAILABLE
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    LibraryCopyRequest:
      type: object
      properties:
        title_ref:
          type: string
          format: uuid
          title: عنوان کتاب
        barcode:
          type: string
          minLength: 1
          title: بارکد
          maxLength: 60
        location:
          type: string
          title: محل قفسه
          maxLength: 100
        is_loanable:
          type: boolean
          title: قابل امانت
        acquisition_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای تملک
        status:
          allOf:
          - $ref: '#/components/schemas/CopyStatusEnum'
          title: وضعیت
      required:
      - barcode
      - title_ref
      example:
        title_ref: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        barcode: '6260123456789'
        location: متن نمونه
        is_loanable: true
        acquisition_cost: 2500000
        status: AVAILABLE
    LibraryLoan:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        copy:
          type: string
          format: uuid
          title: نسخه
        copy_barcode:
          type: string
          readOnly: true
          title: بارکد نسخه
        title_name:
          type: string
          readOnly: true
          title: عنوان
        borrower_person:
          type: string
          format: uuid
          title: امانت‌گیرنده
        borrower_name:
          type: string
          readOnly: true
          title: نام امانت‌گیرنده
        loaned_at:
          type: string
          format: date-time
          title: زمان امانت
        due_at:
          type: string
          format: date-time
          title: مهلت
        returned_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بازگشت
        renewal_count:
          type: integer
          readOnly: true
          title: تعداد تمدید
        fine_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: جریمه (ریال)
        fine_paid:
          type: boolean
          title: جریمه پرداخت‌شده
        status:
          allOf:
          - $ref: '#/components/schemas/LoanStatusEnum'
          readOnly: true
          title: وضعیت
        is_overdue:
          type: boolean
          readOnly: true
          title: دیرکرد دارد
        note:
          type: string
          title: یادداشت
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - borrower_name
      - borrower_person
      - copy
      - copy_barcode
      - created_at
      - due_at
      - id
      - is_overdue
      - loaned_at
      - renewal_count
      - returned_at
      - status
      - title_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        copy: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        copy_barcode: متن نمونه
        title_name: نام نمونه
        borrower_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        borrower_name: نام نمونه
        loaned_at: '2026-09-23T08:30:00+03:30'
        due_at: '2027-03-20T12:00:00+03:30'
        returned_at: '2026-09-23T08:30:00+03:30'
        renewal_count: 12
        fine_amount: 2500000
        fine_paid: true
        status: ACTIVE
        is_overdue: true
        note: یادداشت داخلی
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    LibraryLoanRequest:
      type: object
      properties:
        copy:
          type: string
          format: uuid
          title: نسخه
        borrower_person:
          type: string
          format: uuid
          title: امانت‌گیرنده
        loaned_at:
          type: string
          format: date-time
          title: زمان امانت
        due_at:
          type: string
          format: date-time
          title: مهلت
        fine_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: جریمه (ریال)
        fine_paid:
          type: boolean
          title: جریمه پرداخت‌شده
        note:
          type: string
          title: یادداشت
          maxLength: 300
      required:
      - borrower_person
      - copy
      - due_at
      - loaned_at
      example:
        copy: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        borrower_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        loaned_at: '2026-09-23T08:30:00+03:30'
        due_at: '2027-03-20T12:00:00+03:30'
        fine_amount: 2500000
        fine_paid: true
        note: یادداشت داخلی
    LibraryTitle:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        isbn:
          type: string
          title: شابک
          maxLength: 20
        title:
          type: string
          title: عنوان
          maxLength: 300
        author:
          type: string
          title: نویسنده
          maxLength: 250
        publisher:
          type: string
          title: ناشر
          maxLength: 200
        publish_year:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: سال انتشار
        material_type:
          allOf:
          - $ref: '#/components/schemas/MaterialTypeEnum'
          title: نوع ماده
        classification:
          type: string
          title: رده‌بندی
          maxLength: 60
        subject:
          type: string
          title: موضوع
          maxLength: 200
        language:
          type: string
          title: زبان
          maxLength: 10
        copy_count:
          type: integer
          readOnly: true
          title: تعداد نسخه‌ها
        available_count:
          type: integer
          readOnly: true
          title: تعداد نسخه در دسترس
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - available_count
      - copy_count
      - created_at
      - id
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        isbn: '9786001234567'
        title: عنوان نمونه
        author: محمدرضا کریمی
        publisher: نشر نمونه
        publish_year: 3
        material_type: BOOK
        classification: متن نمونه
        subject: متن نمونه
        language: متن نمونه
        copy_count: 12
        available_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    LibraryTitleRequest:
      type: object
      properties:
        isbn:
          type: string
          title: شابک
          maxLength: 20
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 300
        author:
          type: string
          title: نویسنده
          maxLength: 250
        publisher:
          type: string
          title: ناشر
          maxLength: 200
        publish_year:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: سال انتشار
        material_type:
          allOf:
          - $ref: '#/components/schemas/MaterialTypeEnum'
          title: نوع ماده
        classification:
          type: string
          title: رده‌بندی
          maxLength: 60
        subject:
          type: string
          title: موضوع
          maxLength: 200
        language:
          type: string
          minLength: 1
          title: زبان
          maxLength: 10
      required:
      - title
      example:
        isbn: '9786001234567'
        title: عنوان نمونه
        author: محمدرضا کریمی
        publisher: نشر نمونه
        publish_year: 3
        material_type: BOOK
        classification: متن نمونه
        subject: متن نمونه
        language: متن نمونه
    LoanStatusEnum:
      enum:
      - ACTIVE
      - RETURNED
      - OVERDUE
      - LOST
      - RENEWED
      type: string
      description: |-
        * `ACTIVE` - در امانت
        * `RETURNED` - بازگردانده‌شده
        * `OVERDUE` - سررسید گذشته
        * `LOST` - مفقود
        * `RENEWED` - تمدیدشده
    LoginRequest:
      type: object
      description: بدنه ورود.
      properties:
        username:
          type: string
          minLength: 1
          description: نام کاربری یا تلفن همراه
        password:
          type: string
          writeOnly: true
          minLength: 1
          title: گذرواژه
      required:
      - password
      - username
      example:
        username: s.mohammadi
        password: P@ssw0rd!2026
    MaintenanceOrder:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        asset:
          type: string
          format: uuid
          title: دارایی
        asset_tag:
          type: string
          readOnly: true
          title: پلاک اموال
        order_no:
          type: string
          readOnly: true
          title: شماره سفارش
        maintenance_type:
          allOf:
          - $ref: '#/components/schemas/MaintenanceTypeEnum'
          title: نوع نگهداشت
        description:
          type: string
          title: شرح
        vendor:
          type: string
          format: uuid
          title: تأمین‌کننده
          nullable: true
        vendor_name:
          type: string
          readOnly: true
          title: نام تأمین‌کننده
        opened_at:
          type: string
          format: date-time
          title: زمان بازکردن
        closed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان بستن
        cost_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ هزینه
        status:
          allOf:
          - $ref: '#/components/schemas/MaintenanceStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - asset
      - asset_tag
      - created_at
      - id
      - maintenance_type
      - opened_at
      - order_no
      - updated_at
      - vendor_name
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        asset_tag: AST-004512
        order_no: PO-1405-000027
        maintenance_type: PREVENTIVE
        description: توضیح تکمیلی این رکورد
        vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vendor_name: نام نمونه
        opened_at: '2026-09-23T08:30:00+03:30'
        closed_at: '2027-03-20T12:00:00+03:30'
        cost_amount: 2500000
        status: OPEN
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    MaintenanceOrderRequest:
      type: object
      properties:
        asset:
          type: string
          format: uuid
          title: دارایی
        maintenance_type:
          allOf:
          - $ref: '#/components/schemas/MaintenanceTypeEnum'
          title: نوع نگهداشت
        description:
          type: string
          title: شرح
        vendor:
          type: string
          format: uuid
          title: تأمین‌کننده
          nullable: true
        opened_at:
          type: string
          format: date-time
          title: زمان بازکردن
        closed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان بستن
        cost_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ هزینه
        status:
          allOf:
          - $ref: '#/components/schemas/MaintenanceStatusEnum'
          title: وضعیت
      required:
      - asset
      - maintenance_type
      - opened_at
      example:
        asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        maintenance_type: PREVENTIVE
        description: توضیح تکمیلی این رکورد
        vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        opened_at: '2026-09-23T08:30:00+03:30'
        closed_at: '2027-03-20T12:00:00+03:30'
        cost_amount: 2500000
        status: OPEN
    MaintenanceStatusEnum:
      enum:
      - OPEN
      - IN_PROGRESS
      - COMPLETED
      - CANCELLED
      type: string
      description: |-
        * `OPEN` - باز
        * `IN_PROGRESS` - در حال انجام
        * `COMPLETED` - انجام‌شده
        * `CANCELLED` - لغوشده
    MaintenanceTypeEnum:
      enum:
      - PREVENTIVE
      - CORRECTIVE
      - EMERGENCY
      - INSPECTION
      type: string
      description: |-
        * `PREVENTIVE` - پیشگیرانه
        * `CORRECTIVE` - اصلاحی
        * `EMERGENCY` - اضطراری
        * `INSPECTION` - بازرسی
    ManualGradeRequest:
      type: object
      properties:
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          title: نمره
        feedback:
          type: string
          maxLength: 2000
          title: بازخورد
        review_type:
          allOf:
          - $ref: '#/components/schemas/ManualGradeReviewTypeEnum'
          default: FIRST_PASS
          title: نوع بازبینی دسترسی
      required:
      - score
      example:
        score: '18.50'
        feedback: متن نمونه
        review_type: FIRST_PASS
    ManualGradeReviewTypeEnum:
      enum:
      - FIRST_PASS
      - SECOND_PASS
      - APPEAL
      - MODERATION
      type: string
      description: |-
        * `FIRST_PASS` - FIRST_PASS
        * `SECOND_PASS` - SECOND_PASS
        * `APPEAL` - APPEAL
        * `MODERATION` - MODERATION
    MaterialTypeEnum:
      enum:
      - BOOK
      - MAGAZINE
      - AUDIO
      - VIDEO
      - DIGITAL
      - EQUIPMENT
      type: string
      description: |-
        * `BOOK` - کتاب
        * `MAGAZINE` - مجله
        * `AUDIO` - صوتی
        * `VIDEO` - تصویری
        * `DIGITAL` - دیجیتال
        * `EQUIPMENT` - تجهیزات
    MethodEnum:
      enum:
      - CASH
      - POS
      - TRANSFER
      - CHEQUE
      - ONLINE_GATEWAY
      - WALLET
      - MIXED
      type: string
      description: |-
        * `CASH` - نقدی
        * `POS` - کارت‌خوان
        * `TRANSFER` - انتقال بانکی
        * `CHEQUE` - چک
        * `ONLINE_GATEWAY` - درگاه آنلاین
        * `WALLET` - کیف پول
        * `MIXED` - ترکیبی
    MfaMethodEnum:
      enum:
      - NONE
      - TOTP
      - SMS
      - EMAIL
      type: string
      description: |-
        * `NONE` - بدون احراز دومرحله‌ای
        * `TOTP` - اپلیکیشن رمز یک‌بارمصرف
        * `SMS` - پیامک
        * `EMAIL` - ایمیل
    ModeEnum:
      enum:
      - ONLINE
      - IN_PERSON
      - HYBRID
      - PAPER_ENTRY
      - TAKE_HOME
      type: string
      description: |-
        * `ONLINE` - آنلاین
        * `IN_PERSON` - حضوری
        * `HYBRID` - ترکیبی
        * `PAPER_ENTRY` - کاغذی با ورود نمره
        * `TAKE_HOME` - تکلیف زمان‌دار
    MyTask:
      type: object
      description: یک کار در Inbox «کارهای من» (بخش ۶.۲ سند فرانت).
      properties:
        type:
          type: string
          description: APPROVAL | TICKET | ATTENDANCE_PENDING
        id:
          type: string
          format: uuid
          title: شناسه
        title:
          type: string
          title: عنوان
        subtitle:
          type: string
          title: زیرعنوان
        dueAt:
          type: string
          format: date-time
          nullable: true
          title: مهلت
        priority:
          type: string
          title: اولویت
        link:
          type: string
          description: مسیر پیشنهادی در فرانت
      required:
      - dueAt
      - id
      - link
      - priority
      - subtitle
      - title
      - type
      example:
        type: متن نمونه
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        subtitle: متن نمونه
        dueAt: '2027-03-20T12:00:00+03:30'
        priority: متن نمونه
        link: متن نمونه
    Notification:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        template:
          type: string
          format: uuid
          title: قالب
          nullable: true
        recipient_person:
          type: string
          format: uuid
          title: گیرنده
        recipient_name:
          type: string
          readOnly: true
          title: نام گیرنده
        channel:
          allOf:
          - $ref: '#/components/schemas/NotificationChannelEnum'
          title: کانال
        channel_display:
          type: string
          readOnly: true
          title: برچسب فارسی کانال ارسال
        priority:
          allOf:
          - $ref: '#/components/schemas/NotificationPriorityEnum'
          title: اولویت
        subject:
          type: string
          title: موضوع
          maxLength: 300
        body:
          type: string
          title: متن رندرشده
        deep_link:
          type: string
          title: پیوند پرتال امن
          maxLength: 400
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        scheduled_at:
          type: string
          format: date-time
          nullable: true
          title: زمان زمان‌بندی‌شده
        sent_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ارسال
        read_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان خواندن
        status:
          allOf:
          - $ref: '#/components/schemas/NotificationStatusEnum'
          readOnly: true
          title: وضعیت
        related_type:
          type: string
          title: نوع مرتبط
          maxLength: 80
        related_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه مرتبط
        attempts:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryAttempt'
          readOnly: true
          title: تلاش‌های ارسال
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - attempts
      - body
      - channel
      - channel_display
      - created_at
      - id
      - read_at
      - recipient_name
      - recipient_person
      - sent_at
      - status
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        template: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        recipient_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        recipient_name: نام نمونه
        channel: IN_APP
        channel_display: درون‌برنامه‌ای
        priority: TRANSACTIONAL
        subject: متن نمونه
        body: متن نمونه
        deep_link: متن نمونه
        classification: PUBLIC
        scheduled_at: '2026-09-23T08:30:00+03:30'
        sent_at: '2026-09-23T08:30:00+03:30'
        read_at: '2026-09-23T08:30:00+03:30'
        status: QUEUED
        related_type: متن نمونه
        related_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempts:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          notification: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attempt_no: 3
          provider: متن نمونه
          provider_reference: متن نمونه
          result: SUCCESS
          error_message: متن نمونه
          attempted_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    NotificationChannelEnum:
      enum:
      - IN_APP
      - SMS
      - EMAIL
      - PUSH
      type: string
      description: |-
        * `IN_APP` - درون‌برنامه‌ای
        * `SMS` - پیامک
        * `EMAIL` - ایمیل
        * `PUSH` - اعلان موبایل
    NotificationPreference:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        allow_sms:
          type: boolean
          title: اجازه پیامک
        allow_email:
          type: boolean
          title: اجازه رایانامه
        allow_push:
          type: boolean
          title: اجازه اعلان Push
        allow_promotional:
          type: boolean
          title: اجازه پیام تبلیغاتی
        quiet_hours_start:
          type: string
          format: time
          nullable: true
          title: آغاز ساعات سکوت
        quiet_hours_end:
          type: string
          format: time
          nullable: true
          title: پایان ساعات سکوت
        preferred_locale:
          type: string
          title: زبان ترجیحی
          maxLength: 10
      required:
      - id
      - person
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        allow_sms: true
        allow_email: true
        allow_push: true
        allow_promotional: true
        quiet_hours_start: 08:30:00
        quiet_hours_end: 08:30:00
        preferred_locale: fa-IR
    NotificationPreferenceRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        allow_sms:
          type: boolean
          title: اجازه پیامک
        allow_email:
          type: boolean
          title: اجازه رایانامه
        allow_push:
          type: boolean
          title: اجازه اعلان Push
        allow_promotional:
          type: boolean
          title: اجازه پیام تبلیغاتی
        quiet_hours_start:
          type: string
          format: time
          nullable: true
          title: آغاز ساعات سکوت
        quiet_hours_end:
          type: string
          format: time
          nullable: true
          title: پایان ساعات سکوت
        preferred_locale:
          type: string
          minLength: 1
          title: زبان ترجیحی
          maxLength: 10
      required:
      - person
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        allow_sms: true
        allow_email: true
        allow_push: true
        allow_promotional: true
        quiet_hours_start: 08:30:00
        quiet_hours_end: 08:30:00
        preferred_locale: fa-IR
    NotificationPriorityEnum:
      enum:
      - TRANSACTIONAL
      - OPERATIONAL
      - PROMOTIONAL
      - EMERGENCY
      type: string
      description: |-
        * `TRANSACTIONAL` - تراکنشی
        * `OPERATIONAL` - عملیاتی
        * `PROMOTIONAL` - اطلاع‌رسانی عمومی
        * `EMERGENCY` - اضطراری
    NotificationRequest:
      type: object
      properties:
        template:
          type: string
          format: uuid
          title: قالب
          nullable: true
        recipient_person:
          type: string
          format: uuid
          title: گیرنده
        channel:
          allOf:
          - $ref: '#/components/schemas/NotificationChannelEnum'
          title: کانال
        priority:
          allOf:
          - $ref: '#/components/schemas/NotificationPriorityEnum'
          title: اولویت
        subject:
          type: string
          title: موضوع
          maxLength: 300
        body:
          type: string
          minLength: 1
          title: متن رندرشده
        deep_link:
          type: string
          title: پیوند پرتال امن
          maxLength: 400
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        scheduled_at:
          type: string
          format: date-time
          nullable: true
          title: زمان زمان‌بندی‌شده
        related_type:
          type: string
          title: نوع مرتبط
          maxLength: 80
        related_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه مرتبط
      required:
      - body
      - channel
      - recipient_person
      example:
        template: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        recipient_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        channel: IN_APP
        priority: TRANSACTIONAL
        subject: متن نمونه
        body: متن نمونه
        deep_link: متن نمونه
        classification: PUBLIC
        scheduled_at: '2026-09-23T08:30:00+03:30'
        related_type: متن نمونه
        related_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    NotificationStatusEnum:
      enum:
      - QUEUED
      - SENDING
      - SENT
      - DELIVERED
      - FAILED
      - CANCELLED
      - READ
      type: string
      description: |-
        * `QUEUED` - در صف
        * `SENDING` - در حال ارسال
        * `SENT` - ارسال‌شده
        * `DELIVERED` - تحویل‌شده
        * `FAILED` - ناموفق
        * `CANCELLED` - لغوشده
        * `READ` - خوانده‌شده
    NotificationTemplate:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد
          maxLength: 60
        title:
          type: string
          title: عنوان
          maxLength: 200
        channel:
          allOf:
          - $ref: '#/components/schemas/NotificationChannelEnum'
          title: کانال
        locale:
          type: string
          title: زبان و منطقه
          maxLength: 10
        version_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره نسخه
        subject_template:
          type: string
          title: قالب موضوع
          maxLength: 300
        body_template:
          type: string
          title: متن قالب
          description: متغیرها با {{variable}} نوشته می‌شوند.
        is_active:
          type: boolean
          title: فعال
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - body_template
      - channel
      - code
      - created_at
      - id
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        channel: IN_APP
        locale: fa-IR
        version_no: 3
        subject_template: متن نمونه
        body_template: متن نمونه
        is_active: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    NotificationTemplateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 60
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        channel:
          allOf:
          - $ref: '#/components/schemas/NotificationChannelEnum'
          title: کانال
        locale:
          type: string
          minLength: 1
          title: زبان و منطقه
          maxLength: 10
        version_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره نسخه
        subject_template:
          type: string
          title: قالب موضوع
          maxLength: 300
        body_template:
          type: string
          minLength: 1
          title: متن قالب
          description: متغیرها با {{variable}} نوشته می‌شوند.
        is_active:
          type: boolean
          title: فعال
      required:
      - body_template
      - channel
      - code
      - title
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        channel: IN_APP
        locale: fa-IR
        version_no: 3
        subject_template: متن نمونه
        body_template: متن نمونه
        is_active: true
    OperationResult:
      type: object
      description: پاسخ عمومی عملیات فرمانی.
      properties:
        success:
          type: boolean
          title: عملیات موفق بود
        message:
          type: string
          title: پیام
        affected:
          type: integer
          title: تعداد رکوردهای متأثر
      required:
      - success
      example:
        success: true
        message: عملیات با موفقیت انجام شد.
        affected: 3
    OrgUnit:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        campus:
          type: string
          format: uuid
          title: شعبه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          title: کد
          maxLength: 30
        title:
          type: string
          title: عنوان واحد
          maxLength: 150
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus
      - code
      - created_at
      - id
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    OrgUnitRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان واحد
          maxLength: 150
      required:
      - campus
      - code
      - title
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
    OutboxEvent:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        aggregate_type:
          type: string
          readOnly: true
          title: نوع تجمیع
        aggregate_id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه تجمیع
        event_type:
          type: string
          readOnly: true
          title: نوع رویداد
        schema_version:
          type: integer
          readOnly: true
          title: نسخه قرارداد
        payload:
          readOnly: true
          title: بار داده
        correlation_id:
          type: string
          readOnly: true
          title: شناسه ردیابی
        occurred_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان رخداد
        published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار
        retry_count:
          type: integer
          readOnly: true
          title: تعداد تلاش مجدد
        last_error:
          type: string
          readOnly: true
          title: آخرین خطا
      required:
      - aggregate_id
      - aggregate_type
      - correlation_id
      - event_type
      - id
      - last_error
      - occurred_at
      - payload
      - published_at
      - retry_count
      - schema_version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        aggregate_type: متن نمونه
        aggregate_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: متن نمونه
        schema_version: 3
        correlation_id: 8c4a1f2e
        occurred_at: '2026-09-23T08:30:00+03:30'
        published_at: '2026-09-23T08:30:00+03:30'
        retry_count: 12
        last_error: متن نمونه
    OutcomeEnum:
      enum:
      - RESOLVED_ON_SITE
      - SENT_HOME
      - REFERRED_CLINIC
      - EMERGENCY
      - UNDER_OBSERVATION
      type: string
      description: |-
        * `RESOLVED_ON_SITE` - رفع در محل
        * `SENT_HOME` - ارسال به منزل
        * `REFERRED_CLINIC` - ارجاع به درمانگاه
        * `EMERGENCY` - اورژانس
        * `UNDER_OBSERVATION` - تحت نظر
    PaginatedAbsenceJustificationList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AbsenceJustification'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attendance: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          session_date: '2026-09-23T08:30:00+03:30'
          submitted_by_guardian_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          reason: اصلاح اطلاعات پس از بررسی پرونده
          evidence_file: https://example.school/media/sample.pdf
          decision: PENDING
          decided_at: '2026-09-23T08:30:00+03:30'
          decision_note: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAcademicYearList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AcademicYear'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school_name: دبیرستان نمونه
          title: عنوان نمونه
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          is_default: true
          status: PLANNING
          status_display: در حال برنامه‌ریزی
          is_editable: true
          closed_at: '2027-03-20T12:00:00+03:30'
          close_note: توضیح نمونه
          terms:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            title: عنوان نمونه
            starts_on: '2026-09-23'
            ends_on: '2027-03-20'
            sequence_no: 3
            status: PLANNED
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAccessReviewItemList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AccessReviewItem'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          review: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          assignment_detail:
            id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            username: s.mohammadi
            role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            role_code: SAMPLE-001
            role_title: عنوان نمونه
            scope_type: TENANT
            scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            status: ACTIVE
            effective_from: '2026-09-23'
            effective_to: '2027-03-20'
            is_currently_effective: true
            grant_reason: توضیح نمونه
            revoked_at: '2026-09-23T08:30:00+03:30'
            revoke_reason: توضیح نمونه
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          decision: PENDING
          reviewed_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          reviewed_at: '2026-09-23T08:30:00+03:30'
          note: یادداشت داخلی
    PaginatedAccessReviewList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AccessReview'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          opened_at: '2026-09-23T08:30:00+03:30'
          closed_at: '2027-03-20T12:00:00+03:30'
          status: OPEN
          scope_note: توضیح نمونه
          item_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAccountList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Account'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          parent_code: SAMPLE-001
          code: SAMPLE-001
          title: عنوان نمونه
          account_type: ASSET
          account_type_display: دارایی
          allows_posting: true
          is_active: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAddressList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Address'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          country: IR
          province: تهران
          city: تهران
          district: منطقه ۳
          postal_code: '1234567890'
          line: خیابان نمونه، کوچه دوم، پلاک ۱۲
          latitude: '35.6892'
          longitude: '51.389'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAdmissionApplicationList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AdmissionApplication'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          applicant_name: سارا محمدی
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          preferred_campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus_name: شعبه مرکزی
          preferred_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_level_title: پایه هفتم
          preferred_program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          application_no: SAMPLE-001
          status: DRAFT
          status_display: پیش‌نویس
          submitted_at: '2026-09-23T08:30:00+03:30'
          reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          interview_at: '2026-09-23T08:30:00+03:30'
          entrance_score: '18.50'
          interview_score: '18.50'
          final_score: '18.50'
          waitlist_rank: 3
          conditions: متن نمونه
          decision_reason: توضیح نمونه
          decided_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedApprovalRequestList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalRequest'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - subject_type: متن نمونه
          subject_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          subject_label: متن نمونه
          workflow_code: SAMPLE-001
          workflow_version: 3
          requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          requested_at: '2026-09-23T08:30:00+03:30'
          note: یادداشت داخلی
    PaginatedApprovalStepList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalStep'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          sequence_no: 3
          approver_type: متن نمونه
          approver_role_code: SAMPLE-001
          approver_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          decision: PENDING
          decision_display: در انتظار
          decided_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          decided_on_behalf_of_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          decided_at: '2026-09-23T08:30:00+03:30'
          comment: توضیح کوتاه ثبت‌کننده
          due_at: '2027-03-20T12:00:00+03:30'
    PaginatedAssessmentCategoryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentCategory'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          weight_percent: '18.50'
          drop_policy: NONE
          display_order: 1
          is_active: true
          item_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAssetAssignmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AssetAssignment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          asset_tag: AST-004512
          assignee_type: EMPLOYEE
          assignee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          location_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          assigned_at: '2026-09-23T08:30:00+03:30'
          returned_at: '2026-09-23T08:30:00+03:30'
          condition_on_assign: NEW
          condition_on_return: NEW
          status: متن نمونه
          note: یادداشت داخلی
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAssetList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Asset'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_title: عنوان نمونه
          asset_tag: AST-004512
          title: عنوان نمونه
          serial_no: SN-88213047
          acquired_on: '2024-05-11'
          acquisition_cost: 2500000
          useful_life_months: 3
          accumulated_depreciation: 3
          book_value: 3
          warranty_until: '2027-03-20'
          insurance_policy_no: SAMPLE-001
          condition_status: NEW
          lifecycle_status: REGISTERED
          lifecycle_status_display: ثبت‌نام‌شده
          current_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          current_assignee: {}
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAssignmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Assignment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          class_group_code: G7-A
          title: عنوان نمونه
          description: توضیح تکمیلی این رکورد
          opens_at: '2026-09-23T08:30:00+03:30'
          due_at: '2027-03-20T12:00:00+03:30'
          close_at: '2026-09-23T08:30:00+03:30'
          max_score: '18.50'
          allow_late_submission: true
          late_penalty_percent: '18.50'
          max_attempts: 3
          status: DRAFT
          status_display: پیش‌نویس
          published_at: '2026-09-23T08:30:00+03:30'
          submission_stats: {}
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAssignmentSubmissionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AssignmentSubmission'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          assignment_title: تمرین سری سوم
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          attempt_no: 1
          submitted_at: '2026-09-23T08:30:00+03:30'
          content: متن نمونه
          attachment: https://example.school/media/sample.pdf
          status: DRAFT
          is_late: true
          late_minutes: 45
          feedbacks:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            submission: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            score: '18.50'
            feedback: متن نمونه
            reviewed_at: '2026-09-23T08:30:00+03:30'
            is_final: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAttachmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Attachment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          owner_type: متن نمونه
          owner_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          file: https://example.school/media/sample.pdf
          original_name: sample.pdf
          media_type: application/pdf
          size_bytes: 248576
          sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
          classification: PUBLIC
          scan_status: PENDING
          scanned_at: '2026-09-23T08:30:00+03:30'
          is_available: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAttemptAnswerList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AttemptAnswer'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam_question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attachment: https://example.school/media/sample.pdf
          saved_at: '2026-09-23T08:30:00+03:30'
          save_revision: 3
          awarded_score: '18.50'
          grading_status: PENDING
          time_spent_seconds: 120
    PaginatedAttendanceMonitorRowList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceMonitorRow'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - classGroupId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          classGroupCode: G7-A
          gradeLevel: هفتم
          totalSessions: 12
          finalizedSessions: 3
          pendingSessions: 3
          absentToday: 3
    PaginatedAttendanceRecordList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/AttendanceRecord'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          attendance_status: PRESENT
          status_display: برچسب فارسی مقدار
          late_minutes: 45
          early_leave_minutes: 45
          reason_code: SAMPLE-001
          note: یادداشت داخلی
          finalization_status: DRAFT
          amended_reason: توضیح نمونه
          has_justification: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedAuditLogList:
      type: object
      required:
      - results
      properties:
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        pageSize:
          type: integer
          example: 50
          title: اندازه صفحه
        results:
          type: array
          items:
            $ref: '#/components/schemas/AuditLog'
          title: رکوردهای این صفحه
      example:
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        pageSize: 50
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          actor_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          actor_username: متن نمونه
          action: CREATE
          action_display: ایجاد
          entity_type: متن نمونه
          entity_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          entity_label: متن نمونه
          reason: اصلاح اطلاعات پس از بررسی پرونده
          correlation_id: 8c4a1f2e
          classification: PUBLIC
          client_ip: متن نمونه
          occurred_at: '2026-09-23T08:30:00+03:30'
    PaginatedBankAccountList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/BankAccount'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          bank_name: نام نمونه
          iban_masked: متن نمونه
          ledger_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          currency: IRR
          status: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedBankReconciliationList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/BankReconciliation'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          bank_account_title: عنوان نمونه
          period_start: '2026-09-23'
          period_end: '2027-03-20'
          statement_balance: 3
          ledger_balance: 3
          difference: 3
          status: OPEN
          note: یادداشت داخلی
          reconciled_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedBehaviorActionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/BehaviorAction'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          incident: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          action_type: VERBAL_WARNING
          action_type_display: تذکر شفاهی
          details: متن نمونه
          effective_from: '2026-09-23'
          effective_until: '2027-03-20'
          status: متن نمونه
          completed_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedBehaviorIncidentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/BehaviorIncident'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          occurred_at: '2026-09-23T08:30:00+03:30'
          location: متن نمونه
          incident_type: DISRUPTION
          incident_type_display: اخلال در کلاس
          severity: MINOR
          description: توضیح تکمیلی این رکورد
          witnesses: متن نمونه
          student_statement: متن نمونه
          status: REPORTED
          status_display: گزارش‌شده
          investigation_note: توضیح نمونه
          decided_at: '2026-09-23T08:30:00+03:30'
          points: 3
          guardian_notified_at: '2026-09-23T08:30:00+03:30'
          actions:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            incident: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            action_type: VERBAL_WARNING
            action_type_display: تذکر شفاهی
            details: متن نمونه
            effective_from: '2026-09-23'
            effective_until: '2027-03-20'
            status: متن نمونه
            completed_at: '2026-09-23T08:30:00+03:30'
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCalendarEventList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/CalendarEvent'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          event_type: HOLIDAY
          event_type_display: تعطیلی
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          is_working_day: true
          description: توضیح تکمیلی این رکورد
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCampusList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Campus'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school_name: دبیرستان نمونه
          code: SAMPLE-001
          name: نام نمونه
          address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
          phone: 09121234567
          timezone: Asia/Tehran
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedClassGroupListList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ClassGroupList'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus_name: شعبه مرکزی
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_level: هفتم
          grade_level_title: پایه هفتم
          program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          program_title: عنوان نمونه
          capacity: 3
          occupied_seats: 3
          available_seats: 3
          status: DRAFT
    PaginatedClassMembershipList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ClassMembership'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          class_group_code: G7-A
          is_primary: true
          status: ACTIVE
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          exit_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedConsentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Consent'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian_name: علی محمدی
          consent_type: PHOTO
          consent_type_display: انتشار تصویر
          status: GRANTED
          granted_at: '2026-09-23T08:30:00+03:30'
          revoked_at: '2026-09-23T08:30:00+03:30'
          revoke_reason: توضیح نمونه
          expires_at: '2027-03-20'
          policy_version: متن نمونه
          policy_text_snapshot: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedContactPointList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ContactPoint'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          contact_type: MOBILE
          contact_type_display: تلفن همراه
          value: متن نمونه
          is_primary: true
          is_verified: true
          verified_at: '2026-09-23T08:30:00+03:30'
          label: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCostCenterList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/CostCenter'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          is_active: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCounselingCaseList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/CounselingCase'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          counselor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          referral_source: TEACHER
          subject: متن نمونه
          priority: LOW
          status: OPEN
          status_display: باز
          opened_on: '2026-09-23'
          closed_on: '2027-03-20'
          action_plan: متن نمونه
          confidentiality_level: STANDARD
          session_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCounselingSessionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/CounselingSession'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          case: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          held_at: '2026-09-23T08:30:00+03:30'
          duration_minutes: 45
          summary: متن نمونه
          protected_note: توضیح نمونه
          next_followup_at: '2026-09-23T08:30:00+03:30'
          attendees: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCourseList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Course'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          credit: '0'
          assessment_scheme: NUMERIC
          assessment_scheme_display: عددی
          max_score: '18.50'
          prerequisites:
          - 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCourseOfferingList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/CourseOffering'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          class_group_code: G7-A
          term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          term_title: نیم‌سال اول
          course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_code: SAMPLE-001
          course_title: ریاضی ۷
          weekly_minutes: 45
          status: PLANNED
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedCourseResultList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/CourseResult'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          continuous_score: '18.50'
          final_exam_score: '18.50'
          final_score: '18.50'
          letter_grade: A
          qualitative_level: EXCELLENT
          result_status: IN_PROGRESS
          result_status_display: در حال بررسی
          calculation_version: 3
          calculated_at: '2026-09-23T08:30:00+03:30'
          teacher_comment: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedDiscountAwardList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/DiscountAward'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          discount_type: SIBLING
          discount_type_display: خواهر/برادر
          percent: '18.50'
          amount: 12500000
          reason: اصلاح اطلاعات پس از بررسی پرونده
          approval_status: PENDING
          approved_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedEmployeeAssignmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeAssignment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          position: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          position_title: عنوان نمونه
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus_name: شعبه مرکزی
          allocation_percent: '18.50'
          is_primary: true
          cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedEmployeeAttendanceList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeAttendance'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee_name: مریم رضایی
          shift: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          work_date: '2026-09-23'
          check_in_at: '2026-09-23T08:30:00+03:30'
          check_out_at: '2026-09-23T08:30:00+03:30'
          worked_minutes: 45
          overtime_minutes: 45
          late_minutes: 45
          source: DEVICE
          status: متن نمونه
          note: یادداشت داخلی
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedEmployeeListList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeList'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee_no: SAMPLE-001
          full_name: سارا محمدی
          national_id: 0012345678
          hired_on: '2026-09-23'
          status: ONBOARDING
          status_display: در حال جذب
          primary_position: متن نمونه
          is_teacher: true
    PaginatedEmploymentContractList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/EmploymentContract'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee_name: مریم رضایی
          contract_no: HR-1405-0042
          contract_type: PERMANENT
          contract_type_display: رسمی/دائم
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          workload_ratio: '18.50'
          base_salary_amount: 2500000
          currency: IRR
          status: DRAFT
          termination_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedEnrollmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Enrollment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          academic_year_title: عنوان نمونه
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_level: هفتم
          grade_level_title: پایه هفتم
          program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          admission_application: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment_no: SAMPLE-001
          enrolled_on: '2026-09-23'
          status: PENDING_DOCUMENTS
          status_display: در انتظار مدارک
          current_class_code: SAMPLE-001
          exit_date: '2027-03-20'
          exit_reason: توضیح نمونه
          class_memberships:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            student_name: سارا محمدی
            student_no: '14030127'
            class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            class_group_code: G7-A
            is_primary: true
            status: ACTIVE
            effective_from: '2026-09-23'
            effective_to: '2027-03-20'
            exit_reason: توضیح نمونه
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedExamAttemptList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ExamAttempt'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          registration: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          exam_title: آزمون میان‌ترم ریاضی
          attempt_no: 1
          started_at: '2026-09-23T08:30:00+03:30'
          submitted_at: '2026-09-23T08:30:00+03:30'
          last_saved_at: '2026-09-23T08:30:00+03:30'
          expires_at: '2027-03-20T12:00:00+03:30'
          remaining_seconds: 120
          auto_score: '18.50'
          manual_score: '18.50'
          final_score: '18.50'
          status: CREATED
          status_display: ایجادشده
          calculation_version: 3
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedExamList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Exam'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          class_group_code: G7-A
          code: SAMPLE-001
          title: عنوان نمونه
          mode: ONLINE
          mode_display: آنلاین
          purpose: FORMATIVE
          max_score: '18.50'
          total_question_score: '18.50'
          instructions: متن نمونه
          shuffle_questions: true
          shuffle_options: true
          allow_backtrack: true
          show_result_immediately: true
          status: DRAFT
          status_display: پیش‌نویس
          published_at: '2026-09-23T08:30:00+03:30'
          results_published_at: '2026-09-23T08:30:00+03:30'
          appeal_deadline: '2027-03-20T12:00:00+03:30'
          cancel_reason: توضیح نمونه
          section_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedExamQuestionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ExamQuestion'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          section: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_body: متن نمونه
          question_type: متن نمونه
          score: '18.50'
          display_order: 1
          is_required: true
    PaginatedExamRegistrationList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ExamRegistration'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam_session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          seat_no: SAMPLE-001
          extra_time_minutes: 45
          registration_status: REGISTERED
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedExamSectionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ExamSection'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          instructions: متن نمونه
          display_order: 1
          time_limit_minutes: 45
          question_count: 12
          questions:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            section: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            question_body: متن نمونه
            question_type: متن نمونه
            score: '18.50'
            display_order: 1
            is_required: true
    PaginatedExamSessionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ExamSession'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          exam_title: آزمون میان‌ترم ریاضی
          room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          room_code: R-204
          title: عنوان نمونه
          opens_at: '2026-09-23T08:30:00+03:30'
          closes_at: '2026-09-23T08:30:00+03:30'
          duration_minutes: 45
          attempt_limit: 3
          proctor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: PLANNED
          registration_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedFeePlanItemList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/FeePlanItem'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_type: REGISTRATION
          fee_type_display: ثبت‌نام
          title: عنوان نمونه
          amount: 12500000
          recurrence: ONE_TIME
          is_mandatory: true
          revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PaginatedFeePlanList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/FeePlan'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_level: هفتم
          title: عنوان نمونه
          currency: IRR
          status: متن نمونه
          total_amount: 45000000
          items:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            fee_type: REGISTRATION
            fee_type_display: ثبت‌نام
            title: عنوان نمونه
            amount: 12500000
            recurrence: ONE_TIME
            is_mandatory: true
            revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedFiscalYearList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/FiscalYear'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          status: OPEN
          is_open: true
          closed_at: '2027-03-20T12:00:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedGoodsReceiptList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/GoodsReceipt'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          order_no: PO-1405-000027
          vendor_name: نام نمونه
          stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          receipt_no: GRN-1405-000058
          received_at: '2026-09-23T08:30:00+03:30'
          quality_status: PENDING
          status: PROVISIONAL
          vendor_invoice_no: SAMPLE-001
          vendor_invoice_amount: 2500000
          three_way_matched: true
          note: یادداشت داخلی
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedGradeAppealList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/GradeAppeal'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          submitted_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          reason: اصلاح اطلاعات پس از بررسی پرونده
          status: SUBMITTED
          resolution: متن نمونه
          resolved_at: '2026-09-23T08:30:00+03:30'
          score_before: '18.50'
          score_after: '18.50'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedGradeItemList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/GradeItem'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          category_title: عنوان نمونه
          source_type: EXAM
          source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          max_score: '18.50'
          weight: '18.50'
          due_on: '2027-03-20'
          status: DRAFT
          status_display: پیش‌نویس
          locked_at: '2026-09-23T08:30:00+03:30'
          recorded_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedGradeLevelList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/GradeLevel'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          sequence_no: 3
          stage: متن نمونه
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedGradeReviewList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/GradeReview'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attempt_answer: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          awarded_score: '18.50'
          feedback: متن نمونه
          review_type: FIRST_PASS
          is_anonymous: true
          reviewed_at: '2026-09-23T08:30:00+03:30'
    PaginatedGradingQueueRowList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/GradingQueueRow'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - attemptAnswerId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attemptId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          examTitle: آزمون میان‌ترم ریاضی
          questionBody: متن نمونه
          responseText: متن نمونه
          maxScore: '18.50'
          gradingRubric: متن نمونه
          studentLabel: متن نمونه
    PaginatedGuardianList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Guardian'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person_detail:
            id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            first_name: سارا
            last_name: محمدی
            full_name: سارا محمدی
            national_id: 0012345678
            birth_date: '2012-09-26'
            gender: MALE
            gender_display: مرد
            status: ACTIVE
          full_name: سارا محمدی
          occupation: متن نمونه
          education_level: متن نمونه
          workplace: متن نمونه
          status: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedHealthAlertList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/HealthAlert'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          health_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          alert_type: ALLERGY
          alert_type_display: حساسیت
          title: عنوان نمونه
          safe_summary: متن نمونه
          instructions: متن نمونه
          severity: LOW
          severity_display: کم
          valid_until: '2027-03-20'
          status: ACTIVE
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedHealthAlertSafeList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/HealthAlertSafe'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          alert_type: ALLERGY
          alert_type_display: حساسیت
          title: عنوان نمونه
          safe_summary: متن نمونه
          severity: LOW
    PaginatedHealthIncidentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/HealthIncident'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          occurred_at: '2026-09-23T08:30:00+03:30'
          location: متن نمونه
          description: توضیح تکمیلی این رکورد
          action_taken: متن نمونه
          outcome: RESOLVED_ON_SITE
          outcome_display: رفع در محل
          guardian_notified_at: '2026-09-23T08:30:00+03:30'
          confidentiality_level: STANDARD
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedHealthProfileList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/HealthProfile'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          blood_type: A+
          height_cm: 3
          weight_kg: 3
          accessibility_needs: متن نمونه
          emergency_contact_name: نام نمونه
          emergency_contact_phone: متن نمونه
          family_physician: متن نمونه
          insurance_no: SAMPLE-001
          confidentiality_level: STANDARD
          alerts:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            health_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            alert_type: ALLERGY
            alert_type_display: حساسیت
            title: عنوان نمونه
            safe_summary: متن نمونه
            instructions: متن نمونه
            severity: LOW
            severity_display: کم
            valid_until: '2027-03-20'
            status: ACTIVE
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          active_alert_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedIntegrationMessageList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/IntegrationMessage'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          integration_code: SAMPLE-001
          direction: INBOUND
          external_message_id: متن نمونه
          correlation_id: 8c4a1f2e
          status: RECEIVED
          error_message: متن نمونه
          received_or_sent_at: '2026-09-23T08:30:00+03:30'
    PaginatedInvoiceLineList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/InvoiceLine'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_type: REGISTRATION
          fee_type_display: ثبت‌نام
          description: توضیح تکمیلی این رکورد
          quantity: 3
          unit_amount: 2500000
          discount_amount: 5000000
          net_amount: 2500000
          revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PaginatedInvoiceList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Invoice'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          invoice_no: INV-1405-000137
          issue_date: '2026-09-23'
          due_date: '2027-03-20'
          installment_no: 3
          total_amount: 45000000
          discount_amount: 5000000
          paid_amount: 2500000
          balance: 32500000
          currency: IRR
          status: DRAFT
          status_display: پیش‌نویس
          description: توضیح تکمیلی این رکورد
          cancel_reason: توضیح نمونه
          lines:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            fee_type: REGISTRATION
            fee_type_display: ثبت‌نام
            description: توضیح تکمیلی این رکورد
            quantity: 3
            unit_amount: 2500000
            discount_amount: 5000000
            net_amount: 2500000
            revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedItemCategoryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ItemCategory'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedItemList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Item'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          category_title: عنوان نمونه
          base_uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          uom_title: عنوان نمونه
          sku: STA-A4-80
          title: عنوان نمونه
          description: توضیح تکمیلی این رکورد
          barcode: '6260123456789'
          lot_tracked: true
          serial_tracked: true
          expiry_tracked: true
          is_capital_asset: true
          reorder_point: '18.50'
          min_stock: '18.50'
          max_stock: '18.50'
          allow_negative_stock: true
          expense_account_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: متن نمونه
          total_on_hand: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedJournalEntryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/JournalEntry'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fiscal_year: '1405'
          entry_no: JE-1405-000731
          entry_date: '2026-09-23'
          source_type: MANUAL
          source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          description: توضیح تکمیلی این رکورد
          status: DRAFT
          status_display: پیش‌نویس
          posted_at: '2026-09-23T08:30:00+03:30'
          reversal_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          total_debit: 12
          total_credit: 12
          is_balanced: true
          lines:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            journal_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            account_code: '1101'
            account_title: عنوان نمونه
            cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            cost_center_title: عنوان نمونه
            debit_amount: 2500000
            credit_amount: 2500000
            description: توضیح تکمیلی این رکورد
            line_no: 3
          created_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedJournalLineList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/JournalLine'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          journal_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          account_code: '1101'
          account_title: عنوان نمونه
          cost_center: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          cost_center_title: عنوان نمونه
          debit_amount: 2500000
          credit_amount: 2500000
          description: توضیح تکمیلی این رکورد
          line_no: 3
    PaginatedLearningResourceList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/LearningResource'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          resource_type: FILE
          title: عنوان نمونه
          description: توضیح تکمیلی این رکورد
          file: https://example.school/media/sample.pdf
          url: https://example.school/resources/sample.pdf
          visibility: CLASS_ONLY
          published_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedLeaveRequestList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/LeaveRequest'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          leave_type: ANNUAL
          starts_at: '2026-09-23T08:30:00+03:30'
          ends_at: '2027-03-20T12:00:00+03:30'
          requested_minutes: 45
          reason: اصلاح اطلاعات پس از بررسی پرونده
          substitute_employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          decision_note: توضیح نمونه
    PaginatedLessonPlanList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/LessonPlan'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          objectives: متن نمونه
          content: متن نمونه
          planned_for: '2026-09-23'
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedLibraryCopyList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/LibraryCopy'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title_ref: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title_name: نام نمونه
          barcode: '6260123456789'
          location: متن نمونه
          is_loanable: true
          acquisition_cost: 2500000
          status: AVAILABLE
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedLibraryLoanList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/LibraryLoan'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          copy: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          copy_barcode: متن نمونه
          title_name: نام نمونه
          borrower_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          borrower_name: نام نمونه
          loaned_at: '2026-09-23T08:30:00+03:30'
          due_at: '2027-03-20T12:00:00+03:30'
          returned_at: '2026-09-23T08:30:00+03:30'
          renewal_count: 12
          fine_amount: 2500000
          fine_paid: true
          status: ACTIVE
          is_overdue: true
          note: یادداشت داخلی
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedLibraryTitleList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/LibraryTitle'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          isbn: '9786001234567'
          title: عنوان نمونه
          author: محمدرضا کریمی
          publisher: نشر نمونه
          publish_year: 3
          material_type: BOOK
          classification: متن نمونه
          subject: متن نمونه
          language: متن نمونه
          copy_count: 12
          available_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedMaintenanceOrderList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/MaintenanceOrder'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          asset_tag: AST-004512
          order_no: PO-1405-000027
          maintenance_type: PREVENTIVE
          description: توضیح تکمیلی این رکورد
          vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          vendor_name: نام نمونه
          opened_at: '2026-09-23T08:30:00+03:30'
          closed_at: '2027-03-20T12:00:00+03:30'
          cost_amount: 2500000
          status: OPEN
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedNotificationList:
      type: object
      required:
      - results
      properties:
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        pageSize:
          type: integer
          example: 50
          title: اندازه صفحه
        results:
          type: array
          items:
            $ref: '#/components/schemas/Notification'
          title: رکوردهای این صفحه
      example:
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        pageSize: 50
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          template: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          recipient_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          recipient_name: نام نمونه
          channel: IN_APP
          channel_display: درون‌برنامه‌ای
          priority: TRANSACTIONAL
          subject: متن نمونه
          body: متن نمونه
          deep_link: متن نمونه
          classification: PUBLIC
          scheduled_at: '2026-09-23T08:30:00+03:30'
          sent_at: '2026-09-23T08:30:00+03:30'
          read_at: '2026-09-23T08:30:00+03:30'
          status: QUEUED
          related_type: متن نمونه
          related_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attempts:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            notification: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            attempt_no: 3
            provider: متن نمونه
            provider_reference: متن نمونه
            result: SUCCESS
            error_message: متن نمونه
            attempted_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedNotificationPreferenceList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/NotificationPreference'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          allow_sms: true
          allow_email: true
          allow_push: true
          allow_promotional: true
          quiet_hours_start: 08:30:00
          quiet_hours_end: 08:30:00
          preferred_locale: fa-IR
    PaginatedNotificationTemplateList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/NotificationTemplate'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          channel: IN_APP
          locale: fa-IR
          version_no: 3
          subject_template: متن نمونه
          body_template: متن نمونه
          is_active: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedOrgUnitList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/OrgUnit'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedOutboxEventList:
      type: object
      required:
      - results
      properties:
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        pageSize:
          type: integer
          example: 50
          title: اندازه صفحه
        results:
          type: array
          items:
            $ref: '#/components/schemas/OutboxEvent'
          title: رکوردهای این صفحه
      example:
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        pageSize: 50
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          aggregate_type: متن نمونه
          aggregate_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          event_type: متن نمونه
          schema_version: 3
          correlation_id: 8c4a1f2e
          occurred_at: '2026-09-23T08:30:00+03:30'
          published_at: '2026-09-23T08:30:00+03:30'
          retry_count: 12
          last_error: متن نمونه
    PaginatedPaymentAllocationList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PaymentAllocation'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          invoice_no: INV-1405-000137
          amount: 12500000
          allocated_at: '2026-09-23T08:30:00+03:30'
    PaginatedPaymentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Payment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payer_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payer_name: نام نمونه
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payment_no: PAY-1405-000412
          method: CASH
          method_display: نقدی
          amount: 12500000
          currency: IRR
          received_at: '2026-09-23T08:30:00+03:30'
          bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          gateway_reference: متن نمونه
          cheque_no: SAMPLE-001
          cheque_due_date: '2026-09-23'
          status: PENDING
          status_display: در انتظار
          note: یادداشت داخلی
          void_reason: توضیح نمونه
          allocations: []
          allocated_amount: 2500000
          unallocated_amount: 2500000
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedPayrollRunList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PayrollRun'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          period_from: '2026-09-23'
          period_to: '2027-03-20'
          status: DRAFT
          status_display: پیش‌نویس
          calculated_at: '2026-09-23T08:30:00+03:30'
          approved_at: '2026-09-23T08:30:00+03:30'
          payslip_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedPayslipList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Payslip'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payroll_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee_name: مریم رضایی
          gross_amount: 2500000
          deduction_amount: 2500000
          net_amount: 2500000
          currency: IRR
          status: متن نمونه
          correction_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          items: []
          created_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedPersonAddressList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PersonAddress'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          address_detail:
            id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            country: IR
            province: تهران
            city: تهران
            district: منطقه ۳
            postal_code: '1234567890'
            line: خیابان نمونه، کوچه دوم، پلاک ۱۲
            latitude: '35.6892'
            longitude: '51.389'
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          address_type: HOME
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
    PaginatedPersonDocumentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PersonDocument'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          document_type: متن نمونه
          title: عنوان نمونه
          file: https://example.school/media/sample.pdf
          sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
          classification: PUBLIC
          verification_status: PENDING
          verified_at: '2026-09-23T08:30:00+03:30'
          issued_on: '2024-05-11'
          expires_at: '2027-03-20'
          purpose: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedPersonListList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PersonList'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          first_name: سارا
          last_name: محمدی
          full_name: سارا محمدی
          national_id: 0012345678
          birth_date: '2012-09-26'
          gender: MALE
          gender_display: مرد
          status: ACTIVE
    PaginatedPositionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Position'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          org_unit: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          org_unit_title: عنوان نمونه
          code: SAMPLE-001
          title: عنوان نمونه
          position_type: TEACHING
          position_type_display: آموزشی
          headcount: 3
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedProctorEventList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ProctorEvent'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          event_type: TAB_SWITCH
          severity: INFO
          occurred_at: '2026-09-23T08:30:00+03:30'
          evidence_ref: متن نمونه
          note: یادداشت داخلی
    PaginatedProgramCourseList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ProgramCourse'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_level: هفتم
          grade_level_title: پایه هفتم
          course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          weekly_minutes: 45
          is_required: true
    PaginatedPromotionBatchList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PromotionBatch'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          source_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          target_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          status: متن نمونه
          executed_at: '2026-09-23T08:30:00+03:30'
          decision_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedPromotionDecisionRecordList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PromotionDecisionRecord'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          batch: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          decision: PROMOTED
          target_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          note: یادداشت داخلی
          applied: true
    PaginatedPurchaseOrderLineList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderLine'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_sku: STA-A4-80
          item_title: عنوان نمونه
          ordered_qty: '18.50'
          received_qty: '18.50'
          remaining_qty: '18.50'
          unit_price: 2500000
          line_no: 3
    PaginatedPurchaseOrderList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrder'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          vendor_name: نام نمونه
          purchase_request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          order_no: PO-1405-000027
          ordered_on: '2026-09-23'
          expected_on: '2026-09-23'
          total_amount: 45000000
          tax_amount: 1125000
          currency: IRR
          status: DRAFT
          status_display: پیش‌نویس
          note: یادداشت داخلی
          lines: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedPurchaseRequestLineList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseRequestLine'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_title: عنوان نمونه
          quantity: '18.50'
          estimated_unit_price: 2500000
          note: یادداشت داخلی
          available_stock: 18.5
    PaginatedPurchaseRequestList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseRequest'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          requester_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          request_no: PR-1405-000019
          title: عنوان نمونه
          needed_by: '2026-09-23'
          estimated_amount: 2500000
          justification: متن نمونه
          status: DRAFT
          status_display: پیش‌نویس
          budget_reserved_amount: 2500000
          decision_note: توضیح نمونه
          lines: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedQuestionAnalysisRowList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/QuestionAnalysisRow'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - examQuestionId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          sectionTitle: عنوان نمونه
          questionBody: متن نمونه
          questionType: متن نمونه
          maxScore: 18.5
          responseCount: 12
          correctPercent: 18.5
          averageScore: 18.5
          averageTimeSeconds: 120
    PaginatedQuestionBankList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/QuestionBank'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          owner_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          description: توضیح تکمیلی این رکورد
          course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          visibility: PRIVATE
          status: متن نمونه
          question_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedQuestionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Question'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          bank: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          bank_title: عنوان نمونه
          question_type: SINGLE_CHOICE
          question_type_display: چندگزینه‌ای تک‌پاسخ
          lifecycle_status: DRAFT
          is_auto_graded: true
          current_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          current_version_detail: {}
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedQuestionOptionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/QuestionOption'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          option_key: متن نمونه
          body: متن نمونه
          is_correct: true
          credit_percent: '18.50'
          display_order: 1
    PaginatedQuestionTagList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/QuestionTag'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          tag_type: متن نمونه
          value: متن نمونه
    PaginatedQuestionVersionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/QuestionVersion'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_type: متن نمونه
          version_no: 1
          body: متن نمونه
          explanation: متن نمونه
          grading_rubric: متن نمونه
          default_score: '18.50'
          difficulty: VERY_EASY
          difficulty_display: خیلی آسان
          locale: fa-IR
          grade_level: هفتم
          review_status: PENDING
          reviewed_at: '2026-09-23T08:30:00+03:30'
          media: https://example.school/media/sample.pdf
          options: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedReceivablesAgingList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ReceivablesAging'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - bucket: متن نمونه
          invoiceCount: 12
          totalAmount: 45000000
    PaginatedRefundList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Refund'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payment_no: PAY-1405-000412
          refund_no: SAMPLE-001
          amount: 12500000
          currency: IRR
          reason: اصلاح اطلاعات پس از بررسی پرونده
          requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          approval_status: PENDING
          approved_at: '2026-09-23T08:30:00+03:30'
          status: REQUESTED
          status_display: درخواست‌شده
          completed_at: '2026-09-23T08:30:00+03:30'
          gateway_reference: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedReportCardItemList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ReportCardItem'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          report_card: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_result: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          displayed_score: '18.50'
          displayed_level: متن نمونه
          credit: '0'
          teacher_comment: توضیح نمونه
          display_order: 1
    PaginatedReportCardList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ReportCard'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          term_title: نیم‌سال اول
          version_no: 1
          generated_at: '2026-09-23T08:30:00+03:30'
          published_at: '2026-09-23T08:30:00+03:30'
          status: DRAFT
          status_display: پیش‌نویس
          average_score: '18.50'
          rank_in_class: 3
          principal_comment: توضیح نمونه
          verification_code: SAMPLE-001
          items: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedRidershipEventList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/RidershipEvent'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          route_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          event_type: BOARDED
          event_type_display: سوار شد
          occurred_at: '2026-09-23T08:30:00+03:30'
          source: DEVICE
          guardian_notified_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedRoleList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Role'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          description: توضیح تکمیلی این رکورد
          is_system: true
          requires_mfa: true
          permission_codes: []
          permission_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedRoomList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Room'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          room_type: CLASSROOM
          room_type_display: کلاس درس
          building: متن نمونه
          floor: متن نمونه
          capacity: 3
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedRouteRunList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/RouteRun'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          route_code: R-03
          vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          vehicle_plate: ۱۲ الف ۳۴۵ ایران ۱۰
          driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          supervisor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          run_date: '2026-09-23'
          direction: MORNING
          departed_at: '2026-09-23T08:30:00+03:30'
          completed_at: '2026-09-23T08:30:00+03:30'
          status: PLANNED
          status_display: برنامه‌ریزی‌شده
          event_count: 12
          note: یادداشت داخلی
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedRouteStopList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/RouteStop'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
          latitude: '35.6892'
          longitude: '51.389'
          sequence_no: 3
          scheduled_time: 08:30:00
    PaginatedScheduleConflictList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ScheduleConflict'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - type: متن نمونه
          message: عملیات با موفقیت انجام شد.
          conflictingEntryId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PaginatedScheduleEntryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ScheduleEntry'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          class_group_code: G7-A
          room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          room_code: R-204
          teacher_profile_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          weekday: 0
          weekday_display: شنبه
          starts_at: 08:30:00
          ends_at: 08:30:00
          period_no: 3
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedSchoolList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/School'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          name: نام نمونه
          school_type: PRESCHOOL
          school_type_display: پیش‌دبستان
          gender_policy: متن نمونه
          registration_no: SAMPLE-001
          currency: IRR
          timezone: Asia/Tehran
          logo: https://example.school/media/logo.png
          status: DRAFT
          campus_count: 12
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedScoreChangeList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/ScoreChange'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_score: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          old_score: '18.50'
          new_score: '18.50'
          old_status: متن نمونه
          new_status: متن نمونه
          reason: اصلاح اطلاعات پس از بررسی پرونده
          changed_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          changed_at: '2026-09-23T08:30:00+03:30'
          approval_status: NOT_REQUIRED
          approved_at: '2026-09-23T08:30:00+03:30'
    PaginatedSessionTeacherList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/SessionTeacher'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_name: مریم رضایی
          duty: TEACHER
    PaginatedStockBalanceList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StockBalance'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          warehouse_code: WH-01
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_sku: STA-A4-80
          item_title: عنوان نمونه
          lot_no: SAMPLE-001
          expiry_date: '2027-03-20'
          on_hand_qty: '18.50'
          reserved_qty: '18.50'
          available_qty: '18.50'
          average_cost: 2500000
          below_reorder_point: true
    PaginatedStockDocumentLineList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StockDocumentLine'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_sku: STA-A4-80
          item_title: عنوان نمونه
          quantity: '18.50'
          uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          lot_no: SAMPLE-001
          serial_no: SN-88213047
          expiry_date: '2027-03-20'
          unit_cost: 2500000
          line_no: 3
    PaginatedStockDocumentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StockDocument'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          warehouse_code: WH-01
          document_no: STK-1405-000094
          document_type: RECEIPT
          document_type_display: رسید
          document_at: '2026-09-23T08:30:00+03:30'
          source_type: متن نمونه
          source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          counterpart_warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: DRAFT
          description: توضیح تکمیلی این رکورد
          confirmed_at: '2026-09-23T08:30:00+03:30'
          reversal_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          lines: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedStockMovementList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StockMovement'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          document_line: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          warehouse_code: WH-01
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_sku: STA-A4-80
          lot_no: SAMPLE-001
          serial_no: SN-88213047
          signed_quantity: '18.50'
          unit_cost: 2500000
          occurred_at: '2026-09-23T08:30:00+03:30'
    PaginatedStudentFinancialAgreementList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentFinancialAgreement'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          fee_plan_title: عنوان نمونه
          responsible_guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          responsible_guardian_name: نام نمونه
          agreed_amount: 2500000
          currency: IRR
          installment_count: 12
          status: DRAFT
          note: یادداشت داخلی
          discounts: []
          total_invoiced: 12
          total_paid: 12
          balance: 32500000
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedStudentGuardianList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentGuardian'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian_name: علی محمدی
          guardian_mobile: 09123334455
          relationship_type: FATHER
          relationship_display: برچسب فارسی مقدار
          has_custody: true
          can_pickup: true
          receives_reports: true
          financially_responsible: true
          is_emergency_contact: true
          contact_priority: 3
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedStudentListList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentList'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_no: '14030127'
          full_name: سارا محمدی
          national_id: 0012345678
          gender: متن نمونه
          status: PROSPECTIVE
          status_display: متقاضی
          current_class: متن نمونه
          current_grade: متن نمونه
          joined_on: '2026-09-23'
    PaginatedStudentRouteAssignmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentRouteAssignment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          route_code: R-03
          pickup_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          pickup_stop_title: عنوان نمونه
          dropoff_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          status: متن نمونه
          note: یادداشت داخلی
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedStudentScoreList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentScore'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_item_title: عنوان نمونه
          max_score: '18.50'
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          raw_score: '18.50'
          normalized_score: '18.50'
          letter_grade: A
          qualitative_level: EXCELLENT
          status: NOT_RECORDED
          status_display: ثبت‌نشده
          comment: توضیح کوتاه ثبت‌کننده
          recorded_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedStudentStatusHistoryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentStatusHistory'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          from_status: متن نمونه
          to_status: متن نمونه
          reason_code: SAMPLE-001
          reason: اصلاح اطلاعات پس از بررسی پرونده
          changed_at: '2026-09-23T08:30:00+03:30'
    PaginatedStudentTransferList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudentTransfer'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          transfer_type: INTERNAL_CLASS
          from_class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          from_class_code: SAMPLE-001
          to_class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          to_class_code: SAMPLE-001
          effective_on: '2026-09-23'
          reason: اصلاح اطلاعات پس از بررسی پرونده
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedStudyProgramList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/StudyProgram'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          description: توضیح تکمیلی این رکورد
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedSubmissionFeedbackList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/SubmissionFeedback'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          submission: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          score: '18.50'
          feedback: متن نمونه
          reviewed_at: '2026-09-23T08:30:00+03:30'
          is_final: true
    PaginatedTeacherProfileList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/TeacherProfile'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          employee_no: SAMPLE-001
          full_name: سارا محمدی
          required_weekly_hours: '18.50'
          assigned_weekly_minutes: 45
          qualification_status: PENDING
          specialization: متن نمونه
          qualifications: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedTeacherQualificationList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/TeacherQualification'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          grade_level: هفتم
          grade_level_title: پایه هفتم
          valid_until: '2027-03-20'
          status: PENDING
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedTeachingAssignmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/TeachingAssignment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          class_group_code: G7-A
          teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_name: مریم رضایی
          responsibility: PRIMARY
          share_percent: '18.50'
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedTeachingSessionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/TeachingSession'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          class_group_code: G7-A
          schedule_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          room_code: R-204
          starts_at: '2026-09-23T08:30:00+03:30'
          ends_at: '2027-03-20T12:00:00+03:30'
          session_type: REGULAR
          status: PLANNED
          status_display: برنامه‌ریزی‌شده
          topic: متن نمونه
          cancel_reason: توضیح نمونه
          attendance_finalized_at: '2026-09-23T08:30:00+03:30'
          teachers: []
          attendance_summary: {}
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedTermList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Term'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          starts_on: '2026-09-23'
          ends_on: '2027-03-20'
          sequence_no: 3
          status: PLANNED
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedTicketList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Ticket'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          ticket_no: TK-1405-000203
          requester_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          requester_name: نام نمونه
          category: متن نمونه
          subject: متن نمونه
          description: توضیح تکمیلی این رکورد
          priority: LOW
          status: OPEN
          status_display: باز
          assignee_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          due_at: '2027-03-20T12:00:00+03:30'
          resolved_at: '2026-09-23T08:30:00+03:30'
          resolution: متن نمونه
          messages: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedTicketMessageList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/TicketMessage'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          ticket: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          author_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          body: متن نمونه
          is_internal: true
          created_at: '2026-09-23T08:30:00+03:30'
    PaginatedTransportRouteList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/TransportRoute'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus_name: شعبه مرکزی
          code: SAMPLE-001
          title: عنوان نمونه
          direction: MORNING
          default_vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          vehicle_plate: ۱۲ الف ۳۴۵ ایران ۱۰
          vehicle_capacity: 3
          default_driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          monthly_fee: 3
          status: متن نمونه
          active_rider_count: 12
          stops: []
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedUnitOfMeasureList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/UnitOfMeasure'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          decimal_places: 3
    PaginatedUserAccountList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/UserAccount'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          username: s.mohammadi
          email: user@example.school
          mobile: 09121234567
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person_detail: {}
          display_name: سارا محمدی
          status: INVITED
          mfa_enabled: true
          mfa_method: NONE
          must_change_password: true
          is_active: true
          is_staff: true
          last_login_at: '2026-09-23T08:30:00+03:30'
          date_joined: '2024-08-31T09:00:00+03:30'
          role_assignments: []
    PaginatedUserRoleAssignmentList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/UserRoleAssignment'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          username: s.mohammadi
          role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          role_code: SAMPLE-001
          role_title: عنوان نمونه
          scope_type: TENANT
          scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: ACTIVE
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          is_currently_effective: true
          grant_reason: توضیح نمونه
          revoked_at: '2026-09-23T08:30:00+03:30'
          revoke_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedVehicleList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Vehicle'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          plate_no: SAMPLE-001
          model_name: نام نمونه
          capacity: 3
          inspection_valid_until: '2026-09-23'
          insurance_valid_until: '2026-09-23'
          status: متن نمونه
          documents_valid: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedVendorList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Vendor'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          legal_name: نام نمونه
          tax_id: متن نمونه
          contact_person: متن نمونه
          phone: 09121234567
          email: user@example.school
          address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
          payment_terms_days: 3
          status: ACTIVE
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedWarehouseList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/Warehouse'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus_name: شعبه مرکزی
          code: SAMPLE-001
          title: عنوان نمونه
          location: متن نمونه
          keeper_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedWorkShiftList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/WorkShift'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          starts_at: 08:30:00
          ends_at: 08:30:00
          tolerance_minutes: 45
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PaginatedWorkflowDefinitionList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 137
          description: تعداد کل رکوردها
        pageCount:
          type: integer
          example: 6
          description: تعداد صفحات
        page:
          type: integer
          example: 1
          description: شماره صفحه جاری
        pageSize:
          type: integer
          example: 25
          description: اندازه صفحه
        next:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه بعد (تهی یعنی آخرین صفحه)
        previous:
          type: string
          nullable: true
          format: uri
          title: نشانی صفحه قبل (تهی یعنی اولین صفحه)
        results:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowDefinition'
          title: رکوردهای این صفحه
      example:
        count: 137
        pageCount: 6
        page: 1
        pageSize: 25
        next: https://api.example.school/api/v1/<resource>/?page=3
        previous: https://api.example.school/api/v1/<resource>/?page=1
        results:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          code: SAMPLE-001
          title: عنوان نمونه
          version_no: 3
          subject_type: متن نمونه
          is_active: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    PasswordChangeRequest:
      type: object
      properties:
        current_password:
          type: string
          writeOnly: true
          minLength: 1
          title: گذرواژه جاری
        new_password:
          type: string
          writeOnly: true
          minLength: 10
          title: گذرواژه جدید
      required:
      - current_password
      - new_password
      example:
        current_password: متن نمونه
        new_password: P@ssw0rd!2026
    PasswordResetRequestRequest:
      type: object
      description: |-
        درخواست بازیابی رمز.

        بخش ۱۵.۱: «جلوگیری از افشای وجود حساب» — پاسخ همیشه یکسان است.
      properties:
        identifier:
          type: string
          minLength: 1
          description: نام کاربری، ایمیل یا تلفن همراه
      required:
      - identifier
      example:
        identifier: متن نمونه
    PatchedAbsenceJustificationRequest:
      type: object
      properties:
        attendance:
          type: string
          format: uuid
          title: حضور و غیاب
        submitted_by_guardian_id:
          type: string
          format: uuid
          nullable: true
          title: ولی ارسال‌کننده
        reason:
          type: string
          minLength: 1
          title: دلیل غیبت
        evidence_file:
          type: string
          format: binary
          nullable: true
          title: فایل مستند
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 300
      example:
        attendance: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        submitted_by_guardian_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        evidence_file: sample.pdf
        decision_note: توضیح نمونه
    PatchedAcademicYearRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          description: مثلاً ۱۴۰۵–۱۴۰۶
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          title: تاریخ پایان
        close_note:
          type: string
          title: توضیح بستن
          maxLength: 400
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        close_note: توضیح نمونه
    PatchedAccessReviewItemRequest:
      type: object
      properties:
        review:
          type: string
          format: uuid
          title: بازبینی
        assignment:
          type: string
          format: uuid
          title: تکلیف
        decision:
          allOf:
          - $ref: '#/components/schemas/AccessReviewItemDecisionEnum'
          title: تصمیم
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        review: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision: PENDING
        note: یادداشت داخلی
    PatchedAccessReviewRequest:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          title: عنوان دوره بازبینی
          maxLength: 200
        opened_at:
          type: string
          format: date-time
          title: زمان بازکردن
        status:
          allOf:
          - $ref: '#/components/schemas/AccessReviewStatusEnum'
          title: وضعیت
        scope_note:
          type: string
          title: توضیح دامنه
          maxLength: 300
      example:
        title: عنوان نمونه
        opened_at: '2026-09-23T08:30:00+03:30'
        status: OPEN
        scope_note: توضیح نمونه
    PatchedAccountRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          minLength: 1
          title: کد حساب
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        account_type:
          allOf:
          - $ref: '#/components/schemas/AccountTypeEnum'
          title: نوع حساب
        allows_posting:
          type: boolean
          title: قابل ثبت سند مستقیم
        is_active:
          type: boolean
          title: فعال
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        account_type: ASSET
        allows_posting: true
        is_active: true
    PatchedAddressRequest:
      type: object
      properties:
        country:
          type: string
          minLength: 1
          title: کشور
          maxLength: 60
        province:
          type: string
          title: استان
          maxLength: 80
        city:
          type: string
          title: شهر
          maxLength: 80
        district:
          type: string
          title: منطقه
          maxLength: 80
        postal_code:
          type: string
          title: کد پستی
          maxLength: 20
        line:
          type: string
          minLength: 1
          title: نشانی
          maxLength: 400
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: عرض جغرافیایی
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: طول جغرافیایی
      example:
        country: IR
        province: تهران
        city: تهران
        district: منطقه ۳
        postal_code: '1234567890'
        line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        latitude: '35.6892'
        longitude: '51.389'
    PatchedAdmissionApplicationRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        preferred_campus:
          type: string
          format: uuid
          title: شعبه ترجیحی
        preferred_grade_level:
          type: string
          format: uuid
          title: پایه ترجیحی
        preferred_program:
          type: string
          format: uuid
          title: رشته ترجیحی
          nullable: true
        application_no:
          type: string
          minLength: 1
          title: شماره درخواست
          maxLength: 30
        reviewer_id:
          type: string
          format: uuid
          nullable: true
          title: بررسی‌کننده
        interview_at:
          type: string
          format: date-time
          nullable: true
          title: زمان مصاحبه
        entrance_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره آزمون ورودی
        interview_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره مصاحبه
        final_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: امتیاز نهایی
        waitlist_rank:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: رتبه در فهرست انتظار
        conditions:
          type: string
          title: شرایط پذیرش مشروط
        decision_reason:
          type: string
          title: علت تصمیم
          maxLength: 400
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        preferred_program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        application_no: SAMPLE-001
        reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        interview_at: '2026-09-23T08:30:00+03:30'
        entrance_score: '18.50'
        interview_score: '18.50'
        final_score: '18.50'
        waitlist_rank: 3
        conditions: متن نمونه
        decision_reason: توضیح نمونه
    PatchedAssessmentCategoryRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        title:
          type: string
          minLength: 1
          title: عنوان دسته
          maxLength: 150
        weight_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: وزن درصدی
        drop_policy:
          allOf:
          - $ref: '#/components/schemas/DropPolicyEnum'
          title: سیاست حذف
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        is_active:
          type: boolean
          title: فعال
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        weight_percent: '18.50'
        drop_policy: NONE
        display_order: 1
        is_active: true
    PatchedAssetAssignmentRequest:
      type: object
      properties:
        asset:
          type: string
          format: uuid
          title: دارایی
        assignee_type:
          allOf:
          - $ref: '#/components/schemas/AssetAssignmentAssigneeTypeEnum'
          title: نوع تحویل‌گیرنده
        assignee_id:
          type: string
          format: uuid
          title: شناسه تحویل‌گیرنده
        location_room:
          type: string
          format: uuid
          title: اتاق محل استقرار
          nullable: true
        assigned_at:
          type: string
          format: date-time
          title: زمان تخصیص
        returned_at:
          type: string
          format: date-time
          nullable: true
          title: زمان بازگشت
        condition_on_assign:
          title: وضعیت هنگام تحویل
          oneOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          - $ref: '#/components/schemas/BlankEnum'
        condition_on_return:
          title: وضعیت هنگام بازگشت
          oneOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          - $ref: '#/components/schemas/BlankEnum'
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
      example:
        asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assignee_type: EMPLOYEE
        assignee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        location_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        assigned_at: '2026-09-23T08:30:00+03:30'
        returned_at: '2026-09-23T08:30:00+03:30'
        condition_on_assign: NEW
        condition_on_return: NEW
        status: متن نمونه
        note: یادداشت داخلی
    PatchedAssetRequest:
      type: object
      properties:
        item:
          type: string
          format: uuid
          title: کالا
          nullable: true
        asset_tag:
          type: string
          minLength: 1
          title: پلاک اموال
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        serial_no:
          type: string
          title: شماره سریال
          maxLength: 80
        acquired_on:
          type: string
          format: date
          title: تاریخ تملک
        acquisition_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای تملک
        useful_life_months:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: عمر مفید (ماه)
        warranty_until:
          type: string
          format: date
          nullable: true
          title: گارانتی تا
        insurance_policy_no:
          type: string
          title: شماره بیمه‌نامه
          maxLength: 60
        condition_status:
          allOf:
          - $ref: '#/components/schemas/AssetConditionEnum'
          title: وضعیت فنی
        current_room:
          type: string
          format: uuid
          title: اتاق فعلی
          nullable: true
      example:
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        asset_tag: AST-004512
        title: عنوان نمونه
        serial_no: SN-88213047
        acquired_on: '2024-05-11'
        acquisition_cost: 2500000
        useful_life_months: 3
        warranty_until: '2027-03-20'
        insurance_policy_no: SAMPLE-001
        condition_status: NEW
        current_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PatchedAssignmentRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        title:
          type: string
          minLength: 1
          title: عنوان تکلیف
          maxLength: 200
        description:
          type: string
          title: شرح
        opens_at:
          type: string
          format: date-time
          title: زمان بازشدن
        due_at:
          type: string
          format: date-time
          title: مهلت تحویل
        close_at:
          type: string
          format: date-time
          nullable: true
          title: پایان پذیرش با تأخیر
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم
        allow_late_submission:
          type: boolean
          title: پذیرش تحویل با تأخیر
        late_penalty_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد جریمه تأخیر
        max_attempts:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: حداکثر تلاش
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        opens_at: '2026-09-23T08:30:00+03:30'
        due_at: '2027-03-20T12:00:00+03:30'
        close_at: '2026-09-23T08:30:00+03:30'
        max_score: '18.50'
        allow_late_submission: true
        late_penalty_percent: '18.50'
        max_attempts: 3
    PatchedAssignmentSubmissionRequest:
      type: object
      properties:
        assignment:
          type: string
          format: uuid
          title: تکلیف
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        attempt_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          default: 1
          title: شماره تلاش
        content:
          type: string
          title: محتوا
        attachment:
          type: string
          format: binary
          nullable: true
          title: پیوست
      example:
        assignment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt_no: 1
        content: متن نمونه
        attachment: sample.pdf
    PatchedAttachmentRequest:
      type: object
      properties:
        owner_type:
          type: string
          minLength: 1
          title: نوع مالک
          maxLength: 80
        owner_id:
          type: string
          format: uuid
          title: شناسه مالک
        file:
          type: string
          format: binary
          title: فایل
        original_name:
          type: string
          title: نام اصلی فایل
          maxLength: 250
        media_type:
          type: string
          title: نوع محتوا
          maxLength: 100
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
      example:
        owner_type: متن نمونه
        owner_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        file: sample.pdf
        original_name: sample.pdf
        media_type: application/pdf
        classification: PUBLIC
    PatchedAttendanceRecordRequest:
      type: object
      properties:
        session:
          type: string
          format: uuid
          title: جلسه
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        attendance_status:
          allOf:
          - $ref: '#/components/schemas/AttendanceStatusEnum'
          title: وضعیت حضور
        late_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه تأخیر
        early_leave_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه خروج زودهنگام
        reason_code:
          type: string
          title: کد علت
          maxLength: 60
        note:
          type: string
          title: یادداشت
          maxLength: 300
        amended_reason:
          type: string
          title: علت اصلاح
          maxLength: 300
      example:
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attendance_status: PRESENT
        late_minutes: 45
        early_leave_minutes: 45
        reason_code: SAMPLE-001
        note: یادداشت داخلی
        amended_reason: توضیح نمونه
    PatchedBankAccountRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        bank_name:
          type: string
          title: نام بانک
          maxLength: 120
        iban_masked:
          type: string
          title: شبا (ماسک‌شده)
          description: 'بخش ۱۵.۲: شماره کامل حساب در Log و پاسخ API ذخیره نمی‌شود.'
          maxLength: 40
        ledger_account:
          type: string
          format: uuid
          title: حساب دفتر کل
          nullable: true
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        bank_name: نام نمونه
        iban_masked: متن نمونه
        ledger_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        currency: IRR
        status: متن نمونه
    PatchedBankReconciliationRequest:
      type: object
      properties:
        bank_account:
          type: string
          format: uuid
          title: حساب بانکی
        period_start:
          type: string
          format: date
          title: آغاز دوره
        period_end:
          type: string
          format: date
          title: پایان دوره
        statement_balance:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مانده صورتحساب بانک
        ledger_balance:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مانده دفتر
        status:
          allOf:
          - $ref: '#/components/schemas/ReconciliationStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
      example:
        bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        period_start: '2026-09-23'
        period_end: '2027-03-20'
        statement_balance: 3
        ledger_balance: 3
        status: OPEN
        note: یادداشت داخلی
    PatchedBehaviorActionRequest:
      type: object
      properties:
        incident:
          type: string
          format: uuid
          title: رخداد
        action_type:
          allOf:
          - $ref: '#/components/schemas/ActionTypeEnum'
          title: نوع عمل
        details:
          type: string
          title: جزئیات
        effective_from:
          type: string
          format: date
          nullable: true
          title: معتبر از
        effective_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        completed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان تکمیل
      example:
        incident: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        action_type: VERBAL_WARNING
        details: متن نمونه
        effective_from: '2026-09-23'
        effective_until: '2027-03-20'
        status: متن نمونه
        completed_at: '2026-09-23T08:30:00+03:30'
    PatchedBehaviorIncidentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        location:
          type: string
          title: محل
          maxLength: 150
        incident_type:
          allOf:
          - $ref: '#/components/schemas/IncidentTypeEnum'
          title: نوع رخداد
        severity:
          allOf:
          - $ref: '#/components/schemas/BehaviorSeverityEnum'
          title: شدت
        description:
          type: string
          minLength: 1
          title: شرح
        witnesses:
          type: string
          title: شهود
          maxLength: 400
        student_statement:
          type: string
          title: توضیح دانش‌آموز
        investigation_note:
          type: string
          title: توضیح بررسی
        points:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: امتیاز
          description: منفی برای تخلف، مثبت برای تشویق
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        occurred_at: '2026-09-23T08:30:00+03:30'
        location: متن نمونه
        incident_type: DISRUPTION
        severity: MINOR
        description: توضیح تکمیلی این رکورد
        witnesses: متن نمونه
        student_statement: متن نمونه
        investigation_note: توضیح نمونه
        points: 3
    PatchedCalendarEventRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        campus:
          type: string
          format: uuid
          title: شعبه
          nullable: true
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        title:
          type: string
          minLength: 1
          title: عنوان رویداد
          maxLength: 200
        event_type:
          allOf:
          - $ref: '#/components/schemas/CalendarEventEventTypeEnum'
          title: نوع رویداد
        starts_on:
          type: string
          format: date
          title: از تاریخ
        ends_on:
          type: string
          format: date
          title: تا تاریخ
        is_working_day:
          type: boolean
          title: روز کاری محسوب می‌شود
        description:
          type: string
          title: شرح
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        event_type: HOLIDAY
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        is_working_day: true
        description: توضیح تکمیلی این رکورد
    PatchedCampusRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد شعبه
          maxLength: 30
        name:
          type: string
          minLength: 1
          title: نام شعبه
          maxLength: 200
        address_line:
          type: string
          title: نشانی
          maxLength: 400
        phone:
          type: string
          title: تلفن
          maxLength: 30
        timezone:
          type: string
          minLength: 1
          title: منطقه زمانی
          maxLength: 64
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        name: نام نمونه
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        phone: 09121234567
        timezone: Asia/Tehran
        status: DRAFT
    PatchedClassGroupRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        home_room:
          type: string
          format: uuid
          title: اتاق اصلی
          nullable: true
        homeroom_teacher_id:
          type: string
          format: uuid
          nullable: true
          title: معلم راهنما
        code:
          type: string
          minLength: 1
          title: کد کلاس
          maxLength: 30
        title:
          type: string
          title: عنوان کلاس
          maxLength: 100
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت مصوب
        capacity_override_reason:
          type: string
          title: علت افزایش ظرفیت
          description: افزایش ظرفیت نیازمند مجوز و علت ثبت‌شده است (بخش ۱۱.۳)
          maxLength: 300
        status:
          allOf:
          - $ref: '#/components/schemas/ClassGroupStatusEnum'
          title: وضعیت
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        home_room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        homeroom_teacher_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        capacity: 3
        capacity_override_reason: توضیح نمونه
        status: DRAFT
    PatchedClassMembershipRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        class_group:
          type: string
          format: uuid
          title: کلاس
        is_primary:
          type: boolean
          title: عضویت اصلی
        status:
          allOf:
          - $ref: '#/components/schemas/ClassMembershipStatusEnum'
          title: وضعیت
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        exit_reason:
          type: string
          title: علت خروج
          maxLength: 300
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        is_primary: true
        status: ACTIVE
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        exit_reason: توضیح نمونه
    PatchedConsentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        guardian:
          type: string
          format: uuid
          title: ولی
          nullable: true
        consent_type:
          allOf:
          - $ref: '#/components/schemas/ConsentTypeEnum'
          title: نوع رضایت
        granted_at:
          type: string
          format: date-time
          title: زمان اعطا
        revoke_reason:
          type: string
          title: علت ابطال
          maxLength: 300
        expires_at:
          type: string
          format: date
          nullable: true
          title: زمان انقضا
        policy_version:
          type: string
          minLength: 1
          title: نسخه متن سیاست
          maxLength: 30
        policy_text_snapshot:
          type: string
          title: متن سیاست در زمان اخذ رضایت
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        consent_type: PHOTO
        granted_at: '2026-09-23T08:30:00+03:30'
        revoke_reason: توضیح نمونه
        expires_at: '2027-03-20'
        policy_version: متن نمونه
        policy_text_snapshot: متن نمونه
    PatchedContactPointRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        contact_type:
          allOf:
          - $ref: '#/components/schemas/ContactTypeEnum'
          title: نوع تماس
        value:
          type: string
          minLength: 1
          title: مقدار
          maxLength: 200
        is_primary:
          type: boolean
          title: تماس اصلی
        label:
          type: string
          title: برچسب
          maxLength: 60
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        contact_type: MOBILE
        value: متن نمونه
        is_primary: true
        label: متن نمونه
    PatchedCostCenterRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        is_active:
          type: boolean
          title: فعال
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        is_active: true
    PatchedCounselingCaseRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        counselor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: مشاور
        referral_source:
          allOf:
          - $ref: '#/components/schemas/ReferralSourceEnum'
          title: منبع ارجاع
        subject:
          type: string
          minLength: 1
          title: موضوع
          maxLength: 200
        priority:
          allOf:
          - $ref: '#/components/schemas/PriorityLevelEnum'
          title: اولویت
        status:
          allOf:
          - $ref: '#/components/schemas/CounselingCaseStatusEnum'
          title: وضعیت
        opened_on:
          type: string
          format: date
          title: تاریخ گشایش
        closed_on:
          type: string
          format: date
          nullable: true
          title: تاریخ بستن
        action_plan:
          type: string
          title: برنامه اقدام
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        counselor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        referral_source: TEACHER
        subject: متن نمونه
        priority: LOW
        status: OPEN
        opened_on: '2026-09-23'
        closed_on: '2027-03-20'
        action_plan: متن نمونه
        confidentiality_level: STANDARD
    PatchedCounselingSessionRequest:
      type: object
      description: |-
        جلسه مشاوره.

        `protectedNote` فقط برای مشاور صاحب پرونده و نقش‌های دارای مجوز فیلدی
        برگردانده می‌شود؛ در غیر این صورت رشته خالی است (بخش ۷.۱۰).
      properties:
        case:
          type: string
          format: uuid
          title: پرونده
        held_at:
          type: string
          format: date-time
          title: زمان برگزاری
        duration_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مدت (دقیقه)
        summary:
          type: string
          title: خلاصه قابل مشاهده برای نقش‌های مجاز
        protected_note:
          type: string
          title: یادداشت محرمانه مشاور
        next_followup_at:
          type: string
          format: date-time
          nullable: true
          title: زمان پیگیری بعدی
        attendees:
          type: string
          title: حاضران
          maxLength: 300
      example:
        case: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        held_at: '2026-09-23T08:30:00+03:30'
        duration_minutes: 45
        summary: متن نمونه
        protected_note: توضیح نمونه
        next_followup_at: '2026-09-23T08:30:00+03:30'
        attendees: متن نمونه
    PatchedCourseOfferingRequest:
      type: object
      properties:
        class_group:
          type: string
          format: uuid
          title: کلاس
        term:
          type: string
          format: uuid
          title: ترم
        course:
          type: string
          format: uuid
          title: درس
        weekly_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه در هفته
        status:
          allOf:
          - $ref: '#/components/schemas/CourseOfferingStatusEnum'
          title: وضعیت
      example:
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekly_minutes: 45
        status: PLANNED
    PatchedCourseRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد درس
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان درس
          maxLength: 150
        credit:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: واحد
        assessment_scheme:
          allOf:
          - $ref: '#/components/schemas/AssessmentSchemeEnum'
          title: طرح ارزشیابی
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        prerequisites:
          type: array
          items:
            type: string
            format: uuid
            title: پیش‌نیازها
          title: پیش‌نیازها
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        credit: '0'
        assessment_scheme: NUMERIC
        max_score: '18.50'
        prerequisites:
        - 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: DRAFT
    PatchedCourseResultRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        continuous_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره مستمر
        final_exam_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره پایانی
        letter_grade:
          type: string
          title: نمره حرفی
          maxLength: 5
        qualitative_level:
          title: سطح کیفی
          oneOf:
          - $ref: '#/components/schemas/QualitativeLevelEnum'
          - $ref: '#/components/schemas/BlankEnum'
        result_status:
          allOf:
          - $ref: '#/components/schemas/CourseResultStatusEnum'
          title: وضعیت نتیجه
        teacher_comment:
          type: string
          title: نظر معلم
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        continuous_score: '18.50'
        final_exam_score: '18.50'
        letter_grade: A
        qualitative_level: EXCELLENT
        result_status: IN_PROGRESS
        teacher_comment: توضیح نمونه
    PatchedDiscountAwardRequest:
      type: object
      properties:
        agreement:
          type: string
          format: uuid
          title: توافق‌نامه
        discount_type:
          allOf:
          - $ref: '#/components/schemas/DiscountTypeEnum'
          title: نوع تخفیف
        percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد تخفیف
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ ثابت تخفیف
        reason:
          type: string
          title: علت
          maxLength: 400
      example:
        agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        discount_type: SIBLING
        percent: '18.50'
        amount: 12500000
        reason: اصلاح اطلاعات پس از بررسی پرونده
    PatchedEmployeeAssignmentRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        position:
          type: string
          format: uuid
          title: سِمت
        campus:
          type: string
          format: uuid
          title: شعبه
        allocation_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد تخصیص
        is_primary:
          type: boolean
          title: انتساب اصلی
        cost_center_id:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        position: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        allocation_percent: '18.50'
        is_primary: true
        cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    PatchedEmployeeAttendanceRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        shift:
          type: string
          format: uuid
          title: شیفت
          nullable: true
        work_date:
          type: string
          format: date
          title: تاریخ کاری
        check_in_at:
          type: string
          format: date-time
          nullable: true
          title: زمان ورود
        check_out_at:
          type: string
          format: date-time
          nullable: true
          title: زمان خروج
        worked_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه کارکرد
        overtime_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه اضافه‌کاری
        late_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه تأخیر
        source:
          allOf:
          - $ref: '#/components/schemas/EmployeeAttendanceSourceEnum'
          title: منبع
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        shift: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        work_date: '2026-09-23'
        check_in_at: '2026-09-23T08:30:00+03:30'
        check_out_at: '2026-09-23T08:30:00+03:30'
        worked_minutes: 45
        overtime_minutes: 45
        late_minutes: 45
        source: DEVICE
        status: متن نمونه
        note: یادداشت داخلی
    PatchedEmployeeRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        employee_no:
          type: string
          minLength: 1
          title: شماره پرسنلی
          maxLength: 30
        hired_on:
          type: string
          format: date
          title: تاریخ استخدام
        terminated_on:
          type: string
          format: date
          nullable: true
          title: تاریخ پایان همکاری
        status:
          allOf:
          - $ref: '#/components/schemas/EmployeeStatusEnum'
          title: وضعیت
        bank_account_masked:
          type: string
          title: شماره حساب (ماسک‌شده)
          maxLength: 40
        emergency_contact:
          type: string
          title: تماس اضطراری
          maxLength: 200
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_no: SAMPLE-001
        hired_on: '2026-09-23'
        terminated_on: '2026-09-23'
        status: ONBOARDING
        bank_account_masked: متن نمونه
        emergency_contact: متن نمونه
    PatchedEmploymentContractRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        contract_no:
          type: string
          minLength: 1
          title: شماره قرارداد
          maxLength: 40
        contract_type:
          allOf:
          - $ref: '#/components/schemas/ContractTypeEnum'
          title: نوع قرارداد
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          nullable: true
          title: تاریخ پایان
        workload_ratio:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: ضریب اشتغال
          description: ۱ یعنی تمام‌وقت
        base_salary_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: حقوق پایه
          description: به کوچک‌ترین واحد پولی (ریال)
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        status:
          allOf:
          - $ref: '#/components/schemas/ContractStatusEnum'
          title: وضعیت
        termination_reason:
          type: string
          title: علت خاتمه
          maxLength: 300
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        contract_no: HR-1405-0042
        contract_type: PERMANENT
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        workload_ratio: '18.50'
        base_salary_amount: 2500000
        currency: IRR
        status: DRAFT
        termination_reason: توضیح نمونه
    PatchedEnrollmentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        campus:
          type: string
          format: uuid
          title: شعبه
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        program:
          type: string
          format: uuid
          title: رشته
          nullable: true
        admission_application:
          type: string
          format: uuid
          title: درخواست پذیرش
          nullable: true
        enrolled_on:
          type: string
          format: date
          title: تاریخ ثبت‌نام
        exit_date:
          type: string
          format: date
          nullable: true
          title: تاریخ خروج
        exit_reason:
          type: string
          title: علت خروج
          maxLength: 300
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        admission_application: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrolled_on: '2026-09-23'
        exit_date: '2027-03-20'
        exit_reason: توضیح نمونه
    PatchedExamQuestionRequest:
      type: object
      properties:
        section:
          type: string
          format: uuid
          title: بخش
        question_version:
          type: string
          format: uuid
          title: نسخه سؤال
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        is_required:
          type: boolean
          title: الزامی
      example:
        section: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        score: '18.50'
        display_order: 1
        is_required: true
    PatchedExamRegistrationRequest:
      type: object
      properties:
        exam_session:
          type: string
          format: uuid
          title: جلسه آزمون
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        seat_no:
          type: string
          title: شماره صندلی
          maxLength: 20
        extra_time_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: زمان اضافه فردی
        registration_status:
          allOf:
          - $ref: '#/components/schemas/RegistrationStatusEnum'
          title: وضعیت ثبت‌نام
      example:
        exam_session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        seat_no: SAMPLE-001
        extra_time_minutes: 45
        registration_status: REGISTERED
    PatchedExamRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        mode:
          allOf:
          - $ref: '#/components/schemas/ModeEnum'
          title: حالت
        purpose:
          allOf:
          - $ref: '#/components/schemas/PurposeEnum'
          title: هدف
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: نمره کل
        instructions:
          type: string
          title: دستورالعمل
        shuffle_questions:
          type: boolean
          title: ترتیب تصادفی سؤالات
        shuffle_options:
          type: boolean
          title: ترتیب تصادفی گزینه‌ها
        allow_backtrack:
          type: boolean
          title: امکان بازگشت به سؤال قبلی
        show_result_immediately:
          type: boolean
          title: نمایش فوری نتیجه
        appeal_deadline:
          type: string
          format: date-time
          nullable: true
          title: مهلت اعتراض
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        mode: ONLINE
        purpose: FORMATIVE
        max_score: '18.50'
        instructions: متن نمونه
        shuffle_questions: true
        shuffle_options: true
        allow_backtrack: true
        show_result_immediately: true
        appeal_deadline: '2027-03-20T12:00:00+03:30'
        cancel_reason: توضیح نمونه
    PatchedExamSectionRequest:
      type: object
      properties:
        exam:
          type: string
          format: uuid
          title: آزمون
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        instructions:
          type: string
          title: دستورالعمل
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
        time_limit_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: محدودیت زمان (دقیقه)
      example:
        exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        instructions: متن نمونه
        display_order: 1
        time_limit_minutes: 45
    PatchedExamSessionRequest:
      type: object
      properties:
        exam:
          type: string
          format: uuid
          title: آزمون
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        title:
          type: string
          title: عنوان
          maxLength: 150
        opens_at:
          type: string
          format: date-time
          title: شروع پنجره
        closes_at:
          type: string
          format: date-time
          title: پایان پنجره
        duration_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مدت آزمون
        attempt_limit:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: سقف تلاش
        proctor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: مراقب آزمون
        status:
          allOf:
          - $ref: '#/components/schemas/ExamSessionStatusEnum'
          title: وضعیت
      example:
        exam: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        opens_at: '2026-09-23T08:30:00+03:30'
        closes_at: '2026-09-23T08:30:00+03:30'
        duration_minutes: 45
        attempt_limit: 3
        proctor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: PLANNED
    PatchedFeePlanItemRequest:
      type: object
      properties:
        fee_plan:
          type: string
          format: uuid
          title: طرح شهریه
        fee_type:
          allOf:
          - $ref: '#/components/schemas/FeeTypeEnum'
          title: نوع شهریه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ (ریال)
        recurrence:
          allOf:
          - $ref: '#/components/schemas/RecurrenceEnum'
          title: تکرار
        is_mandatory:
          type: boolean
          title: اجباری
        revenue_account:
          type: string
          format: uuid
          title: حساب درآمد
          nullable: true
      example:
        fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_type: REGISTRATION
        title: عنوان نمونه
        amount: 12500000
        recurrence: ONE_TIME
        is_mandatory: true
        revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PatchedFeePlanRequest:
      type: object
      properties:
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        currency:
          allOf:
          - $ref: '#/components/schemas/CurrencyEnum'
          title: واحد پول
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        title: عنوان نمونه
        currency: IRR
        status: متن نمونه
    PatchedFiscalYearRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: تاریخ شروع
        ends_on:
          type: string
          format: date
          title: تاریخ پایان
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
    PatchedGoodsReceiptRequest:
      type: object
      properties:
        order:
          type: string
          format: uuid
          title: سفارش
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        quality_status:
          allOf:
          - $ref: '#/components/schemas/QualityStatusEnum'
          title: وضعیت کیفی
        status:
          allOf:
          - $ref: '#/components/schemas/ReceiptStatusEnum'
          title: وضعیت
        vendor_invoice_no:
          type: string
          title: شماره فاکتور تأمین‌کننده
          maxLength: 60
        vendor_invoice_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ فاکتور تأمین‌کننده
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        received_at: '2026-09-23T08:30:00+03:30'
        quality_status: PENDING
        status: PROVISIONAL
        vendor_invoice_no: SAMPLE-001
        vendor_invoice_amount: 2500000
        note: یادداشت داخلی
    PatchedGradeAppealRequest:
      type: object
      properties:
        attempt:
          type: string
          format: uuid
          title: تلاش
        submitted_by_id:
          type: string
          format: uuid
          nullable: true
          title: ارسال‌کننده
        reason:
          type: string
          minLength: 1
          title: دلیل اعتراض
        resolution:
          type: string
          title: نتیجه رسیدگی
      example:
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        submitted_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        resolution: متن نمونه
    PatchedGradeItemRequest:
      type: object
      properties:
        category:
          type: string
          format: uuid
          title: دسته
        source_type:
          allOf:
          - $ref: '#/components/schemas/GradeItemSourceTypeEnum'
          title: نوع منبع
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
          description: شناسه آزمون یا تکلیف مرتبط
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم
        weight:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: ضریب داخل دسته
        due_on:
          type: string
          format: date
          nullable: true
          title: سررسید
      example:
        category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        source_type: EXAM
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        max_score: '18.50'
        weight: '18.50'
        due_on: '2027-03-20'
    PatchedGradeLevelRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد پایه
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان پایه
          maxLength: 100
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب
          description: برای منطق ارتقای پایه استفاده می‌شود
        stage:
          type: string
          title: مقطع
          maxLength: 40
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        sequence_no: 3
        stage: متن نمونه
        status: DRAFT
    PatchedGuardianRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        occupation:
          type: string
          title: شغل
          maxLength: 120
        education_level:
          type: string
          title: تحصیلات
          maxLength: 80
        workplace:
          type: string
          title: محل کار
          maxLength: 200
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        occupation: متن نمونه
        education_level: متن نمونه
        workplace: متن نمونه
        status: متن نمونه
    PatchedHealthAlertRequest:
      type: object
      description: نمای کامل هشدار — فقط برای نقش‌های دارای مجوز `health.read`.
      properties:
        health_profile:
          type: string
          format: uuid
          title: پرونده سلامت
        alert_type:
          allOf:
          - $ref: '#/components/schemas/AlertTypeEnum'
          title: نوع هشدار
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        safe_summary:
          type: string
          minLength: 1
          title: خلاصه قابل نمایش به معلم
          description: بدون جزئیات پزشکی غیرضروری
          maxLength: 300
        instructions:
          type: string
          title: دستورالعمل اقدام (محرمانه)
        severity:
          allOf:
          - $ref: '#/components/schemas/AlertSeverityEnum'
          title: شدت
        valid_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/HealthAlertStatusEnum'
          title: وضعیت
      example:
        health_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        alert_type: ALLERGY
        title: عنوان نمونه
        safe_summary: متن نمونه
        instructions: متن نمونه
        severity: LOW
        valid_until: '2027-03-20'
        status: ACTIVE
    PatchedHealthIncidentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        location:
          type: string
          title: محل
          maxLength: 150
        description:
          type: string
          minLength: 1
          title: شرح
        action_taken:
          type: string
          title: اقدام انجام‌شده
        outcome:
          allOf:
          - $ref: '#/components/schemas/OutcomeEnum'
          title: نتیجه
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        occurred_at: '2026-09-23T08:30:00+03:30'
        location: متن نمونه
        description: توضیح تکمیلی این رکورد
        action_taken: متن نمونه
        outcome: RESOLVED_ON_SITE
        confidentiality_level: STANDARD
    PatchedHealthProfileRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        blood_type:
          allOf:
          - $ref: '#/components/schemas/BloodTypeEnum'
          title: گروه خونی
        height_cm:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: قد (سانتی‌متر)
        weight_kg:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: وزن (کیلوگرم)
        accessibility_needs:
          type: string
          title: نیازهای دسترس‌پذیری
        emergency_contact_name:
          type: string
          title: نام تماس اضطراری
          maxLength: 150
        emergency_contact_phone:
          type: string
          title: تلفن تماس اضطراری
          maxLength: 30
        family_physician:
          type: string
          title: پزشک خانواده
          maxLength: 150
        insurance_no:
          type: string
          title: شماره بیمه
          maxLength: 60
        confidentiality_level:
          allOf:
          - $ref: '#/components/schemas/ConfidentialityLevelEnum'
          title: سطح محرمانگی
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        blood_type: A+
        height_cm: 3
        weight_kg: 3
        accessibility_needs: متن نمونه
        emergency_contact_name: نام نمونه
        emergency_contact_phone: متن نمونه
        family_physician: متن نمونه
        insurance_no: SAMPLE-001
        confidentiality_level: STANDARD
    PatchedInvoiceLineRequest:
      type: object
      properties:
        invoice:
          type: string
          format: uuid
          title: صورتحساب
        fee_type:
          allOf:
          - $ref: '#/components/schemas/FeeTypeEnum'
          title: نوع شهریه
        description:
          type: string
          minLength: 1
          title: شرح
          maxLength: 300
        quantity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد
        unit_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ واحد
        discount_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ تخفیف
        revenue_account:
          type: string
          format: uuid
          title: حساب درآمد
          nullable: true
      example:
        invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_type: REGISTRATION
        description: توضیح تکمیلی این رکورد
        quantity: 3
        unit_amount: 2500000
        discount_amount: 5000000
        revenue_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PatchedInvoiceRequest:
      type: object
      properties:
        agreement:
          type: string
          format: uuid
          title: توافق‌نامه
        issue_date:
          type: string
          format: date
          title: تاریخ صدور
        due_date:
          type: string
          format: date
          title: سررسید
        installment_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: شماره قسط
        total_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ کل
        discount_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ تخفیف
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        description:
          type: string
          title: شرح
          maxLength: 400
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
      example:
        agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        issue_date: '2026-09-23'
        due_date: '2027-03-20'
        installment_no: 3
        total_amount: 45000000
        discount_amount: 5000000
        currency: IRR
        description: توضیح تکمیلی این رکورد
        cancel_reason: توضیح نمونه
    PatchedItemCategoryRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
    PatchedItemRequest:
      type: object
      properties:
        category:
          type: string
          format: uuid
          title: دسته
        base_uom:
          type: string
          format: uuid
          title: واحد سنجش پایه
        sku:
          type: string
          minLength: 1
          title: کد کالا
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
          maxLength: 400
        barcode:
          type: string
          title: بارکد
          maxLength: 60
        lot_tracked:
          type: boolean
          title: ردیابی بچ
        serial_tracked:
          type: boolean
          title: ردیابی سریال
        expiry_tracked:
          type: boolean
          title: ردیابی انقضا
        is_capital_asset:
          type: boolean
          title: مال سرمایه‌ای (نه مصرفی)
        reorder_point:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: نقطه سفارش
        min_stock:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: حداقل موجودی
        max_stock:
          type: string
          format: decimal
          pattern: ^-?\d{0,9}(?:\.\d{0,3})?$
          title: حداکثر موجودی
        allow_negative_stock:
          type: boolean
          title: اجازه موجودی منفی
          description: 'بخش ۷.۹: منفی‌شدن فقط برای اقلام مجاز با تأیید'
        expense_account_id:
          type: string
          format: uuid
          nullable: true
          title: حساب هزینه
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        category: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        base_uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sku: STA-A4-80
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        barcode: '6260123456789'
        lot_tracked: true
        serial_tracked: true
        expiry_tracked: true
        is_capital_asset: true
        reorder_point: '18.50'
        min_stock: '18.50'
        max_stock: '18.50'
        allow_negative_stock: true
        expense_account_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
    PatchedLearningResourceRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        resource_type:
          allOf:
          - $ref: '#/components/schemas/ResourceTypeEnum'
          title: نوع منبع
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
        file:
          type: string
          format: binary
          nullable: true
          title: فایل
        url:
          title: نشانی اینترنتی
          oneOf:
          - type: string
            format: uri
            maxLength: 200
          - type: string
            maxLength: 0
        visibility:
          allOf:
          - $ref: '#/components/schemas/LearningResourceVisibilityEnum'
          title: سطح نمایش
        published_at:
          type: string
          format: date-time
          nullable: true
          title: زمان انتشار
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        resource_type: FILE
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        file: sample.pdf
        url: https://example.school/resources/sample.pdf
        visibility: CLASS_ONLY
        published_at: '2026-09-23T08:30:00+03:30'
    PatchedLeaveRequestRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        leave_type:
          allOf:
          - $ref: '#/components/schemas/LeaveTypeEnum'
          title: نوع مرخصی
        starts_at:
          type: string
          format: date-time
          title: زمان شروع
        ends_at:
          type: string
          format: date-time
          title: زمان پایان
        requested_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه درخواستی
        reason:
          type: string
          title: علت
          maxLength: 400
        substitute_employee:
          type: string
          format: uuid
          title: جانشین
          nullable: true
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 400
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        leave_type: ANNUAL
        starts_at: '2026-09-23T08:30:00+03:30'
        ends_at: '2027-03-20T12:00:00+03:30'
        requested_minutes: 45
        reason: اصلاح اطلاعات پس از بررسی پرونده
        substitute_employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision_note: توضیح نمونه
    PatchedLessonPlanRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        session:
          type: string
          format: uuid
          title: جلسه
          nullable: true
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        objectives:
          type: string
          title: اهداف یادگیری
        content:
          type: string
          title: محتوا
        planned_for:
          type: string
          format: date
          nullable: true
          title: برنامه‌ریزی‌شده برای
        status:
          allOf:
          - $ref: '#/components/schemas/LessonPlanStatusEnum'
          title: وضعیت
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        objectives: متن نمونه
        content: متن نمونه
        planned_for: '2026-09-23'
        status: DRAFT
    PatchedLibraryCopyRequest:
      type: object
      properties:
        title_ref:
          type: string
          format: uuid
          title: عنوان کتاب
        barcode:
          type: string
          minLength: 1
          title: بارکد
          maxLength: 60
        location:
          type: string
          title: محل قفسه
          maxLength: 100
        is_loanable:
          type: boolean
          title: قابل امانت
        acquisition_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای تملک
        status:
          allOf:
          - $ref: '#/components/schemas/CopyStatusEnum'
          title: وضعیت
      example:
        title_ref: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        barcode: '6260123456789'
        location: متن نمونه
        is_loanable: true
        acquisition_cost: 2500000
        status: AVAILABLE
    PatchedLibraryLoanRequest:
      type: object
      properties:
        copy:
          type: string
          format: uuid
          title: نسخه
        borrower_person:
          type: string
          format: uuid
          title: امانت‌گیرنده
        loaned_at:
          type: string
          format: date-time
          title: زمان امانت
        due_at:
          type: string
          format: date-time
          title: مهلت
        fine_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: جریمه (ریال)
        fine_paid:
          type: boolean
          title: جریمه پرداخت‌شده
        note:
          type: string
          title: یادداشت
          maxLength: 300
      example:
        copy: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        borrower_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        loaned_at: '2026-09-23T08:30:00+03:30'
        due_at: '2027-03-20T12:00:00+03:30'
        fine_amount: 2500000
        fine_paid: true
        note: یادداشت داخلی
    PatchedLibraryTitleRequest:
      type: object
      properties:
        isbn:
          type: string
          title: شابک
          maxLength: 20
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 300
        author:
          type: string
          title: نویسنده
          maxLength: 250
        publisher:
          type: string
          title: ناشر
          maxLength: 200
        publish_year:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: سال انتشار
        material_type:
          allOf:
          - $ref: '#/components/schemas/MaterialTypeEnum'
          title: نوع ماده
        classification:
          type: string
          title: رده‌بندی
          maxLength: 60
        subject:
          type: string
          title: موضوع
          maxLength: 200
        language:
          type: string
          minLength: 1
          title: زبان
          maxLength: 10
      example:
        isbn: '9786001234567'
        title: عنوان نمونه
        author: محمدرضا کریمی
        publisher: نشر نمونه
        publish_year: 3
        material_type: BOOK
        classification: متن نمونه
        subject: متن نمونه
        language: متن نمونه
    PatchedMaintenanceOrderRequest:
      type: object
      properties:
        asset:
          type: string
          format: uuid
          title: دارایی
        maintenance_type:
          allOf:
          - $ref: '#/components/schemas/MaintenanceTypeEnum'
          title: نوع نگهداشت
        description:
          type: string
          title: شرح
        vendor:
          type: string
          format: uuid
          title: تأمین‌کننده
          nullable: true
        opened_at:
          type: string
          format: date-time
          title: زمان بازکردن
        closed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان بستن
        cost_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ هزینه
        status:
          allOf:
          - $ref: '#/components/schemas/MaintenanceStatusEnum'
          title: وضعیت
      example:
        asset: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        maintenance_type: PREVENTIVE
        description: توضیح تکمیلی این رکورد
        vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        opened_at: '2026-09-23T08:30:00+03:30'
        closed_at: '2027-03-20T12:00:00+03:30'
        cost_amount: 2500000
        status: OPEN
    PatchedNotificationPreferenceRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        allow_sms:
          type: boolean
          title: اجازه پیامک
        allow_email:
          type: boolean
          title: اجازه رایانامه
        allow_push:
          type: boolean
          title: اجازه اعلان Push
        allow_promotional:
          type: boolean
          title: اجازه پیام تبلیغاتی
        quiet_hours_start:
          type: string
          format: time
          nullable: true
          title: آغاز ساعات سکوت
        quiet_hours_end:
          type: string
          format: time
          nullable: true
          title: پایان ساعات سکوت
        preferred_locale:
          type: string
          minLength: 1
          title: زبان ترجیحی
          maxLength: 10
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        allow_sms: true
        allow_email: true
        allow_push: true
        allow_promotional: true
        quiet_hours_start: 08:30:00
        quiet_hours_end: 08:30:00
        preferred_locale: fa-IR
    PatchedNotificationRequest:
      type: object
      properties:
        template:
          type: string
          format: uuid
          title: قالب
          nullable: true
        recipient_person:
          type: string
          format: uuid
          title: گیرنده
        channel:
          allOf:
          - $ref: '#/components/schemas/NotificationChannelEnum'
          title: کانال
        priority:
          allOf:
          - $ref: '#/components/schemas/NotificationPriorityEnum'
          title: اولویت
        subject:
          type: string
          title: موضوع
          maxLength: 300
        body:
          type: string
          minLength: 1
          title: متن رندرشده
        deep_link:
          type: string
          title: پیوند پرتال امن
          maxLength: 400
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        scheduled_at:
          type: string
          format: date-time
          nullable: true
          title: زمان زمان‌بندی‌شده
        related_type:
          type: string
          title: نوع مرتبط
          maxLength: 80
        related_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه مرتبط
      example:
        template: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        recipient_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        channel: IN_APP
        priority: TRANSACTIONAL
        subject: متن نمونه
        body: متن نمونه
        deep_link: متن نمونه
        classification: PUBLIC
        scheduled_at: '2026-09-23T08:30:00+03:30'
        related_type: متن نمونه
        related_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PatchedNotificationTemplateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 60
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        channel:
          allOf:
          - $ref: '#/components/schemas/NotificationChannelEnum'
          title: کانال
        locale:
          type: string
          minLength: 1
          title: زبان و منطقه
          maxLength: 10
        version_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره نسخه
        subject_template:
          type: string
          title: قالب موضوع
          maxLength: 300
        body_template:
          type: string
          minLength: 1
          title: متن قالب
          description: متغیرها با {{variable}} نوشته می‌شوند.
        is_active:
          type: boolean
          title: فعال
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        channel: IN_APP
        locale: fa-IR
        version_no: 3
        subject_template: متن نمونه
        body_template: متن نمونه
        is_active: true
    PatchedOrgUnitRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        parent:
          type: string
          format: uuid
          title: والد
          nullable: true
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان واحد
          maxLength: 150
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        parent: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
    PatchedPaymentRequest:
      type: object
      properties:
        payer_person:
          type: string
          format: uuid
          title: پرداخت‌کننده
        school:
          type: string
          format: uuid
          title: مدرسه
          nullable: true
          description: برای تعیین کدینگ حساب و سال مالی هنگام ثبت سند
        method:
          allOf:
          - $ref: '#/components/schemas/MethodEnum'
          title: روش
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        bank_account:
          type: string
          format: uuid
          title: حساب بانکی
          nullable: true
        gateway_reference:
          type: string
          title: کد پیگیری درگاه
          maxLength: 200
        cheque_no:
          type: string
          title: شماره چک
          maxLength: 40
        cheque_due_date:
          type: string
          format: date
          nullable: true
          title: تاریخ سررسید چک
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        payer_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        method: CASH
        amount: 12500000
        currency: IRR
        received_at: '2026-09-23T08:30:00+03:30'
        bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        gateway_reference: متن نمونه
        cheque_no: SAMPLE-001
        cheque_due_date: '2026-09-23'
        note: یادداشت داخلی
    PatchedPayrollRunRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        period_from:
          type: string
          format: date
          title: از دوره
        period_to:
          type: string
          format: date
          title: تا دوره
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        period_from: '2026-09-23'
        period_to: '2027-03-20'
    PatchedPersonAddressRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        address:
          type: string
          format: uuid
          title: نشانی
        address_type:
          allOf:
          - $ref: '#/components/schemas/AddressTypeEnum'
          title: نوع نشانی
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        address_type: HOME
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    PatchedPersonDocumentRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        document_type:
          type: string
          minLength: 1
          title: نوع مدرک
          maxLength: 60
        title:
          type: string
          title: عنوان
          maxLength: 200
        file:
          type: string
          format: binary
          title: فایل
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        issued_on:
          type: string
          format: date
          nullable: true
          title: تاریخ صدور
        expires_at:
          type: string
          format: date
          nullable: true
          title: زمان انقضا
        purpose:
          type: string
          title: هدف
          maxLength: 120
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        document_type: متن نمونه
        title: عنوان نمونه
        file: sample.pdf
        classification: PUBLIC
        issued_on: '2024-05-11'
        expires_at: '2027-03-20'
        purpose: متن نمونه
    PatchedPersonRequest:
      type: object
      properties:
        national_id:
          type: string
          title: شماره ملی
          maxLength: 20
        passport_no:
          type: string
          title: شماره گذرنامه
          maxLength: 30
        first_name:
          type: string
          minLength: 1
          title: نام
          maxLength: 100
        last_name:
          type: string
          minLength: 1
          title: نام خانوادگی
          maxLength: 150
        father_name:
          type: string
          title: نام پدر
          maxLength: 100
        birth_date:
          type: string
          format: date
          nullable: true
          title: تاریخ تولد
        birth_place:
          type: string
          title: محل تولد
          maxLength: 120
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          title: جنسیت
        nationality:
          type: string
          title: تابعیت
          maxLength: 60
        preferred_language:
          type: string
          minLength: 1
          title: زبان ترجیحی
          maxLength: 10
        photo:
          type: string
          format: binary
          nullable: true
          title: تصویر
        accessibility_needs:
          type: string
          title: نیازهای دسترس‌پذیری
        status:
          allOf:
          - $ref: '#/components/schemas/PersonStatusEnum'
          title: وضعیت
      example:
        national_id: 0012345678
        passport_no: K12345678
        first_name: سارا
        last_name: محمدی
        father_name: علی
        birth_date: '2012-09-26'
        birth_place: تهران
        gender: MALE
        nationality: ایرانی
        preferred_language: متن نمونه
        photo: https://example.school/media/photos/sample.jpg
        accessibility_needs: متن نمونه
        status: ACTIVE
    PatchedPositionRequest:
      type: object
      properties:
        org_unit:
          type: string
          format: uuid
          title: واحد سازمانی
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان پست
          maxLength: 150
        position_type:
          allOf:
          - $ref: '#/components/schemas/PositionTypeEnum'
          title: نوع سِمت
        headcount:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد پست مصوب
      example:
        org_unit: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        position_type: TEACHING
        headcount: 3
    PatchedProctorEventRequest:
      type: object
      properties:
        attempt:
          type: string
          format: uuid
          title: تلاش
        event_type:
          allOf:
          - $ref: '#/components/schemas/ProctorEventEventTypeEnum'
          title: نوع رویداد
        severity:
          allOf:
          - $ref: '#/components/schemas/ProctorEventSeverityEnum'
          title: شدت
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        evidence_ref:
          type: string
          title: ارجاع مستند
          maxLength: 300
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: TAB_SWITCH
        severity: INFO
        occurred_at: '2026-09-23T08:30:00+03:30'
        evidence_ref: متن نمونه
        note: یادداشت داخلی
    PatchedProgramCourseRequest:
      type: object
      properties:
        program:
          type: string
          format: uuid
          title: رشته
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        course:
          type: string
          format: uuid
          title: درس
        weekly_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه در هفته
        is_required:
          type: boolean
          title: اجباری
      example:
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekly_minutes: 45
        is_required: true
    PatchedPromotionBatchRequest:
      type: object
      properties:
        source_year:
          type: string
          format: uuid
          title: سال مبدأ
        target_year:
          type: string
          format: uuid
          title: سال مقصد
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
      example:
        source_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        target_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
    PatchedPromotionDecisionRecordRequest:
      type: object
      properties:
        batch:
          type: string
          format: uuid
          title: دسته
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        decision:
          allOf:
          - $ref: '#/components/schemas/PromotionDecisionRecordDecisionEnum'
          title: تصمیم
        target_grade_level:
          type: string
          format: uuid
          title: پایه مقصد
          nullable: true
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        batch: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision: PROMOTED
        target_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        note: یادداشت داخلی
    PatchedPurchaseOrderLineRequest:
      type: object
      properties:
        order:
          type: string
          format: uuid
          title: سفارش
        item:
          type: string
          format: uuid
          title: کالا
        ordered_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: مقدار سفارش
        unit_price:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: قیمت واحد
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      example:
        order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        ordered_qty: '18.50'
        unit_price: 2500000
        line_no: 3
    PatchedPurchaseOrderRequest:
      type: object
      properties:
        vendor:
          type: string
          format: uuid
          title: تأمین‌کننده
        purchase_request:
          type: string
          format: uuid
          title: درخواست خرید
          nullable: true
        ordered_on:
          type: string
          format: date
          title: تاریخ سفارش
        expected_on:
          type: string
          format: date
          nullable: true
          title: تاریخ مورد انتظار
        total_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ کل
        tax_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ مالیات
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        purchase_request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        ordered_on: '2026-09-23'
        expected_on: '2026-09-23'
        total_amount: 45000000
        tax_amount: 1125000
        currency: IRR
        note: یادداشت داخلی
    PatchedPurchaseRequestLineRequest:
      type: object
      properties:
        request:
          type: string
          format: uuid
          title: درخواست
        item:
          type: string
          format: uuid
          title: کالا
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        estimated_unit_price:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: قیمت واحد برآوردی
        note:
          type: string
          title: یادداشت
          maxLength: 300
      example:
        request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        quantity: '18.50'
        estimated_unit_price: 2500000
        note: یادداشت داخلی
    PatchedPurchaseRequestRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        cost_center_id:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        needed_by:
          type: string
          format: date
          nullable: true
          title: نیاز تا تاریخ
        estimated_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ برآوردی
        justification:
          type: string
          title: توجیه
        budget_reserved_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بودجه رزروشده
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 400
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        needed_by: '2026-09-23'
        estimated_amount: 2500000
        justification: متن نمونه
        budget_reserved_amount: 2500000
        decision_note: توضیح نمونه
    PatchedQuestionBankRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        owner_user_id:
          type: string
          format: uuid
          nullable: true
          title: مالک
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
          maxLength: 400
        course:
          type: string
          format: uuid
          title: درس
          nullable: true
        visibility:
          allOf:
          - $ref: '#/components/schemas/QuestionBankVisibilityEnum'
          title: سطح نمایش
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        owner_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        visibility: PRIVATE
        status: متن نمونه
    PatchedQuestionOptionRequest:
      type: object
      properties:
        question_version:
          type: string
          format: uuid
          title: نسخه سؤال
        option_key:
          type: string
          minLength: 1
          title: کلید گزینه
          maxLength: 10
        body:
          type: string
          minLength: 1
          title: متن گزینه
        is_correct:
          type: boolean
          title: پاسخ درست
        credit_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد نمره این گزینه
          description: برای سؤال چندپاسخی با نمره جزئی
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
      example:
        question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        option_key: متن نمونه
        body: متن نمونه
        is_correct: true
        credit_percent: '18.50'
        display_order: 1
    PatchedQuestionRequest:
      type: object
      properties:
        bank:
          type: string
          format: uuid
          title: بانک سؤال
        question_type:
          allOf:
          - $ref: '#/components/schemas/QuestionQuestionTypeEnum'
          title: نوع سؤال
      example:
        bank: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_type: SINGLE_CHOICE
    PatchedQuestionTagRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        tag_type:
          type: string
          minLength: 1
          title: نوع برچسب
          description: TOPIC | OBJECTIVE | SOURCE | SKILL
          maxLength: 40
        value:
          type: string
          minLength: 1
          title: مقدار
          maxLength: 150
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        tag_type: متن نمونه
        value: متن نمونه
    PatchedQuestionVersionRequest:
      type: object
      properties:
        question:
          type: string
          format: uuid
          title: سؤال
        body:
          type: string
          minLength: 1
          title: متن سؤال
        explanation:
          type: string
          title: پاسخ تشریحی
        grading_rubric:
          type: string
          title: راهنمای تصحیح (Rubric)
        default_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم پیش‌فرض
        difficulty:
          allOf:
          - $ref: '#/components/schemas/DifficultyEnum'
          title: دشواری
        locale:
          type: string
          minLength: 1
          title: زبان و منطقه
          maxLength: 10
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        correct_answer:
          title: کلید پاسخ
          description: برای انواع کوتاه‌پاسخ/عددی/تطبیقی
        media:
          type: string
          format: binary
          nullable: true
          title: رسانه
      example:
        question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        body: متن نمونه
        explanation: متن نمونه
        grading_rubric: متن نمونه
        default_score: '18.50'
        difficulty: VERY_EASY
        locale: fa-IR
        grade_level: هفتم
        media: sample.pdf
    PatchedRefundRequest:
      type: object
      properties:
        payment:
          type: string
          format: uuid
          title: پرداخت
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        reason:
          type: string
          minLength: 1
          title: علت
          maxLength: 400
        requested_by_id:
          type: string
          format: uuid
          nullable: true
          title: درخواست‌دهنده
        gateway_reference:
          type: string
          title: ارجاع درگاه پرداخت
          maxLength: 200
      example:
        payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        amount: 12500000
        currency: IRR
        reason: اصلاح اطلاعات پس از بررسی پرونده
        requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        gateway_reference: متن نمونه
    PatchedReportCardRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        term:
          type: string
          format: uuid
          title: ترم
        rank_in_class:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: رتبه در کلاس
          description: فقط در صورت مجاز بودن سیاست رتبه‌بندی نمایش داده می‌شود
        attendance_summary:
          title: خلاصه حضور
        principal_comment:
          type: string
          title: نظر مدیر
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        rank_in_class: 3
        principal_comment: توضیح نمونه
    PatchedRidershipEventRequest:
      type: object
      properties:
        route_run:
          type: string
          format: uuid
          title: سرویس مسیر
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        stop:
          type: string
          format: uuid
          title: ایستگاه
          nullable: true
        event_type:
          allOf:
          - $ref: '#/components/schemas/RidershipEventEventTypeEnum'
          title: نوع رویداد
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        source:
          allOf:
          - $ref: '#/components/schemas/RidershipEventSourceEnum'
          title: منبع
      example:
        route_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: BOARDED
        occurred_at: '2026-09-23T08:30:00+03:30'
        source: DEVICE
    PatchedRoleRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد نقش
          maxLength: 60
        title:
          type: string
          minLength: 1
          title: عنوان نقش
          maxLength: 150
        description:
          type: string
          title: شرح
          maxLength: 400
        requires_mfa:
          type: boolean
          title: نیازمند احراز دومرحله‌ای
        allowed_scope_types:
          title: انواع Scope مجاز
          description: مثلاً ["SCHOOL", "CAMPUS"]
        permission_codes:
          type: array
          items:
            type: string
            minLength: 1
            title: کد مجوز
          description: فهرست کدهای مجوز، مثلاً ["student.read", "student.create"]
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        requires_mfa: true
        permission_codes:
        - متن نمونه
    PatchedRoomRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          minLength: 1
          title: کد اتاق
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 100
        room_type:
          allOf:
          - $ref: '#/components/schemas/RoomTypeEnum'
          title: نوع اتاق
        building:
          type: string
          title: ساختمان
          maxLength: 60
        floor:
          type: string
          title: طبقه
          maxLength: 20
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت فیزیکی
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        room_type: CLASSROOM
        building: متن نمونه
        floor: متن نمونه
        capacity: 3
        status: DRAFT
    PatchedRouteRunRequest:
      type: object
      properties:
        route:
          type: string
          format: uuid
          title: مسیر
        vehicle:
          type: string
          format: uuid
          title: خودرو
        driver_employee_id:
          type: string
          format: uuid
          nullable: true
          title: راننده
        supervisor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: سرپرست
        run_date:
          type: string
          format: date
          title: تاریخ سرویس
        direction:
          allOf:
          - $ref: '#/components/schemas/RouteDirectionEnum'
          title: جهت
        departed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان حرکت
        completed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان تکمیل
        status:
          allOf:
          - $ref: '#/components/schemas/RouteRunStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 300
      example:
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        supervisor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        run_date: '2026-09-23'
        direction: MORNING
        departed_at: '2026-09-23T08:30:00+03:30'
        completed_at: '2026-09-23T08:30:00+03:30'
        status: PLANNED
        note: یادداشت داخلی
    PatchedRouteStopRequest:
      type: object
      properties:
        route:
          type: string
          format: uuid
          title: مسیر
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        address_line:
          type: string
          title: سطر نشانی
          maxLength: 300
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: عرض جغرافیایی
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: طول جغرافیایی
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره ترتیب
        scheduled_time:
          type: string
          format: time
          nullable: true
          title: ساعت برنامه
      example:
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        latitude: '35.6892'
        longitude: '51.389'
        sequence_no: 3
        scheduled_time: 08:30:00
    PatchedScheduleEntryRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        teacher_profile_id:
          type: string
          format: uuid
          nullable: true
          title: معلم
        weekday:
          allOf:
          - $ref: '#/components/schemas/WeekdayEnum'
          title: روز هفته
          minimum: 0
          maximum: 9223372036854775807
        starts_at:
          type: string
          format: time
          title: ساعت شروع
        ends_at:
          type: string
          format: time
          title: ساعت پایان
        period_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: زنگ
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/ScheduleStatusEnum'
          title: وضعیت
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekday: 0
        starts_at: 08:30:00
        ends_at: 08:30:00
        period_no: 3
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        status: DRAFT
    PatchedSchoolRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد مدرسه
          maxLength: 30
        name:
          type: string
          minLength: 1
          title: نام مدرسه
          maxLength: 200
        school_type:
          allOf:
          - $ref: '#/components/schemas/SchoolTypeEnum'
          title: نوع مدرسه
        gender_policy:
          type: string
          title: سیاست جنسیتی
          description: مثلاً MALE / FEMALE / MIXED
          maxLength: 15
        registration_no:
          type: string
          title: شماره ثبت / مجوز
          maxLength: 50
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        timezone:
          type: string
          minLength: 1
          title: منطقه زمانی
          maxLength: 64
        logo:
          type: string
          format: binary
          nullable: true
          title: نشان
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      example:
        code: SAMPLE-001
        name: نام نمونه
        school_type: PRESCHOOL
        gender_policy: متن نمونه
        registration_no: SAMPLE-001
        currency: IRR
        timezone: Asia/Tehran
        logo: https://example.school/media/logo.png
        status: DRAFT
    PatchedSessionTeacherRequest:
      type: object
      properties:
        session:
          type: string
          format: uuid
          title: جلسه
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        duty:
          allOf:
          - $ref: '#/components/schemas/DutyEnum'
          title: وظیفه
      example:
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        duty: TEACHER
    PatchedStockDocumentLineRequest:
      type: object
      properties:
        stock_document:
          type: string
          format: uuid
          title: سند انبار
        item:
          type: string
          format: uuid
          title: کالا
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        uom:
          type: string
          format: uuid
          title: واحد سنجش
        lot_no:
          type: string
          title: شماره بچ
          maxLength: 60
        serial_no:
          type: string
          title: شماره سریال
          maxLength: 80
        expiry_date:
          type: string
          format: date
          nullable: true
          title: تاریخ انقضا
        unit_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای واحد (ریال)
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      example:
        stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        quantity: '18.50'
        uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        lot_no: SAMPLE-001
        serial_no: SN-88213047
        expiry_date: '2027-03-20'
        unit_cost: 2500000
        line_no: 3
    PatchedStockDocumentRequest:
      type: object
      properties:
        warehouse:
          type: string
          format: uuid
          title: انبار
        document_type:
          allOf:
          - $ref: '#/components/schemas/DocumentTypeEnum'
          title: نوع سند
        document_at:
          type: string
          format: date-time
          title: تاریخ سند
        source_type:
          type: string
          title: نوع منبع
          maxLength: 60
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
        counterpart_warehouse:
          type: string
          format: uuid
          title: انبار مقابل (در انتقال)
          nullable: true
        description:
          type: string
          title: شرح
          maxLength: 400
      example:
        warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        document_type: RECEIPT
        document_at: '2026-09-23T08:30:00+03:30'
        source_type: متن نمونه
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        counterpart_warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        description: توضیح تکمیلی این رکورد
    PatchedStudentFinancialAgreementRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        fee_plan:
          type: string
          format: uuid
          title: طرح شهریه
        responsible_guardian:
          type: string
          format: uuid
          title: مسئول مالی
          nullable: true
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        installment_count:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد اقساط
        status:
          allOf:
          - $ref: '#/components/schemas/AgreementStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 400
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        responsible_guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        currency: IRR
        installment_count: 12
        status: DRAFT
        note: یادداشت داخلی
    PatchedStudentGuardianRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        guardian:
          type: string
          format: uuid
          title: ولی
        relationship_type:
          allOf:
          - $ref: '#/components/schemas/RelationshipTypeEnum'
          title: نوع رابطه
        has_custody:
          type: boolean
          title: دارای حضانت
        can_pickup:
          type: boolean
          title: مجاز به تحویل گرفتن دانش‌آموز
        receives_reports:
          type: boolean
          title: دریافت‌کننده گزارش و کارنامه
        financially_responsible:
          type: boolean
          title: مسئول مالی
        is_emergency_contact:
          type: boolean
          title: تماس اضطراری
        contact_priority:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: اولویت تماس
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        relationship_type: FATHER
        has_custody: true
        can_pickup: true
        receives_reports: true
        financially_responsible: true
        is_emergency_contact: true
        contact_priority: 3
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    PatchedStudentRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        joined_on:
          type: string
          format: date
          title: تاریخ ورود به مدرسه
        status:
          allOf:
          - $ref: '#/components/schemas/StudentStatusEnum'
          title: وضعیت
        previous_school:
          type: string
          title: مدرسه مبدأ
          maxLength: 200
        notes:
          type: string
          title: یادداشت‌ها
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        joined_on: '2026-09-23'
        status: PROSPECTIVE
        previous_school: متن نمونه
        notes: متن نمونه
    PatchedStudentRouteAssignmentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        route:
          type: string
          format: uuid
          title: مسیر
        pickup_stop:
          type: string
          format: uuid
          title: ایستگاه سوارشدن
          nullable: true
        dropoff_stop:
          type: string
          format: uuid
          title: ایستگاه پیاده‌شدن
          nullable: true
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        pickup_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        dropoff_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        status: متن نمونه
        note: یادداشت داخلی
    PatchedStudentScoreRequest:
      type: object
      properties:
        grade_item:
          type: string
          format: uuid
          title: قلم نمره
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        raw_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره خام
        letter_grade:
          type: string
          title: نمره حرفی
          maxLength: 5
        qualitative_level:
          title: سطح کیفی
          oneOf:
          - $ref: '#/components/schemas/QualitativeLevelEnum'
          - $ref: '#/components/schemas/BlankEnum'
        status:
          allOf:
          - $ref: '#/components/schemas/ScoreStatusEnum'
          title: وضعیت
        comment:
          type: string
          title: توضیح
          maxLength: 400
      example:
        grade_item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        raw_score: '18.50'
        letter_grade: A
        qualitative_level: EXCELLENT
        status: NOT_RECORDED
        comment: توضیح کوتاه ثبت‌کننده
    PatchedStudyProgramRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد رشته
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان رشته
          maxLength: 150
        description:
          type: string
          title: شرح
          maxLength: 400
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        status: DRAFT
    PatchedSubmissionFeedbackRequest:
      type: object
      properties:
        submission:
          type: string
          format: uuid
          title: تحویل
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره
        feedback:
          type: string
          title: بازخورد
        is_final:
          type: boolean
          title: نهایی
      example:
        submission: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        score: '18.50'
        feedback: متن نمونه
        is_final: true
    PatchedTeacherProfileRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        required_weekly_hours:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: بار موظف هفتگی
        qualification_status:
          allOf:
          - $ref: '#/components/schemas/TeacherQualificationStatusEnum'
          title: وضعیت صلاحیت
        specialization:
          type: string
          title: تخصص
          maxLength: 200
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        required_weekly_hours: '18.50'
        qualification_status: PENDING
        specialization: متن نمونه
    PatchedTeacherQualificationRequest:
      type: object
      properties:
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        course:
          type: string
          format: uuid
          title: درس
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        valid_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/TeacherQualificationStatusEnum'
          title: وضعیت
      example:
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        valid_until: '2027-03-20'
        status: PENDING
    PatchedTeachingAssignmentRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        responsibility:
          allOf:
          - $ref: '#/components/schemas/ResponsibilityEnum'
          title: مسئولیت
        share_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: سهم تدریس
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        responsibility: PRIMARY
        share_percent: '18.50'
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    PatchedTeachingSessionRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        schedule_entry:
          type: string
          format: uuid
          title: سطر برنامه هفتگی
          nullable: true
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        starts_at:
          type: string
          format: date-time
          title: شروع
        ends_at:
          type: string
          format: date-time
          title: پایان
        session_type:
          allOf:
          - $ref: '#/components/schemas/SessionTypeEnum'
          title: نوع جلسه
        status:
          allOf:
          - $ref: '#/components/schemas/SessionStatusEnum'
          title: وضعیت
        topic:
          type: string
          title: موضوع جلسه
          maxLength: 250
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        schedule_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        starts_at: '2026-09-23T08:30:00+03:30'
        ends_at: '2027-03-20T12:00:00+03:30'
        session_type: REGULAR
        status: PLANNED
        topic: متن نمونه
        cancel_reason: توضیح نمونه
    PatchedTermRequest:
      type: object
      properties:
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        title:
          type: string
          minLength: 1
          title: عنوان ترم
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: شروع
        ends_on:
          type: string
          format: date
          title: پایان
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب
        status:
          allOf:
          - $ref: '#/components/schemas/TermStatusEnum'
          title: وضعیت
      example:
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        sequence_no: 3
        status: PLANNED
    PatchedTicketMessageRequest:
      type: object
      properties:
        ticket:
          type: string
          format: uuid
          title: تیکت
        body:
          type: string
          minLength: 1
          title: متن
        is_internal:
          type: boolean
          title: یادداشت داخلی (برای درخواست‌کننده نمایش داده نمی‌شود)
      example:
        ticket: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        body: متن نمونه
        is_internal: true
    PatchedTicketRequest:
      type: object
      properties:
        requester_person:
          type: string
          format: uuid
          title: درخواست‌دهنده
        category:
          type: string
          minLength: 1
          title: دسته درخواست
          maxLength: 60
        subject:
          type: string
          minLength: 1
          title: موضوع
          maxLength: 250
        description:
          type: string
          title: شرح
        priority:
          allOf:
          - $ref: '#/components/schemas/PriorityLevelEnum'
          title: اولویت
        assignee_user_id:
          type: string
          format: uuid
          nullable: true
          title: کاربر مسئول
        due_at:
          type: string
          format: date-time
          nullable: true
          title: مهلت SLA
        resolution:
          type: string
          title: نتیجه رسیدگی
      example:
        requester_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category: متن نمونه
        subject: متن نمونه
        description: توضیح تکمیلی این رکورد
        priority: LOW
        assignee_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        due_at: '2027-03-20T12:00:00+03:30'
        resolution: متن نمونه
    PatchedTransportRouteRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        direction:
          allOf:
          - $ref: '#/components/schemas/RouteDirectionEnum'
          title: جهت
        default_vehicle:
          type: string
          format: uuid
          title: خودرو پیش‌فرض
          nullable: true
        default_driver_employee_id:
          type: string
          format: uuid
          nullable: true
          title: راننده پیش‌فرض
        monthly_fee:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: هزینه ماهانه
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        direction: MORNING
        default_vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        default_driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        monthly_fee: 3
        status: متن نمونه
    PatchedUnitOfMeasureRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 20
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 80
        decimal_places:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد رقم اعشار
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        decimal_places: 3
    PatchedUserAccountRequest:
      type: object
      properties:
        username:
          type: string
          minLength: 1
          title: نام کاربری
          maxLength: 150
        email:
          title: ایمیل
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        mobile:
          type: string
          title: تلفن همراه
          maxLength: 20
        person:
          type: string
          format: uuid
          title: شخص
          nullable: true
        status:
          allOf:
          - $ref: '#/components/schemas/UserStatusEnum'
          title: وضعیت حساب
        mfa_enabled:
          type: boolean
          title: احراز هویت دومرحله‌ای
        mfa_method:
          allOf:
          - $ref: '#/components/schemas/MfaMethodEnum'
          title: روش احراز چندعاملی
        must_change_password:
          type: boolean
          title: الزام تغییر گذرواژه
        is_active:
          type: boolean
          title: فعال
      example:
        username: s.mohammadi
        email: user@example.school
        mobile: 09121234567
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: INVITED
        mfa_enabled: true
        mfa_method: NONE
        must_change_password: true
        is_active: true
    PatchedUserRoleAssignmentRequest:
      type: object
      properties:
        user:
          type: string
          format: uuid
          title: کاربر
        role:
          type: string
          format: uuid
          title: نقش
        scope_type:
          allOf:
          - $ref: '#/components/schemas/ScopeTypeEnum'
          title: نوع دامنه
        scope_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه دامنه
        status:
          allOf:
          - $ref: '#/components/schemas/RoleAssignmentStatusEnum'
          title: وضعیت
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        grant_reason:
          type: string
          title: علت اعطا
          maxLength: 300
        revoke_reason:
          type: string
          title: علت ابطال
          maxLength: 300
      example:
        user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        scope_type: TENANT
        scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: ACTIVE
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        grant_reason: توضیح نمونه
        revoke_reason: توضیح نمونه
    PatchedVehicleRequest:
      type: object
      properties:
        plate_no:
          type: string
          minLength: 1
          title: پلاک
          maxLength: 30
        model_name:
          type: string
          title: نام مدل
          maxLength: 120
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت
        inspection_valid_until:
          type: string
          format: date
          nullable: true
          title: اعتبار معاینه فنی
        insurance_valid_until:
          type: string
          format: date
          nullable: true
          title: اعتبار بیمه تا
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        plate_no: SAMPLE-001
        model_name: نام نمونه
        capacity: 3
        inspection_valid_until: '2026-09-23'
        insurance_valid_until: '2026-09-23'
        status: متن نمونه
    PatchedVendorRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        legal_name:
          type: string
          minLength: 1
          title: نام حقوقی
          maxLength: 250
        tax_id:
          type: string
          title: شناسه مالیاتی
          maxLength: 40
        contact_person:
          type: string
          title: شخص رابط
          maxLength: 150
        phone:
          type: string
          title: تلفن
          maxLength: 30
        email:
          title: رایانامه
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        address_line:
          type: string
          title: سطر نشانی
          maxLength: 400
        payment_terms_days:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مهلت پرداخت (روز)
        status:
          allOf:
          - $ref: '#/components/schemas/VendorStatusEnum'
          title: وضعیت
      example:
        code: SAMPLE-001
        legal_name: نام نمونه
        tax_id: متن نمونه
        contact_person: متن نمونه
        phone: 09121234567
        email: user@example.school
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        payment_terms_days: 3
        status: ACTIVE
    PatchedWarehouseRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        location:
          type: string
          title: محل
          maxLength: 200
        keeper_employee_id:
          type: string
          format: uuid
          nullable: true
          title: انباردار
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        location: متن نمونه
        keeper_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
    PatchedWorkShiftRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 100
        starts_at:
          type: string
          format: time
          title: زمان شروع
        ends_at:
          type: string
          format: time
          title: زمان پایان
        tolerance_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تلورانس تأخیر
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_at: 08:30:00
        ends_at: 08:30:00
        tolerance_minutes: 45
    PatchedWorkflowDefinitionRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد گردش
          maxLength: 60
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        version_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره نسخه
        subject_type:
          type: string
          minLength: 1
          title: نوع موضوع
          description: مثلاً finance.Refund یا inventory.PurchaseRequest
          maxLength: 80
        steps_definition:
          title: تعریف گام‌ها
          description: 'مثلاً [{"sequence": 1, "roleCode": "ACCOUNTANT"}]'
        is_active:
          type: boolean
          title: فعال
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        version_no: 3
        subject_type: متن نمونه
        is_active: true
    Payment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        payer_person:
          type: string
          format: uuid
          title: پرداخت‌کننده
        payer_name:
          type: string
          readOnly: true
          title: نام پرداخت‌کننده
        school:
          type: string
          format: uuid
          title: مدرسه
          nullable: true
          description: برای تعیین کدینگ حساب و سال مالی هنگام ثبت سند
        payment_no:
          type: string
          readOnly: true
          title: شماره پرداخت
        method:
          allOf:
          - $ref: '#/components/schemas/MethodEnum'
          title: روش
        method_display:
          type: string
          readOnly: true
          title: برچسب فارسی روش
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        bank_account:
          type: string
          format: uuid
          title: حساب بانکی
          nullable: true
        gateway_reference:
          type: string
          title: کد پیگیری درگاه
          maxLength: 200
        cheque_no:
          type: string
          title: شماره چک
          maxLength: 40
        cheque_due_date:
          type: string
          format: date
          nullable: true
          title: تاریخ سررسید چک
        status:
          allOf:
          - $ref: '#/components/schemas/PaymentStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 400
        void_reason:
          type: string
          readOnly: true
          title: علت ابطال
        allocations:
          type: array
          items:
            $ref: '#/components/schemas/PaymentAllocation'
          readOnly: true
          title: تخصیص‌ها
        allocated_amount:
          type: integer
          readOnly: true
          title: مبلغ تخصیص‌یافته (ریال)
        unallocated_amount:
          type: integer
          readOnly: true
          title: مبلغ تخصیص‌نیافته (ریال)
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - allocated_amount
      - allocations
      - amount
      - created_at
      - id
      - method
      - method_display
      - payer_name
      - payer_person
      - payment_no
      - received_at
      - status
      - status_display
      - unallocated_amount
      - updated_at
      - version
      - void_reason
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payer_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payer_name: نام نمونه
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payment_no: PAY-1405-000412
        method: CASH
        method_display: نقدی
        amount: 12500000
        currency: IRR
        received_at: '2026-09-23T08:30:00+03:30'
        bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        gateway_reference: متن نمونه
        cheque_no: SAMPLE-001
        cheque_due_date: '2026-09-23'
        status: PENDING
        status_display: در انتظار
        note: یادداشت داخلی
        void_reason: توضیح نمونه
        allocations:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          invoice_no: INV-1405-000137
          amount: 12500000
          allocated_at: '2026-09-23T08:30:00+03:30'
        allocated_amount: 2500000
        unallocated_amount: 2500000
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PaymentAllocation:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        payment:
          type: string
          format: uuid
          title: پرداخت
        invoice:
          type: string
          format: uuid
          title: صورتحساب
        invoice_no:
          type: string
          readOnly: true
          title: شماره صورتحساب
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        allocated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان تخصیص
      required:
      - allocated_at
      - amount
      - id
      - invoice
      - invoice_no
      - payment
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        invoice_no: INV-1405-000137
        amount: 12500000
        allocated_at: '2026-09-23T08:30:00+03:30'
    PaymentAllocationRequest:
      type: object
      properties:
        payment:
          type: string
          format: uuid
          title: پرداخت
        invoice:
          type: string
          format: uuid
          title: صورتحساب
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
      required:
      - amount
      - invoice
      - payment
      example:
        payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        invoice: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        amount: 12500000
    PaymentRequest:
      type: object
      properties:
        payer_person:
          type: string
          format: uuid
          title: پرداخت‌کننده
        school:
          type: string
          format: uuid
          title: مدرسه
          nullable: true
          description: برای تعیین کدینگ حساب و سال مالی هنگام ثبت سند
        method:
          allOf:
          - $ref: '#/components/schemas/MethodEnum'
          title: روش
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        bank_account:
          type: string
          format: uuid
          title: حساب بانکی
          nullable: true
        gateway_reference:
          type: string
          title: کد پیگیری درگاه
          maxLength: 200
        cheque_no:
          type: string
          title: شماره چک
          maxLength: 40
        cheque_due_date:
          type: string
          format: date
          nullable: true
          title: تاریخ سررسید چک
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - amount
      - method
      - payer_person
      - received_at
      example:
        payer_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        method: CASH
        amount: 12500000
        currency: IRR
        received_at: '2026-09-23T08:30:00+03:30'
        bank_account: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        gateway_reference: متن نمونه
        cheque_no: SAMPLE-001
        cheque_due_date: '2026-09-23'
        note: یادداشت داخلی
    PaymentStatusEnum:
      enum:
      - PENDING
      - SUCCEEDED
      - FAILED
      - VOIDED
      - REFUNDED
      - PARTIALLY_REFUNDED
      type: string
      description: |-
        * `PENDING` - در انتظار
        * `SUCCEEDED` - موفق
        * `FAILED` - ناموفق
        * `VOIDED` - ابطال‌شده
        * `REFUNDED` - مسترد شده
        * `PARTIALLY_REFUNDED` - استرداد جزئی
    PayrollRun:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          title: عنوان
          maxLength: 150
        period_from:
          type: string
          format: date
          title: از دوره
        period_to:
          type: string
          format: date
          title: تا دوره
        status:
          allOf:
          - $ref: '#/components/schemas/PayrollStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        calculated_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان محاسبه
        approved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید
        payslip_count:
          type: integer
          readOnly: true
          title: تعداد فیش حقوقی
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - approved_at
      - calculated_at
      - created_at
      - id
      - payslip_count
      - period_from
      - period_to
      - school
      - status
      - status_display
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        period_from: '2026-09-23'
        period_to: '2027-03-20'
        status: DRAFT
        status_display: پیش‌نویس
        calculated_at: '2026-09-23T08:30:00+03:30'
        approved_at: '2026-09-23T08:30:00+03:30'
        payslip_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PayrollRunRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        period_from:
          type: string
          format: date
          title: از دوره
        period_to:
          type: string
          format: date
          title: تا دوره
      required:
      - period_from
      - period_to
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        period_from: '2026-09-23'
        period_to: '2027-03-20'
    PayrollStatusEnum:
      enum:
      - DRAFT
      - CALCULATED
      - APPROVED
      - PAID
      - CLOSED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `CALCULATED` - محاسبه‌شده
        * `APPROVED` - تأییدشده
        * `PAID` - پرداخت‌شده
        * `CLOSED` - بسته
    Payslip:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        payroll_run:
          type: string
          format: uuid
          title: دوره حقوق
        employee:
          type: string
          format: uuid
          title: کارمند
        employee_name:
          type: string
          readOnly: true
          title: نام کارمند
        gross_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: ناخالص
        deduction_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: کسورات
        net_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: خالص پرداختی
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        status:
          type: string
          title: وضعیت
          maxLength: 20
        correction_of:
          type: string
          format: uuid
          title: فیش مابه‌التفاوت برای
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/PayslipItem'
          readOnly: true
          title: اقلام
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - employee
      - employee_name
      - id
      - items
      - payroll_run
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payroll_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_name: مریم رضایی
        gross_amount: 2500000
        deduction_amount: 2500000
        net_amount: 2500000
        currency: IRR
        status: متن نمونه
        correction_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        items:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          payslip: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_type: EARNING
          item_type_display: مزایا
          code: SAMPLE-001
          title: عنوان نمونه
          amount: 12500000
          quantity: '18.50'
        created_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PayslipItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        payslip:
          type: string
          format: uuid
          title: فیش حقوقی
        item_type:
          allOf:
          - $ref: '#/components/schemas/ItemTypeEnum'
          title: نوع قلم
        item_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع قلم
        code:
          type: string
          title: کد
          maxLength: 40
        title:
          type: string
          title: عنوان
          maxLength: 150
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,2})?$
          title: تعداد
      required:
      - code
      - id
      - item_type
      - item_type_display
      - payslip
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payslip: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_type: EARNING
        item_type_display: مزایا
        code: SAMPLE-001
        title: عنوان نمونه
        amount: 12500000
        quantity: '18.50'
    PayslipItemRequest:
      type: object
      properties:
        payslip:
          type: string
          format: uuid
          title: فیش حقوقی
        item_type:
          allOf:
          - $ref: '#/components/schemas/ItemTypeEnum'
          title: نوع قلم
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 40
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,2})?$
          title: تعداد
      required:
      - code
      - item_type
      - payslip
      - title
      example:
        payslip: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_type: EARNING
        code: SAMPLE-001
        title: عنوان نمونه
        amount: 12500000
        quantity: '18.50'
    PayslipRequest:
      type: object
      properties:
        payroll_run:
          type: string
          format: uuid
          title: دوره حقوق
        employee:
          type: string
          format: uuid
          title: کارمند
        gross_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: ناخالص
        deduction_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: کسورات
        net_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: خالص پرداختی
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        correction_of:
          type: string
          format: uuid
          title: فیش مابه‌التفاوت برای
          nullable: true
      required:
      - employee
      - payroll_run
      example:
        payroll_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        gross_amount: 2500000
        deduction_amount: 2500000
        net_amount: 2500000
        currency: IRR
        status: متن نمونه
        correction_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    PendingApprovalStateEnum:
      enum:
      - PENDING
      - APPROVED
      - REJECTED
      type: string
      description: |-
        * `PENDING` - در انتظار تأیید
        * `APPROVED` - تأییدشده
        * `REJECTED` - ردشده
    Permission:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد مجوز
          maxLength: 100
        resource:
          type: string
          title: منبع
          maxLength: 60
        action:
          type: string
          title: عمل
          maxLength: 40
        title:
          type: string
          title: عنوان فارسی
          maxLength: 150
        module:
          type: string
          title: ماژول
          maxLength: 40
        description:
          type: string
          title: شرح
          maxLength: 300
        is_sensitive:
          type: boolean
          title: حساس (نیازمند بازبینی دوره‌ای)
      required:
      - action
      - code
      - id
      - resource
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        resource: متن نمونه
        action: متن نمونه
        title: عنوان نمونه
        module: متن نمونه
        description: توضیح تکمیلی این رکورد
        is_sensitive: true
    Person:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        national_id:
          type: string
          title: شماره ملی
          maxLength: 20
        passport_no:
          type: string
          title: شماره گذرنامه
          maxLength: 30
        first_name:
          type: string
          title: نام
          maxLength: 100
        last_name:
          type: string
          title: نام خانوادگی
          maxLength: 150
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        father_name:
          type: string
          title: نام پدر
          maxLength: 100
        birth_date:
          type: string
          format: date
          nullable: true
          title: تاریخ تولد
        birth_place:
          type: string
          title: محل تولد
          maxLength: 120
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          title: جنسیت
        nationality:
          type: string
          title: تابعیت
          maxLength: 60
        preferred_language:
          type: string
          title: زبان ترجیحی
          maxLength: 10
        photo:
          type: string
          format: uri
          nullable: true
          title: تصویر
        accessibility_needs:
          type: string
          title: نیازهای دسترس‌پذیری
        status:
          allOf:
          - $ref: '#/components/schemas/PersonStatusEnum'
          title: وضعیت
        contact_points:
          type: array
          items:
            $ref: '#/components/schemas/ContactPoint'
          readOnly: true
          title: راه‌های تماس
        addresses:
          type: array
          items:
            $ref: '#/components/schemas/PersonAddress'
          readOnly: true
          title: نشانی‌ها
        has_user_account:
          type: boolean
          readOnly: true
          title: حساب کاربری دارد
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - addresses
      - contact_points
      - created_at
      - first_name
      - full_name
      - has_user_account
      - id
      - last_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        national_id: 0012345678
        passport_no: K12345678
        first_name: سارا
        last_name: محمدی
        full_name: سارا محمدی
        father_name: علی
        birth_date: '2012-09-26'
        birth_place: تهران
        gender: MALE
        nationality: ایرانی
        preferred_language: متن نمونه
        photo: https://example.school/media/photos/sample.jpg
        accessibility_needs: متن نمونه
        status: ACTIVE
        contact_points:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          contact_type: MOBILE
          contact_type_display: تلفن همراه
          value: متن نمونه
          is_primary: true
          is_verified: true
          verified_at: '2026-09-23T08:30:00+03:30'
          label: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        addresses:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          address_detail:
            id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            country: IR
            province: تهران
            city: تهران
            district: منطقه ۳
            postal_code: '1234567890'
            line: خیابان نمونه، کوچه دوم، پلاک ۱۲
            latitude: '35.6892'
            longitude: '51.389'
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          address_type: HOME
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
        has_user_account: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PersonAddress:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        address:
          type: string
          format: uuid
          title: نشانی
        address_detail:
          allOf:
          - $ref: '#/components/schemas/Address'
          readOnly: true
          title: مشخصات نشانی
        address_type:
          allOf:
          - $ref: '#/components/schemas/AddressTypeEnum'
          title: نوع نشانی
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      required:
      - address
      - address_detail
      - effective_from
      - id
      - person
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        address_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          country: IR
          province: تهران
          city: تهران
          district: منطقه ۳
          postal_code: '1234567890'
          line: خیابان نمونه، کوچه دوم، پلاک ۱۲
          latitude: '35.6892'
          longitude: '51.389'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        address_type: HOME
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    PersonAddressRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        address:
          type: string
          format: uuid
          title: نشانی
        address_type:
          allOf:
          - $ref: '#/components/schemas/AddressTypeEnum'
          title: نوع نشانی
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      required:
      - address
      - effective_from
      - person
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        address_type: HOME
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    PersonDocument:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        document_type:
          type: string
          title: نوع مدرک
          maxLength: 60
        title:
          type: string
          title: عنوان
          maxLength: 200
        file:
          type: string
          format: uri
          title: فایل
        sha256:
          type: string
          readOnly: true
          title: هش فایل
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        verification_status:
          allOf:
          - $ref: '#/components/schemas/VerificationStatusEnum'
          readOnly: true
          title: وضعیت تأیید
        verified_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید هویت
        issued_on:
          type: string
          format: date
          nullable: true
          title: تاریخ صدور
        expires_at:
          type: string
          format: date
          nullable: true
          title: زمان انقضا
        purpose:
          type: string
          title: هدف
          maxLength: 120
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - document_type
      - file
      - id
      - person
      - sha256
      - updated_at
      - verification_status
      - verified_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        document_type: متن نمونه
        title: عنوان نمونه
        file: https://example.school/media/sample.pdf
        sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
        classification: PUBLIC
        verification_status: PENDING
        verified_at: '2026-09-23T08:30:00+03:30'
        issued_on: '2024-05-11'
        expires_at: '2027-03-20'
        purpose: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PersonDocumentRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        document_type:
          type: string
          minLength: 1
          title: نوع مدرک
          maxLength: 60
        title:
          type: string
          title: عنوان
          maxLength: 200
        file:
          type: string
          format: binary
          title: فایل
        classification:
          allOf:
          - $ref: '#/components/schemas/DataClassificationEnum'
          title: طبقه‌بندی
        issued_on:
          type: string
          format: date
          nullable: true
          title: تاریخ صدور
        expires_at:
          type: string
          format: date
          nullable: true
          title: زمان انقضا
        purpose:
          type: string
          title: هدف
          maxLength: 120
      required:
      - document_type
      - file
      - person
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        document_type: متن نمونه
        title: عنوان نمونه
        file: sample.pdf
        classification: PUBLIC
        issued_on: '2024-05-11'
        expires_at: '2027-03-20'
        purpose: متن نمونه
    PersonList:
      type: object
      description: نمای سبک برای فهرست و جست‌وجو.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        first_name:
          type: string
          title: نام
          maxLength: 100
        last_name:
          type: string
          title: نام خانوادگی
          maxLength: 150
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        national_id:
          type: string
          title: شماره ملی
          maxLength: 20
        birth_date:
          type: string
          format: date
          nullable: true
          title: تاریخ تولد
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          title: جنسیت
        gender_display:
          type: string
          readOnly: true
          title: برچسب فارسی جنسیت
        status:
          allOf:
          - $ref: '#/components/schemas/PersonStatusEnum'
          title: وضعیت
      required:
      - first_name
      - full_name
      - gender_display
      - id
      - last_name
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        first_name: سارا
        last_name: محمدی
        full_name: سارا محمدی
        national_id: 0012345678
        birth_date: '2012-09-26'
        gender: MALE
        gender_display: مرد
        status: ACTIVE
    PersonListRequest:
      type: object
      description: نمای سبک برای فهرست و جست‌وجو.
      properties:
        first_name:
          type: string
          minLength: 1
          title: نام
          maxLength: 100
        last_name:
          type: string
          minLength: 1
          title: نام خانوادگی
          maxLength: 150
        national_id:
          type: string
          title: شماره ملی
          maxLength: 20
        birth_date:
          type: string
          format: date
          nullable: true
          title: تاریخ تولد
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          title: جنسیت
        status:
          allOf:
          - $ref: '#/components/schemas/PersonStatusEnum'
          title: وضعیت
      required:
      - first_name
      - last_name
      example:
        first_name: سارا
        last_name: محمدی
        national_id: 0012345678
        birth_date: '2012-09-26'
        gender: MALE
        status: ACTIVE
    PersonRequest:
      type: object
      properties:
        national_id:
          type: string
          title: شماره ملی
          maxLength: 20
        passport_no:
          type: string
          title: شماره گذرنامه
          maxLength: 30
        first_name:
          type: string
          minLength: 1
          title: نام
          maxLength: 100
        last_name:
          type: string
          minLength: 1
          title: نام خانوادگی
          maxLength: 150
        father_name:
          type: string
          title: نام پدر
          maxLength: 100
        birth_date:
          type: string
          format: date
          nullable: true
          title: تاریخ تولد
        birth_place:
          type: string
          title: محل تولد
          maxLength: 120
        gender:
          allOf:
          - $ref: '#/components/schemas/GenderEnum'
          title: جنسیت
        nationality:
          type: string
          title: تابعیت
          maxLength: 60
        preferred_language:
          type: string
          minLength: 1
          title: زبان ترجیحی
          maxLength: 10
        photo:
          type: string
          format: binary
          nullable: true
          title: تصویر
        accessibility_needs:
          type: string
          title: نیازهای دسترس‌پذیری
        status:
          allOf:
          - $ref: '#/components/schemas/PersonStatusEnum'
          title: وضعیت
      required:
      - first_name
      - last_name
      example:
        national_id: 0012345678
        passport_no: K12345678
        first_name: سارا
        last_name: محمدی
        father_name: علی
        birth_date: '2012-09-26'
        birth_place: تهران
        gender: MALE
        nationality: ایرانی
        preferred_language: متن نمونه
        photo: https://example.school/media/photos/sample.jpg
        accessibility_needs: متن نمونه
        status: ACTIVE
    PersonStatusEnum:
      enum:
      - ACTIVE
      - INACTIVE
      - MERGED
      - DECEASED
      type: string
      description: |-
        * `ACTIVE` - فعال
        * `INACTIVE` - غیرفعال
        * `MERGED` - ادغام‌شده در پرونده دیگر
        * `DECEASED` - متوفی
    PlaceInClassRequest:
      type: object
      properties:
        class_group:
          type: string
          format: uuid
          description: شناسه کلاس مقصد
        effective_from:
          type: string
          format: date
          description: تاریخ اثر؛ پیش‌فرض امروز
      required:
      - class_group
      example:
        class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
    Position:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        org_unit:
          type: string
          format: uuid
          title: واحد سازمانی
        org_unit_title:
          type: string
          readOnly: true
          title: عنوان واحد سازمانی
        code:
          type: string
          title: کد
          maxLength: 30
        title:
          type: string
          title: عنوان پست
          maxLength: 150
        position_type:
          allOf:
          - $ref: '#/components/schemas/PositionTypeEnum'
          title: نوع سِمت
        position_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع سمت
        headcount:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد پست مصوب
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - org_unit
      - org_unit_title
      - position_type
      - position_type_display
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        org_unit: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        org_unit_title: عنوان نمونه
        code: SAMPLE-001
        title: عنوان نمونه
        position_type: TEACHING
        position_type_display: آموزشی
        headcount: 3
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PositionRequest:
      type: object
      properties:
        org_unit:
          type: string
          format: uuid
          title: واحد سازمانی
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان پست
          maxLength: 150
        position_type:
          allOf:
          - $ref: '#/components/schemas/PositionTypeEnum'
          title: نوع سِمت
        headcount:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد پست مصوب
      required:
      - code
      - org_unit
      - position_type
      - title
      example:
        org_unit: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        position_type: TEACHING
        headcount: 3
    PositionTypeEnum:
      enum:
      - TEACHING
      - ADMINISTRATIVE
      - MANAGEMENT
      - SUPPORT
      - HEALTH
      - COUNSELING
      type: string
      description: |-
        * `TEACHING` - آموزشی
        * `ADMINISTRATIVE` - اداری
        * `MANAGEMENT` - مدیریتی
        * `SUPPORT` - پشتیبانی
        * `HEALTH` - بهداشت و درمان
        * `COUNSELING` - مشاوره
    PriorityLevelEnum:
      enum:
      - LOW
      - NORMAL
      - HIGH
      - URGENT
      type: string
      description: |-
        * `LOW` - کم
        * `NORMAL` - عادی
        * `HIGH` - زیاد
        * `URGENT` - فوری
    ProctorEvent:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        attempt:
          type: string
          format: uuid
          title: تلاش
        event_type:
          allOf:
          - $ref: '#/components/schemas/ProctorEventEventTypeEnum'
          title: نوع رویداد
        severity:
          allOf:
          - $ref: '#/components/schemas/ProctorEventSeverityEnum'
          title: شدت
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        evidence_ref:
          type: string
          title: ارجاع مستند
          maxLength: 300
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - attempt
      - event_type
      - id
      - occurred_at
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: TAB_SWITCH
        severity: INFO
        occurred_at: '2026-09-23T08:30:00+03:30'
        evidence_ref: متن نمونه
        note: یادداشت داخلی
    ProctorEventEventTypeEnum:
      enum:
      - TAB_SWITCH
      - FOCUS_LOST
      - COPY_PASTE
      - MULTIPLE_SESSION
      - DISCONNECT
      - RECONNECT
      - IP_CHANGE
      - MANUAL_FLAG
      type: string
      description: |-
        * `TAB_SWITCH` - خروج از پنجره آزمون
        * `FOCUS_LOST` - از دست رفتن تمرکز صفحه
        * `COPY_PASTE` - کپی/چسباندن
        * `MULTIPLE_SESSION` - ورود هم‌زمان
        * `DISCONNECT` - قطع اتصال
        * `RECONNECT` - اتصال مجدد
        * `IP_CHANGE` - تغییر نشانی شبکه
        * `MANUAL_FLAG` - ثبت دستی مراقب
    ProctorEventRequest:
      type: object
      properties:
        attempt:
          type: string
          format: uuid
          title: تلاش
        event_type:
          allOf:
          - $ref: '#/components/schemas/ProctorEventEventTypeEnum'
          title: نوع رویداد
        severity:
          allOf:
          - $ref: '#/components/schemas/ProctorEventSeverityEnum'
          title: شدت
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        evidence_ref:
          type: string
          title: ارجاع مستند
          maxLength: 300
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - attempt
      - event_type
      - occurred_at
      example:
        attempt: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: TAB_SWITCH
        severity: INFO
        occurred_at: '2026-09-23T08:30:00+03:30'
        evidence_ref: متن نمونه
        note: یادداشت داخلی
    ProctorEventSeverityEnum:
      enum:
      - INFO
      - WARNING
      - CRITICAL
      type: string
      description: |-
        * `INFO` - اطلاعاتی
        * `WARNING` - هشدار
        * `CRITICAL` - بحرانی
    ProgramCourse:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        program:
          type: string
          format: uuid
          title: رشته
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        grade_level_title:
          type: string
          readOnly: true
          title: عنوان پایه تحصیلی
        course:
          type: string
          format: uuid
          title: درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        weekly_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه در هفته
        is_required:
          type: boolean
          title: اجباری
      required:
      - course
      - course_title
      - grade_level
      - grade_level_title
      - id
      - program
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        grade_level_title: پایه هفتم
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        weekly_minutes: 45
        is_required: true
    ProgramCourseRequest:
      type: object
      properties:
        program:
          type: string
          format: uuid
          title: رشته
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
        course:
          type: string
          format: uuid
          title: درس
        weekly_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: دقیقه در هفته
        is_required:
          type: boolean
          title: اجباری
      required:
      - course
      - grade_level
      - program
      example:
        program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekly_minutes: 45
        is_required: true
    PromotionBatch:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        source_year:
          type: string
          format: uuid
          title: سال مبدأ
        target_year:
          type: string
          format: uuid
          title: سال مقصد
        title:
          type: string
          title: عنوان
          maxLength: 200
        status:
          type: string
          readOnly: true
          title: وضعیت
        executed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان اجرا
        decision_count:
          type: integer
          readOnly: true
          title: تعداد تصمیم
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - decision_count
      - executed_at
      - id
      - source_year
      - status
      - target_year
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        source_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        target_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        status: متن نمونه
        executed_at: '2026-09-23T08:30:00+03:30'
        decision_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PromotionBatchRequest:
      type: object
      properties:
        source_year:
          type: string
          format: uuid
          title: سال مبدأ
        target_year:
          type: string
          format: uuid
          title: سال مقصد
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
      required:
      - source_year
      - target_year
      - title
      example:
        source_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        target_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
    PromotionDecisionRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        batch:
          type: string
          format: uuid
          title: دسته
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        decision:
          allOf:
          - $ref: '#/components/schemas/PromotionDecisionRecordDecisionEnum'
          title: تصمیم
        target_grade_level:
          type: string
          format: uuid
          title: پایه مقصد
          nullable: true
        note:
          type: string
          title: یادداشت
          maxLength: 400
        applied:
          type: boolean
          readOnly: true
          title: اعمال‌شده
      required:
      - applied
      - batch
      - decision
      - enrollment
      - id
      - student_name
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        batch: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        decision: PROMOTED
        target_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        note: یادداشت داخلی
        applied: true
    PromotionDecisionRecordDecisionEnum:
      enum:
      - PROMOTED
      - REPEATED
      - CONDITIONAL
      - COUNCIL_REVIEW
      - TRANSFERRED
      - GRADUATED
      type: string
      description: |-
        * `PROMOTED` - ارتقا
        * `REPEATED` - تکرار پایه
        * `CONDITIONAL` - مشروط
        * `COUNCIL_REVIEW` - نیازمند تصمیم شورا
        * `TRANSFERRED` - انتقال
        * `GRADUATED` - فارغ‌التحصیل
    PromotionDecisionRecordRequest:
      type: object
      properties:
        batch:
          type: string
          format: uuid
          title: دسته
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        decision:
          allOf:
          - $ref: '#/components/schemas/PromotionDecisionRecordDecisionEnum'
          title: تصمیم
        target_grade_level:
          type: string
          format: uuid
          title: پایه مقصد
          nullable: true
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - batch
      - decision
      - enrollment
      example:
        batch: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        decision: PROMOTED
        target_grade_level: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        note: یادداشت داخلی
    PromotionPreview:
      type: object
      description: خروجی مرحله پیش‌نمایش عملیات گروهی (بخش ۱۱.۶).
      properties:
        totalRows:
          type: integer
          title: مجموع سطرها
        validRows:
          type: integer
          title: سطرهای معتبر
        invalidRows:
          type: integer
          title: سطرهای نامعتبر
        rows:
          type: array
          items:
            $ref: '#/components/schemas/PromotionPreviewRow'
          title: سطرها
      required:
      - invalidRows
      - rows
      - totalRows
      - validRows
      example:
        totalRows: 12
        validRows: 3
        invalidRows: 3
        rows:
        - enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          studentNo: '14030127'
          studentName: سارا محمدی
          currentGrade: متن نمونه
          suggestedDecision: متن نمونه
          targetGradeLevelId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          targetGrade: متن نمونه
          errors: []
    PromotionPreviewRow:
      type: object
      properties:
        enrollmentId:
          type: string
          format: uuid
          title: شناسه ثبت‌نام
        studentNo:
          type: string
          title: شماره دانش‌آموزی
        studentName:
          type: string
          title: نام دانش‌آموز
        currentGrade:
          type: string
          title: پایه فعلی
        suggestedDecision:
          type: string
          title: تصمیم پیشنهادی سامانه
        targetGradeLevelId:
          type: string
          format: uuid
          nullable: true
          title: شناسه پایه مقصد
        targetGrade:
          type: string
          nullable: true
          title: پایه مقصد
        errors:
          type: array
          items:
            type: string
          title: خطاها
      required:
      - currentGrade
      - enrollmentId
      - errors
      - studentName
      - studentNo
      - suggestedDecision
      - targetGrade
      - targetGradeLevelId
      example:
        enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        studentNo: '14030127'
        studentName: سارا محمدی
        currentGrade: متن نمونه
        suggestedDecision: متن نمونه
        targetGradeLevelId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        targetGrade: متن نمونه
        errors:
        - متن نمونه
    PurchaseOrder:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        vendor:
          type: string
          format: uuid
          title: تأمین‌کننده
        vendor_name:
          type: string
          readOnly: true
          title: نام تأمین‌کننده
        purchase_request:
          type: string
          format: uuid
          title: درخواست خرید
          nullable: true
        order_no:
          type: string
          readOnly: true
          title: شماره سفارش
        ordered_on:
          type: string
          format: date
          title: تاریخ سفارش
        expected_on:
          type: string
          format: date
          nullable: true
          title: تاریخ مورد انتظار
        total_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ کل
        tax_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ مالیات
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        status:
          allOf:
          - $ref: '#/components/schemas/PurchaseOrderStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 400
        lines:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseOrderLine'
          readOnly: true
          title: سطرهای سند
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - id
      - lines
      - order_no
      - ordered_on
      - status
      - status_display
      - updated_at
      - vendor
      - vendor_name
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vendor_name: نام نمونه
        purchase_request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        order_no: PO-1405-000027
        ordered_on: '2026-09-23'
        expected_on: '2026-09-23'
        total_amount: 45000000
        tax_amount: 1125000
        currency: IRR
        status: DRAFT
        status_display: پیش‌نویس
        note: یادداشت داخلی
        lines:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_sku: STA-A4-80
          item_title: عنوان نمونه
          ordered_qty: '18.50'
          received_qty: '18.50'
          remaining_qty: '18.50'
          unit_price: 2500000
          line_no: 3
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    PurchaseOrderLine:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        order:
          type: string
          format: uuid
          title: سفارش
        item:
          type: string
          format: uuid
          title: کالا
        item_sku:
          type: string
          readOnly: true
          title: کد کالا (SKU)
        item_title:
          type: string
          readOnly: true
          title: عنوان قلم
        ordered_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: مقدار سفارش
        received_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          readOnly: true
          title: مقدار دریافت
        remaining_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          readOnly: true
          title: مقدار باقی‌مانده
        unit_price:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: قیمت واحد
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      required:
      - id
      - item
      - item_sku
      - item_title
      - order
      - ordered_qty
      - received_qty
      - remaining_qty
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_sku: STA-A4-80
        item_title: عنوان نمونه
        ordered_qty: '18.50'
        received_qty: '18.50'
        remaining_qty: '18.50'
        unit_price: 2500000
        line_no: 3
    PurchaseOrderLineRequest:
      type: object
      properties:
        order:
          type: string
          format: uuid
          title: سفارش
        item:
          type: string
          format: uuid
          title: کالا
        ordered_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: مقدار سفارش
        unit_price:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: قیمت واحد
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      required:
      - item
      - order
      - ordered_qty
      example:
        order: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        ordered_qty: '18.50'
        unit_price: 2500000
        line_no: 3
    PurchaseOrderRequest:
      type: object
      properties:
        vendor:
          type: string
          format: uuid
          title: تأمین‌کننده
        purchase_request:
          type: string
          format: uuid
          title: درخواست خرید
          nullable: true
        ordered_on:
          type: string
          format: date
          title: تاریخ سفارش
        expected_on:
          type: string
          format: date
          nullable: true
          title: تاریخ مورد انتظار
        total_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ کل
        tax_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ مالیات
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - ordered_on
      - vendor
      example:
        vendor: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        purchase_request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        ordered_on: '2026-09-23'
        expected_on: '2026-09-23'
        total_amount: 45000000
        tax_amount: 1125000
        currency: IRR
        note: یادداشت داخلی
    PurchaseOrderStatusEnum:
      enum:
      - DRAFT
      - ISSUED
      - PARTIALLY_RECEIVED
      - RECEIVED
      - CLOSED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `ISSUED` - صادرشده
        * `PARTIALLY_RECEIVED` - دریافت جزئی
        * `RECEIVED` - دریافت کامل
        * `CLOSED` - بسته
        * `CANCELLED` - لغوشده
    PurchaseRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        requester_user_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: کاربر درخواست‌دهنده
        campus:
          type: string
          format: uuid
          title: شعبه
        cost_center_id:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        request_no:
          type: string
          readOnly: true
          title: شماره درخواست
        title:
          type: string
          title: عنوان
          maxLength: 200
        needed_by:
          type: string
          format: date
          nullable: true
          title: نیاز تا تاریخ
        estimated_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ برآوردی
        justification:
          type: string
          title: توجیه
        status:
          allOf:
          - $ref: '#/components/schemas/PurchaseRequestStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        budget_reserved_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بودجه رزروشده
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 400
        lines:
          type: array
          items:
            $ref: '#/components/schemas/PurchaseRequestLine'
          readOnly: true
          title: سطرهای سند
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus
      - created_at
      - id
      - lines
      - request_no
      - requester_user_id
      - status
      - status_display
      - title
      - updated_at
      - version
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        needed_by: '2026-09-23'
        estimated_amount: 2500000
        justification: متن نمونه
        budget_reserved_amount: 2500000
        decision_note: توضیح نمونه
    PurchaseRequestLine:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        request:
          type: string
          format: uuid
          title: درخواست
        item:
          type: string
          format: uuid
          title: کالا
        item_title:
          type: string
          readOnly: true
          title: عنوان قلم
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        estimated_unit_price:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: قیمت واحد برآوردی
        note:
          type: string
          title: یادداشت
          maxLength: 300
        available_stock:
          type: number
          format: double
          description: |-
            موجودی فعلی کالا — برای کنترل «آیا واقعاً نیاز به خرید است؟»
            (بخش ۱۳.۲ سند فرانت).
          readOnly: true
      required:
      - available_stock
      - id
      - item
      - item_title
      - quantity
      - request
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_title: عنوان نمونه
        quantity: '18.50'
        estimated_unit_price: 2500000
        note: یادداشت داخلی
        available_stock: 18.5
    PurchaseRequestLineRequest:
      type: object
      properties:
        request:
          type: string
          format: uuid
          title: درخواست
        item:
          type: string
          format: uuid
          title: کالا
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        estimated_unit_price:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: قیمت واحد برآوردی
        note:
          type: string
          title: یادداشت
          maxLength: 300
      required:
      - item
      - quantity
      - request
      example:
        request: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        quantity: '18.50'
        estimated_unit_price: 2500000
        note: یادداشت داخلی
    PurchaseRequestRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        cost_center_id:
          type: string
          format: uuid
          nullable: true
          title: مرکز هزینه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        needed_by:
          type: string
          format: date
          nullable: true
          title: نیاز تا تاریخ
        estimated_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ برآوردی
        justification:
          type: string
          title: توجیه
        budget_reserved_amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بودجه رزروشده
        decision_note:
          type: string
          title: توضیح تصمیم
          maxLength: 400
      required:
      - campus
      - title
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        cost_center_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        needed_by: '2026-09-23'
        estimated_amount: 2500000
        justification: متن نمونه
        budget_reserved_amount: 2500000
        decision_note: توضیح نمونه
    PurchaseRequestStatusEnum:
      enum:
      - DRAFT
      - SUBMITTED
      - BUDGET_CHECK
      - APPROVAL
      - APPROVED
      - REJECTED
      - SOURCING
      - ORDERED
      - PARTIALLY_RECEIVED
      - RECEIVED
      - CLOSED
      - CANCELLED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `SUBMITTED` - ارسال‌شده
        * `BUDGET_CHECK` - کنترل بودجه
        * `APPROVAL` - در انتظار تأیید
        * `APPROVED` - تأییدشده
        * `REJECTED` - ردشده
        * `SOURCING` - در حال استعلام
        * `ORDERED` - سفارش‌شده
        * `PARTIALLY_RECEIVED` - دریافت جزئی
        * `RECEIVED` - دریافت کامل
        * `CLOSED` - بسته
        * `CANCELLED` - لغوشده
    PurposeEnum:
      enum:
      - FORMATIVE
      - MIDTERM
      - FINAL
      - PRACTICE
      - MAKEUP
      - PLACEMENT
      type: string
      description: |-
        * `FORMATIVE` - مستمر
        * `MIDTERM` - میان‌ترم
        * `FINAL` - پایانی
        * `PRACTICE` - تمرینی
        * `MAKEUP` - جبرانی
        * `PLACEMENT` - تعیین سطح
    QualitativeLevelEnum:
      enum:
      - EXCELLENT
      - GOOD
      - ACCEPTABLE
      - NEEDS_IMPROVEMENT
      type: string
      description: |-
        * `EXCELLENT` - خیلی خوب
        * `GOOD` - خوب
        * `ACCEPTABLE` - قابل قبول
        * `NEEDS_IMPROVEMENT` - نیازمند تلاش بیشتر
    QualityStatusEnum:
      enum:
      - PENDING
      - ACCEPTED
      - PARTIALLY_ACCEPTED
      - REJECTED
      type: string
      description: |-
        * `PENDING` - در انتظار کنترل کیفیت
        * `ACCEPTED` - پذیرفته‌شده
        * `PARTIALLY_ACCEPTED` - پذیرش جزئی
        * `REJECTED` - مردود
    Question:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        bank:
          type: string
          format: uuid
          title: بانک سؤال
        bank_title:
          type: string
          readOnly: true
          title: عنوان بانک سؤال
        question_type:
          allOf:
          - $ref: '#/components/schemas/QuestionQuestionTypeEnum'
          title: نوع سؤال
        question_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع سؤال
        lifecycle_status:
          allOf:
          - $ref: '#/components/schemas/QuestionLifecycleEnum'
          readOnly: true
          title: وضعیت چرخه عمر
        is_auto_graded:
          type: boolean
          readOnly: true
          title: تصحیح خودکار دارد
        current_version:
          type: string
          format: uuid
          title: نسخه جاری
          readOnly: true
          nullable: true
        current_version_detail:
          allOf:
          - $ref: '#/components/schemas/QuestionVersion'
          readOnly: true
          title: مشخصات نسخه جاری
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - bank
      - bank_title
      - created_at
      - current_version
      - current_version_detail
      - id
      - is_auto_graded
      - lifecycle_status
      - question_type
      - question_type_display
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        bank: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        bank_title: عنوان نمونه
        question_type: SINGLE_CHOICE
        question_type_display: چندگزینه‌ای تک‌پاسخ
        lifecycle_status: DRAFT
        is_auto_graded: true
        current_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        current_version_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_type: متن نمونه
          version_no: 1
          body: متن نمونه
          explanation: متن نمونه
          grading_rubric: متن نمونه
          default_score: '18.50'
          difficulty: VERY_EASY
          difficulty_display: خیلی آسان
          locale: fa-IR
          grade_level: هفتم
          review_status: PENDING
          reviewed_at: '2026-09-23T08:30:00+03:30'
          media: https://example.school/media/sample.pdf
          options:
          - {}
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    QuestionAnalysisRow:
      type: object
      description: تحلیل یک سؤال آزمون (بخش ۴.۴).
      properties:
        examQuestionId:
          type: string
          format: uuid
          title: شناسه سؤال آزمون
        sectionTitle:
          type: string
          title: عنوان بخش
        questionBody:
          type: string
          title: متن سؤال
        questionType:
          type: string
          title: نوع سؤال
        maxScore:
          type: number
          format: double
          title: نمره کل
        responseCount:
          type: integer
          title: تعداد پاسخ‌ها
        correctPercent:
          type: number
          format: double
          nullable: true
          title: درصد پاسخ صحیح
        averageScore:
          type: number
          format: double
          nullable: true
          title: میانگین نمره
        averageTimeSeconds:
          type: integer
          title: میانگین زمان پاسخ (ثانیه)
      required:
      - averageScore
      - averageTimeSeconds
      - correctPercent
      - examQuestionId
      - maxScore
      - questionBody
      - questionType
      - responseCount
      - sectionTitle
      example:
        examQuestionId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        sectionTitle: عنوان نمونه
        questionBody: متن نمونه
        questionType: متن نمونه
        maxScore: 18.5
        responseCount: 12
        correctPercent: 18.5
        averageScore: 18.5
        averageTimeSeconds: 120
    QuestionBank:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        owner_user_id:
          type: string
          format: uuid
          nullable: true
          title: مالک
        title:
          type: string
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
          maxLength: 400
        course:
          type: string
          format: uuid
          title: درس
          nullable: true
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        visibility:
          allOf:
          - $ref: '#/components/schemas/QuestionBankVisibilityEnum'
          title: سطح نمایش
        status:
          type: string
          title: وضعیت
          maxLength: 20
        question_count:
          type: integer
          readOnly: true
          title: تعداد سؤالات
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - course_title
      - created_at
      - id
      - question_count
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        owner_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        visibility: PRIVATE
        status: متن نمونه
        question_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    QuestionBankRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        owner_user_id:
          type: string
          format: uuid
          nullable: true
          title: مالک
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        description:
          type: string
          title: شرح
          maxLength: 400
        course:
          type: string
          format: uuid
          title: درس
          nullable: true
        visibility:
          allOf:
          - $ref: '#/components/schemas/QuestionBankVisibilityEnum'
          title: سطح نمایش
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        owner_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        visibility: PRIVATE
        status: متن نمونه
    QuestionBankVisibilityEnum:
      enum:
      - PRIVATE
      - DEPARTMENT
      - SCHOOL
      - SHARED
      type: string
      description: |-
        * `PRIVATE` - شخصی
        * `DEPARTMENT` - گروه درسی
        * `SCHOOL` - مدرسه
        * `SHARED` - مشترک
    QuestionCreateQuestionTypeEnum:
      enum:
      - SINGLE_CHOICE
      - MULTIPLE_CHOICE
      - TRUE_FALSE
      - SHORT_ANSWER
      - ESSAY
      - NUMERIC
      - MATCHING
      - ORDERING
      - FILE_UPLOAD
      - AUDIO
      type: string
      description: |-
        * `SINGLE_CHOICE` - SINGLE_CHOICE
        * `MULTIPLE_CHOICE` - MULTIPLE_CHOICE
        * `TRUE_FALSE` - TRUE_FALSE
        * `SHORT_ANSWER` - SHORT_ANSWER
        * `ESSAY` - ESSAY
        * `NUMERIC` - NUMERIC
        * `MATCHING` - MATCHING
        * `ORDERING` - ORDERING
        * `FILE_UPLOAD` - FILE_UPLOAD
        * `AUDIO` - AUDIO
    QuestionCreateRequest:
      type: object
      description: |-
        ایجاد سؤال به‌همراه نسخه اول و گزینه‌ها در یک درخواست.

        این Endpoint برای «ویرایشگر سؤال» فرانت (بخش ۹.۲ سند فرانت) طراحی شده است.
      properties:
        bank:
          type: string
          format: uuid
          title: بانک سؤال
        question_type:
          $ref: '#/components/schemas/QuestionCreateQuestionTypeEnum'
          title: نوع سؤال
        body:
          type: string
          minLength: 1
          title: متن
        explanation:
          type: string
          title: پاسخ تشریحی
        grading_rubric:
          type: string
          title: راهنمای تصحیح
        default_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          default: '1.00'
          title: بارم پیش‌فرض
        difficulty:
          type: string
          minLength: 1
          default: MEDIUM
          title: سطح دشواری
        grade_level:
          type: string
          format: uuid
          nullable: true
          title: پایه تحصیلی
        correct_answer:
          title: کلید پاسخ
        options:
          type: array
          items:
            $ref: '#/components/schemas/QuestionOptionRequest'
          title: گزینه‌ها
        tag_ids:
          type: array
          items:
            type: string
            format: uuid
          title: شناسه برچسب‌ها
      required:
      - bank
      - body
      - question_type
      example:
        bank: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_type: SINGLE_CHOICE
        body: متن نمونه
        explanation: متن نمونه
        grading_rubric: متن نمونه
        default_score: '1.00'
        difficulty: MEDIUM
        grade_level: هفتم
        options:
        - question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          option_key: متن نمونه
          body: متن نمونه
          is_correct: true
          credit_percent: '18.50'
          display_order: 1
        tag_ids:
        - 3fa85f64-5717-4562-b3fc-2c963f66afa6
    QuestionLifecycleEnum:
      enum:
      - DRAFT
      - UNDER_REVIEW
      - APPROVED
      - PUBLISHED
      - RETIRED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `UNDER_REVIEW` - در حال بازبینی
        * `APPROVED` - تأییدشده
        * `PUBLISHED` - منتشرشده
        * `RETIRED` - بازنشسته
    QuestionOption:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        question_version:
          type: string
          format: uuid
          title: نسخه سؤال
        option_key:
          type: string
          title: کلید گزینه
          maxLength: 10
        body:
          type: string
          title: متن گزینه
        is_correct:
          type: boolean
          title: پاسخ درست
        credit_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد نمره این گزینه
          description: برای سؤال چندپاسخی با نمره جزئی
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
      required:
      - body
      - id
      - option_key
      - question_version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        option_key: متن نمونه
        body: متن نمونه
        is_correct: true
        credit_percent: '18.50'
        display_order: 1
    QuestionOptionPublic:
      type: object
      description: |-
        نمای گزینه برای دانش‌آموز — بدون افشای کلید پاسخ.

        بخش ۱۵.۲: «Log و Trace نباید متن پاسخ آزمون … را ذخیره کنند.» به‌طریق اولی
        پاسخ صحیح نباید در پاسخ API آزمون در حال اجرا برگردد.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        option_key:
          type: string
          readOnly: true
          title: کلید گزینه
        body:
          type: string
          readOnly: true
          title: متن گزینه
        display_order:
          type: integer
          readOnly: true
          title: ترتیب نمایش
      required:
      - body
      - display_order
      - id
      - option_key
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        option_key: متن نمونه
        body: متن نمونه
        display_order: 1
    QuestionOptionRequest:
      type: object
      properties:
        question_version:
          type: string
          format: uuid
          title: نسخه سؤال
        option_key:
          type: string
          minLength: 1
          title: کلید گزینه
          maxLength: 10
        body:
          type: string
          minLength: 1
          title: متن گزینه
        is_correct:
          type: boolean
          title: پاسخ درست
        credit_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: درصد نمره این گزینه
          description: برای سؤال چندپاسخی با نمره جزئی
        display_order:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب نمایش
      required:
      - body
      - option_key
      - question_version
      example:
        question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        option_key: متن نمونه
        body: متن نمونه
        is_correct: true
        credit_percent: '18.50'
        display_order: 1
    QuestionQuestionTypeEnum:
      enum:
      - SINGLE_CHOICE
      - MULTIPLE_CHOICE
      - TRUE_FALSE
      - SHORT_ANSWER
      - ESSAY
      - NUMERIC
      - MATCHING
      - ORDERING
      - FILE_UPLOAD
      - AUDIO
      type: string
      description: |-
        * `SINGLE_CHOICE` - چندگزینه‌ای تک‌پاسخ
        * `MULTIPLE_CHOICE` - چندگزینه‌ای چندپاسخی
        * `TRUE_FALSE` - درست/نادرست
        * `SHORT_ANSWER` - کوتاه‌پاسخ
        * `ESSAY` - تشریحی
        * `NUMERIC` - عددی
        * `MATCHING` - تطبیقی
        * `ORDERING` - ترتیبی
        * `FILE_UPLOAD` - بارگذاری فایل
        * `AUDIO` - پاسخ صوتی
    QuestionRequest:
      type: object
      properties:
        bank:
          type: string
          format: uuid
          title: بانک سؤال
        question_type:
          allOf:
          - $ref: '#/components/schemas/QuestionQuestionTypeEnum'
          title: نوع سؤال
      required:
      - bank
      - question_type
      example:
        bank: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_type: SINGLE_CHOICE
    QuestionReviewStatusEnum:
      enum:
      - PENDING
      - APPROVED
      - CHANGES_REQUESTED
      - REJECTED
      type: string
      description: |-
        * `PENDING` - در انتظار بازبینی
        * `APPROVED` - تأییدشده
        * `CHANGES_REQUESTED` - نیازمند اصلاح
        * `REJECTED` - ردشده
    QuestionTag:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        tag_type:
          type: string
          title: نوع برچسب
          description: TOPIC | OBJECTIVE | SOURCE | SKILL
          maxLength: 40
        value:
          type: string
          title: مقدار
          maxLength: 150
      required:
      - id
      - school
      - tag_type
      - value
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        tag_type: متن نمونه
        value: متن نمونه
    QuestionTagRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        tag_type:
          type: string
          minLength: 1
          title: نوع برچسب
          description: TOPIC | OBJECTIVE | SOURCE | SKILL
          maxLength: 40
        value:
          type: string
          minLength: 1
          title: مقدار
          maxLength: 150
      required:
      - school
      - tag_type
      - value
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        tag_type: متن نمونه
        value: متن نمونه
    QuestionVersion:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        question:
          type: string
          format: uuid
          title: سؤال
        question_type:
          type: string
          readOnly: true
          title: نوع سؤال
        version_no:
          type: integer
          readOnly: true
          default: 1
          title: شماره نسخه
        body:
          type: string
          title: متن سؤال
        explanation:
          type: string
          title: پاسخ تشریحی
        grading_rubric:
          type: string
          title: راهنمای تصحیح (Rubric)
        default_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم پیش‌فرض
        difficulty:
          allOf:
          - $ref: '#/components/schemas/DifficultyEnum'
          title: دشواری
        difficulty_display:
          type: string
          readOnly: true
          title: برچسب فارسی سطح دشواری
        locale:
          type: string
          title: زبان و منطقه
          maxLength: 10
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        correct_answer:
          title: کلید پاسخ
          description: برای انواع کوتاه‌پاسخ/عددی/تطبیقی
        review_status:
          allOf:
          - $ref: '#/components/schemas/QuestionReviewStatusEnum'
          readOnly: true
          title: وضعیت بازبینی
        reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بازبینی
        media:
          type: string
          format: uri
          nullable: true
          title: رسانه
        options:
          type: array
          items:
            $ref: '#/components/schemas/QuestionOption'
          readOnly: true
          title: گزینه‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - body
      - created_at
      - difficulty_display
      - id
      - options
      - question
      - question_type
      - review_status
      - reviewed_at
      - updated_at
      - version
      - version_no
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        question_type: متن نمونه
        version_no: 1
        body: متن نمونه
        explanation: متن نمونه
        grading_rubric: متن نمونه
        default_score: '18.50'
        difficulty: VERY_EASY
        difficulty_display: خیلی آسان
        locale: fa-IR
        grade_level: هفتم
        review_status: PENDING
        reviewed_at: '2026-09-23T08:30:00+03:30'
        media: https://example.school/media/sample.pdf
        options:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          question_version: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          option_key: متن نمونه
          body: متن نمونه
          is_correct: true
          credit_percent: '18.50'
          display_order: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    QuestionVersionRequest:
      type: object
      properties:
        question:
          type: string
          format: uuid
          title: سؤال
        body:
          type: string
          minLength: 1
          title: متن سؤال
        explanation:
          type: string
          title: پاسخ تشریحی
        grading_rubric:
          type: string
          title: راهنمای تصحیح (Rubric)
        default_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          title: بارم پیش‌فرض
        difficulty:
          allOf:
          - $ref: '#/components/schemas/DifficultyEnum'
          title: دشواری
        locale:
          type: string
          minLength: 1
          title: زبان و منطقه
          maxLength: 10
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        correct_answer:
          title: کلید پاسخ
          description: برای انواع کوتاه‌پاسخ/عددی/تطبیقی
        media:
          type: string
          format: binary
          nullable: true
          title: رسانه
      required:
      - body
      - question
      example:
        question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        body: متن نمونه
        explanation: متن نمونه
        grading_rubric: متن نمونه
        default_score: '18.50'
        difficulty: VERY_EASY
        locale: fa-IR
        grade_level: هفتم
        media: sample.pdf
    ReasonRequest:
      type: object
      description: بدنه عملیاتی که نیازمند ثبت علت است (لغو، ابطال، بازگشایی).
      properties:
        reason:
          type: string
          minLength: 1
          description: علت انجام عملیات؛ در ممیزی ثبت می‌شود.
          maxLength: 500
      required:
      - reason
      example:
        reason: اصلاح اطلاعات پس از بررسی پرونده
    ReceiptStatusEnum:
      enum:
      - PROVISIONAL
      - FINAL
      - CANCELLED
      type: string
      description: |-
        * `PROVISIONAL` - رسید موقت
        * `FINAL` - رسید قطعی
        * `CANCELLED` - لغوشده
    ReceivablesAging:
      type: object
      description: گزارش سنی مطالبات (بخش ۱۴.۳ — داشبورد مالی).
      properties:
        bucket:
          type: string
          description: CURRENT | 1_30 | 31_60 | 61_90 | OVER_90
        invoiceCount:
          type: integer
          title: تعداد صورتحساب
        totalAmount:
          type: integer
          title: مبلغ کل (ریال)
      required:
      - bucket
      - invoiceCount
      - totalAmount
      example:
        bucket: متن نمونه
        invoiceCount: 12
        totalAmount: 45000000
    ReceiveGoodsLineRequest:
      type: object
      properties:
        order_line:
          type: string
          format: uuid
          title: شناسه سطر سفارش
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        lot_no:
          type: string
          default: ''
          title: شماره بچ
        serial_no:
          type: string
          default: ''
          title: شماره سریال
        expiry_date:
          type: string
          format: date
          nullable: true
          title: تاریخ انقضا
        unit_cost:
          type: integer
          nullable: true
          title: بهای واحد (ریال)
      required:
      - order_line
      - quantity
      example:
        order_line: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        quantity: '18.50'
        lot_no: ''
        serial_no: ''
        expiry_date: '2027-03-20'
        unit_cost: 2500000
    ReceiveGoodsRequest:
      type: object
      description: |-
        ثبت رسید کالا.

        مقدار دریافتی هر قلم نباید از باقیمانده سفارش بیشتر باشد. برای کالای
        سریال‌دار/بچ‌دار، فیلدهای مربوطه الزامی‌اند (بخش ۷.۹).
      properties:
        warehouse:
          type: string
          format: uuid
          title: انبار
        received_at:
          type: string
          format: date-time
          title: زمان دریافت
        vendor_invoice_no:
          type: string
          default: ''
          title: شماره فاکتور فروشنده
        vendor_invoice_amount:
          type: integer
          default: 0
          title: مبلغ فاکتور فروشنده (ریال)
        lines:
          type: array
          items:
            $ref: '#/components/schemas/ReceiveGoodsLineRequest'
          title: سطرهای سند
      required:
      - lines
      - warehouse
      example:
        warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        received_at: '2026-09-23T08:30:00+03:30'
        vendor_invoice_no: ''
        vendor_invoice_amount: 0
        lines:
        - order_line: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          quantity: '18.50'
          lot_no: ''
          serial_no: ''
          expiry_date: '2027-03-20'
          unit_cost: 2500000
    ReconciliationStatusEnum:
      enum:
      - OPEN
      - MATCHED
      - DISCREPANCY
      - CLOSED
      type: string
      description: |-
        * `OPEN` - باز
        * `MATCHED` - تطبیق‌شده
        * `DISCREPANCY` - دارای مغایرت
        * `CLOSED` - بسته
    RecordStatusEnum:
      enum:
      - DRAFT
      - ACTIVE
      - INACTIVE
      - ARCHIVED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `ACTIVE` - فعال
        * `INACTIVE` - غیرفعال
        * `ARCHIVED` - بایگانی‌شده
    RecurrenceEnum:
      enum:
      - ONE_TIME
      - MONTHLY
      - TERM
      - ANNUAL
      type: string
      description: |-
        * `ONE_TIME` - یک‌بار
        * `MONTHLY` - ماهانه
        * `TERM` - هر ترم
        * `ANNUAL` - سالانه
    ReferralSourceEnum:
      enum:
      - TEACHER
      - GUARDIAN
      - SELF
      - PRINCIPAL
      - HEALTH
      - DISCIPLINE
      type: string
      description: |-
        * `TEACHER` - معلم
        * `GUARDIAN` - ولی
        * `SELF` - خود دانش‌آموز
        * `PRINCIPAL` - مدیر
        * `HEALTH` - مربی بهداشت
        * `DISCIPLINE` - مسئول انضباط
    Refund:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        payment:
          type: string
          format: uuid
          title: پرداخت
        payment_no:
          type: string
          readOnly: true
          title: شماره پرداخت
        refund_no:
          type: string
          readOnly: true
          title: شماره استرداد
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        reason:
          type: string
          title: علت
          maxLength: 400
        requested_by_id:
          type: string
          format: uuid
          nullable: true
          title: درخواست‌دهنده
        approval_status:
          allOf:
          - $ref: '#/components/schemas/PendingApprovalStateEnum'
          readOnly: true
          title: وضعیت تأیید
        approved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید
        status:
          allOf:
          - $ref: '#/components/schemas/RefundStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        completed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تکمیل
        gateway_reference:
          type: string
          title: ارجاع درگاه پرداخت
          maxLength: 200
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - amount
      - approval_status
      - approved_at
      - completed_at
      - created_at
      - id
      - payment
      - payment_no
      - reason
      - refund_no
      - status
      - status_display
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        payment_no: PAY-1405-000412
        refund_no: SAMPLE-001
        amount: 12500000
        currency: IRR
        reason: اصلاح اطلاعات پس از بررسی پرونده
        requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        approval_status: PENDING
        approved_at: '2026-09-23T08:30:00+03:30'
        status: REQUESTED
        status_display: درخواست‌شده
        completed_at: '2026-09-23T08:30:00+03:30'
        gateway_reference: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    RefundRequest:
      type: object
      properties:
        payment:
          type: string
          format: uuid
          title: پرداخت
        amount:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: مبلغ
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        reason:
          type: string
          minLength: 1
          title: علت
          maxLength: 400
        requested_by_id:
          type: string
          format: uuid
          nullable: true
          title: درخواست‌دهنده
        gateway_reference:
          type: string
          title: ارجاع درگاه پرداخت
          maxLength: 200
      required:
      - amount
      - payment
      - reason
      example:
        payment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        amount: 12500000
        currency: IRR
        reason: اصلاح اطلاعات پس از بررسی پرونده
        requested_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        gateway_reference: متن نمونه
    RefundStatusEnum:
      enum:
      - REQUESTED
      - APPROVED
      - PROCESSING
      - COMPLETED
      - REJECTED
      - FAILED
      type: string
      description: |-
        * `REQUESTED` - درخواست‌شده
        * `APPROVED` - تأییدشده
        * `PROCESSING` - در حال پردازش
        * `COMPLETED` - انجام‌شده
        * `REJECTED` - ردشده
        * `FAILED` - ناموفق
    RegistrationStatusEnum:
      enum:
      - REGISTERED
      - ABSENT
      - EXCUSED
      - DISQUALIFIED
      type: string
      description: |-
        * `REGISTERED` - ثبت‌نام‌شده
        * `ABSENT` - غایب
        * `EXCUSED` - غیبت موجه
        * `DISQUALIFIED` - محروم
    RelationshipTypeEnum:
      enum:
      - FATHER
      - MOTHER
      - STEP_PARENT
      - GRANDPARENT
      - SIBLING
      - LEGAL_GUARDIAN
      - OTHER
      type: string
      description: |-
        * `FATHER` - پدر
        * `MOTHER` - مادر
        * `STEP_PARENT` - ناپدری/نامادری
        * `GRANDPARENT` - پدربزرگ/مادربزرگ
        * `SIBLING` - خواهر/برادر
        * `LEGAL_GUARDIAN` - قیم قانونی
        * `OTHER` - سایر
    ReportCard:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        term:
          type: string
          format: uuid
          title: ترم
        term_title:
          type: string
          readOnly: true
          title: عنوان ترم
        version_no:
          type: integer
          readOnly: true
          default: 1
          title: شماره نسخه
        generated_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تولید
        published_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان انتشار
        status:
          allOf:
          - $ref: '#/components/schemas/ReportCardStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        average_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: معدل
        rank_in_class:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: رتبه در کلاس
          description: فقط در صورت مجاز بودن سیاست رتبه‌بندی نمایش داده می‌شود
        attendance_summary:
          title: خلاصه حضور
        principal_comment:
          type: string
          title: نظر مدیر
        verification_code:
          type: string
          readOnly: true
          title: کد اعتبارسنجی
        items:
          type: array
          items:
            $ref: '#/components/schemas/ReportCardItem'
          readOnly: true
          title: اقلام
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - average_score
      - created_at
      - enrollment
      - generated_at
      - id
      - items
      - published_at
      - status
      - status_display
      - student_name
      - student_no
      - term
      - term_title
      - updated_at
      - verification_code
      - version
      - version_no
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term_title: نیم‌سال اول
        version_no: 1
        generated_at: '2026-09-23T08:30:00+03:30'
        published_at: '2026-09-23T08:30:00+03:30'
        status: DRAFT
        status_display: پیش‌نویس
        average_score: '18.50'
        rank_in_class: 3
        principal_comment: توضیح نمونه
        verification_code: SAMPLE-001
        items:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          report_card: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_result: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          displayed_score: '18.50'
          displayed_level: متن نمونه
          credit: '0'
          teacher_comment: توضیح نمونه
          display_order: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ReportCardItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        report_card:
          type: string
          format: uuid
          title: کارنامه
          readOnly: true
        course_result:
          type: string
          format: uuid
          title: نتیجه درس
          readOnly: true
        course_title:
          type: string
          readOnly: true
          title: عنوان درس (Snapshot)
        displayed_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره نمایشی
        displayed_level:
          type: string
          readOnly: true
          title: سطح نمایشی
        credit:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          readOnly: true
          title: بستانکار
        teacher_comment:
          type: string
          readOnly: true
          title: نظر معلم
        display_order:
          type: integer
          readOnly: true
          title: ترتیب نمایش
      required:
      - course_result
      - course_title
      - credit
      - display_order
      - displayed_level
      - displayed_score
      - id
      - report_card
      - teacher_comment
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        report_card: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_result: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        displayed_score: '18.50'
        displayed_level: متن نمونه
        credit: '0'
        teacher_comment: توضیح نمونه
        display_order: 1
    ReportCardRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        term:
          type: string
          format: uuid
          title: ترم
        rank_in_class:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: رتبه در کلاس
          description: فقط در صورت مجاز بودن سیاست رتبه‌بندی نمایش داده می‌شود
        attendance_summary:
          title: خلاصه حضور
        principal_comment:
          type: string
          title: نظر مدیر
      required:
      - enrollment
      - term
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        term: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        rank_in_class: 3
        principal_comment: توضیح نمونه
    ReportCardStatusEnum:
      enum:
      - DRAFT
      - GENERATED
      - UNDER_REVIEW
      - PUBLISHED
      - SUPERSEDED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `GENERATED` - تولیدشده
        * `UNDER_REVIEW` - در حال بازبینی
        * `PUBLISHED` - منتشرشده
        * `SUPERSEDED` - جایگزین‌شده با نسخه جدید
    ResolveAppealRequest:
      type: object
      properties:
        accepted:
          type: boolean
          description: آیا اعتراض پذیرفته شد؟
        resolution:
          type: string
          minLength: 1
          maxLength: 2000
          title: نتیجه رسیدگی
      required:
      - accepted
      - resolution
      example:
        accepted: true
        resolution: متن نمونه
    ResourceTypeEnum:
      enum:
      - FILE
      - LINK
      - VIDEO
      - TEXT
      - LIVE_SESSION
      type: string
      description: |-
        * `FILE` - فایل
        * `LINK` - پیوند
        * `VIDEO` - ویدئو
        * `TEXT` - متن
        * `LIVE_SESSION` - کلاس زنده
    ResponsibilityEnum:
      enum:
      - PRIMARY
      - ASSISTANT
      - SUBSTITUTE
      - CO_TEACHER
      type: string
      description: |-
        * `PRIMARY` - مدرس اصلی
        * `ASSISTANT` - دستیار
        * `SUBSTITUTE` - جانشین
        * `CO_TEACHER` - هم‌تدریس
    ResultEnum:
      enum:
      - SUCCESS
      - FAILED
      - RETRYING
      - REJECTED
      type: string
      description: |-
        * `SUCCESS` - موفق
        * `FAILED` - ناموفق
        * `RETRYING` - در حال تلاش مجدد
        * `REJECTED` - رد شده توسط ارائه‌دهنده
    ReturnAssetRequest:
      type: object
      properties:
        condition_on_return:
          type: string
          default: ''
          title: وضعیت هنگام بازگشت
        note:
          type: string
          default: ''
          title: یادداشت
      example:
        condition_on_return: ''
        note: ''
    ReverseJournalRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 400
          title: علت
      required:
      - reason
      example:
        reason: اصلاح اطلاعات پس از بررسی پرونده
    RidershipEvent:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        route_run:
          type: string
          format: uuid
          title: سرویس مسیر
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        stop:
          type: string
          format: uuid
          title: ایستگاه
          nullable: true
        event_type:
          allOf:
          - $ref: '#/components/schemas/RidershipEventEventTypeEnum'
          title: نوع رویداد
        event_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع رخداد
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        source:
          allOf:
          - $ref: '#/components/schemas/RidershipEventSourceEnum'
          title: منبع
        guardian_notified_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان اطلاع به ولی
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - event_type
      - event_type_display
      - guardian_notified_at
      - id
      - occurred_at
      - route_run
      - student
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: BOARDED
        event_type_display: سوار شد
        occurred_at: '2026-09-23T08:30:00+03:30'
        source: DEVICE
        guardian_notified_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    RidershipEventEventTypeEnum:
      enum:
      - BOARDED
      - ALIGHTED
      - NO_SHOW
      type: string
      description: |-
        * `BOARDED` - سوار شد
        * `ALIGHTED` - پیاده شد
        * `NO_SHOW` - حاضر نشد
    RidershipEventRequest:
      type: object
      properties:
        route_run:
          type: string
          format: uuid
          title: سرویس مسیر
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        stop:
          type: string
          format: uuid
          title: ایستگاه
          nullable: true
        event_type:
          allOf:
          - $ref: '#/components/schemas/RidershipEventEventTypeEnum'
          title: نوع رویداد
        occurred_at:
          type: string
          format: date-time
          title: زمان رخداد
        source:
          allOf:
          - $ref: '#/components/schemas/RidershipEventSourceEnum'
          title: منبع
      required:
      - event_type
      - occurred_at
      - route_run
      - student
      example:
        route_run: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        event_type: BOARDED
        occurred_at: '2026-09-23T08:30:00+03:30'
        source: DEVICE
    RidershipEventSourceEnum:
      enum:
      - DEVICE
      - DRIVER
      - SUPERVISOR
      - MANUAL
      type: string
      description: |-
        * `DEVICE` - دستگاه
        * `DRIVER` - ثبت راننده
        * `SUPERVISOR` - ثبت مسئول
        * `MANUAL` - دستی
    Role:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد نقش
          maxLength: 60
        title:
          type: string
          title: عنوان نقش
          maxLength: 150
        description:
          type: string
          title: شرح
          maxLength: 400
        is_system:
          type: boolean
          readOnly: true
          title: نقش سیستمی (غیرقابل حذف)
        requires_mfa:
          type: boolean
          title: نیازمند احراز دومرحله‌ای
        allowed_scope_types:
          title: انواع Scope مجاز
          description: مثلاً ["SCHOOL", "CAMPUS"]
        permission_codes:
          type: array
          items:
            type: string
            title: کد مجوز
          description: فهرست کدهای مجوز، مثلاً ["student.read", "student.create"]
        permission_count:
          type: integer
          readOnly: true
          title: تعداد مجوز
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - is_system
      - permission_count
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        is_system: true
        requires_mfa: true
        permission_codes:
        - متن نمونه
        permission_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    RoleAssignmentStatusEnum:
      enum:
      - ACTIVE
      - EXPIRED
      - REVOKED
      type: string
      description: |-
        * `ACTIVE` - فعال
        * `EXPIRED` - منقضی
        * `REVOKED` - لغوشده
    RoleRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد نقش
          maxLength: 60
        title:
          type: string
          minLength: 1
          title: عنوان نقش
          maxLength: 150
        description:
          type: string
          title: شرح
          maxLength: 400
        requires_mfa:
          type: boolean
          title: نیازمند احراز دومرحله‌ای
        allowed_scope_types:
          title: انواع Scope مجاز
          description: مثلاً ["SCHOOL", "CAMPUS"]
        permission_codes:
          type: array
          items:
            type: string
            minLength: 1
            title: کد مجوز
          description: فهرست کدهای مجوز، مثلاً ["student.read", "student.create"]
      required:
      - code
      - title
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        requires_mfa: true
        permission_codes:
        - متن نمونه
    Room:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          title: کد اتاق
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 100
        room_type:
          allOf:
          - $ref: '#/components/schemas/RoomTypeEnum'
          title: نوع اتاق
        room_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع فضا
        building:
          type: string
          title: ساختمان
          maxLength: 60
        floor:
          type: string
          title: طبقه
          maxLength: 20
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت فیزیکی
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus
      - code
      - created_at
      - id
      - room_type_display
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        room_type: CLASSROOM
        room_type_display: کلاس درس
        building: متن نمونه
        floor: متن نمونه
        capacity: 3
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    RoomRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          minLength: 1
          title: کد اتاق
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 100
        room_type:
          allOf:
          - $ref: '#/components/schemas/RoomTypeEnum'
          title: نوع اتاق
        building:
          type: string
          title: ساختمان
          maxLength: 60
        floor:
          type: string
          title: طبقه
          maxLength: 20
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت فیزیکی
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      required:
      - campus
      - code
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        room_type: CLASSROOM
        building: متن نمونه
        floor: متن نمونه
        capacity: 3
        status: DRAFT
    RoomTypeEnum:
      enum:
      - CLASSROOM
      - LAB
      - WORKSHOP
      - GYM
      - LIBRARY
      - EXAM_HALL
      - OFFICE
      - OTHER
      type: string
      description: |-
        * `CLASSROOM` - کلاس درس
        * `LAB` - آزمایشگاه
        * `WORKSHOP` - کارگاه
        * `GYM` - سالن ورزش
        * `LIBRARY` - کتابخانه
        * `EXAM_HALL` - حوزه آزمون
        * `OFFICE` - اداری
        * `OTHER` - سایر
    RouteDirectionEnum:
      enum:
      - MORNING
      - AFTERNOON
      - BOTH
      type: string
      description: |-
        * `MORNING` - رفت (صبح)
        * `AFTERNOON` - برگشت (بعدازظهر)
        * `BOTH` - رفت و برگشت
    RouteManifest:
      type: object
      description: فهرست مسافران یک اجرای مسیر (بخش ۱۴.۵ سند فرانت).
      properties:
        routeRunId:
          type: string
          format: uuid
          title: شناسه سرویس
        routeCode:
          type: string
          title: کد مسیر
        runDate:
          type: string
          format: date
          title: تاریخ سرویس
        direction:
          type: string
          title: جهت
        vehiclePlate:
          type: string
          title: پلاک خودرو
        capacity:
          type: integer
          title: ظرفیت
        riderCount:
          type: integer
          title: تعداد مسافران
        rows:
          type: array
          items:
            $ref: '#/components/schemas/RouteManifestRow'
          title: سطرها
      required:
      - capacity
      - direction
      - riderCount
      - routeCode
      - routeRunId
      - rows
      - runDate
      - vehiclePlate
      example:
        routeRunId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        routeCode: R-03
        runDate: '2026-09-23'
        direction: متن نمونه
        vehiclePlate: ۱۲ الف ۳۴۵ ایران ۱۰
        capacity: 3
        riderCount: 12
        rows:
        - studentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          studentNo: '14030127'
          studentName: سارا محمدی
          pickupStop: متن نمونه
          dropoffStop: متن نمونه
          lastEvent: متن نمونه
          lastEventAt: '2026-09-23T08:30:00+03:30'
    RouteManifestRow:
      type: object
      properties:
        studentId:
          type: string
          format: uuid
          title: شناسه دانش‌آموز
        studentNo:
          type: string
          title: شماره دانش‌آموزی
        studentName:
          type: string
          title: نام دانش‌آموز
        pickupStop:
          type: string
          nullable: true
          title: ایستگاه سوارشدن
        dropoffStop:
          type: string
          nullable: true
          title: ایستگاه پیاده‌شدن
        lastEvent:
          type: string
          nullable: true
          title: آخرین رخداد
        lastEventAt:
          type: string
          format: date-time
          nullable: true
          title: زمان آخرین رخداد
      required:
      - dropoffStop
      - lastEvent
      - lastEventAt
      - pickupStop
      - studentId
      - studentName
      - studentNo
      example:
        studentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        studentNo: '14030127'
        studentName: سارا محمدی
        pickupStop: متن نمونه
        dropoffStop: متن نمونه
        lastEvent: متن نمونه
        lastEventAt: '2026-09-23T08:30:00+03:30'
    RouteRun:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        route:
          type: string
          format: uuid
          title: مسیر
        route_code:
          type: string
          readOnly: true
          title: کد مسیر
        vehicle:
          type: string
          format: uuid
          title: خودرو
        vehicle_plate:
          type: string
          readOnly: true
          title: پلاک خودرو
        driver_employee_id:
          type: string
          format: uuid
          nullable: true
          title: راننده
        supervisor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: سرپرست
        run_date:
          type: string
          format: date
          title: تاریخ سرویس
        direction:
          allOf:
          - $ref: '#/components/schemas/RouteDirectionEnum'
          title: جهت
        departed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان حرکت
        completed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان تکمیل
        status:
          allOf:
          - $ref: '#/components/schemas/RouteRunStatusEnum'
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        event_count:
          type: integer
          readOnly: true
          title: تعداد رخدادها
        note:
          type: string
          title: یادداشت
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - direction
      - event_count
      - id
      - route
      - route_code
      - run_date
      - status_display
      - updated_at
      - vehicle
      - vehicle_plate
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route_code: R-03
        vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vehicle_plate: ۱۲ الف ۳۴۵ ایران ۱۰
        driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        supervisor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        run_date: '2026-09-23'
        direction: MORNING
        departed_at: '2026-09-23T08:30:00+03:30'
        completed_at: '2026-09-23T08:30:00+03:30'
        status: PLANNED
        status_display: برنامه‌ریزی‌شده
        event_count: 12
        note: یادداشت داخلی
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    RouteRunRequest:
      type: object
      properties:
        route:
          type: string
          format: uuid
          title: مسیر
        vehicle:
          type: string
          format: uuid
          title: خودرو
        driver_employee_id:
          type: string
          format: uuid
          nullable: true
          title: راننده
        supervisor_employee_id:
          type: string
          format: uuid
          nullable: true
          title: سرپرست
        run_date:
          type: string
          format: date
          title: تاریخ سرویس
        direction:
          allOf:
          - $ref: '#/components/schemas/RouteDirectionEnum'
          title: جهت
        departed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان حرکت
        completed_at:
          type: string
          format: date-time
          nullable: true
          title: زمان تکمیل
        status:
          allOf:
          - $ref: '#/components/schemas/RouteRunStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 300
      required:
      - direction
      - route
      - run_date
      - vehicle
      example:
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        supervisor_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        run_date: '2026-09-23'
        direction: MORNING
        departed_at: '2026-09-23T08:30:00+03:30'
        completed_at: '2026-09-23T08:30:00+03:30'
        status: PLANNED
        note: یادداشت داخلی
    RouteRunStatusEnum:
      enum:
      - PLANNED
      - IN_PROGRESS
      - COMPLETED
      - CANCELLED
      type: string
      description: |-
        * `PLANNED` - برنامه‌ریزی‌شده
        * `IN_PROGRESS` - در حال اجرا
        * `COMPLETED` - پایان‌یافته
        * `CANCELLED` - لغوشده
    RouteStop:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        route:
          type: string
          format: uuid
          title: مسیر
        title:
          type: string
          title: عنوان
          maxLength: 200
        address_line:
          type: string
          title: سطر نشانی
          maxLength: 300
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: عرض جغرافیایی
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: طول جغرافیایی
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره ترتیب
        scheduled_time:
          type: string
          format: time
          nullable: true
          title: ساعت برنامه
      required:
      - id
      - route
      - sequence_no
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        latitude: '35.6892'
        longitude: '51.389'
        sequence_no: 3
        scheduled_time: 08:30:00
    RouteStopRequest:
      type: object
      properties:
        route:
          type: string
          format: uuid
          title: مسیر
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        address_line:
          type: string
          title: سطر نشانی
          maxLength: 300
        latitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: عرض جغرافیایی
        longitude:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,6})?$
          nullable: true
          title: طول جغرافیایی
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره ترتیب
        scheduled_time:
          type: string
          format: time
          nullable: true
          title: ساعت برنامه
      required:
      - route
      - sequence_no
      - title
      example:
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        latitude: '35.6892'
        longitude: '51.389'
        sequence_no: 3
        scheduled_time: 08:30:00
    SaveAnswerRequest:
      type: object
      description: |-
        ذخیره خودکار پاسخ.

        قالب `responsePayload` بر اساس نوع سؤال:
        - چندگزینه‌ای: `{"selectedKeys": ["B"]}`
        - چندپاسخی: `{"selectedKeys": ["A", "C"]}`
        - عددی: `{"value": 3.14}`
        - کوتاه‌پاسخ/تشریحی: `{"text": "..."}`
        - تطبیقی: `{"pairs": {"1": "ب", "2": "الف"}}`
        - ترتیبی: `{"order": ["c", "a", "b"]}`
      properties:
        exam_question:
          type: string
          format: uuid
          title: سؤال آزمون
        response_payload:
          title: بار پاسخ دانش‌آموز
        time_spent_seconds:
          type: integer
          default: 0
          title: زمان صرف‌شده (ثانیه)
      required:
      - exam_question
      - response_payload
      example:
        exam_question: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        time_spent_seconds: 0
    ScanStatusEnum:
      enum:
      - PENDING
      - CLEAN
      - INFECTED
      - FAILED
      type: string
      description: |-
        * `PENDING` - در انتظار اسکن
        * `CLEAN` - سالم
        * `INFECTED` - آلوده
        * `FAILED` - اسکن ناموفق
    ScheduleConflict:
      type: object
      description: خروجی بررسی تداخل برنامه.
      properties:
        type:
          type: string
          description: ROOM_CONFLICT | TEACHER_CONFLICT | CLASS_CONFLICT
        message:
          type: string
          title: پیام
        conflictingEntryId:
          type: string
          format: uuid
          title: شناسه ردیف متعارض
      required:
      - conflictingEntryId
      - message
      - type
      example:
        type: متن نمونه
        message: عملیات با موفقیت انجام شد.
        conflictingEntryId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    ScheduleEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        room_code:
          type: string
          readOnly: true
          title: کد اتاق
        teacher_profile_id:
          type: string
          format: uuid
          nullable: true
          title: معلم
        weekday:
          allOf:
          - $ref: '#/components/schemas/WeekdayEnum'
          title: روز هفته
          minimum: 0
          maximum: 9223372036854775807
        weekday_display:
          type: string
          readOnly: true
          title: برچسب فارسی روز هفته
        starts_at:
          type: string
          format: time
          title: ساعت شروع
        ends_at:
          type: string
          format: time
          title: ساعت پایان
        period_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: زنگ
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/ScheduleStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - class_group_code
      - course_offering
      - course_title
      - created_at
      - effective_from
      - ends_at
      - id
      - room_code
      - starts_at
      - updated_at
      - version
      - weekday
      - weekday_display
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        class_group_code: G7-A
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room_code: R-204
        teacher_profile_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekday: 0
        weekday_display: شنبه
        starts_at: 08:30:00
        ends_at: 08:30:00
        period_no: 3
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    ScheduleEntryRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        teacher_profile_id:
          type: string
          format: uuid
          nullable: true
          title: معلم
        weekday:
          allOf:
          - $ref: '#/components/schemas/WeekdayEnum'
          title: روز هفته
          minimum: 0
          maximum: 9223372036854775807
        starts_at:
          type: string
          format: time
          title: ساعت شروع
        ends_at:
          type: string
          format: time
          title: ساعت پایان
        period_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          nullable: true
          title: زنگ
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/ScheduleStatusEnum'
          title: وضعیت
      required:
      - course_offering
      - effective_from
      - ends_at
      - starts_at
      - weekday
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        weekday: 0
        starts_at: 08:30:00
        ends_at: 08:30:00
        period_no: 3
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        status: DRAFT
    ScheduleStatusEnum:
      enum:
      - DRAFT
      - PUBLISHED
      - SUPERSEDED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `PUBLISHED` - منتشرشده
        * `SUPERSEDED` - جایگزین‌شده
    School:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد مدرسه
          maxLength: 30
        name:
          type: string
          title: نام مدرسه
          maxLength: 200
        school_type:
          allOf:
          - $ref: '#/components/schemas/SchoolTypeEnum'
          title: نوع مدرسه
        school_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع مدرسه
        gender_policy:
          type: string
          title: سیاست جنسیتی
          description: مثلاً MALE / FEMALE / MIXED
          maxLength: 15
        registration_no:
          type: string
          title: شماره ثبت / مجوز
          maxLength: 50
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        timezone:
          type: string
          title: منطقه زمانی
          maxLength: 64
        logo:
          type: string
          format: uri
          nullable: true
          title: نشان
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
        campus_count:
          type: integer
          readOnly: true
          title: تعداد شعبه
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus_count
      - code
      - created_at
      - id
      - name
      - school_type
      - school_type_display
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        name: نام نمونه
        school_type: PRESCHOOL
        school_type_display: پیش‌دبستان
        gender_policy: متن نمونه
        registration_no: SAMPLE-001
        currency: IRR
        timezone: Asia/Tehran
        logo: https://example.school/media/logo.png
        status: DRAFT
        campus_count: 12
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    SchoolRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد مدرسه
          maxLength: 30
        name:
          type: string
          minLength: 1
          title: نام مدرسه
          maxLength: 200
        school_type:
          allOf:
          - $ref: '#/components/schemas/SchoolTypeEnum'
          title: نوع مدرسه
        gender_policy:
          type: string
          title: سیاست جنسیتی
          description: مثلاً MALE / FEMALE / MIXED
          maxLength: 15
        registration_no:
          type: string
          title: شماره ثبت / مجوز
          maxLength: 50
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        timezone:
          type: string
          minLength: 1
          title: منطقه زمانی
          maxLength: 64
        logo:
          type: string
          format: binary
          nullable: true
          title: نشان
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      required:
      - code
      - name
      - school_type
      example:
        code: SAMPLE-001
        name: نام نمونه
        school_type: PRESCHOOL
        gender_policy: متن نمونه
        registration_no: SAMPLE-001
        currency: IRR
        timezone: Asia/Tehran
        logo: https://example.school/media/logo.png
        status: DRAFT
    SchoolTypeEnum:
      enum:
      - PRESCHOOL
      - PRIMARY
      - LOWER_SECONDARY
      - UPPER_SECONDARY
      - COMBINED
      type: string
      description: |-
        * `PRESCHOOL` - پیش‌دبستان
        * `PRIMARY` - دبستان
        * `LOWER_SECONDARY` - متوسطه اول
        * `UPPER_SECONDARY` - متوسطه دوم
        * `COMBINED` - مجتمع چندمقطعی
    ScopeContext:
      type: object
      description: یک محیط کاری مجاز برای کاربر (بخش ۵.۲ سند فرانت).
      properties:
        roleCode:
          type: string
          title: کد نقش
        roleTitle:
          type: string
          title: عنوان نقش
        scopeType:
          type: string
          title: نوع دامنه دسترسی
        scopeId:
          type: string
          format: uuid
          nullable: true
          title: شناسه دامنه دسترسی
      required:
      - roleCode
      - roleTitle
      - scopeId
      - scopeType
      example:
        roleCode: SAMPLE-001
        roleTitle: عنوان نمونه
        scopeType: متن نمونه
        scopeId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    ScopeTypeEnum:
      enum:
      - TENANT
      - SCHOOL
      - CAMPUS
      - ACADEMIC_YEAR
      - CLASS_GROUP
      - COURSE_OFFERING
      - SELF
      type: string
      description: |-
        * `TENANT` - کل سازمان
        * `SCHOOL` - مدرسه
        * `CAMPUS` - شعبه
        * `ACADEMIC_YEAR` - سال تحصیلی
        * `CLASS_GROUP` - کلاس
        * `COURSE_OFFERING` - ارائه درس
        * `SELF` - فقط رکوردهای خود
    ScoreChange:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student_score:
          type: string
          format: uuid
          title: نمره دانش‌آموز
          readOnly: true
        old_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره پیشین
        new_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره جدید
        old_status:
          type: string
          readOnly: true
          title: وضعیت پیشین
        new_status:
          type: string
          readOnly: true
          title: وضعیت جدید
        reason:
          type: string
          readOnly: true
          title: علت
        changed_by_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: تغییردهنده
        changed_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان تغییر
        approval_status:
          allOf:
          - $ref: '#/components/schemas/ScoreChangeApprovalStatusEnum'
          readOnly: true
          title: وضعیت تأیید
        approved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید
      required:
      - approval_status
      - approved_at
      - changed_at
      - changed_by_id
      - id
      - new_score
      - new_status
      - old_score
      - old_status
      - reason
      - student_score
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_score: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        old_score: '18.50'
        new_score: '18.50'
        old_status: متن نمونه
        new_status: متن نمونه
        reason: اصلاح اطلاعات پس از بررسی پرونده
        changed_by_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        changed_at: '2026-09-23T08:30:00+03:30'
        approval_status: NOT_REQUIRED
        approved_at: '2026-09-23T08:30:00+03:30'
    ScoreChangeApprovalStatusEnum:
      enum:
      - NOT_REQUIRED
      - PENDING
      - APPROVED
      - REJECTED
      type: string
      description: |-
        * `NOT_REQUIRED` - بدون نیاز به تأیید
        * `PENDING` - در انتظار تأیید
        * `APPROVED` - تأییدشده
        * `REJECTED` - ردشده
    ScoreStatusEnum:
      enum:
      - NOT_RECORDED
      - RECORDED
      - ABSENT
      - EXEMPT
      - EXCUSED
      - PENDING_REVIEW
      type: string
      description: |-
        * `NOT_RECORDED` - ثبت‌نشده
        * `RECORDED` - ثبت‌شده
        * `ABSENT` - غایب
        * `EXEMPT` - معاف
        * `EXCUSED` - غیبت موجه
        * `PENDING_REVIEW` - در انتظار بازبینی
    SessionStatusEnum:
      enum:
      - PLANNED
      - IN_PROGRESS
      - HELD
      - CANCELLED
      - POSTPONED
      type: string
      description: |-
        * `PLANNED` - برنامه‌ریزی‌شده
        * `IN_PROGRESS` - در حال برگزاری
        * `HELD` - برگزارشده
        * `CANCELLED` - لغوشده
        * `POSTPONED` - به تعویق افتاده
    SessionTeacher:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        session:
          type: string
          format: uuid
          title: جلسه
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        teacher_name:
          type: string
          readOnly: true
          title: نام معلم
        duty:
          allOf:
          - $ref: '#/components/schemas/DutyEnum'
          title: وظیفه
      required:
      - id
      - session
      - teacher_name
      - teacher_profile
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_name: مریم رضایی
        duty: TEACHER
    SessionTeacherRequest:
      type: object
      properties:
        session:
          type: string
          format: uuid
          title: جلسه
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        duty:
          allOf:
          - $ref: '#/components/schemas/DutyEnum'
          title: وظیفه
      required:
      - session
      - teacher_profile
      example:
        session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        duty: TEACHER
    SessionTypeEnum:
      enum:
      - REGULAR
      - MAKEUP
      - EXTRA
      - SUBSTITUTE
      - REMOTE
      - EXAM
      type: string
      description: |-
        * `REGULAR` - عادی
        * `MAKEUP` - جبرانی
        * `EXTRA` - فوق‌العاده
        * `SUBSTITUTE` - با معلم جانشین
        * `REMOTE` - مجازی
        * `EXAM` - جلسه آزمون
    StartApprovalRequest:
      type: object
      properties:
        subject_type:
          type: string
          minLength: 1
          description: مثلاً finance.Refund
          maxLength: 80
        subject_id:
          type: string
          format: uuid
          title: شناسه موضوع
        workflow_code:
          type: string
          minLength: 1
          maxLength: 60
          title: کد گردش کار
        subject_label:
          type: string
          maxLength: 250
          title: برچسب موضوع
      required:
      - subject_id
      - subject_type
      - workflow_code
      example:
        subject_type: متن نمونه
        subject_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        workflow_code: SAMPLE-001
        subject_label: متن نمونه
    StartAttemptRequest:
      type: object
      description: بدنه شروع تلاش.
      properties:
        registration:
          type: string
          format: uuid
          description: شناسه ثبت‌نام آزمون
      required:
      - registration
      example:
        registration: 3fa85f64-5717-4562-b3fc-2c963f66afa6
    StockBalance:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        warehouse:
          type: string
          format: uuid
          title: انبار
          readOnly: true
        warehouse_code:
          type: string
          readOnly: true
          title: کد انبار
        item:
          type: string
          format: uuid
          title: کالا
          readOnly: true
        item_sku:
          type: string
          readOnly: true
          title: کد کالا (SKU)
        item_title:
          type: string
          readOnly: true
          title: عنوان قلم
        lot_no:
          type: string
          readOnly: true
          title: شماره بچ
        expiry_date:
          type: string
          format: date
          readOnly: true
          nullable: true
          title: تاریخ انقضا
        on_hand_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          readOnly: true
          title: موجودی
        reserved_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          readOnly: true
          title: رزروشده
        available_qty:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          readOnly: true
          title: موجودی قابل‌دسترس
        average_cost:
          type: integer
          readOnly: true
          title: بهای تمام‌شده میانگین
        below_reorder_point:
          type: boolean
          readOnly: true
          title: زیر نقطه سفارش
      required:
      - available_qty
      - average_cost
      - below_reorder_point
      - expiry_date
      - id
      - item
      - item_sku
      - item_title
      - lot_no
      - on_hand_qty
      - reserved_qty
      - warehouse
      - warehouse_code
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        warehouse_code: WH-01
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_sku: STA-A4-80
        item_title: عنوان نمونه
        lot_no: SAMPLE-001
        expiry_date: '2027-03-20'
        on_hand_qty: '18.50'
        reserved_qty: '18.50'
        available_qty: '18.50'
        average_cost: 2500000
        below_reorder_point: true
    StockDocument:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        warehouse:
          type: string
          format: uuid
          title: انبار
        warehouse_code:
          type: string
          readOnly: true
          title: کد انبار
        document_no:
          type: string
          readOnly: true
          title: شماره سند
        document_type:
          allOf:
          - $ref: '#/components/schemas/DocumentTypeEnum'
          title: نوع سند
        document_type_display:
          type: string
          readOnly: true
          title: برچسب فارسی نوع مدرک
        document_at:
          type: string
          format: date-time
          title: تاریخ سند
        source_type:
          type: string
          title: نوع منبع
          maxLength: 60
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
        counterpart_warehouse:
          type: string
          format: uuid
          title: انبار مقابل (در انتقال)
          nullable: true
        status:
          allOf:
          - $ref: '#/components/schemas/StockDocumentStatusEnum'
          readOnly: true
          title: وضعیت
        description:
          type: string
          title: شرح
          maxLength: 400
        confirmed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان تأیید سند
        reversal_of:
          type: string
          format: uuid
          title: برگشت سند
          readOnly: true
          nullable: true
        lines:
          type: array
          items:
            $ref: '#/components/schemas/StockDocumentLine'
          readOnly: true
          title: سطرهای سند
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - confirmed_at
      - created_at
      - document_at
      - document_no
      - document_type
      - document_type_display
      - id
      - lines
      - reversal_of
      - status
      - updated_at
      - version
      - warehouse
      - warehouse_code
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        warehouse_code: WH-01
        document_no: STK-1405-000094
        document_type: RECEIPT
        document_type_display: رسید
        document_at: '2026-09-23T08:30:00+03:30'
        source_type: متن نمونه
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        counterpart_warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: DRAFT
        description: توضیح تکمیلی این رکورد
        confirmed_at: '2026-09-23T08:30:00+03:30'
        reversal_of: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        lines:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          item_sku: STA-A4-80
          item_title: عنوان نمونه
          quantity: '18.50'
          uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          lot_no: SAMPLE-001
          serial_no: SN-88213047
          expiry_date: '2027-03-20'
          unit_cost: 2500000
          line_no: 3
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StockDocumentLine:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        stock_document:
          type: string
          format: uuid
          title: سند انبار
        item:
          type: string
          format: uuid
          title: کالا
        item_sku:
          type: string
          readOnly: true
          title: کد کالا (SKU)
        item_title:
          type: string
          readOnly: true
          title: عنوان قلم
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        uom:
          type: string
          format: uuid
          title: واحد سنجش
        lot_no:
          type: string
          title: شماره بچ
          maxLength: 60
        serial_no:
          type: string
          title: شماره سریال
          maxLength: 80
        expiry_date:
          type: string
          format: date
          nullable: true
          title: تاریخ انقضا
        unit_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای واحد (ریال)
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      required:
      - id
      - item
      - item_sku
      - item_title
      - quantity
      - stock_document
      - uom
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_sku: STA-A4-80
        item_title: عنوان نمونه
        quantity: '18.50'
        uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        lot_no: SAMPLE-001
        serial_no: SN-88213047
        expiry_date: '2027-03-20'
        unit_cost: 2500000
        line_no: 3
    StockDocumentLineRequest:
      type: object
      properties:
        stock_document:
          type: string
          format: uuid
          title: سند انبار
        item:
          type: string
          format: uuid
          title: کالا
        quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          title: تعداد
        uom:
          type: string
          format: uuid
          title: واحد سنجش
        lot_no:
          type: string
          title: شماره بچ
          maxLength: 60
        serial_no:
          type: string
          title: شماره سریال
          maxLength: 80
        expiry_date:
          type: string
          format: date
          nullable: true
          title: تاریخ انقضا
        unit_cost:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: بهای واحد (ریال)
        line_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره سطر
      required:
      - item
      - quantity
      - stock_document
      - uom
      example:
        stock_document: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        quantity: '18.50'
        uom: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        lot_no: SAMPLE-001
        serial_no: SN-88213047
        expiry_date: '2027-03-20'
        unit_cost: 2500000
        line_no: 3
    StockDocumentRequest:
      type: object
      properties:
        warehouse:
          type: string
          format: uuid
          title: انبار
        document_type:
          allOf:
          - $ref: '#/components/schemas/DocumentTypeEnum'
          title: نوع سند
        document_at:
          type: string
          format: date-time
          title: تاریخ سند
        source_type:
          type: string
          title: نوع منبع
          maxLength: 60
        source_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه منبع
        counterpart_warehouse:
          type: string
          format: uuid
          title: انبار مقابل (در انتقال)
          nullable: true
        description:
          type: string
          title: شرح
          maxLength: 400
      required:
      - document_at
      - document_type
      - warehouse
      example:
        warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        document_type: RECEIPT
        document_at: '2026-09-23T08:30:00+03:30'
        source_type: متن نمونه
        source_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        counterpart_warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        description: توضیح تکمیلی این رکورد
    StockDocumentStatusEnum:
      enum:
      - DRAFT
      - CONFIRMED
      - CANCELLED
      - REVERSED
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `CONFIRMED` - قطعی
        * `CANCELLED` - لغوشده
        * `REVERSED` - برگشت‌خورده
    StockMovement:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        document_line:
          type: string
          format: uuid
          title: سطر سند
          readOnly: true
        warehouse:
          type: string
          format: uuid
          title: انبار
          readOnly: true
        warehouse_code:
          type: string
          readOnly: true
          title: کد انبار
        item:
          type: string
          format: uuid
          title: کالا
          readOnly: true
        item_sku:
          type: string
          readOnly: true
          title: کد کالا (SKU)
        lot_no:
          type: string
          readOnly: true
          title: شماره بچ
        serial_no:
          type: string
          readOnly: true
          title: شماره سریال
        signed_quantity:
          type: string
          format: decimal
          pattern: ^-?\d{0,11}(?:\.\d{0,3})?$
          readOnly: true
          title: مقدار علامت‌دار
        unit_cost:
          type: integer
          readOnly: true
          title: بهای واحد
        occurred_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان رخداد
      required:
      - document_line
      - id
      - item
      - item_sku
      - lot_no
      - occurred_at
      - serial_no
      - signed_quantity
      - unit_cost
      - warehouse
      - warehouse_code
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        document_line: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        warehouse: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        warehouse_code: WH-01
        item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        item_sku: STA-A4-80
        lot_no: SAMPLE-001
        serial_no: SN-88213047
        signed_quantity: '18.50'
        unit_cost: 2500000
        occurred_at: '2026-09-23T08:30:00+03:30'
    Student:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        person:
          type: string
          format: uuid
          title: شخص
        person_detail:
          allOf:
          - $ref: '#/components/schemas/Person'
          readOnly: true
          title: مشخصات شخص
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        joined_on:
          type: string
          format: date
          title: تاریخ ورود به مدرسه
        status:
          allOf:
          - $ref: '#/components/schemas/StudentStatusEnum'
          title: وضعیت
        previous_school:
          type: string
          title: مدرسه مبدأ
          maxLength: 200
        notes:
          type: string
          title: یادداشت‌ها
        guardians:
          type: array
          items:
            $ref: '#/components/schemas/StudentGuardian'
          readOnly: true
          title: اولیا
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - full_name
      - guardians
      - id
      - joined_on
      - person
      - person_detail
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          national_id: 0012345678
          passport_no: K12345678
          first_name: سارا
          last_name: محمدی
          full_name: سارا محمدی
          father_name: علی
          birth_date: '2012-09-26'
          birth_place: تهران
          gender: MALE
          nationality: ایرانی
          preferred_language: متن نمونه
          photo: https://example.school/media/photos/sample.jpg
          accessibility_needs: متن نمونه
          status: ACTIVE
          contact_points:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            contact_type: MOBILE
            contact_type_display: تلفن همراه
            value: متن نمونه
            is_primary: true
            is_verified: true
            verified_at: '2026-09-23T08:30:00+03:30'
            label: متن نمونه
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          addresses:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            address_detail:
              id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
              country: IR
              province: تهران
              city: تهران
              district: منطقه ۳
              postal_code: '1234567890'
              line: خیابان نمونه، کوچه دوم، پلاک ۱۲
              latitude: '35.6892'
              longitude: '51.389'
              created_at: '2026-09-23T08:30:00+03:30'
              updated_at: '2026-09-23T08:30:00+03:30'
              version: 1
            address_type: HOME
            effective_from: '2026-09-23'
            effective_to: '2027-03-20'
          has_user_account: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        full_name: سارا محمدی
        student_no: '14030127'
        joined_on: '2026-09-23'
        status: PROSPECTIVE
        previous_school: متن نمونه
        notes: متن نمونه
        guardians:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian_name: علی محمدی
          guardian_mobile: 09123334455
          relationship_type: FATHER
          relationship_display: برچسب فارسی مقدار
          has_custody: true
          can_pickup: true
          receives_reports: true
          financially_responsible: true
          is_emergency_contact: true
          contact_priority: 3
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    Student360:
      type: object
      description: |-
        پرونده ۳۶۰ درجه دانش‌آموز (بخش ۷.۲ سند فرانت و ۱۲.۲ سند تحلیل).

        هر بخش فقط در صورت داشتن مجوز پر می‌شود؛ در غیر این صورت `null` است.
      properties:
        student:
          $ref: '#/components/schemas/StudentList'
          title: دانش‌آموز
        person:
          $ref: '#/components/schemas/Person'
          title: شخص
        guardians:
          type: array
          items:
            $ref: '#/components/schemas/StudentGuardian'
          title: اولیا
        enrollments:
          type: array
          items:
            $ref: '#/components/schemas/Enrollment'
          title: ثبت‌نام‌ها
        consents:
          type: array
          items:
            $ref: '#/components/schemas/Consent'
          title: رضایت‌نامه‌ها
        attendanceSummary:
          type: object
          additionalProperties: {}
          nullable: true
          title: خلاصه حضور و غیاب
        financialSummary:
          type: object
          additionalProperties: {}
          nullable: true
          title: خلاصه مالی
        academicSummary:
          type: object
          additionalProperties: {}
          nullable: true
          title: خلاصه آموزشی
        healthSummary:
          type: object
          additionalProperties: {}
          nullable: true
          title: خلاصه سلامت
      required:
      - academicSummary
      - attendanceSummary
      - consents
      - enrollments
      - financialSummary
      - guardians
      - healthSummary
      - person
      - student
      example:
        student:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_no: '14030127'
          full_name: سارا محمدی
          national_id: 0012345678
          gender: متن نمونه
          status: PROSPECTIVE
          status_display: متقاضی
          current_class: متن نمونه
          current_grade: متن نمونه
          joined_on: '2026-09-23'
        person:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          national_id: 0012345678
          passport_no: K12345678
          first_name: سارا
          last_name: محمدی
          full_name: سارا محمدی
          father_name: علی
          birth_date: '2012-09-26'
          birth_place: تهران
          gender: MALE
          nationality: ایرانی
          preferred_language: متن نمونه
          photo: https://example.school/media/photos/sample.jpg
          accessibility_needs: متن نمونه
          status: ACTIVE
          contact_points:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            contact_type: MOBILE
            contact_type_display: تلفن همراه
            value: متن نمونه
            is_primary: true
            is_verified: true
            verified_at: '2026-09-23T08:30:00+03:30'
            label: متن نمونه
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          addresses:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            address: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            address_detail:
              id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
              country: IR
              province: تهران
              city: تهران
              district: منطقه ۳
              postal_code: '1234567890'
              line: خیابان نمونه، کوچه دوم، پلاک ۱۲
              latitude: '35.6892'
              longitude: '51.389'
              created_at: '2026-09-23T08:30:00+03:30'
              updated_at: '2026-09-23T08:30:00+03:30'
              version: 1
            address_type: HOME
            effective_from: '2026-09-23'
            effective_to: '2027-03-20'
          has_user_account: true
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        guardians:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian_name: علی محمدی
          guardian_mobile: 09123334455
          relationship_type: FATHER
          relationship_display: برچسب فارسی مقدار
          has_custody: true
          can_pickup: true
          receives_reports: true
          financially_responsible: true
          is_emergency_contact: true
          contact_priority: 3
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        enrollments:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student_name: سارا محمدی
          student_no: '14030127'
          academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          academic_year_title: عنوان نمونه
          campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          grade_level: هفتم
          grade_level_title: پایه هفتم
          program: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          admission_application: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          enrollment_no: SAMPLE-001
          enrolled_on: '2026-09-23'
          status: PENDING_DOCUMENTS
          status_display: در انتظار مدارک
          current_class_code: SAMPLE-001
          exit_date: '2027-03-20'
          exit_reason: توضیح نمونه
          class_memberships:
          - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            student_name: سارا محمدی
            student_no: '14030127'
            class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
            class_group_code: G7-A
            is_primary: true
            status: ACTIVE
            effective_from: '2026-09-23'
            effective_to: '2027-03-20'
            exit_reason: توضیح نمونه
            created_at: '2026-09-23T08:30:00+03:30'
            updated_at: '2026-09-23T08:30:00+03:30'
            version: 1
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        consents:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          guardian_name: علی محمدی
          consent_type: PHOTO
          consent_type_display: انتشار تصویر
          status: GRANTED
          granted_at: '2026-09-23T08:30:00+03:30'
          revoked_at: '2026-09-23T08:30:00+03:30'
          revoke_reason: توضیح نمونه
          expires_at: '2027-03-20'
          policy_version: متن نمونه
          policy_text_snapshot: متن نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        attendanceSummary: {}
        financialSummary: {}
        academicSummary: {}
        healthSummary: {}
    StudentAttendanceSummary:
      type: object
      description: خلاصه حضور یک دانش‌آموز برای هشدار نصاب حضور (بخش ۴.۳).
      properties:
        enrollmentId:
          type: string
          format: uuid
          title: شناسه ثبت‌نام
        studentName:
          type: string
          title: نام دانش‌آموز
        totalSessions:
          type: integer
          title: تعداد کل جلسات
        byStatus:
          type: object
          additionalProperties:
            type: integer
          title: تفکیک بر اساس وضعیت
        presentPercent:
          type: number
          format: double
          nullable: true
          title: درصد حضور
        consecutiveAbsences:
          type: integer
          title: غیبت‌های پیاپی
        belowThreshold:
          type: boolean
          title: زیر آستانه هشدار
      required:
      - belowThreshold
      - byStatus
      - consecutiveAbsences
      - enrollmentId
      - presentPercent
      - studentName
      - totalSessions
      example:
        enrollmentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        studentName: سارا محمدی
        totalSessions: 12
        byStatus: {}
        presentPercent: 18.5
        consecutiveAbsences: 3
        belowThreshold: true
    StudentBehaviorSummary:
      type: object
      properties:
        studentId:
          type: string
          format: uuid
          title: شناسه دانش‌آموز
        studentName:
          type: string
          title: نام دانش‌آموز
        totalPoints:
          type: integer
          title: جمع امتیاز
        incidentCount:
          type: integer
          title: تعداد رخدادهای انضباطی
        commendationCount:
          type: integer
          title: تعداد تشویق‌ها
        bySeverity:
          type: object
          additionalProperties:
            type: integer
          title: تفکیک بر اساس شدت
      required:
      - bySeverity
      - commendationCount
      - incidentCount
      - studentId
      - studentName
      - totalPoints
      example:
        studentId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        studentName: سارا محمدی
        totalPoints: 12
        incidentCount: 12
        commendationCount: 12
        bySeverity: {}
    StudentFinancialAgreement:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        fee_plan:
          type: string
          format: uuid
          title: طرح شهریه
        fee_plan_title:
          type: string
          readOnly: true
          title: عنوان طرح شهریه
        responsible_guardian:
          type: string
          format: uuid
          title: مسئول مالی
          nullable: true
        responsible_guardian_name:
          type: string
          readOnly: true
          title: نام ولی مسئول پرداخت
        agreed_amount:
          type: integer
          readOnly: true
          title: مبلغ توافق‌شده پس از تخفیف
        currency:
          type: string
          title: واحد پول
          maxLength: 3
        installment_count:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد اقساط
        status:
          allOf:
          - $ref: '#/components/schemas/AgreementStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 400
        discounts:
          type: array
          items:
            $ref: '#/components/schemas/DiscountAward'
          readOnly: true
          title: تخفیف‌ها
        total_invoiced:
          type: integer
          readOnly: true
          title: جمع صورتحساب‌ها (ریال)
        total_paid:
          type: integer
          readOnly: true
          title: جمع پرداختی‌ها (ریال)
        balance:
          type: integer
          readOnly: true
          title: مانده
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - agreed_amount
      - balance
      - created_at
      - discounts
      - enrollment
      - fee_plan
      - fee_plan_title
      - id
      - responsible_guardian_name
      - student_name
      - student_no
      - total_invoiced
      - total_paid
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_plan_title: عنوان نمونه
        responsible_guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        responsible_guardian_name: نام نمونه
        agreed_amount: 2500000
        currency: IRR
        installment_count: 12
        status: DRAFT
        note: یادداشت داخلی
        discounts:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          agreement: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          discount_type: SIBLING
          discount_type_display: خواهر/برادر
          percent: '18.50'
          amount: 12500000
          reason: اصلاح اطلاعات پس از بررسی پرونده
          approval_status: PENDING
          approved_at: '2026-09-23T08:30:00+03:30'
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        total_invoiced: 12
        total_paid: 12
        balance: 32500000
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StudentFinancialAgreementRequest:
      type: object
      properties:
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        fee_plan:
          type: string
          format: uuid
          title: طرح شهریه
        responsible_guardian:
          type: string
          format: uuid
          title: مسئول مالی
          nullable: true
        currency:
          type: string
          minLength: 1
          title: واحد پول
          maxLength: 3
        installment_count:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد اقساط
        status:
          allOf:
          - $ref: '#/components/schemas/AgreementStatusEnum'
          title: وضعیت
        note:
          type: string
          title: یادداشت
          maxLength: 400
      required:
      - enrollment
      - fee_plan
      example:
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        fee_plan: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        responsible_guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        currency: IRR
        installment_count: 12
        status: DRAFT
        note: یادداشت داخلی
    StudentGuardian:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        guardian:
          type: string
          format: uuid
          title: ولی
        guardian_name:
          type: string
          readOnly: true
          title: نام ولی
        guardian_mobile:
          type: string
          readOnly: true
          title: تلفن همراه ولی
        relationship_type:
          allOf:
          - $ref: '#/components/schemas/RelationshipTypeEnum'
          title: نوع رابطه
        relationship_display:
          type: string
          readOnly: true
          title: برچسب فارسی نسبت
        has_custody:
          type: boolean
          title: دارای حضانت
        can_pickup:
          type: boolean
          title: مجاز به تحویل گرفتن دانش‌آموز
        receives_reports:
          type: boolean
          title: دریافت‌کننده گزارش و کارنامه
        financially_responsible:
          type: boolean
          title: مسئول مالی
        is_emergency_contact:
          type: boolean
          title: تماس اضطراری
        contact_priority:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: اولویت تماس
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - effective_from
      - guardian
      - guardian_mobile
      - guardian_name
      - id
      - relationship_display
      - relationship_type
      - student
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian_name: علی محمدی
        guardian_mobile: 09123334455
        relationship_type: FATHER
        relationship_display: برچسب فارسی مقدار
        has_custody: true
        can_pickup: true
        receives_reports: true
        financially_responsible: true
        is_emergency_contact: true
        contact_priority: 3
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StudentGuardianRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        guardian:
          type: string
          format: uuid
          title: ولی
        relationship_type:
          allOf:
          - $ref: '#/components/schemas/RelationshipTypeEnum'
          title: نوع رابطه
        has_custody:
          type: boolean
          title: دارای حضانت
        can_pickup:
          type: boolean
          title: مجاز به تحویل گرفتن دانش‌آموز
        receives_reports:
          type: boolean
          title: دریافت‌کننده گزارش و کارنامه
        financially_responsible:
          type: boolean
          title: مسئول مالی
        is_emergency_contact:
          type: boolean
          title: تماس اضطراری
        contact_priority:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: اولویت تماس
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      required:
      - effective_from
      - guardian
      - relationship_type
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        guardian: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        relationship_type: FATHER
        has_custody: true
        can_pickup: true
        receives_reports: true
        financially_responsible: true
        is_emergency_contact: true
        contact_priority: 3
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    StudentList:
      type: object
      description: نمای سبک فهرست دانش‌آموزان (بخش ۷.۱ سند فرانت).
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student_no:
          type: string
          title: شماره دانش‌آموزی
          maxLength: 30
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        national_id:
          type: string
          readOnly: true
          title: کد ملی
        gender:
          type: string
          readOnly: true
          title: جنسیت
        status:
          allOf:
          - $ref: '#/components/schemas/StudentStatusEnum'
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        current_class:
          type: string
          nullable: true
          readOnly: true
          title: کلاس فعلی
        current_grade:
          type: string
          nullable: true
          readOnly: true
          title: پایه فعلی
        joined_on:
          type: string
          format: date
          title: تاریخ ورود به مدرسه
      required:
      - current_class
      - current_grade
      - full_name
      - gender
      - id
      - joined_on
      - national_id
      - status_display
      - student_no
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_no: '14030127'
        full_name: سارا محمدی
        national_id: 0012345678
        gender: متن نمونه
        status: PROSPECTIVE
        status_display: متقاضی
        current_class: متن نمونه
        current_grade: متن نمونه
        joined_on: '2026-09-23'
    StudentRequest:
      type: object
      properties:
        person:
          type: string
          format: uuid
          title: شخص
        joined_on:
          type: string
          format: date
          title: تاریخ ورود به مدرسه
        status:
          allOf:
          - $ref: '#/components/schemas/StudentStatusEnum'
          title: وضعیت
        previous_school:
          type: string
          title: مدرسه مبدأ
          maxLength: 200
        notes:
          type: string
          title: یادداشت‌ها
      required:
      - joined_on
      - person
      example:
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        joined_on: '2026-09-23'
        status: PROSPECTIVE
        previous_school: متن نمونه
        notes: متن نمونه
    StudentRouteAssignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        route:
          type: string
          format: uuid
          title: مسیر
        route_code:
          type: string
          readOnly: true
          title: کد مسیر
        pickup_stop:
          type: string
          format: uuid
          title: ایستگاه سوارشدن
          nullable: true
        pickup_stop_title:
          type: string
          readOnly: true
          title: عنوان ایستگاه سوارشدن
        dropoff_stop:
          type: string
          format: uuid
          title: ایستگاه پیاده‌شدن
          nullable: true
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          type: string
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - effective_from
      - id
      - pickup_stop_title
      - route
      - route_code
      - student
      - student_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route_code: R-03
        pickup_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        pickup_stop_title: عنوان نمونه
        dropoff_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        status: متن نمونه
        note: یادداشت داخلی
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StudentRouteAssignmentRequest:
      type: object
      properties:
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        route:
          type: string
          format: uuid
          title: مسیر
        pickup_stop:
          type: string
          format: uuid
          title: ایستگاه سوارشدن
          nullable: true
        dropoff_stop:
          type: string
          format: uuid
          title: ایستگاه پیاده‌شدن
          nullable: true
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
        note:
          type: string
          title: یادداشت
          maxLength: 300
      required:
      - effective_from
      - route
      - student
      example:
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        pickup_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        dropoff_stop: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        status: متن نمونه
        note: یادداشت داخلی
    StudentScore:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        grade_item:
          type: string
          format: uuid
          title: قلم نمره
        grade_item_title:
          type: string
          readOnly: true
          title: عنوان قلم نمره
        max_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          readOnly: true
          title: نمره کل
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        student_name:
          type: string
          readOnly: true
          title: نام دانش‌آموز
        student_no:
          type: string
          readOnly: true
          title: شماره دانش‌آموزی
        raw_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره خام
        normalized_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          title: نمره نرمال‌شده
          description: نمره خام تقسیم بر بارم، ضرب در ۱۰۰
        letter_grade:
          type: string
          title: نمره حرفی
          maxLength: 5
        qualitative_level:
          title: سطح کیفی
          oneOf:
          - $ref: '#/components/schemas/QualitativeLevelEnum'
          - $ref: '#/components/schemas/BlankEnum'
        status:
          allOf:
          - $ref: '#/components/schemas/ScoreStatusEnum'
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        comment:
          type: string
          title: توضیح
          maxLength: 400
        recorded_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ثبت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - enrollment
      - grade_item
      - grade_item_title
      - id
      - max_score
      - normalized_score
      - recorded_at
      - status_display
      - student_name
      - student_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_item_title: عنوان نمونه
        max_score: '18.50'
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student_name: سارا محمدی
        student_no: '14030127'
        raw_score: '18.50'
        normalized_score: '18.50'
        letter_grade: A
        qualitative_level: EXCELLENT
        status: NOT_RECORDED
        status_display: ثبت‌نشده
        comment: توضیح کوتاه ثبت‌کننده
        recorded_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StudentScoreRequest:
      type: object
      properties:
        grade_item:
          type: string
          format: uuid
          title: قلم نمره
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        raw_score:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
          title: نمره خام
        letter_grade:
          type: string
          title: نمره حرفی
          maxLength: 5
        qualitative_level:
          title: سطح کیفی
          oneOf:
          - $ref: '#/components/schemas/QualitativeLevelEnum'
          - $ref: '#/components/schemas/BlankEnum'
        status:
          allOf:
          - $ref: '#/components/schemas/ScoreStatusEnum'
          title: وضعیت
        comment:
          type: string
          title: توضیح
          maxLength: 400
      required:
      - enrollment
      - grade_item
      example:
        grade_item: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        raw_score: '18.50'
        letter_grade: A
        qualitative_level: EXCELLENT
        status: NOT_RECORDED
        comment: توضیح کوتاه ثبت‌کننده
    StudentStatusEnum:
      enum:
      - PROSPECTIVE
      - ACTIVE
      - SUSPENDED
      - TRANSFERRED_OUT
      - WITHDRAWN
      - GRADUATED
      type: string
      description: |-
        * `PROSPECTIVE` - متقاضی
        * `ACTIVE` - در حال تحصیل
        * `SUSPENDED` - تعلیق
        * `TRANSFERRED_OUT` - انتقال به مدرسه دیگر
        * `WITHDRAWN` - ترک تحصیل
        * `GRADUATED` - فارغ‌التحصیل
    StudentStatusHistory:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
          readOnly: true
        from_status:
          type: string
          readOnly: true
          title: وضعیت مبدأ
        to_status:
          type: string
          readOnly: true
          title: وضعیت مقصد
        reason_code:
          type: string
          readOnly: true
          title: کد علت
        reason:
          type: string
          readOnly: true
          title: علت
        changed_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان تغییر
      required:
      - changed_at
      - from_status
      - id
      - reason
      - reason_code
      - student
      - to_status
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        from_status: متن نمونه
        to_status: متن نمونه
        reason_code: SAMPLE-001
        reason: اصلاح اطلاعات پس از بررسی پرونده
        changed_at: '2026-09-23T08:30:00+03:30'
    StudentTransfer:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        student:
          type: string
          format: uuid
          title: دانش‌آموز
        enrollment:
          type: string
          format: uuid
          title: ثبت‌نام
        transfer_type:
          allOf:
          - $ref: '#/components/schemas/TransferTypeEnum'
          title: نوع انتقال
        from_class_group:
          type: string
          format: uuid
          title: کلاس مبدأ
          nullable: true
        from_class_code:
          type: string
          readOnly: true
          title: کد کلاس مبدأ
        to_class_group:
          type: string
          format: uuid
          title: کلاس مقصد
          nullable: true
        to_class_code:
          type: string
          readOnly: true
          title: کد کلاس مقصد
        effective_on:
          type: string
          format: date
          title: تاریخ اجرا
        reason:
          type: string
          title: علت انتقال
          maxLength: 400
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - effective_on
      - enrollment
      - from_class_code
      - id
      - reason
      - student
      - to_class_code
      - transfer_type
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        student: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        enrollment: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        transfer_type: INTERNAL_CLASS
        from_class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        from_class_code: SAMPLE-001
        to_class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        to_class_code: SAMPLE-001
        effective_on: '2026-09-23'
        reason: اصلاح اطلاعات پس از بررسی پرونده
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StudyProgram:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          title: کد رشته
          maxLength: 30
        title:
          type: string
          title: عنوان رشته
          maxLength: 150
        description:
          type: string
          title: شرح
          maxLength: 400
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - school
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        status: DRAFT
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    StudyProgramRequest:
      type: object
      properties:
        school:
          type: string
          format: uuid
          title: مدرسه
        code:
          type: string
          minLength: 1
          title: کد رشته
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان رشته
          maxLength: 150
        description:
          type: string
          title: شرح
          maxLength: 400
        status:
          allOf:
          - $ref: '#/components/schemas/RecordStatusEnum'
          title: وضعیت
      required:
      - code
      - school
      - title
      example:
        school: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        description: توضیح تکمیلی این رکورد
        status: DRAFT
    SubmissionFeedback:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        submission:
          type: string
          format: uuid
          title: تحویل
        reviewer_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: شناسه بازبین
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره
        feedback:
          type: string
          title: بازخورد
        reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان بازبینی
        is_final:
          type: boolean
          title: نهایی
      required:
      - id
      - reviewed_at
      - reviewer_id
      - submission
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        submission: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reviewer_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        score: '18.50'
        feedback: متن نمونه
        reviewed_at: '2026-09-23T08:30:00+03:30'
        is_final: true
    SubmissionFeedbackRequest:
      type: object
      properties:
        submission:
          type: string
          format: uuid
          title: تحویل
        score:
          type: string
          format: decimal
          pattern: ^-?\d{0,4}(?:\.\d{0,2})?$
          nullable: true
          title: نمره
        feedback:
          type: string
          title: بازخورد
        is_final:
          type: boolean
          title: نهایی
      required:
      - submission
      example:
        submission: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        score: '18.50'
        feedback: متن نمونه
        is_final: true
    SubmissionStatusEnum:
      enum:
      - DRAFT
      - SUBMITTED
      - LATE
      - GRADED
      - RETURNED
      - MISSING
      type: string
      description: |-
        * `DRAFT` - پیش‌نویس
        * `SUBMITTED` - ارسال‌شده
        * `LATE` - ارسال با تأخیر
        * `GRADED` - تصحیح‌شده
        * `RETURNED` - بازگشت برای اصلاح
        * `MISSING` - ارسال‌نشده
    TeacherProfile:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        employee:
          type: string
          format: uuid
          title: کارمند
        employee_no:
          type: string
          readOnly: true
          title: شماره کارمند
        full_name:
          type: string
          readOnly: true
          title: نام کامل
        required_weekly_hours:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: بار موظف هفتگی
        assigned_weekly_minutes:
          type: integer
          readOnly: true
          title: دقایق هفتگی تخصیص‌یافته
        qualification_status:
          allOf:
          - $ref: '#/components/schemas/TeacherQualificationStatusEnum'
          title: وضعیت صلاحیت
        specialization:
          type: string
          title: تخصص
          maxLength: 200
        qualifications:
          type: array
          items:
            $ref: '#/components/schemas/TeacherQualification'
          readOnly: true
          title: صلاحیت‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - assigned_weekly_minutes
      - created_at
      - employee
      - employee_no
      - full_name
      - id
      - qualifications
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        employee_no: SAMPLE-001
        full_name: سارا محمدی
        required_weekly_hours: '18.50'
        assigned_weekly_minutes: 45
        qualification_status: PENDING
        specialization: متن نمونه
        qualifications:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          grade_level: هفتم
          grade_level_title: پایه هفتم
          valid_until: '2027-03-20'
          status: PENDING
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TeacherProfileRequest:
      type: object
      properties:
        employee:
          type: string
          format: uuid
          title: کارمند
        required_weekly_hours:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: بار موظف هفتگی
        qualification_status:
          allOf:
          - $ref: '#/components/schemas/TeacherQualificationStatusEnum'
          title: وضعیت صلاحیت
        specialization:
          type: string
          title: تخصص
          maxLength: 200
      required:
      - employee
      example:
        employee: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        required_weekly_hours: '18.50'
        qualification_status: PENDING
        specialization: متن نمونه
    TeacherQualification:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        course:
          type: string
          format: uuid
          title: درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        grade_level_title:
          type: string
          readOnly: true
          title: عنوان پایه تحصیلی
        valid_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/TeacherQualificationStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - course
      - course_title
      - created_at
      - grade_level_title
      - id
      - teacher_profile
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        grade_level: هفتم
        grade_level_title: پایه هفتم
        valid_until: '2027-03-20'
        status: PENDING
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TeacherQualificationRequest:
      type: object
      properties:
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        course:
          type: string
          format: uuid
          title: درس
        grade_level:
          type: string
          format: uuid
          title: پایه تحصیلی
          nullable: true
        valid_until:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        status:
          allOf:
          - $ref: '#/components/schemas/TeacherQualificationStatusEnum'
          title: وضعیت
      required:
      - course
      - teacher_profile
      example:
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        grade_level: هفتم
        valid_until: '2027-03-20'
        status: PENDING
    TeacherQualificationStatusEnum:
      enum:
      - PENDING
      - APPROVED
      - EXPIRED
      - REVOKED
      type: string
      description: |-
        * `PENDING` - در انتظار تأیید
        * `APPROVED` - تأییدشده
        * `EXPIRED` - منقضی
        * `REVOKED` - لغوشده
    TeacherWorkload:
      type: object
      description: گزارش بار تدریس معلم (بخش ۱۱.۲ سند فرانت).
      properties:
        teacherProfileId:
          type: string
          format: uuid
          title: شناسه پرونده معلم
        teacherName:
          type: string
          title: نام معلم
        requiredWeeklyMinutes:
          type: integer
          title: دقایق هفتگی موظفی
        assignedWeeklyMinutes:
          type: integer
          title: دقایق هفتگی تخصیص‌یافته
        remainingMinutes:
          type: integer
          title: دقایق باقی‌مانده
        offerings:
          type: array
          items:
            type: object
            additionalProperties: {}
          title: ارائه‌های درس
      required:
      - assignedWeeklyMinutes
      - offerings
      - remainingMinutes
      - requiredWeeklyMinutes
      - teacherName
      - teacherProfileId
      example:
        teacherProfileId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacherName: مریم رضایی
        requiredWeeklyMinutes: 45
        assignedWeeklyMinutes: 45
        remainingMinutes: 45
        offerings:
        - {}
    TeachingAssignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        teacher_name:
          type: string
          readOnly: true
          title: نام معلم
        responsibility:
          allOf:
          - $ref: '#/components/schemas/ResponsibilityEnum'
          title: مسئولیت
        share_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: سهم تدریس
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - class_group_code
      - course_offering
      - course_title
      - created_at
      - effective_from
      - id
      - teacher_name
      - teacher_profile
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        class_group_code: G7-A
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_name: مریم رضایی
        responsibility: PRIMARY
        share_percent: '18.50'
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TeachingAssignmentRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        teacher_profile:
          type: string
          format: uuid
          title: پرونده معلم
        responsibility:
          allOf:
          - $ref: '#/components/schemas/ResponsibilityEnum'
          title: مسئولیت
        share_percent:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          title: سهم تدریس
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
      required:
      - course_offering
      - effective_from
      - teacher_profile
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        responsibility: PRIMARY
        share_percent: '18.50'
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
    TeachingSession:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        course_title:
          type: string
          readOnly: true
          title: عنوان درس
        class_group_code:
          type: string
          readOnly: true
          title: کد کلاس
        schedule_entry:
          type: string
          format: uuid
          title: سطر برنامه هفتگی
          nullable: true
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        room_code:
          type: string
          readOnly: true
          title: کد اتاق
        starts_at:
          type: string
          format: date-time
          title: شروع
        ends_at:
          type: string
          format: date-time
          title: پایان
        session_type:
          allOf:
          - $ref: '#/components/schemas/SessionTypeEnum'
          title: نوع جلسه
        status:
          allOf:
          - $ref: '#/components/schemas/SessionStatusEnum'
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        topic:
          type: string
          title: موضوع جلسه
          maxLength: 250
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
        attendance_finalized_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان قطعی‌شدن حضور
        teachers:
          type: array
          items:
            $ref: '#/components/schemas/SessionTeacher'
          readOnly: true
          title: معلمان
        attendance_summary:
          type: object
          additionalProperties: {}
          readOnly: true
          title: خلاصه حضور و غیاب
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - attendance_finalized_at
      - attendance_summary
      - class_group_code
      - course_offering
      - course_title
      - created_at
      - ends_at
      - id
      - room_code
      - starts_at
      - status_display
      - teachers
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        course_title: ریاضی ۷
        class_group_code: G7-A
        schedule_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room_code: R-204
        starts_at: '2026-09-23T08:30:00+03:30'
        ends_at: '2027-03-20T12:00:00+03:30'
        session_type: REGULAR
        status: PLANNED
        status_display: برنامه‌ریزی‌شده
        topic: متن نمونه
        cancel_reason: توضیح نمونه
        attendance_finalized_at: '2026-09-23T08:30:00+03:30'
        teachers:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          session: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_profile: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          teacher_name: مریم رضایی
          duty: TEACHER
        attendance_summary: {}
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TeachingSessionRequest:
      type: object
      properties:
        course_offering:
          type: string
          format: uuid
          title: ارائه درس
        schedule_entry:
          type: string
          format: uuid
          title: سطر برنامه هفتگی
          nullable: true
        room:
          type: string
          format: uuid
          title: اتاق
          nullable: true
        starts_at:
          type: string
          format: date-time
          title: شروع
        ends_at:
          type: string
          format: date-time
          title: پایان
        session_type:
          allOf:
          - $ref: '#/components/schemas/SessionTypeEnum'
          title: نوع جلسه
        status:
          allOf:
          - $ref: '#/components/schemas/SessionStatusEnum'
          title: وضعیت
        topic:
          type: string
          title: موضوع جلسه
          maxLength: 250
        cancel_reason:
          type: string
          title: علت لغو
          maxLength: 300
      required:
      - course_offering
      - ends_at
      - starts_at
      example:
        course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        schedule_entry: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        starts_at: '2026-09-23T08:30:00+03:30'
        ends_at: '2027-03-20T12:00:00+03:30'
        session_type: REGULAR
        status: PLANNED
        topic: متن نمونه
        cancel_reason: توضیح نمونه
    Term:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        title:
          type: string
          title: عنوان ترم
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: شروع
        ends_on:
          type: string
          format: date
          title: پایان
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب
        status:
          allOf:
          - $ref: '#/components/schemas/TermStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - academic_year
      - created_at
      - ends_on
      - id
      - sequence_no
      - starts_on
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        sequence_no: 3
        status: PLANNED
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TermRequest:
      type: object
      properties:
        academic_year:
          type: string
          format: uuid
          title: سال تحصیلی
        title:
          type: string
          minLength: 1
          title: عنوان ترم
          maxLength: 60
        starts_on:
          type: string
          format: date
          title: شروع
        ends_on:
          type: string
          format: date
          title: پایان
        sequence_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ترتیب
        status:
          allOf:
          - $ref: '#/components/schemas/TermStatusEnum'
          title: وضعیت
      required:
      - academic_year
      - ends_on
      - sequence_no
      - starts_on
      - title
      example:
        academic_year: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_on: '2026-09-23'
        ends_on: '2027-03-20'
        sequence_no: 3
        status: PLANNED
    TermStatusEnum:
      enum:
      - PLANNED
      - ACTIVE
      - CLOSED
      type: string
      description: |-
        * `PLANNED` - برنامه‌ریزی‌شده
        * `ACTIVE` - جاری
        * `CLOSED` - بسته
    ThreeWayMatch:
      type: object
      description: نتیجه تطبیق سه‌طرفه.
      properties:
        orderNo:
          type: string
          title: شماره سفارش
        receiptNo:
          type: string
          title: شماره رسید
        orderAmount:
          type: integer
          title: مبلغ سفارش (ریال)
        receivedValue:
          type: integer
          title: ارزش کالای دریافتی (ریال)
        invoiceAmount:
          type: integer
          title: مبلغ صورتحساب (ریال)
        difference:
          type: integer
          title: اختلاف
        tolerance:
          type: integer
          title: تلورانس مجاز
        matched:
          type: boolean
          title: تطبیق برقرار است
      required:
      - difference
      - invoiceAmount
      - matched
      - orderAmount
      - orderNo
      - receiptNo
      - receivedValue
      - tolerance
      example:
        orderNo: PO-1405-000027
        receiptNo: GRN-1405-000058
        orderAmount: 2500000
        receivedValue: 3
        invoiceAmount: 2500000
        difference: 3
        tolerance: 3
        matched: true
    Ticket:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        ticket_no:
          type: string
          readOnly: true
          title: شماره تیکت
        requester_person:
          type: string
          format: uuid
          title: درخواست‌دهنده
        requester_name:
          type: string
          readOnly: true
          title: نام درخواست‌دهنده
        category:
          type: string
          title: دسته درخواست
          maxLength: 60
        subject:
          type: string
          title: موضوع
          maxLength: 250
        description:
          type: string
          title: شرح
        priority:
          allOf:
          - $ref: '#/components/schemas/PriorityLevelEnum'
          title: اولویت
        status:
          allOf:
          - $ref: '#/components/schemas/TicketStatusEnum'
          readOnly: true
          title: وضعیت
        status_display:
          type: string
          readOnly: true
          title: برچسب فارسی وضعیت
        assignee_user_id:
          type: string
          format: uuid
          nullable: true
          title: کاربر مسئول
        due_at:
          type: string
          format: date-time
          nullable: true
          title: مهلت SLA
        resolved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان رسیدگی
        resolution:
          type: string
          title: نتیجه رسیدگی
        messages:
          type: array
          items:
            $ref: '#/components/schemas/TicketMessage'
          readOnly: true
          title: پیام‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - category
      - created_at
      - id
      - messages
      - requester_name
      - requester_person
      - resolved_at
      - status
      - status_display
      - subject
      - ticket_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        ticket_no: TK-1405-000203
        requester_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        requester_name: نام نمونه
        category: متن نمونه
        subject: متن نمونه
        description: توضیح تکمیلی این رکورد
        priority: LOW
        status: OPEN
        status_display: باز
        assignee_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        due_at: '2027-03-20T12:00:00+03:30'
        resolved_at: '2026-09-23T08:30:00+03:30'
        resolution: متن نمونه
        messages:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          ticket: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          author_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          body: متن نمونه
          is_internal: true
          created_at: '2026-09-23T08:30:00+03:30'
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TicketMessage:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        ticket:
          type: string
          format: uuid
          title: تیکت
        author_user_id:
          type: string
          format: uuid
          readOnly: true
          nullable: true
          title: کاربر نویسنده
        body:
          type: string
          title: متن
        is_internal:
          type: boolean
          title: یادداشت داخلی (برای درخواست‌کننده نمایش داده نمی‌شود)
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
      required:
      - author_user_id
      - body
      - created_at
      - id
      - ticket
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        ticket: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        author_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        body: متن نمونه
        is_internal: true
        created_at: '2026-09-23T08:30:00+03:30'
    TicketMessageRequest:
      type: object
      properties:
        ticket:
          type: string
          format: uuid
          title: تیکت
        body:
          type: string
          minLength: 1
          title: متن
        is_internal:
          type: boolean
          title: یادداشت داخلی (برای درخواست‌کننده نمایش داده نمی‌شود)
      required:
      - body
      - ticket
      example:
        ticket: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        body: متن نمونه
        is_internal: true
    TicketRequest:
      type: object
      properties:
        requester_person:
          type: string
          format: uuid
          title: درخواست‌دهنده
        category:
          type: string
          minLength: 1
          title: دسته درخواست
          maxLength: 60
        subject:
          type: string
          minLength: 1
          title: موضوع
          maxLength: 250
        description:
          type: string
          title: شرح
        priority:
          allOf:
          - $ref: '#/components/schemas/PriorityLevelEnum'
          title: اولویت
        assignee_user_id:
          type: string
          format: uuid
          nullable: true
          title: کاربر مسئول
        due_at:
          type: string
          format: date-time
          nullable: true
          title: مهلت SLA
        resolution:
          type: string
          title: نتیجه رسیدگی
      required:
      - category
      - requester_person
      - subject
      example:
        requester_person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        category: متن نمونه
        subject: متن نمونه
        description: توضیح تکمیلی این رکورد
        priority: LOW
        assignee_user_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        due_at: '2027-03-20T12:00:00+03:30'
        resolution: متن نمونه
    TicketStatusEnum:
      enum:
      - OPEN
      - IN_PROGRESS
      - WAITING_REQUESTER
      - RESOLVED
      - CLOSED
      type: string
      description: |-
        * `OPEN` - باز
        * `IN_PROGRESS` - در حال بررسی
        * `WAITING_REQUESTER` - در انتظار پاسخ درخواست‌کننده
        * `RESOLVED` - حل‌شده
        * `CLOSED` - بسته
    TokenPair:
      type: object
      description: پاسخ ورود موفق.
      properties:
        access:
          type: string
          description: توکن دسترسی (عمر کوتاه)
        refresh:
          type: string
          description: توکن تازه‌سازی
        expiresIn:
          type: integer
          description: عمر توکن دسترسی به ثانیه
        mustChangePassword:
          type: boolean
          title: تغییر اجباری گذرواژه در ورود بعدی
        mfaRequired:
          type: boolean
          title: احراز هویت دومرحله‌ای لازم است
      required:
      - access
      - expiresIn
      - mfaRequired
      - mustChangePassword
      - refresh
      example:
        access: متن نمونه
        refresh: متن نمونه
        expiresIn: 3
        mustChangePassword: true
        mfaRequired: true
    TokenRefresh:
      type: object
      properties:
        access:
          type: string
          readOnly: true
          title: توکن دسترسی
        refresh:
          type: string
          title: توکن تازه‌سازی
      required:
      - access
      - refresh
      example:
        access: متن نمونه
        refresh: متن نمونه
    TokenRefreshRequest:
      type: object
      properties:
        refresh:
          type: string
          minLength: 1
          title: توکن تازه‌سازی
      required:
      - refresh
      example:
        refresh: متن نمونه
    TransferClassRequest:
      type: object
      properties:
        target_class_group:
          type: string
          format: uuid
          description: شناسه کلاس مقصد
        reason:
          type: string
          minLength: 1
          description: علت انتقال
          maxLength: 400
        effective_on:
          type: string
          format: date
          title: تاریخ اعمال
      required:
      - reason
      - target_class_group
      example:
        target_class_group: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        reason: اصلاح اطلاعات پس از بررسی پرونده
        effective_on: '2026-09-23'
    TransferTypeEnum:
      enum:
      - INTERNAL_CLASS
      - INTERNAL_CAMPUS
      - EXTERNAL_OUT
      - EXTERNAL_IN
      type: string
      description: |-
        * `INTERNAL_CLASS` - انتقال بین کلاس
        * `INTERNAL_CAMPUS` - انتقال بین شعبه
        * `EXTERNAL_OUT` - انتقال به مدرسه دیگر
        * `EXTERNAL_IN` - انتقال از مدرسه دیگر
    TransportRoute:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        campus:
          type: string
          format: uuid
          title: شعبه
        campus_name:
          type: string
          readOnly: true
          title: نام شعبه
        code:
          type: string
          title: کد
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 150
        direction:
          allOf:
          - $ref: '#/components/schemas/RouteDirectionEnum'
          title: جهت
        default_vehicle:
          type: string
          format: uuid
          title: خودرو پیش‌فرض
          nullable: true
        vehicle_plate:
          type: string
          readOnly: true
          title: پلاک خودرو
        vehicle_capacity:
          type: integer
          readOnly: true
          title: ظرفیت خودرو
        default_driver_employee_id:
          type: string
          format: uuid
          nullable: true
          title: راننده پیش‌فرض
        monthly_fee:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: هزینه ماهانه
        status:
          type: string
          title: وضعیت
          maxLength: 20
        active_rider_count:
          type: integer
          readOnly: true
          title: تعداد مسافران فعال
        stops:
          type: array
          items:
            $ref: '#/components/schemas/RouteStop'
          readOnly: true
          title: ایستگاه‌ها
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - active_rider_count
      - campus
      - campus_name
      - code
      - created_at
      - id
      - stops
      - title
      - updated_at
      - vehicle_capacity
      - vehicle_plate
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus_name: شعبه مرکزی
        code: SAMPLE-001
        title: عنوان نمونه
        direction: MORNING
        default_vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        vehicle_plate: ۱۲ الف ۳۴۵ ایران ۱۰
        vehicle_capacity: 3
        default_driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        monthly_fee: 3
        status: متن نمونه
        active_rider_count: 12
        stops:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          route: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          title: عنوان نمونه
          address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
          latitude: '35.6892'
          longitude: '51.389'
          sequence_no: 3
          scheduled_time: 08:30:00
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    TransportRouteRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        direction:
          allOf:
          - $ref: '#/components/schemas/RouteDirectionEnum'
          title: جهت
        default_vehicle:
          type: string
          format: uuid
          title: خودرو پیش‌فرض
          nullable: true
        default_driver_employee_id:
          type: string
          format: uuid
          nullable: true
          title: راننده پیش‌فرض
        monthly_fee:
          type: integer
          maximum: 9223372036854775807
          minimum: -9223372036854775808
          format: int64
          title: هزینه ماهانه
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - campus
      - code
      - title
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        direction: MORNING
        default_vehicle: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        default_driver_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        monthly_fee: 3
        status: متن نمونه
    UnitOfMeasure:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد
          maxLength: 20
        title:
          type: string
          title: عنوان
          maxLength: 80
        decimal_places:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد رقم اعشار
      required:
      - code
      - id
      - title
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        decimal_places: 3
    UnitOfMeasureRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 20
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 80
        decimal_places:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تعداد رقم اعشار
      required:
      - code
      - title
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        decimal_places: 3
    UnlockGradeItemRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 400
          title: علت
      required:
      - reason
      example:
        reason: اصلاح اطلاعات پس از بررسی پرونده
    UserAccount:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        username:
          type: string
          title: نام کاربری
          maxLength: 150
        email:
          title: ایمیل
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        mobile:
          type: string
          title: تلفن همراه
          maxLength: 20
        person:
          type: string
          format: uuid
          title: شخص
          nullable: true
        person_detail:
          allOf:
          - $ref: '#/components/schemas/PersonList'
          readOnly: true
          title: مشخصات شخص
        display_name:
          type: string
          readOnly: true
          title: نام نمایشی
        status:
          allOf:
          - $ref: '#/components/schemas/UserStatusEnum'
          title: وضعیت حساب
        mfa_enabled:
          type: boolean
          title: احراز هویت دومرحله‌ای
        mfa_method:
          allOf:
          - $ref: '#/components/schemas/MfaMethodEnum'
          title: روش احراز چندعاملی
        must_change_password:
          type: boolean
          title: الزام تغییر گذرواژه
        is_active:
          type: boolean
          title: فعال
        is_staff:
          type: boolean
          readOnly: true
          title: دسترسی پنل مدیریت
        last_login_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: آخرین ورود
        date_joined:
          type: string
          format: date-time
          readOnly: true
          title: تاریخ عضویت
        role_assignments:
          type: array
          items:
            $ref: '#/components/schemas/UserRoleAssignment'
          readOnly: true
          title: انتساب‌های نقش
      required:
      - date_joined
      - display_name
      - id
      - is_staff
      - last_login_at
      - person_detail
      - role_assignments
      - username
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        username: s.mohammadi
        email: user@example.school
        mobile: 09121234567
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        person_detail:
          id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          first_name: سارا
          last_name: محمدی
          full_name: سارا محمدی
          national_id: 0012345678
          birth_date: '2012-09-26'
          gender: MALE
          gender_display: مرد
          status: ACTIVE
        display_name: سارا محمدی
        status: INVITED
        mfa_enabled: true
        mfa_method: NONE
        must_change_password: true
        is_active: true
        is_staff: true
        last_login_at: '2026-09-23T08:30:00+03:30'
        date_joined: '2024-08-31T09:00:00+03:30'
        role_assignments:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          username: s.mohammadi
          role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          role_code: SAMPLE-001
          role_title: عنوان نمونه
          scope_type: TENANT
          scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          status: ACTIVE
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          is_currently_effective: true
          grant_reason: توضیح نمونه
          revoked_at: '2026-09-23T08:30:00+03:30'
          revoke_reason: توضیح نمونه
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    UserAccountCreate:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        username:
          type: string
          title: نام کاربری
          maxLength: 150
        email:
          title: ایمیل
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        mobile:
          type: string
          title: تلفن همراه
          maxLength: 20
        person:
          type: string
          format: uuid
          title: شخص
          nullable: true
        status:
          allOf:
          - $ref: '#/components/schemas/UserStatusEnum'
          title: وضعیت حساب
        mfa_enabled:
          type: boolean
          title: احراز هویت دومرحله‌ای
        mfa_method:
          allOf:
          - $ref: '#/components/schemas/MfaMethodEnum'
          title: روش احراز چندعاملی
      required:
      - id
      - username
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        username: s.mohammadi
        email: user@example.school
        mobile: 09121234567
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: INVITED
        mfa_enabled: true
        mfa_method: NONE
    UserAccountCreateRequest:
      type: object
      properties:
        username:
          type: string
          minLength: 1
          title: نام کاربری
          maxLength: 150
        email:
          title: ایمیل
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        mobile:
          type: string
          title: تلفن همراه
          maxLength: 20
        person:
          type: string
          format: uuid
          title: شخص
          nullable: true
        status:
          allOf:
          - $ref: '#/components/schemas/UserStatusEnum'
          title: وضعیت حساب
        password:
          type: string
          writeOnly: true
          minLength: 10
          title: گذرواژه
        mfa_enabled:
          type: boolean
          title: احراز هویت دومرحله‌ای
        mfa_method:
          allOf:
          - $ref: '#/components/schemas/MfaMethodEnum'
          title: روش احراز چندعاملی
      required:
      - username
      example:
        username: s.mohammadi
        email: user@example.school
        mobile: 09121234567
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: INVITED
        password: P@ssw0rd!2026
        mfa_enabled: true
        mfa_method: NONE
    UserAccountRequest:
      type: object
      properties:
        username:
          type: string
          minLength: 1
          title: نام کاربری
          maxLength: 150
        email:
          title: ایمیل
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        mobile:
          type: string
          title: تلفن همراه
          maxLength: 20
        person:
          type: string
          format: uuid
          title: شخص
          nullable: true
        status:
          allOf:
          - $ref: '#/components/schemas/UserStatusEnum'
          title: وضعیت حساب
        mfa_enabled:
          type: boolean
          title: احراز هویت دومرحله‌ای
        mfa_method:
          allOf:
          - $ref: '#/components/schemas/MfaMethodEnum'
          title: روش احراز چندعاملی
        must_change_password:
          type: boolean
          title: الزام تغییر گذرواژه
        is_active:
          type: boolean
          title: فعال
      required:
      - username
      example:
        username: s.mohammadi
        email: user@example.school
        mobile: 09121234567
        person: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: INVITED
        mfa_enabled: true
        mfa_method: NONE
        must_change_password: true
        is_active: true
    UserRoleAssignment:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        user:
          type: string
          format: uuid
          title: کاربر
        username:
          type: string
          readOnly: true
          title: نام کاربری
        role:
          type: string
          format: uuid
          title: نقش
        role_code:
          type: string
          readOnly: true
          title: کد نقش
        role_title:
          type: string
          readOnly: true
          title: عنوان نقش
        scope_type:
          allOf:
          - $ref: '#/components/schemas/ScopeTypeEnum'
          title: نوع دامنه
        scope_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه دامنه
        status:
          allOf:
          - $ref: '#/components/schemas/RoleAssignmentStatusEnum'
          title: وضعیت
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        is_currently_effective:
          type: boolean
          readOnly: true
          title: هم‌اکنون معتبر است
        grant_reason:
          type: string
          title: علت اعطا
          maxLength: 300
        revoked_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: زمان ابطال
        revoke_reason:
          type: string
          title: علت ابطال
          maxLength: 300
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - created_at
      - effective_from
      - id
      - is_currently_effective
      - revoked_at
      - role
      - role_code
      - role_title
      - updated_at
      - user
      - username
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        username: s.mohammadi
        role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        role_code: SAMPLE-001
        role_title: عنوان نمونه
        scope_type: TENANT
        scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: ACTIVE
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        is_currently_effective: true
        grant_reason: توضیح نمونه
        revoked_at: '2026-09-23T08:30:00+03:30'
        revoke_reason: توضیح نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    UserRoleAssignmentRequest:
      type: object
      properties:
        user:
          type: string
          format: uuid
          title: کاربر
        role:
          type: string
          format: uuid
          title: نقش
        scope_type:
          allOf:
          - $ref: '#/components/schemas/ScopeTypeEnum'
          title: نوع دامنه
        scope_id:
          type: string
          format: uuid
          nullable: true
          title: شناسه دامنه
        status:
          allOf:
          - $ref: '#/components/schemas/RoleAssignmentStatusEnum'
          title: وضعیت
        effective_from:
          type: string
          format: date
          title: معتبر از
        effective_to:
          type: string
          format: date
          nullable: true
          title: معتبر تا
        grant_reason:
          type: string
          title: علت اعطا
          maxLength: 300
        revoke_reason:
          type: string
          title: علت ابطال
          maxLength: 300
      required:
      - effective_from
      - role
      - user
      example:
        user: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        role: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        scope_type: TENANT
        scope_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: ACTIVE
        effective_from: '2026-09-23'
        effective_to: '2027-03-20'
        grant_reason: توضیح نمونه
        revoke_reason: توضیح نمونه
    UserStatusEnum:
      enum:
      - INVITED
      - ACTIVE
      - LOCKED
      - SUSPENDED
      - DISABLED
      type: string
      description: |-
        * `INVITED` - دعوت‌شده
        * `ACTIVE` - فعال
        * `LOCKED` - قفل‌شده
        * `SUSPENDED` - تعلیق‌شده
        * `DISABLED` - غیرفعال
    Vehicle:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        plate_no:
          type: string
          title: پلاک
          maxLength: 30
        model_name:
          type: string
          title: نام مدل
          maxLength: 120
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت
        inspection_valid_until:
          type: string
          format: date
          nullable: true
          title: اعتبار معاینه فنی
        insurance_valid_until:
          type: string
          format: date
          nullable: true
          title: اعتبار بیمه تا
        status:
          type: string
          title: وضعیت
          maxLength: 20
        documents_valid:
          type: boolean
          description: مدارک راننده/خودرو باید معتبر باشد (بخش ۷.۱۰).
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - capacity
      - created_at
      - documents_valid
      - id
      - plate_no
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        plate_no: SAMPLE-001
        model_name: نام نمونه
        capacity: 3
        inspection_valid_until: '2026-09-23'
        insurance_valid_until: '2026-09-23'
        status: متن نمونه
        documents_valid: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    VehicleRequest:
      type: object
      properties:
        plate_no:
          type: string
          minLength: 1
          title: پلاک
          maxLength: 30
        model_name:
          type: string
          title: نام مدل
          maxLength: 120
        capacity:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: ظرفیت
        inspection_valid_until:
          type: string
          format: date
          nullable: true
          title: اعتبار معاینه فنی
        insurance_valid_until:
          type: string
          format: date
          nullable: true
          title: اعتبار بیمه تا
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - capacity
      - plate_no
      example:
        plate_no: SAMPLE-001
        model_name: نام نمونه
        capacity: 3
        inspection_valid_until: '2026-09-23'
        insurance_valid_until: '2026-09-23'
        status: متن نمونه
    Vendor:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد
          maxLength: 30
        legal_name:
          type: string
          title: نام حقوقی
          maxLength: 250
        tax_id:
          type: string
          title: شناسه مالیاتی
          maxLength: 40
        contact_person:
          type: string
          title: شخص رابط
          maxLength: 150
        phone:
          type: string
          title: تلفن
          maxLength: 30
        email:
          title: رایانامه
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        address_line:
          type: string
          title: سطر نشانی
          maxLength: 400
        payment_terms_days:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مهلت پرداخت (روز)
        status:
          allOf:
          - $ref: '#/components/schemas/VendorStatusEnum'
          title: وضعیت
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - legal_name
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        legal_name: نام نمونه
        tax_id: متن نمونه
        contact_person: متن نمونه
        phone: 09121234567
        email: user@example.school
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        payment_terms_days: 3
        status: ACTIVE
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    VendorRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        legal_name:
          type: string
          minLength: 1
          title: نام حقوقی
          maxLength: 250
        tax_id:
          type: string
          title: شناسه مالیاتی
          maxLength: 40
        contact_person:
          type: string
          title: شخص رابط
          maxLength: 150
        phone:
          type: string
          title: تلفن
          maxLength: 30
        email:
          title: رایانامه
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        address_line:
          type: string
          title: سطر نشانی
          maxLength: 400
        payment_terms_days:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: مهلت پرداخت (روز)
        status:
          allOf:
          - $ref: '#/components/schemas/VendorStatusEnum'
          title: وضعیت
      required:
      - code
      - legal_name
      example:
        code: SAMPLE-001
        legal_name: نام نمونه
        tax_id: متن نمونه
        contact_person: متن نمونه
        phone: 09121234567
        email: user@example.school
        address_line: خیابان نمونه، کوچه دوم، پلاک ۱۲
        payment_terms_days: 3
        status: ACTIVE
    VendorStatusEnum:
      enum:
      - ACTIVE
      - SUSPENDED
      - BLACKLISTED
      - INACTIVE
      type: string
      description: |-
        * `ACTIVE` - فعال
        * `SUSPENDED` - تعلیق
        * `BLACKLISTED` - لیست سیاه
        * `INACTIVE` - غیرفعال
    VerificationStatusEnum:
      enum:
      - PENDING
      - VERIFIED
      - REJECTED
      - EXPIRED
      type: string
      description: |-
        * `PENDING` - در انتظار بررسی
        * `VERIFIED` - تأییدشده
        * `REJECTED` - ردشده
        * `EXPIRED` - منقضی
    Warehouse:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        campus:
          type: string
          format: uuid
          title: شعبه
        campus_name:
          type: string
          readOnly: true
          title: نام شعبه
        code:
          type: string
          title: کد
          maxLength: 30
        title:
          type: string
          title: عنوان
          maxLength: 150
        location:
          type: string
          title: محل
          maxLength: 200
        keeper_employee_id:
          type: string
          format: uuid
          nullable: true
          title: انباردار
        status:
          type: string
          title: وضعیت
          maxLength: 20
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus
      - campus_name
      - code
      - created_at
      - id
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus_name: شعبه مرکزی
        code: SAMPLE-001
        title: عنوان نمونه
        location: متن نمونه
        keeper_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    WarehouseRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        code:
          type: string
          minLength: 1
          title: کد
          maxLength: 30
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 150
        location:
          type: string
          title: محل
          maxLength: 200
        keeper_employee_id:
          type: string
          format: uuid
          nullable: true
          title: انباردار
        status:
          type: string
          minLength: 1
          title: وضعیت
          maxLength: 20
      required:
      - campus
      - code
      - title
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        location: متن نمونه
        keeper_employee_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status: متن نمونه
    WeekdayEnum:
      enum:
      - 0
      - 1
      - 2
      - 3
      - 4
      - 5
      - 6
      type: integer
      description: |-
        * `0` - شنبه
        * `1` - یکشنبه
        * `2` - دوشنبه
        * `3` - سه‌شنبه
        * `4` - چهارشنبه
        * `5` - پنجشنبه
        * `6` - جمعه
    WeeklyTimetable:
      type: object
      description: نمای برنامه هفتگی یک کلاس، برای رندر جدول در فرانت.
      properties:
        classGroupId:
          type: string
          format: uuid
          title: شناسه کلاس
        classGroupCode:
          type: string
          title: کد کلاس
        entries:
          type: array
          items:
            $ref: '#/components/schemas/ScheduleEntry'
          title: ردیف‌ها
      required:
      - classGroupCode
      - classGroupId
      - entries
      example:
        classGroupId: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        classGroupCode: G7-A
        entries:
        - id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_offering: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          course_title: ریاضی ۷
          class_group_code: G7-A
          room: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          room_code: R-204
          teacher_profile_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
          weekday: 0
          weekday_display: شنبه
          starts_at: 08:30:00
          ends_at: 08:30:00
          period_no: 3
          effective_from: '2026-09-23'
          effective_to: '2027-03-20'
          status: DRAFT
          created_at: '2026-09-23T08:30:00+03:30'
          updated_at: '2026-09-23T08:30:00+03:30'
          version: 1
    WithdrawRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 400
          title: علت
        exit_date:
          type: string
          format: date
          title: تاریخ خروج
      required:
      - reason
      example:
        reason: اصلاح اطلاعات پس از بررسی پرونده
        exit_date: '2027-03-20'
    WorkShift:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        campus:
          type: string
          format: uuid
          title: شعبه
        title:
          type: string
          title: عنوان
          maxLength: 100
        starts_at:
          type: string
          format: time
          title: زمان شروع
        ends_at:
          type: string
          format: time
          title: زمان پایان
        tolerance_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تلورانس تأخیر
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - campus
      - created_at
      - ends_at
      - id
      - starts_at
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_at: 08:30:00
        ends_at: 08:30:00
        tolerance_minutes: 45
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    WorkShiftRequest:
      type: object
      properties:
        campus:
          type: string
          format: uuid
          title: شعبه
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 100
        starts_at:
          type: string
          format: time
          title: زمان شروع
        ends_at:
          type: string
          format: time
          title: زمان پایان
        tolerance_minutes:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: تلورانس تأخیر
      required:
      - campus
      - ends_at
      - starts_at
      - title
      example:
        campus: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        title: عنوان نمونه
        starts_at: 08:30:00
        ends_at: 08:30:00
        tolerance_minutes: 45
    WorkflowApprovalStatusEnum:
      enum:
      - PENDING
      - APPROVED
      - REJECTED
      - CANCELLED
      - EXPIRED
      type: string
      description: |-
        * `PENDING` - در جریان
        * `APPROVED` - تأییدشده
        * `REJECTED` - ردشده
        * `CANCELLED` - لغوشده
        * `EXPIRED` - منقضی
    WorkflowDefinition:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
          title: شناسه
        code:
          type: string
          title: کد گردش
          maxLength: 60
        title:
          type: string
          title: عنوان
          maxLength: 200
        version_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره نسخه
        subject_type:
          type: string
          title: نوع موضوع
          description: مثلاً finance.Refund یا inventory.PurchaseRequest
          maxLength: 80
        steps_definition:
          title: تعریف گام‌ها
          description: 'مثلاً [{"sequence": 1, "roleCode": "ACCOUNTANT"}]'
        is_active:
          type: boolean
          title: فعال
        created_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ایجاد
        updated_at:
          type: string
          format: date-time
          readOnly: true
          title: زمان ویرایش
        version:
          type: integer
          readOnly: true
          title: نسخه رکورد
      required:
      - code
      - created_at
      - id
      - subject_type
      - title
      - updated_at
      - version
      example:
        id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        code: SAMPLE-001
        title: عنوان نمونه
        version_no: 3
        subject_type: متن نمونه
        is_active: true
        created_at: '2026-09-23T08:30:00+03:30'
        updated_at: '2026-09-23T08:30:00+03:30'
        version: 1
    WorkflowDefinitionRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          title: کد گردش
          maxLength: 60
        title:
          type: string
          minLength: 1
          title: عنوان
          maxLength: 200
        version_no:
          type: integer
          maximum: 9223372036854775807
          minimum: 0
          format: int64
          title: شماره نسخه
        subject_type:
          type: string
          minLength: 1
          title: نوع موضوع
          description: مثلاً finance.Refund یا inventory.PurchaseRequest
          maxLength: 80
        steps_definition:
          title: تعریف گام‌ها
          description: 'مثلاً [{"sequence": 1, "roleCode": "ACCOUNTANT"}]'
        is_active:
          type: boolean
          title: فعال
      required:
      - code
      - subject_type
      - title
      example:
        code: SAMPLE-001
        title: عنوان نمونه
        version_no: 3
        subject_type: متن نمونه
        is_active: true
    YearCloseRequest:
      type: object
      properties:
        note:
          type: string
          description: یادداشت بستن سال
          maxLength: 400
      example:
        note: یادداشت داخلی
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: sessionid
    jwtAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  examples:
    ValidationError:
      summary: خطای اعتبارسنجی ورودی
      description: قالب یا مقدار یکی از فیلدهای بدنه درست نیست.
      value:
        code: VALIDATION_ERROR
        message: داده ورودی معتبر نیست.
        correlationId: 8c4a1f2e
        fieldErrors:
        - field: title
          reason: این فیلد نمی‌تواند خالی باشد.
        retryable: false
    Unauthenticated:
      summary: احراز هویت نشده
      description: توکن ارسال نشده، منقضی شده یا نامعتبر است.
      value:
        code: AUTHENTICATION_REQUIRED
        message: برای این عملیات باید وارد سامانه شوید.
        correlationId: 8c4a1f2e
        fieldErrors: []
        retryable: false
    PermissionDenied:
      summary: بدون مجوز
      description: کاربر احراز هویت شده اما مجوز لازم را ندارد. اگر رکورد خارج از
        دامنه دسترسی باشد کد `SCOPE_FORBIDDEN` برمی‌گردد.
      value:
        code: PERMISSION_DENIED
        message: شما مجوز لازم برای این عملیات را ندارید.
        correlationId: 8c4a1f2e
        fieldErrors: []
        retryable: false
    NotFound:
      summary: یافت نشد
      description: رکورد وجود ندارد، حذف نرم شده، یا خارج از دامنه دسترسی است.
      value:
        code: RESOURCE_NOT_FOUND
        message: منبع درخواستی یافت نشد.
        correlationId: 8c4a1f2e
        fieldErrors: []
        retryable: false
    VersionConflict:
      summary: تعارض نسخه یا وضعیت
      description: رکورد پس از خواندن شما تغییر کرده (`VERSION_CONFLICT`)، گذار وضعیت
        مجاز نیست (`INVALID_STATE_TRANSITION`) یا دوره بسته است (`PERIOD_CLOSED`).
      value:
        code: VERSION_CONFLICT
        message: این رکورد توسط کاربر دیگری تغییر کرده است. نسخه ارسالی 3 و نسخه فعلی
          4 است.
        correlationId: 8c4a1f2e
        fieldErrors: []
        retryable: true
    BusinessRule:
      summary: نقض قاعده کسب‌وکار
      description: ورودی از نظر قالب درست است اما قاعده دامنه اجازه نمی‌دهد. مقدار
        `code` به قاعده نقض‌شده اشاره دارد؛ فرانت باید روی همان تصمیم بگیرد.
      value:
        code: CLASS_CAPACITY_EXCEEDED
        message: ظرفیت کلاس تکمیل است.
        correlationId: 8c4a1f2e
        fieldErrors:
        - field: class_group
          reason: capacity
        retryable: false
  responses:
    ValidationError:
      description: داده ورودی معتبر نیست.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          examples:
            ValidationError:
              $ref: '#/components/examples/ValidationError'
    Unauthenticated:
      description: احراز هویت نشده است.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          examples:
            Unauthenticated:
              $ref: '#/components/examples/Unauthenticated'
    PermissionDenied:
      description: مجوز لازم برای این عملیات وجود ندارد.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          examples:
            PermissionDenied:
              $ref: '#/components/examples/PermissionDenied'
    NotFound:
      description: رکورد یافت نشد یا خارج از دامنه دسترسی است.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          examples:
            NotFound:
              $ref: '#/components/examples/NotFound'
    VersionConflict:
      description: تعارض نسخه یا وضعیت.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          examples:
            VersionConflict:
              $ref: '#/components/examples/VersionConflict'
    BusinessRule:
      description: نقض قاعده کسب‌وکار.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          examples:
            BusinessRule:
              $ref: '#/components/examples/BusinessRule'
tags:
- name: Auth
  description: ورود، تازه‌سازی توکن، پروفایل و Context کاری
- name: IAM
  description: اشخاص، کاربران، نقش، مجوز و ممیزی دسترسی
- name: Organization
  description: مدرسه، شعبه، سال تحصیلی، پایه، درس، کلاس و برنامه هفتگی
- name: Students
  description: پذیرش، دانش‌آموز، اولیا، ثبت‌نام و رضایت‌نامه
- name: HR
  description: پرسنل، قرارداد، انتساب تدریس، مرخصی و حقوق
- name: Teaching
  description: جلسه، حضور و غیاب، تکلیف و منابع آموزشی
- name: Assessment
  description: بانک سؤال، آزمون، جلسه آزمون، تلاش و تصحیح
- name: Gradebook
  description: دفتر نمره، نتیجه درس و کارنامه
- name: Finance
  description: تعرفه، صورتحساب، پرداخت، استرداد و حسابداری دوبل
- name: Inventory
  description: تأمین‌کننده، خرید، انبار، حرکت موجودی و اموال
- name: Welfare
  description: سلامت، مشاوره، انضباط، کتابخانه و حمل‌ونقل
- name: Workflow
  description: گردش تأیید، اعلان، پیوست و Outbox
- name: Reports
  description: داشبورد و گزارش‌های تجمیعی
